import { createVueIcon } from '../../createVueIcon';
import { Physics as iconData } from '@dga-icons/core/stroke-standard';

const Physics = createVueIcon('physics', iconData);

export default Physics;

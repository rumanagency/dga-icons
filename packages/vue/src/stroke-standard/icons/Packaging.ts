import { createVueIcon } from '../../createVueIcon';
import { Packaging as iconData } from '@dga-icons/core/stroke-standard';

const Packaging = createVueIcon('packaging', iconData);

export default Packaging;

import { createVueIcon } from '../../createVueIcon';
import { Signature as iconData } from '@dga-icons/core/stroke-standard';

const Signature = createVueIcon('signature', iconData);

export default Signature;

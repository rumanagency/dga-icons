import { createVueIcon } from '../../createVueIcon';
import { BitcoinCpu as iconData } from '@dga-icons/core/stroke-standard';

const BitcoinCpu = createVueIcon('bitcoin-cpu', iconData);

export default BitcoinCpu;

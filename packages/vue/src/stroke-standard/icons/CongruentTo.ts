import { createVueIcon } from '../../createVueIcon';
import { CongruentTo as iconData } from '@dga-icons/core/stroke-standard';

const CongruentTo = createVueIcon('congruent-to', iconData);

export default CongruentTo;

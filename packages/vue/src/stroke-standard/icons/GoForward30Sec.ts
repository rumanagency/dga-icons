import { createVueIcon } from '../../createVueIcon';
import { GoForward30Sec as iconData } from '@dga-icons/core/stroke-standard';

const GoForward30Sec = createVueIcon('go-forward-30-sec', iconData);

export default GoForward30Sec;

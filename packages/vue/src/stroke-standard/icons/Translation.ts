import { createVueIcon } from '../../createVueIcon';
import { Translation as iconData } from '@dga-icons/core/stroke-standard';

const Translation = createVueIcon('translation', iconData);

export default Translation;

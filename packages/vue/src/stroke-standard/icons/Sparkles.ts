import { createVueIcon } from '../../createVueIcon';
import { Sparkles as iconData } from '@dga-icons/core/stroke-standard';

const Sparkles = createVueIcon('sparkles', iconData);

export default Sparkles;

import { createVueIcon } from '../../createVueIcon';
import { ShippingLoading as iconData } from '@dga-icons/core/stroke-standard';

const ShippingLoading = createVueIcon('shipping-loading', iconData);

export default ShippingLoading;

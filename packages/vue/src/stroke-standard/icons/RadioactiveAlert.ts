import { createVueIcon } from '../../createVueIcon';
import { RadioactiveAlert as iconData } from '@dga-icons/core/stroke-standard';

const RadioactiveAlert = createVueIcon('radioactive-alert', iconData);

export default RadioactiveAlert;

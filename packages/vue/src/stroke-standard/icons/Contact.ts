import { createVueIcon } from '../../createVueIcon';
import { Contact as iconData } from '@dga-icons/core/stroke-standard';

const Contact = createVueIcon('contact', iconData);

export default Contact;

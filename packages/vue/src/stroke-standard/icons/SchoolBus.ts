import { createVueIcon } from '../../createVueIcon';
import { SchoolBus as iconData } from '@dga-icons/core/stroke-standard';

const SchoolBus = createVueIcon('school-bus', iconData);

export default SchoolBus;

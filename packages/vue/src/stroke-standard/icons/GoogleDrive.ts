import { createVueIcon } from '../../createVueIcon';
import { GoogleDrive as iconData } from '@dga-icons/core/stroke-standard';

const GoogleDrive = createVueIcon('google-drive', iconData);

export default GoogleDrive;

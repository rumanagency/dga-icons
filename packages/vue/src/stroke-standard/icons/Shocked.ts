import { createVueIcon } from '../../createVueIcon';
import { Shocked as iconData } from '@dga-icons/core/stroke-standard';

const Shocked = createVueIcon('shocked', iconData);

export default Shocked;

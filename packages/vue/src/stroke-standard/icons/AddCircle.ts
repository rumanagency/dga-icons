import { createVueIcon } from '../../createVueIcon';
import { AddCircle as iconData } from '@dga-icons/core/stroke-standard';

const AddCircle = createVueIcon('add-circle', iconData);

export default AddCircle;

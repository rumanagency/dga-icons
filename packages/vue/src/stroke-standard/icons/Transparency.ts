import { createVueIcon } from '../../createVueIcon';
import { Transparency as iconData } from '@dga-icons/core/stroke-standard';

const Transparency = createVueIcon('transparency', iconData);

export default Transparency;

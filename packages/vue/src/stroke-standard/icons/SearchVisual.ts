import { createVueIcon } from '../../createVueIcon';
import { SearchVisual as iconData } from '@dga-icons/core/stroke-standard';

const SearchVisual = createVueIcon('search-visual', iconData);

export default SearchVisual;

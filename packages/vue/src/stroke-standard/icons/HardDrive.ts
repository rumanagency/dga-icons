import { createVueIcon } from '../../createVueIcon';
import { HardDrive as iconData } from '@dga-icons/core/stroke-standard';

const HardDrive = createVueIcon('hard-drive', iconData);

export default HardDrive;

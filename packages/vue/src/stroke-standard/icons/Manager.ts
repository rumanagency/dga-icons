import { createVueIcon } from '../../createVueIcon';
import { Manager as iconData } from '@dga-icons/core/stroke-standard';

const Manager = createVueIcon('manager', iconData);

export default Manager;

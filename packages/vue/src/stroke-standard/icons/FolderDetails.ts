import { createVueIcon } from '../../createVueIcon';
import { FolderDetails as iconData } from '@dga-icons/core/stroke-standard';

const FolderDetails = createVueIcon('folder-details', iconData);

export default FolderDetails;

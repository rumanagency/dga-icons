import { createVueIcon } from '../../createVueIcon';
import { RotateSquare as iconData } from '@dga-icons/core/stroke-standard';

const RotateSquare = createVueIcon('rotate-square', iconData);

export default RotateSquare;

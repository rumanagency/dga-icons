import { createVueIcon } from '../../createVueIcon';
import { Swimming as iconData } from '@dga-icons/core/stroke-standard';

const Swimming = createVueIcon('swimming', iconData);

export default Swimming;

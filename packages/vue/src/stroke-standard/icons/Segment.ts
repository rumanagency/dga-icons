import { createVueIcon } from '../../createVueIcon';
import { Segment as iconData } from '@dga-icons/core/stroke-standard';

const Segment = createVueIcon('segment', iconData);

export default Segment;

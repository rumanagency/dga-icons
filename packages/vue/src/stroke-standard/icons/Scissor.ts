import { createVueIcon } from '../../createVueIcon';
import { Scissor as iconData } from '@dga-icons/core/stroke-standard';

const Scissor = createVueIcon('scissor', iconData);

export default Scissor;

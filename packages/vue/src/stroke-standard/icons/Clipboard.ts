import { createVueIcon } from '../../createVueIcon';
import { Clipboard as iconData } from '@dga-icons/core/stroke-standard';

const Clipboard = createVueIcon('clipboard', iconData);

export default Clipboard;

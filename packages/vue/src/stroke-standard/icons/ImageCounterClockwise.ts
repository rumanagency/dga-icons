import { createVueIcon } from '../../createVueIcon';
import { ImageCounterClockwise as iconData } from '@dga-icons/core/stroke-standard';

const ImageCounterClockwise = createVueIcon('image-counter-clockwise', iconData);

export default ImageCounterClockwise;

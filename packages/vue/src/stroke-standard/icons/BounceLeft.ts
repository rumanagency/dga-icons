import { createVueIcon } from '../../createVueIcon';
import { BounceLeft as iconData } from '@dga-icons/core/stroke-standard';

const BounceLeft = createVueIcon('bounce-left', iconData);

export default BounceLeft;

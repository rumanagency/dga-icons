import { createVueIcon } from '../../createVueIcon';
import { Variable as iconData } from '@dga-icons/core/stroke-standard';

const Variable = createVueIcon('variable', iconData);

export default Variable;

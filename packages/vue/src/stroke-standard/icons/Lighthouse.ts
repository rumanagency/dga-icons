import { createVueIcon } from '../../createVueIcon';
import { Lighthouse as iconData } from '@dga-icons/core/stroke-standard';

const Lighthouse = createVueIcon('lighthouse', iconData);

export default Lighthouse;

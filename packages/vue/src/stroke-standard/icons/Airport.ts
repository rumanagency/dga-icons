import { createVueIcon } from '../../createVueIcon';
import { Airport as iconData } from '@dga-icons/core/stroke-standard';

const Airport = createVueIcon('airport', iconData);

export default Airport;

import { createVueIcon } from '../../createVueIcon';
import { SearchAdd as iconData } from '@dga-icons/core/stroke-standard';

const SearchAdd = createVueIcon('search-add', iconData);

export default SearchAdd;

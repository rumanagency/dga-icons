import { createVueIcon } from '../../createVueIcon';
import { Pipeline as iconData } from '@dga-icons/core/stroke-standard';

const Pipeline = createVueIcon('pipeline', iconData);

export default Pipeline;

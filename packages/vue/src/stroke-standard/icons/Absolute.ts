import { createVueIcon } from '../../createVueIcon';
import { Absolute as iconData } from '@dga-icons/core/stroke-standard';

const Absolute = createVueIcon('absolute', iconData);

export default Absolute;

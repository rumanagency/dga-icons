import { createVueIcon } from '../../createVueIcon';
import { Student as iconData } from '@dga-icons/core/stroke-standard';

const Student = createVueIcon('student', iconData);

export default Student;

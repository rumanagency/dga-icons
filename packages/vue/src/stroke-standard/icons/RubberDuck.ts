import { createVueIcon } from '../../createVueIcon';
import { RubberDuck as iconData } from '@dga-icons/core/stroke-standard';

const RubberDuck = createVueIcon('rubber-duck', iconData);

export default RubberDuck;

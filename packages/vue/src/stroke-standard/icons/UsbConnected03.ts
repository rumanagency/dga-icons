import { createVueIcon } from '../../createVueIcon';
import { UsbConnected03 as iconData } from '@dga-icons/core/stroke-standard';

const UsbConnected03 = createVueIcon('usb-connected-03', iconData);

export default UsbConnected03;

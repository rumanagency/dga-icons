import { createVueIcon } from '../../createVueIcon';
import { Snapchat as iconData } from '@dga-icons/core/stroke-standard';

const Snapchat = createVueIcon('snapchat', iconData);

export default Snapchat;

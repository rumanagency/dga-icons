import { createVueIcon } from '../../createVueIcon';
import { BorderHorizontal as iconData } from '@dga-icons/core/stroke-standard';

const BorderHorizontal = createVueIcon('border-horizontal', iconData);

export default BorderHorizontal;

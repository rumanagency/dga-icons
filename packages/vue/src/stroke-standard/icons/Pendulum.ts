import { createVueIcon } from '../../createVueIcon';
import { Pendulum as iconData } from '@dga-icons/core/stroke-standard';

const Pendulum = createVueIcon('pendulum', iconData);

export default Pendulum;

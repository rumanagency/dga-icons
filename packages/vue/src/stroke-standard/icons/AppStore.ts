import { createVueIcon } from '../../createVueIcon';
import { AppStore as iconData } from '@dga-icons/core/stroke-standard';

const AppStore = createVueIcon('app-store', iconData);

export default AppStore;

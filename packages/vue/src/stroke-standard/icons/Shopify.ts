import { createVueIcon } from '../../createVueIcon';
import { Shopify as iconData } from '@dga-icons/core/stroke-standard';

const Shopify = createVueIcon('shopify', iconData);

export default Shopify;

import { createVueIcon } from '../../createVueIcon';
import { StopCircle as iconData } from '@dga-icons/core/stroke-standard';

const StopCircle = createVueIcon('stop-circle', iconData);

export default StopCircle;

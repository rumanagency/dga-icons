import { createVueIcon } from '../../createVueIcon';
import { Shuffle as iconData } from '@dga-icons/core/stroke-standard';

const Shuffle = createVueIcon('shuffle', iconData);

export default Shuffle;

import { createVueIcon } from '../../createVueIcon';
import { JobSearch as iconData } from '@dga-icons/core/stroke-standard';

const JobSearch = createVueIcon('job-search', iconData);

export default JobSearch;

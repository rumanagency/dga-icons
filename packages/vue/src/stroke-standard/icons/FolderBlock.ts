import { createVueIcon } from '../../createVueIcon';
import { FolderBlock as iconData } from '@dga-icons/core/stroke-standard';

const FolderBlock = createVueIcon('folder-block', iconData);

export default FolderBlock;

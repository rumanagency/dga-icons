import { createVueIcon } from '../../createVueIcon';
import { FileYen as iconData } from '@dga-icons/core/stroke-standard';

const FileYen = createVueIcon('file-yen', iconData);

export default FileYen;

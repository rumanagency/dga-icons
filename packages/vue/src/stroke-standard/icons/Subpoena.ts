import { createVueIcon } from '../../createVueIcon';
import { Subpoena as iconData } from '@dga-icons/core/stroke-standard';

const Subpoena = createVueIcon('subpoena', iconData);

export default Subpoena;

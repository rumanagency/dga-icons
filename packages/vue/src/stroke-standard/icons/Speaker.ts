import { createVueIcon } from '../../createVueIcon';
import { Speaker as iconData } from '@dga-icons/core/stroke-standard';

const Speaker = createVueIcon('speaker', iconData);

export default Speaker;

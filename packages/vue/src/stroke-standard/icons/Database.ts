import { createVueIcon } from '../../createVueIcon';
import { Database as iconData } from '@dga-icons/core/stroke-standard';

const Database = createVueIcon('database', iconData);

export default Database;

import { createVueIcon } from '../../createVueIcon';
import { HairClips as iconData } from '@dga-icons/core/stroke-standard';

const HairClips = createVueIcon('hair-clips', iconData);

export default HairClips;

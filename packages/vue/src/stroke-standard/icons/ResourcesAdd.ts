import { createVueIcon } from '../../createVueIcon';
import { ResourcesAdd as iconData } from '@dga-icons/core/stroke-standard';

const ResourcesAdd = createVueIcon('resources-add', iconData);

export default ResourcesAdd;

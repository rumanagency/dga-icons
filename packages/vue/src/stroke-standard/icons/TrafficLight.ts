import { createVueIcon } from '../../createVueIcon';
import { TrafficLight as iconData } from '@dga-icons/core/stroke-standard';

const TrafficLight = createVueIcon('traffic-light', iconData);

export default TrafficLight;

import { createVueIcon } from '../../createVueIcon';
import { PackageRemove as iconData } from '@dga-icons/core/stroke-standard';

const PackageRemove = createVueIcon('package-remove', iconData);

export default PackageRemove;

import { createVueIcon } from '../../createVueIcon';
import { FileSearch as iconData } from '@dga-icons/core/stroke-standard';

const FileSearch = createVueIcon('file-search', iconData);

export default FileSearch;

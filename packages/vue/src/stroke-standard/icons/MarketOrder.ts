import { createVueIcon } from '../../createVueIcon';
import { MarketOrder as iconData } from '@dga-icons/core/stroke-standard';

const MarketOrder = createVueIcon('market-order', iconData);

export default MarketOrder;

import { createVueIcon } from '../../createVueIcon';
import { SearchArea as iconData } from '@dga-icons/core/stroke-standard';

const SearchArea = createVueIcon('search-area', iconData);

export default SearchArea;

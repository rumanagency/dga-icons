import { createVueIcon } from '../../createVueIcon';
import { Latitude as iconData } from '@dga-icons/core/stroke-standard';

const Latitude = createVueIcon('latitude', iconData);

export default Latitude;

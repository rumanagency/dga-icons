import { createVueIcon } from '../../createVueIcon';
import { FileLink as iconData } from '@dga-icons/core/stroke-standard';

const FileLink = createVueIcon('file-link', iconData);

export default FileLink;

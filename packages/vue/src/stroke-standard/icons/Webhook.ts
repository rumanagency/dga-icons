import { createVueIcon } from '../../createVueIcon';
import { Webhook as iconData } from '@dga-icons/core/stroke-standard';

const Webhook = createVueIcon('webhook', iconData);

export default Webhook;

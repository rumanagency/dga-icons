import { createVueIcon } from '../../createVueIcon';
import { Hangout as iconData } from '@dga-icons/core/stroke-standard';

const Hangout = createVueIcon('hangout', iconData);

export default Hangout;

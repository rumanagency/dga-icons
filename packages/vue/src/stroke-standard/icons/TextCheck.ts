import { createVueIcon } from '../../createVueIcon';
import { TextCheck as iconData } from '@dga-icons/core/stroke-standard';

const TextCheck = createVueIcon('text-check', iconData);

export default TextCheck;

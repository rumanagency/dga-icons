import { createVueIcon } from '../../createVueIcon';
import { BinaryCode as iconData } from '@dga-icons/core/stroke-standard';

const BinaryCode = createVueIcon('binary-code', iconData);

export default BinaryCode;

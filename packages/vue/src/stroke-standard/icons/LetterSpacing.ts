import { createVueIcon } from '../../createVueIcon';
import { LetterSpacing as iconData } from '@dga-icons/core/stroke-standard';

const LetterSpacing = createVueIcon('letter-spacing', iconData);

export default LetterSpacing;

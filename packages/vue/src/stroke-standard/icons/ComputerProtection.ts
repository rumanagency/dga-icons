import { createVueIcon } from '../../createVueIcon';
import { ComputerProtection as iconData } from '@dga-icons/core/stroke-standard';

const ComputerProtection = createVueIcon('computer-protection', iconData);

export default ComputerProtection;

import { createVueIcon } from '../../createVueIcon';
import { ChartRadar as iconData } from '@dga-icons/core/stroke-standard';

const ChartRadar = createVueIcon('chart-radar', iconData);

export default ChartRadar;

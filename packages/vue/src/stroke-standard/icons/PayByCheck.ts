import { createVueIcon } from '../../createVueIcon';
import { PayByCheck as iconData } from '@dga-icons/core/stroke-standard';

const PayByCheck = createVueIcon('pay-by-check', iconData);

export default PayByCheck;

import { createVueIcon } from '../../createVueIcon';
import { Strategy as iconData } from '@dga-icons/core/stroke-standard';

const Strategy = createVueIcon('strategy', iconData);

export default Strategy;

import { createVueIcon } from '../../createVueIcon';
import { PushUpBar as iconData } from '@dga-icons/core/stroke-standard';

const PushUpBar = createVueIcon('push-up-bar', iconData);

export default PushUpBar;

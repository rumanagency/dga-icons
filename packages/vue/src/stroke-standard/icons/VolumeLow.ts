import { createVueIcon } from '../../createVueIcon';
import { VolumeLow as iconData } from '@dga-icons/core/stroke-standard';

const VolumeLow = createVueIcon('volume-low', iconData);

export default VolumeLow;

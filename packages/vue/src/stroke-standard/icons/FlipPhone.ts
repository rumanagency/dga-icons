import { createVueIcon } from '../../createVueIcon';
import { FlipPhone as iconData } from '@dga-icons/core/stroke-standard';

const FlipPhone = createVueIcon('flip-phone', iconData);

export default FlipPhone;

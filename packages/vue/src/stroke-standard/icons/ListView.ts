import { createVueIcon } from '../../createVueIcon';
import { ListView as iconData } from '@dga-icons/core/stroke-standard';

const ListView = createVueIcon('list-view', iconData);

export default ListView;

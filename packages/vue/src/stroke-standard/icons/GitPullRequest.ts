import { createVueIcon } from '../../createVueIcon';
import { GitPullRequest as iconData } from '@dga-icons/core/stroke-standard';

const GitPullRequest = createVueIcon('git-pull-request', iconData);

export default GitPullRequest;

import { createVueIcon } from '../../createVueIcon';
import { Fencing as iconData } from '@dga-icons/core/stroke-standard';

const Fencing = createVueIcon('fencing', iconData);

export default Fencing;

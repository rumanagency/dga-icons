import { createVueIcon } from '../../createVueIcon';
import { Tractor as iconData } from '@dga-icons/core/stroke-standard';

const Tractor = createVueIcon('tractor', iconData);

export default Tractor;

import { createVueIcon } from '../../createVueIcon';
import { Unavailable as iconData } from '@dga-icons/core/stroke-standard';

const Unavailable = createVueIcon('unavailable', iconData);

export default Unavailable;

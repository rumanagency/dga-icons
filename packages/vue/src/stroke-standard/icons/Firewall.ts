import { createVueIcon } from '../../createVueIcon';
import { Firewall as iconData } from '@dga-icons/core/stroke-standard';

const Firewall = createVueIcon('firewall', iconData);

export default Firewall;

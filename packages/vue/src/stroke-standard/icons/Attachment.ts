import { createVueIcon } from '../../createVueIcon';
import { Attachment as iconData } from '@dga-icons/core/stroke-standard';

const Attachment = createVueIcon('attachment', iconData);

export default Attachment;

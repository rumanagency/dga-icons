import { createVueIcon } from '../../createVueIcon';
import { NotificationCircle as iconData } from '@dga-icons/core/stroke-standard';

const NotificationCircle = createVueIcon('notification-circle', iconData);

export default NotificationCircle;

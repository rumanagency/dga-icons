import { createVueIcon } from '../../createVueIcon';
import { Mailbox as iconData } from '@dga-icons/core/stroke-standard';

const Mailbox = createVueIcon('mailbox', iconData);

export default Mailbox;

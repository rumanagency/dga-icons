import { createVueIcon } from '../../createVueIcon';
import { DeleteRow as iconData } from '@dga-icons/core/stroke-standard';

const DeleteRow = createVueIcon('delete-row', iconData);

export default DeleteRow;

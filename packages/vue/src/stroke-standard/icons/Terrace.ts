import { createVueIcon } from '../../createVueIcon';
import { Terrace as iconData } from '@dga-icons/core/stroke-standard';

const Terrace = createVueIcon('terrace', iconData);

export default Terrace;

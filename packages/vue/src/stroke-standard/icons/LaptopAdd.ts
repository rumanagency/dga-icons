import { createVueIcon } from '../../createVueIcon';
import { LaptopAdd as iconData } from '@dga-icons/core/stroke-standard';

const LaptopAdd = createVueIcon('laptop-add', iconData);

export default LaptopAdd;

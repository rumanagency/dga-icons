import { createVueIcon } from '../../createVueIcon';
import { SearchReplace as iconData } from '@dga-icons/core/stroke-standard';

const SearchReplace = createVueIcon('search-replace', iconData);

export default SearchReplace;

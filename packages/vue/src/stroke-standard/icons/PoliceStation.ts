import { createVueIcon } from '../../createVueIcon';
import { PoliceStation as iconData } from '@dga-icons/core/stroke-standard';

const PoliceStation = createVueIcon('police-station', iconData);

export default PoliceStation;

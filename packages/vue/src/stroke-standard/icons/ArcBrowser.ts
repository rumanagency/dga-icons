import { createVueIcon } from '../../createVueIcon';
import { ArcBrowser as iconData } from '@dga-icons/core/stroke-standard';

const ArcBrowser = createVueIcon('arc-browser', iconData);

export default ArcBrowser;

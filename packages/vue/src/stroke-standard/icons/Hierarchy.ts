import { createVueIcon } from '../../createVueIcon';
import { Hierarchy as iconData } from '@dga-icons/core/stroke-standard';

const Hierarchy = createVueIcon('hierarchy', iconData);

export default Hierarchy;

import { createVueIcon } from '../../createVueIcon';
import { BloodType as iconData } from '@dga-icons/core/stroke-standard';

const BloodType = createVueIcon('blood-type', iconData);

export default BloodType;

import { createVueIcon } from '../../createVueIcon';
import { Blogger as iconData } from '@dga-icons/core/stroke-standard';

const Blogger = createVueIcon('blogger', iconData);

export default Blogger;

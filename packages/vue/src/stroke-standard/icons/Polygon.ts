import { createVueIcon } from '../../createVueIcon';
import { Polygon as iconData } from '@dga-icons/core/stroke-standard';

const Polygon = createVueIcon('polygon', iconData);

export default Polygon;

import { createVueIcon } from '../../createVueIcon';
import { HospitalLocation as iconData } from '@dga-icons/core/stroke-standard';

const HospitalLocation = createVueIcon('hospital-location', iconData);

export default HospitalLocation;

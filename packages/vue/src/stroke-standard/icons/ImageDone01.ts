import { createVueIcon } from '../../createVueIcon';
import { ImageDone01 as iconData } from '@dga-icons/core/stroke-standard';

const ImageDone01 = createVueIcon('image-done-01', iconData);

export default ImageDone01;

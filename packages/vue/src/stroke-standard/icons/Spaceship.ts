import { createVueIcon } from '../../createVueIcon';
import { Spaceship as iconData } from '@dga-icons/core/stroke-standard';

const Spaceship = createVueIcon('spaceship', iconData);

export default Spaceship;

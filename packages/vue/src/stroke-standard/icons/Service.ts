import { createVueIcon } from '../../createVueIcon';
import { Service as iconData } from '@dga-icons/core/stroke-standard';

const Service = createVueIcon('service', iconData);

export default Service;

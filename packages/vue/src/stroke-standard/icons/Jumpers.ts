import { createVueIcon } from '../../createVueIcon';
import { Jumpers as iconData } from '@dga-icons/core/stroke-standard';

const Jumpers = createVueIcon('jumpers', iconData);

export default Jumpers;

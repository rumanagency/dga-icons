import { createVueIcon } from '../../createVueIcon';
import { BatteryEmpty as iconData } from '@dga-icons/core/stroke-standard';

const BatteryEmpty = createVueIcon('battery-empty', iconData);

export default BatteryEmpty;

import { createVueIcon } from '../../createVueIcon';
import { Gravity as iconData } from '@dga-icons/core/stroke-standard';

const Gravity = createVueIcon('gravity', iconData);

export default Gravity;

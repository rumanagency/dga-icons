import { createVueIcon } from '../../createVueIcon';
import { Ranking as iconData } from '@dga-icons/core/stroke-standard';

const Ranking = createVueIcon('ranking', iconData);

export default Ranking;

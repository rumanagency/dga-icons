import { createVueIcon } from '../../createVueIcon';
import { BorderInner as iconData } from '@dga-icons/core/stroke-standard';

const BorderInner = createVueIcon('border-inner', iconData);

export default BorderInner;

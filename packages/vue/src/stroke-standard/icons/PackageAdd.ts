import { createVueIcon } from '../../createVueIcon';
import { PackageAdd as iconData } from '@dga-icons/core/stroke-standard';

const PackageAdd = createVueIcon('package-add', iconData);

export default PackageAdd;

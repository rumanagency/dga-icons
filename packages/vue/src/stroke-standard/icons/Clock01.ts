import { createVueIcon } from '../../createVueIcon';
import { Clock01 as iconData } from '@dga-icons/core/stroke-standard';

const Clock01 = createVueIcon('clock-01', iconData);

export default Clock01;

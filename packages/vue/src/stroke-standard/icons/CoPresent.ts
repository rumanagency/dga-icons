import { createVueIcon } from '../../createVueIcon';
import { CoPresent as iconData } from '@dga-icons/core/stroke-standard';

const CoPresent = createVueIcon('co-present', iconData);

export default CoPresent;

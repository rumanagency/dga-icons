import { createVueIcon } from '../../createVueIcon';
import { BitcoinLocation as iconData } from '@dga-icons/core/stroke-standard';

const BitcoinLocation = createVueIcon('bitcoin-location', iconData);

export default BitcoinLocation;

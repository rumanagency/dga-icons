import { createVueIcon } from '../../createVueIcon';
import { FolderRemove as iconData } from '@dga-icons/core/stroke-standard';

const FolderRemove = createVueIcon('folder-remove', iconData);

export default FolderRemove;

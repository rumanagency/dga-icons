import { createVueIcon } from '../../createVueIcon';
import { Authorized as iconData } from '@dga-icons/core/stroke-standard';

const Authorized = createVueIcon('authorized', iconData);

export default Authorized;

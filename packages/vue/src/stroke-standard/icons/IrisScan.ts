import { createVueIcon } from '../../createVueIcon';
import { IrisScan as iconData } from '@dga-icons/core/stroke-standard';

const IrisScan = createVueIcon('iris-scan', iconData);

export default IrisScan;

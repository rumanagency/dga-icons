import { createVueIcon } from '../../createVueIcon';
import { Teacher as iconData } from '@dga-icons/core/stroke-standard';

const Teacher = createVueIcon('teacher', iconData);

export default Teacher;

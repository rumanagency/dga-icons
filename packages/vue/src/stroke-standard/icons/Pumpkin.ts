import { createVueIcon } from '../../createVueIcon';
import { Pumpkin as iconData } from '@dga-icons/core/stroke-standard';

const Pumpkin = createVueIcon('pumpkin', iconData);

export default Pumpkin;

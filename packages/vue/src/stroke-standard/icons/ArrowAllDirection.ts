import { createVueIcon } from '../../createVueIcon';
import { ArrowAllDirection as iconData } from '@dga-icons/core/stroke-standard';

const ArrowAllDirection = createVueIcon('arrow-all-direction', iconData);

export default ArrowAllDirection;

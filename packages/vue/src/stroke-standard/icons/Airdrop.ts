import { createVueIcon } from '../../createVueIcon';
import { Airdrop as iconData } from '@dga-icons/core/stroke-standard';

const Airdrop = createVueIcon('airdrop', iconData);

export default Airdrop;

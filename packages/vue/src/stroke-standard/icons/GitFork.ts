import { createVueIcon } from '../../createVueIcon';
import { GitFork as iconData } from '@dga-icons/core/stroke-standard';

const GitFork = createVueIcon('git-fork', iconData);

export default GitFork;

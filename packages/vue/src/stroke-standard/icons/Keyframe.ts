import { createVueIcon } from '../../createVueIcon';
import { Keyframe as iconData } from '@dga-icons/core/stroke-standard';

const Keyframe = createVueIcon('keyframe', iconData);

export default Keyframe;

import { createVueIcon } from '../../createVueIcon';
import { Pokeball as iconData } from '@dga-icons/core/stroke-standard';

const Pokeball = createVueIcon('pokeball', iconData);

export default Pokeball;

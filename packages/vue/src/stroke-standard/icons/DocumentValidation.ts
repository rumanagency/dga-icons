import { createVueIcon } from '../../createVueIcon';
import { DocumentValidation as iconData } from '@dga-icons/core/stroke-standard';

const DocumentValidation = createVueIcon('document-validation', iconData);

export default DocumentValidation;

import { createVueIcon } from '../../createVueIcon';
import { BitcoinCloud as iconData } from '@dga-icons/core/stroke-standard';

const BitcoinCloud = createVueIcon('bitcoin-cloud', iconData);

export default BitcoinCloud;

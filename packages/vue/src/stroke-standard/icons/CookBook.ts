import { createVueIcon } from '../../createVueIcon';
import { CookBook as iconData } from '@dga-icons/core/stroke-standard';

const CookBook = createVueIcon('cook-book', iconData);

export default CookBook;

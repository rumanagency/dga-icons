import { createVueIcon } from '../../createVueIcon';
import { LimitOrder as iconData } from '@dga-icons/core/stroke-standard';

const LimitOrder = createVueIcon('limit-order', iconData);

export default LimitOrder;

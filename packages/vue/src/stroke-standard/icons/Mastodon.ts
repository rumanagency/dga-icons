import { createVueIcon } from '../../createVueIcon';
import { Mastodon as iconData } from '@dga-icons/core/stroke-standard';

const Mastodon = createVueIcon('mastodon', iconData);

export default Mastodon;

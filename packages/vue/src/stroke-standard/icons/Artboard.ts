import { createVueIcon } from '../../createVueIcon';
import { Artboard as iconData } from '@dga-icons/core/stroke-standard';

const Artboard = createVueIcon('artboard', iconData);

export default Artboard;

import { createVueIcon } from '../../createVueIcon';
import { FileAdd as iconData } from '@dga-icons/core/stroke-standard';

const FileAdd = createVueIcon('file-add', iconData);

export default FileAdd;

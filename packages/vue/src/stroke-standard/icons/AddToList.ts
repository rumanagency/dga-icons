import { createVueIcon } from '../../createVueIcon';
import { AddToList as iconData } from '@dga-icons/core/stroke-standard';

const AddToList = createVueIcon('add-to-list', iconData);

export default AddToList;

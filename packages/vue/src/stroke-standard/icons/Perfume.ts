import { createVueIcon } from '../../createVueIcon';
import { Perfume as iconData } from '@dga-icons/core/stroke-standard';

const Perfume = createVueIcon('perfume', iconData);

export default Perfume;

import { createVueIcon } from '../../createVueIcon';
import { Heading as iconData } from '@dga-icons/core/stroke-standard';

const Heading = createVueIcon('heading', iconData);

export default Heading;

import { createVueIcon } from '../../createVueIcon';
import { Constellation as iconData } from '@dga-icons/core/stroke-standard';

const Constellation = createVueIcon('constellation', iconData);

export default Constellation;

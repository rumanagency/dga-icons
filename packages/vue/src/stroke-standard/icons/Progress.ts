import { createVueIcon } from '../../createVueIcon';
import { Progress as iconData } from '@dga-icons/core/stroke-standard';

const Progress = createVueIcon('progress', iconData);

export default Progress;

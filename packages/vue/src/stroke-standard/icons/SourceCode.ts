import { createVueIcon } from '../../createVueIcon';
import { SourceCode as iconData } from '@dga-icons/core/stroke-standard';

const SourceCode = createVueIcon('source-code', iconData);

export default SourceCode;

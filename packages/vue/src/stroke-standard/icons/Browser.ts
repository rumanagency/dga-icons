import { createVueIcon } from '../../createVueIcon';
import { Browser as iconData } from '@dga-icons/core/stroke-standard';

const Browser = createVueIcon('browser', iconData);

export default Browser;

import { createVueIcon } from '../../createVueIcon';
import { GoogleMaps as iconData } from '@dga-icons/core/stroke-standard';

const GoogleMaps = createVueIcon('google-maps', iconData);

export default GoogleMaps;

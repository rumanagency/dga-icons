import { createVueIcon } from '../../createVueIcon';
import { Neutral as iconData } from '@dga-icons/core/stroke-standard';

const Neutral = createVueIcon('neutral', iconData);

export default Neutral;

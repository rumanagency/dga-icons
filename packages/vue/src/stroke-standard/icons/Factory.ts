import { createVueIcon } from '../../createVueIcon';
import { Factory as iconData } from '@dga-icons/core/stroke-standard';

const Factory = createVueIcon('factory', iconData);

export default Factory;

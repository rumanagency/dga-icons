import { createVueIcon } from '../../createVueIcon';
import { RemoteControl as iconData } from '@dga-icons/core/stroke-standard';

const RemoteControl = createVueIcon('remote-control', iconData);

export default RemoteControl;

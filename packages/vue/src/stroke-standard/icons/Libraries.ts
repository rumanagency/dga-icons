import { createVueIcon } from '../../createVueIcon';
import { Libraries as iconData } from '@dga-icons/core/stroke-standard';

const Libraries = createVueIcon('libraries', iconData);

export default Libraries;

import { createVueIcon } from '../../createVueIcon';
import { MoveRight as iconData } from '@dga-icons/core/stroke-standard';

const MoveRight = createVueIcon('move-right', iconData);

export default MoveRight;

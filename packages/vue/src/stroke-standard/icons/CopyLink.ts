import { createVueIcon } from '../../createVueIcon';
import { CopyLink as iconData } from '@dga-icons/core/stroke-standard';

const CopyLink = createVueIcon('copy-link', iconData);

export default CopyLink;

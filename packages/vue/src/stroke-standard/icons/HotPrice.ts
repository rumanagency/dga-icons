import { createVueIcon } from '../../createVueIcon';
import { HotPrice as iconData } from '@dga-icons/core/stroke-standard';

const HotPrice = createVueIcon('hot-price', iconData);

export default HotPrice;

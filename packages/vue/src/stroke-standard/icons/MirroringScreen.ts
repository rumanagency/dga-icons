import { createVueIcon } from '../../createVueIcon';
import { MirroringScreen as iconData } from '@dga-icons/core/stroke-standard';

const MirroringScreen = createVueIcon('mirroring-screen', iconData);

export default MirroringScreen;

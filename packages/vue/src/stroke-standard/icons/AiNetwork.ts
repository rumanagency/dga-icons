import { createVueIcon } from '../../createVueIcon';
import { AiNetwork as iconData } from '@dga-icons/core/stroke-standard';

const AiNetwork = createVueIcon('ai-network', iconData);

export default AiNetwork;

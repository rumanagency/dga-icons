import { createVueIcon } from '../../createVueIcon';
import { Popcorn as iconData } from '@dga-icons/core/stroke-standard';

const Popcorn = createVueIcon('popcorn', iconData);

export default Popcorn;

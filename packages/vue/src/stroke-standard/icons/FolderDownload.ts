import { createVueIcon } from '../../createVueIcon';
import { FolderDownload as iconData } from '@dga-icons/core/stroke-standard';

const FolderDownload = createVueIcon('folder-download', iconData);

export default FolderDownload;

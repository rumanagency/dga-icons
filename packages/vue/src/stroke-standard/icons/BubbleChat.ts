import { createVueIcon } from '../../createVueIcon';
import { BubbleChat as iconData } from '@dga-icons/core/stroke-standard';

const BubbleChat = createVueIcon('bubble-chat', iconData);

export default BubbleChat;

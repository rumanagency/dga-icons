import { createVueIcon } from '../../createVueIcon';
import { ZoomSquare as iconData } from '@dga-icons/core/stroke-standard';

const ZoomSquare = createVueIcon('zoom-square', iconData);

export default ZoomSquare;

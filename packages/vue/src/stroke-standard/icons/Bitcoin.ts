import { createVueIcon } from '../../createVueIcon';
import { Bitcoin as iconData } from '@dga-icons/core/stroke-standard';

const Bitcoin = createVueIcon('bitcoin', iconData);

export default Bitcoin;

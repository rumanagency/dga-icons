import { createVueIcon } from '../../createVueIcon';
import { Console as iconData } from '@dga-icons/core/stroke-standard';

const Console = createVueIcon('console', iconData);

export default Console;

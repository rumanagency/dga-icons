import { createVueIcon } from '../../createVueIcon';
import { Perspective as iconData } from '@dga-icons/core/stroke-standard';

const Perspective = createVueIcon('perspective', iconData);

export default Perspective;

import { createVueIcon } from '../../createVueIcon';
import { FloppyDisk as iconData } from '@dga-icons/core/stroke-standard';

const FloppyDisk = createVueIcon('floppy-disk', iconData);

export default FloppyDisk;

import { createVueIcon } from '../../createVueIcon';
import { SoilTemperatureGlobal as iconData } from '@dga-icons/core/stroke-standard';

const SoilTemperatureGlobal = createVueIcon('soil-temperature-global', iconData);

export default SoilTemperatureGlobal;

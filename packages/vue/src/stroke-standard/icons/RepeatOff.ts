import { createVueIcon } from '../../createVueIcon';
import { RepeatOff as iconData } from '@dga-icons/core/stroke-standard';

const RepeatOff = createVueIcon('repeat-off', iconData);

export default RepeatOff;

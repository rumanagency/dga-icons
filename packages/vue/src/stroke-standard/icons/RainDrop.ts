import { createVueIcon } from '../../createVueIcon';
import { RainDrop as iconData } from '@dga-icons/core/stroke-standard';

const RainDrop = createVueIcon('rain-drop', iconData);

export default RainDrop;

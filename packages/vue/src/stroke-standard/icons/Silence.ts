import { createVueIcon } from '../../createVueIcon';
import { Silence as iconData } from '@dga-icons/core/stroke-standard';

const Silence = createVueIcon('silence', iconData);

export default Silence;

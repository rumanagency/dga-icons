import { createVueIcon } from '../../createVueIcon';
import { Camera01 as iconData } from '@dga-icons/core/stroke-standard';

const Camera01 = createVueIcon('camera-01', iconData);

export default Camera01;

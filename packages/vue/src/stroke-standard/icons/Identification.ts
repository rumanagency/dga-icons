import { createVueIcon } from '../../createVueIcon';
import { Identification as iconData } from '@dga-icons/core/stroke-standard';

const Identification = createVueIcon('identification', iconData);

export default Identification;

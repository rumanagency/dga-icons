import { createVueIcon } from '../../createVueIcon';
import { Cashier as iconData } from '@dga-icons/core/stroke-standard';

const Cashier = createVueIcon('cashier', iconData);

export default Cashier;

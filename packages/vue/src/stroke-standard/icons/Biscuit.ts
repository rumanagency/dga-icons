import { createVueIcon } from '../../createVueIcon';
import { Biscuit as iconData } from '@dga-icons/core/stroke-standard';

const Biscuit = createVueIcon('biscuit', iconData);

export default Biscuit;

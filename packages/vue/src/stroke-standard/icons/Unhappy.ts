import { createVueIcon } from '../../createVueIcon';
import { Unhappy as iconData } from '@dga-icons/core/stroke-standard';

const Unhappy = createVueIcon('unhappy', iconData);

export default Unhappy;

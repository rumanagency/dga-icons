import { createVueIcon } from '../../createVueIcon';
import { BitcoinEye as iconData } from '@dga-icons/core/stroke-standard';

const BitcoinEye = createVueIcon('bitcoin-eye', iconData);

export default BitcoinEye;

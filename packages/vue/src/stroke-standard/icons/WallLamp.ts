import { createVueIcon } from '../../createVueIcon';
import { WallLamp as iconData } from '@dga-icons/core/stroke-standard';

const WallLamp = createVueIcon('wall-lamp', iconData);

export default WallLamp;

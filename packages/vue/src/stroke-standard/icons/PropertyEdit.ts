import { createVueIcon } from '../../createVueIcon';
import { PropertyEdit as iconData } from '@dga-icons/core/stroke-standard';

const PropertyEdit = createVueIcon('property-edit', iconData);

export default PropertyEdit;

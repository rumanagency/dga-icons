import { createVueIcon } from '../../createVueIcon';
import { SidebarLeft as iconData } from '@dga-icons/core/stroke-standard';

const SidebarLeft = createVueIcon('sidebar-left', iconData);

export default SidebarLeft;

import { createVueIcon } from '../../createVueIcon';
import { Pyramid as iconData } from '@dga-icons/core/stroke-standard';

const Pyramid = createVueIcon('pyramid', iconData);

export default Pyramid;

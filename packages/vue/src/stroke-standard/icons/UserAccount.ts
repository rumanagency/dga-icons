import { createVueIcon } from '../../createVueIcon';
import { UserAccount as iconData } from '@dga-icons/core/stroke-standard';

const UserAccount = createVueIcon('user-account', iconData);

export default UserAccount;

import { createVueIcon } from '../../createVueIcon';
import { CallDone as iconData } from '@dga-icons/core/stroke-standard';

const CallDone = createVueIcon('call-done', iconData);

export default CallDone;

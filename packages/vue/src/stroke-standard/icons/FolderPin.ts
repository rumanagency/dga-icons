import { createVueIcon } from '../../createVueIcon';
import { FolderPin as iconData } from '@dga-icons/core/stroke-standard';

const FolderPin = createVueIcon('folder-pin', iconData);

export default FolderPin;

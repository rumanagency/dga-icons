import { createVueIcon } from '../../createVueIcon';
import { Gameboy as iconData } from '@dga-icons/core/stroke-standard';

const Gameboy = createVueIcon('gameboy', iconData);

export default Gameboy;

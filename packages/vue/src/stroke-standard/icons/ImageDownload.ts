import { createVueIcon } from '../../createVueIcon';
import { ImageDownload as iconData } from '@dga-icons/core/stroke-standard';

const ImageDownload = createVueIcon('image-download', iconData);

export default ImageDownload;

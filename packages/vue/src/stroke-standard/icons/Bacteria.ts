import { createVueIcon } from '../../createVueIcon';
import { Bacteria as iconData } from '@dga-icons/core/stroke-standard';

const Bacteria = createVueIcon('bacteria', iconData);

export default Bacteria;

import { createVueIcon } from '../../createVueIcon';
import { Monster as iconData } from '@dga-icons/core/stroke-standard';

const Monster = createVueIcon('monster', iconData);

export default Monster;

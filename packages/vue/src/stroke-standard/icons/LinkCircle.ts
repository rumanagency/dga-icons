import { createVueIcon } from '../../createVueIcon';
import { LinkCircle as iconData } from '@dga-icons/core/stroke-standard';

const LinkCircle = createVueIcon('link-circle', iconData);

export default LinkCircle;

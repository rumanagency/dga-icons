import { createVueIcon } from '../../createVueIcon';
import { Profile as iconData } from '@dga-icons/core/stroke-standard';

const Profile = createVueIcon('profile', iconData);

export default Profile;

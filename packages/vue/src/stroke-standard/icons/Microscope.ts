import { createVueIcon } from '../../createVueIcon';
import { Microscope as iconData } from '@dga-icons/core/stroke-standard';

const Microscope = createVueIcon('microscope', iconData);

export default Microscope;

import { createVueIcon } from '../../createVueIcon';
import { FolderAdd as iconData } from '@dga-icons/core/stroke-standard';

const FolderAdd = createVueIcon('folder-add', iconData);

export default FolderAdd;

import { createVueIcon } from '../../createVueIcon';
import { BitcoinKey as iconData } from '@dga-icons/core/stroke-standard';

const BitcoinKey = createVueIcon('bitcoin-key', iconData);

export default BitcoinKey;

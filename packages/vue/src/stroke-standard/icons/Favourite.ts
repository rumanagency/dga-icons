import { createVueIcon } from '../../createVueIcon';
import { Favourite as iconData } from '@dga-icons/core/stroke-standard';

const Favourite = createVueIcon('favourite', iconData);

export default Favourite;

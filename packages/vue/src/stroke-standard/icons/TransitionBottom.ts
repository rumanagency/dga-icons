import { createVueIcon } from '../../createVueIcon';
import { TransitionBottom as iconData } from '@dga-icons/core/stroke-standard';

const TransitionBottom = createVueIcon('transition-bottom', iconData);

export default TransitionBottom;

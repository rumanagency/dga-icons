import { createVueIcon } from '../../createVueIcon';
import { Campfire as iconData } from '@dga-icons/core/stroke-standard';

const Campfire = createVueIcon('campfire', iconData);

export default Campfire;

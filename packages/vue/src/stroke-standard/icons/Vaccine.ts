import { createVueIcon } from '../../createVueIcon';
import { Vaccine as iconData } from '@dga-icons/core/stroke-standard';

const Vaccine = createVueIcon('vaccine', iconData);

export default Vaccine;

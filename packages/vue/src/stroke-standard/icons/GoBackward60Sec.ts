import { createVueIcon } from '../../createVueIcon';
import { GoBackward60Sec as iconData } from '@dga-icons/core/stroke-standard';

const GoBackward60Sec = createVueIcon('go-backward-60-sec', iconData);

export default GoBackward60Sec;

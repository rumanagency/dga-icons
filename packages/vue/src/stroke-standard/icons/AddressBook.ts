import { createVueIcon } from '../../createVueIcon';
import { AddressBook as iconData } from '@dga-icons/core/stroke-standard';

const AddressBook = createVueIcon('address-book', iconData);

export default AddressBook;

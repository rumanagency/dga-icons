import { createVueIcon } from '../../createVueIcon';
import { CloudUpload as iconData } from '@dga-icons/core/stroke-standard';

const CloudUpload = createVueIcon('cloud-upload', iconData);

export default CloudUpload;

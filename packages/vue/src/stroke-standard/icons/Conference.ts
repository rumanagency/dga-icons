import { createVueIcon } from '../../createVueIcon';
import { Conference as iconData } from '@dga-icons/core/stroke-standard';

const Conference = createVueIcon('conference', iconData);

export default Conference;

import { createVueIcon } from '../../createVueIcon';
import { Stethoscope as iconData } from '@dga-icons/core/stroke-standard';

const Stethoscope = createVueIcon('stethoscope', iconData);

export default Stethoscope;

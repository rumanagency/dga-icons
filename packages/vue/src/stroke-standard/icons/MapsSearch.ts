import { createVueIcon } from '../../createVueIcon';
import { MapsSearch as iconData } from '@dga-icons/core/stroke-standard';

const MapsSearch = createVueIcon('maps-search', iconData);

export default MapsSearch;

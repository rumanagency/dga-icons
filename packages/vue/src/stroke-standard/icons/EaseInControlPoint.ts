import { createVueIcon } from '../../createVueIcon';
import { EaseInControlPoint as iconData } from '@dga-icons/core/stroke-standard';

const EaseInControlPoint = createVueIcon('ease-in-control-point', iconData);

export default EaseInControlPoint;

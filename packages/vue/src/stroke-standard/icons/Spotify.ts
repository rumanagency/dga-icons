import { createVueIcon } from '../../createVueIcon';
import { Spotify as iconData } from '@dga-icons/core/stroke-standard';

const Spotify = createVueIcon('spotify', iconData);

export default Spotify;

import { createVueIcon } from '../../createVueIcon';
import { StructureCheck as iconData } from '@dga-icons/core/stroke-standard';

const StructureCheck = createVueIcon('structure-check', iconData);

export default StructureCheck;

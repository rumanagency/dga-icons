import { createVueIcon } from '../../createVueIcon';
import { SidebarTop as iconData } from '@dga-icons/core/stroke-standard';

const SidebarTop = createVueIcon('sidebar-top', iconData);

export default SidebarTop;

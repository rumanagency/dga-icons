import { createVueIcon } from '../../createVueIcon';
import { UserSquare as iconData } from '@dga-icons/core/stroke-standard';

const UserSquare = createVueIcon('user-square', iconData);

export default UserSquare;

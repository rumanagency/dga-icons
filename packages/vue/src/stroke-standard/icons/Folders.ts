import { createVueIcon } from '../../createVueIcon';
import { Folders as iconData } from '@dga-icons/core/stroke-standard';

const Folders = createVueIcon('folders', iconData);

export default Folders;

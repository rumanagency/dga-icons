import { createVueIcon } from '../../createVueIcon';
import { Curtains as iconData } from '@dga-icons/core/stroke-standard';

const Curtains = createVueIcon('curtains', iconData);

export default Curtains;

import { createVueIcon } from '../../createVueIcon';
import { Microsoft as iconData } from '@dga-icons/core/stroke-standard';

const Microsoft = createVueIcon('microsoft', iconData);

export default Microsoft;

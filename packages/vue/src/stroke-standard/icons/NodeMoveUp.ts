import { createVueIcon } from '../../createVueIcon';
import { NodeMoveUp as iconData } from '@dga-icons/core/stroke-standard';

const NodeMoveUp = createVueIcon('node-move-up', iconData);

export default NodeMoveUp;

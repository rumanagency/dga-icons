import { createVueIcon } from '../../createVueIcon';
import { Refresh as iconData } from '@dga-icons/core/stroke-standard';

const Refresh = createVueIcon('refresh', iconData);

export default Refresh;

import { createVueIcon } from '../../createVueIcon';
import { Blocked as iconData } from '@dga-icons/core/stroke-standard';

const Blocked = createVueIcon('blocked', iconData);

export default Blocked;

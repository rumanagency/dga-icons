import { createVueIcon } from '../../createVueIcon';
import { Unlink05 as iconData } from '@dga-icons/core/stroke-standard';

const Unlink05 = createVueIcon('unlink-05', iconData);

export default Unlink05;

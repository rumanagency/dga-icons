import { createVueIcon } from '../../createVueIcon';
import { MarketAnalysis as iconData } from '@dga-icons/core/stroke-standard';

const MarketAnalysis = createVueIcon('market-analysis', iconData);

export default MarketAnalysis;

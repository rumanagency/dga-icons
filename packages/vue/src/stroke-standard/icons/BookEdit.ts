import { createVueIcon } from '../../createVueIcon';
import { BookEdit as iconData } from '@dga-icons/core/stroke-standard';

const BookEdit = createVueIcon('book-edit', iconData);

export default BookEdit;

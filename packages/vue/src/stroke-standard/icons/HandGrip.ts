import { createVueIcon } from '../../createVueIcon';
import { HandGrip as iconData } from '@dga-icons/core/stroke-standard';

const HandGrip = createVueIcon('hand-grip', iconData);

export default HandGrip;

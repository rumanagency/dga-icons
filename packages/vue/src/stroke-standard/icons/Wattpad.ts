import { createVueIcon } from '../../createVueIcon';
import { Wattpad as iconData } from '@dga-icons/core/stroke-standard';

const Wattpad = createVueIcon('wattpad', iconData);

export default Wattpad;

import { createVueIcon } from '../../createVueIcon';
import { Glasses as iconData } from '@dga-icons/core/stroke-standard';

const Glasses = createVueIcon('glasses', iconData);

export default Glasses;

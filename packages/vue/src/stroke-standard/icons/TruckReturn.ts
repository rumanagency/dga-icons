import { createVueIcon } from '../../createVueIcon';
import { TruckReturn as iconData } from '@dga-icons/core/stroke-standard';

const TruckReturn = createVueIcon('truck-return', iconData);

export default TruckReturn;

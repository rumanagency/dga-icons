import { createVueIcon } from '../../createVueIcon';
import { Android as iconData } from '@dga-icons/core/stroke-standard';

const Android = createVueIcon('android', iconData);

export default Android;

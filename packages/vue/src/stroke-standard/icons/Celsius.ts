import { createVueIcon } from '../../createVueIcon';
import { Celsius as iconData } from '@dga-icons/core/stroke-standard';

const Celsius = createVueIcon('celsius', iconData);

export default Celsius;

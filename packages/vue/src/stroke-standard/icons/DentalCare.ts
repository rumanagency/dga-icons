import { createVueIcon } from '../../createVueIcon';
import { DentalCare as iconData } from '@dga-icons/core/stroke-standard';

const DentalCare = createVueIcon('dental-care', iconData);

export default DentalCare;

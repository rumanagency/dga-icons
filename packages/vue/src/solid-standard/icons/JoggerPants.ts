import { createVueIcon } from '../../createVueIcon';
import { JoggerPants as iconData } from '@dga-icons/core/solid-standard';

const JoggerPants = createVueIcon('jogger-pants', iconData);

export default JoggerPants;

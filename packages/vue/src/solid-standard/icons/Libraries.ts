import { createVueIcon } from '../../createVueIcon';
import { Libraries as iconData } from '@dga-icons/core/solid-standard';

const Libraries = createVueIcon('libraries', iconData);

export default Libraries;

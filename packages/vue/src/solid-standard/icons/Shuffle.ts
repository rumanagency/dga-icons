import { createVueIcon } from '../../createVueIcon';
import { Shuffle as iconData } from '@dga-icons/core/solid-standard';

const Shuffle = createVueIcon('shuffle', iconData);

export default Shuffle;

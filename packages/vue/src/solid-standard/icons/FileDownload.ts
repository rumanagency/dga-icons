import { createVueIcon } from '../../createVueIcon';
import { FileDownload as iconData } from '@dga-icons/core/solid-standard';

const FileDownload = createVueIcon('file-download', iconData);

export default FileDownload;

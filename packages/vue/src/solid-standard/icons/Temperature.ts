import { createVueIcon } from '../../createVueIcon';
import { Temperature as iconData } from '@dga-icons/core/solid-standard';

const Temperature = createVueIcon('temperature', iconData);

export default Temperature;

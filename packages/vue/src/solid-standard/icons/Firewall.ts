import { createVueIcon } from '../../createVueIcon';
import { Firewall as iconData } from '@dga-icons/core/solid-standard';

const Firewall = createVueIcon('firewall', iconData);

export default Firewall;

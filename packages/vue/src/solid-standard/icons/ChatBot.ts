import { createVueIcon } from '../../createVueIcon';
import { ChatBot as iconData } from '@dga-icons/core/solid-standard';

const ChatBot = createVueIcon('chat-bot', iconData);

export default ChatBot;

import { createVueIcon } from '../../createVueIcon';
import { GitMerge as iconData } from '@dga-icons/core/solid-standard';

const GitMerge = createVueIcon('git-merge', iconData);

export default GitMerge;

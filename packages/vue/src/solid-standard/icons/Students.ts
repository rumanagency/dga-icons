import { createVueIcon } from '../../createVueIcon';
import { Students as iconData } from '@dga-icons/core/solid-standard';

const Students = createVueIcon('students', iconData);

export default Students;

import { createVueIcon } from '../../createVueIcon';
import { StraightEdge as iconData } from '@dga-icons/core/solid-standard';

const StraightEdge = createVueIcon('straight-edge', iconData);

export default StraightEdge;

import { createVueIcon } from '../../createVueIcon';
import { Background as iconData } from '@dga-icons/core/solid-standard';

const Background = createVueIcon('background', iconData);

export default Background;

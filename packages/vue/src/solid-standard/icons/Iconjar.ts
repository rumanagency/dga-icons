import { createVueIcon } from '../../createVueIcon';
import { Iconjar as iconData } from '@dga-icons/core/solid-standard';

const Iconjar = createVueIcon('iconjar', iconData);

export default Iconjar;

import { createVueIcon } from '../../createVueIcon';
import { Folders as iconData } from '@dga-icons/core/solid-standard';

const Folders = createVueIcon('folders', iconData);

export default Folders;

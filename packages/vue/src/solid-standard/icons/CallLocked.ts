import { createVueIcon } from '../../createVueIcon';
import { CallLocked as iconData } from '@dga-icons/core/solid-standard';

const CallLocked = createVueIcon('call-locked', iconData);

export default CallLocked;

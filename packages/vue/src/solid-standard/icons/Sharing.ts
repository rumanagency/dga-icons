import { createVueIcon } from '../../createVueIcon';
import { Sharing as iconData } from '@dga-icons/core/solid-standard';

const Sharing = createVueIcon('sharing', iconData);

export default Sharing;

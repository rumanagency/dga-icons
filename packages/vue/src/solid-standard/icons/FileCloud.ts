import { createVueIcon } from '../../createVueIcon';
import { FileCloud as iconData } from '@dga-icons/core/solid-standard';

const FileCloud = createVueIcon('file-cloud', iconData);

export default FileCloud;

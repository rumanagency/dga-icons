import { createVueIcon } from '../../createVueIcon';
import { VolumeHigh as iconData } from '@dga-icons/core/solid-standard';

const VolumeHigh = createVueIcon('volume-high', iconData);

export default VolumeHigh;

import { createVueIcon } from '../../createVueIcon';
import { Percent as iconData } from '@dga-icons/core/solid-standard';

const Percent = createVueIcon('percent', iconData);

export default Percent;

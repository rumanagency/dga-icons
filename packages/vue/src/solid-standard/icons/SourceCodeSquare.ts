import { createVueIcon } from '../../createVueIcon';
import { SourceCodeSquare as iconData } from '@dga-icons/core/solid-standard';

const SourceCodeSquare = createVueIcon('source-code-square', iconData);

export default SourceCodeSquare;

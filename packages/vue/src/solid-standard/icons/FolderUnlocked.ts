import { createVueIcon } from '../../createVueIcon';
import { FolderUnlocked as iconData } from '@dga-icons/core/solid-standard';

const FolderUnlocked = createVueIcon('folder-unlocked', iconData);

export default FolderUnlocked;

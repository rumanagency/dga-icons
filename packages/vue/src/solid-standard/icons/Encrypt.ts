import { createVueIcon } from '../../createVueIcon';
import { Encrypt as iconData } from '@dga-icons/core/solid-standard';

const Encrypt = createVueIcon('encrypt', iconData);

export default Encrypt;

import { createVueIcon } from '../../createVueIcon';
import { ComputerSettings as iconData } from '@dga-icons/core/solid-standard';

const ComputerSettings = createVueIcon('computer-settings', iconData);

export default ComputerSettings;

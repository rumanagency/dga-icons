import { createVueIcon } from '../../createVueIcon';
import { FileBlock as iconData } from '@dga-icons/core/solid-standard';

const FileBlock = createVueIcon('file-block', iconData);

export default FileBlock;

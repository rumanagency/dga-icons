import { createVueIcon } from '../../createVueIcon';
import { Calculate as iconData } from '@dga-icons/core/solid-standard';

const Calculate = createVueIcon('calculate', iconData);

export default Calculate;

import { createVueIcon } from '../../createVueIcon';
import { ClosedCaption as iconData } from '@dga-icons/core/solid-standard';

const ClosedCaption = createVueIcon('closed-caption', iconData);

export default ClosedCaption;

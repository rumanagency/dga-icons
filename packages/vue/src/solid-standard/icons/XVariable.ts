import { createVueIcon } from '../../createVueIcon';
import { XVariable as iconData } from '@dga-icons/core/solid-standard';

const XVariable = createVueIcon('x-variable', iconData);

export default XVariable;

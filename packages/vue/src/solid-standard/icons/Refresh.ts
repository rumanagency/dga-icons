import { createVueIcon } from '../../createVueIcon';
import { Refresh as iconData } from '@dga-icons/core/solid-standard';

const Refresh = createVueIcon('refresh', iconData);

export default Refresh;

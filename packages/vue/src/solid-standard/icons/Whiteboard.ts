import { createVueIcon } from '../../createVueIcon';
import { Whiteboard as iconData } from '@dga-icons/core/solid-standard';

const Whiteboard = createVueIcon('whiteboard', iconData);

export default Whiteboard;

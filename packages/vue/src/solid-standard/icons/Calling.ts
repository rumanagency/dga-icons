import { createVueIcon } from '../../createVueIcon';
import { Calling as iconData } from '@dga-icons/core/solid-standard';

const Calling = createVueIcon('calling', iconData);

export default Calling;

import { createVueIcon } from '../../createVueIcon';
import { PlayList as iconData } from '@dga-icons/core/solid-standard';

const PlayList = createVueIcon('play-list', iconData);

export default PlayList;

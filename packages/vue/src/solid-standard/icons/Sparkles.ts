import { createVueIcon } from '../../createVueIcon';
import { Sparkles as iconData } from '@dga-icons/core/solid-standard';

const Sparkles = createVueIcon('sparkles', iconData);

export default Sparkles;

import { createVueIcon } from '../../createVueIcon';
import { CoinsEuro as iconData } from '@dga-icons/core/solid-standard';

const CoinsEuro = createVueIcon('coins-euro', iconData);

export default CoinsEuro;

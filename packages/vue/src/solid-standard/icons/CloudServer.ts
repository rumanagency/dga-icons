import { createVueIcon } from '../../createVueIcon';
import { CloudServer as iconData } from '@dga-icons/core/solid-standard';

const CloudServer = createVueIcon('cloud-server', iconData);

export default CloudServer;

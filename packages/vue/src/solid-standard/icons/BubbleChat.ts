import { createVueIcon } from '../../createVueIcon';
import { BubbleChat as iconData } from '@dga-icons/core/solid-standard';

const BubbleChat = createVueIcon('bubble-chat', iconData);

export default BubbleChat;

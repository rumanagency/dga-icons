import { createVueIcon } from '../../createVueIcon';
import { Surprise as iconData } from '@dga-icons/core/solid-standard';

const Surprise = createVueIcon('surprise', iconData);

export default Surprise;

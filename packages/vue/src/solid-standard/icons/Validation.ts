import { createVueIcon } from '../../createVueIcon';
import { Validation as iconData } from '@dga-icons/core/solid-standard';

const Validation = createVueIcon('validation', iconData);

export default Validation;

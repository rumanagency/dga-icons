import { createVueIcon } from '../../createVueIcon';
import { Rainbow as iconData } from '@dga-icons/core/solid-standard';

const Rainbow = createVueIcon('rainbow', iconData);

export default Rainbow;

import { createVueIcon } from '../../createVueIcon';
import { TapeMeasure as iconData } from '@dga-icons/core/solid-standard';

const TapeMeasure = createVueIcon('tape-measure', iconData);

export default TapeMeasure;

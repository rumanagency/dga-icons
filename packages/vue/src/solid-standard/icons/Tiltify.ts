import { createVueIcon } from '../../createVueIcon';
import { Tiltify as iconData } from '@dga-icons/core/solid-standard';

const Tiltify = createVueIcon('tiltify', iconData);

export default Tiltify;

import { createVueIcon } from '../../createVueIcon';
import { Blender as iconData } from '@dga-icons/core/solid-standard';

const Blender = createVueIcon('blender', iconData);

export default Blender;

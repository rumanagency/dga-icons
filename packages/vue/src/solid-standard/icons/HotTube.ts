import { createVueIcon } from '../../createVueIcon';
import { HotTube as iconData } from '@dga-icons/core/solid-standard';

const HotTube = createVueIcon('hot-tube', iconData);

export default HotTube;

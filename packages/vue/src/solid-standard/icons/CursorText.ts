import { createVueIcon } from '../../createVueIcon';
import { CursorText as iconData } from '@dga-icons/core/solid-standard';

const CursorText = createVueIcon('cursor-text', iconData);

export default CursorText;

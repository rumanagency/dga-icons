import { createVueIcon } from '../../createVueIcon';
import { GoogleDoc as iconData } from '@dga-icons/core/solid-standard';

const GoogleDoc = createVueIcon('google-doc', iconData);

export default GoogleDoc;

import { createVueIcon } from '../../createVueIcon';
import { PackageSent as iconData } from '@dga-icons/core/solid-standard';

const PackageSent = createVueIcon('package-sent', iconData);

export default PackageSent;

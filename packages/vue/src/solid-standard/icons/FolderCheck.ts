import { createVueIcon } from '../../createVueIcon';
import { FolderCheck as iconData } from '@dga-icons/core/solid-standard';

const FolderCheck = createVueIcon('folder-check', iconData);

export default FolderCheck;

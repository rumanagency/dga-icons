import { createVueIcon } from '../../createVueIcon';
import { VolumeOff as iconData } from '@dga-icons/core/solid-standard';

const VolumeOff = createVueIcon('volume-off', iconData);

export default VolumeOff;

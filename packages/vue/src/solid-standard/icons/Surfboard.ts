import { createVueIcon } from '../../createVueIcon';
import { Surfboard as iconData } from '@dga-icons/core/solid-standard';

const Surfboard = createVueIcon('surfboard', iconData);

export default Surfboard;

import { createVueIcon } from '../../createVueIcon';
import { HierarchyFiles as iconData } from '@dga-icons/core/solid-standard';

const HierarchyFiles = createVueIcon('hierarchy-files', iconData);

export default HierarchyFiles;

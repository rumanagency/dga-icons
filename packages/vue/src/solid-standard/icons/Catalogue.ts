import { createVueIcon } from '../../createVueIcon';
import { Catalogue as iconData } from '@dga-icons/core/solid-standard';

const Catalogue = createVueIcon('catalogue', iconData);

export default Catalogue;

import { createVueIcon } from '../../createVueIcon';
import { Highlighter as iconData } from '@dga-icons/core/solid-standard';

const Highlighter = createVueIcon('highlighter', iconData);

export default Highlighter;

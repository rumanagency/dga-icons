import { createVueIcon } from '../../createVueIcon';
import { Library as iconData } from '@dga-icons/core/solid-standard';

const Library = createVueIcon('library', iconData);

export default Library;

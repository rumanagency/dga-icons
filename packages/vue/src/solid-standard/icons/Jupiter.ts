import { createVueIcon } from '../../createVueIcon';
import { Jupiter as iconData } from '@dga-icons/core/solid-standard';

const Jupiter = createVueIcon('jupiter', iconData);

export default Jupiter;

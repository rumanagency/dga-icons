import { createVueIcon } from '../../createVueIcon';
import { Mailbox as iconData } from '@dga-icons/core/solid-standard';

const Mailbox = createVueIcon('mailbox', iconData);

export default Mailbox;

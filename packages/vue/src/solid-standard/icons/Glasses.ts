import { createVueIcon } from '../../createVueIcon';
import { Glasses as iconData } from '@dga-icons/core/solid-standard';

const Glasses = createVueIcon('glasses', iconData);

export default Glasses;

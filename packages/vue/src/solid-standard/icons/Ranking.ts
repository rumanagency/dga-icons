import { createVueIcon } from '../../createVueIcon';
import { Ranking as iconData } from '@dga-icons/core/solid-standard';

const Ranking = createVueIcon('ranking', iconData);

export default Ranking;

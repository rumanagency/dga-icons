import { createVueIcon } from '../../createVueIcon';
import { BoundingBox as iconData } from '@dga-icons/core/solid-standard';

const BoundingBox = createVueIcon('bounding-box', iconData);

export default BoundingBox;

import { createVueIcon } from '../../createVueIcon';
import { FilePin as iconData } from '@dga-icons/core/solid-standard';

const FilePin = createVueIcon('file-pin', iconData);

export default FilePin;

import { createVueIcon } from '../../createVueIcon';
import { Passport as iconData } from '@dga-icons/core/solid-standard';

const Passport = createVueIcon('passport', iconData);

export default Passport;

import { createVueIcon } from '../../createVueIcon';
import { Contact as iconData } from '@dga-icons/core/solid-standard';

const Contact = createVueIcon('contact', iconData);

export default Contact;

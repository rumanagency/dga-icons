import { createVueIcon } from '../../createVueIcon';
import { CovidInfo as iconData } from '@dga-icons/core/solid-standard';

const CovidInfo = createVueIcon('covid-info', iconData);

export default CovidInfo;

import { createVueIcon } from '../../createVueIcon';
import { FileSync as iconData } from '@dga-icons/core/solid-standard';

const FileSync = createVueIcon('file-sync', iconData);

export default FileSync;

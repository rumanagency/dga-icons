import { createVueIcon } from '../../createVueIcon';
import { FolderLocked as iconData } from '@dga-icons/core/solid-standard';

const FolderLocked = createVueIcon('folder-locked', iconData);

export default FolderLocked;

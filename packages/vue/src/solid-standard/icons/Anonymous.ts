import { createVueIcon } from '../../createVueIcon';
import { Anonymous as iconData } from '@dga-icons/core/solid-standard';

const Anonymous = createVueIcon('anonymous', iconData);

export default Anonymous;

import { createVueIcon } from '../../createVueIcon';
import { LicensePin as iconData } from '@dga-icons/core/solid-standard';

const LicensePin = createVueIcon('license-pin', iconData);

export default LicensePin;

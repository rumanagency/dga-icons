import { createVueIcon } from '../../createVueIcon';
import { SourceCode as iconData } from '@dga-icons/core/solid-standard';

const SourceCode = createVueIcon('source-code', iconData);

export default SourceCode;

import { createVueIcon } from '../../createVueIcon';
import { DishWasher as iconData } from '@dga-icons/core/solid-standard';

const DishWasher = createVueIcon('dish-washer', iconData);

export default DishWasher;

import { createVueIcon } from '../../createVueIcon';
import { Favourite as iconData } from '@dga-icons/core/solid-standard';

const Favourite = createVueIcon('favourite', iconData);

export default Favourite;

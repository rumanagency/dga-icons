import { createVueIcon } from '../../createVueIcon';
import { Previous as iconData } from '@dga-icons/core/solid-standard';

const Previous = createVueIcon('previous', iconData);

export default Previous;

import { createVueIcon } from '../../createVueIcon';
import { PlusSign as iconData } from '@dga-icons/core/solid-standard';

const PlusSign = createVueIcon('plus-sign', iconData);

export default PlusSign;

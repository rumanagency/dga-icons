import { createVueIcon } from '../../createVueIcon';
import { FolderSearch as iconData } from '@dga-icons/core/solid-standard';

const FolderSearch = createVueIcon('folder-search', iconData);

export default FolderSearch;

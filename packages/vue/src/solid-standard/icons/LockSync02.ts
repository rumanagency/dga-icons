import { createVueIcon } from '../../createVueIcon';
import { LockSync02 as iconData } from '@dga-icons/core/solid-standard';

const LockSync02 = createVueIcon('lock-sync-02', iconData);

export default LockSync02;

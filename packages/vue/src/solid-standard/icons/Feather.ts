import { createVueIcon } from '../../createVueIcon';
import { Feather as iconData } from '@dga-icons/core/solid-standard';

const Feather = createVueIcon('feather', iconData);

export default Feather;

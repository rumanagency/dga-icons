import { createVueIcon } from '../../createVueIcon';
import { Celsius as iconData } from '@dga-icons/core/solid-standard';

const Celsius = createVueIcon('celsius', iconData);

export default Celsius;

import { createVueIcon } from '../../createVueIcon';
import { FolderExport as iconData } from '@dga-icons/core/solid-standard';

const FolderExport = createVueIcon('folder-export', iconData);

export default FolderExport;

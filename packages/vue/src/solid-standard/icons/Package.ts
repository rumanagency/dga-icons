import { createVueIcon } from '../../createVueIcon';
import { Package as iconData } from '@dga-icons/core/solid-standard';

const Package = createVueIcon('package', iconData);

export default Package;

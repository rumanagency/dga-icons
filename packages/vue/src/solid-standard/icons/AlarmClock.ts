import { createVueIcon } from '../../createVueIcon';
import { AlarmClock as iconData } from '@dga-icons/core/solid-standard';

const AlarmClock = createVueIcon('alarm-clock', iconData);

export default AlarmClock;

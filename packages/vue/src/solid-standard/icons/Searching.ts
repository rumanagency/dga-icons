import { createVueIcon } from '../../createVueIcon';
import { Searching as iconData } from '@dga-icons/core/solid-standard';

const Searching = createVueIcon('searching', iconData);

export default Searching;

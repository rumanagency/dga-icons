import { createVueIcon } from '../../createVueIcon';
import { FolderTransfer as iconData } from '@dga-icons/core/solid-standard';

const FolderTransfer = createVueIcon('folder-transfer', iconData);

export default FolderTransfer;

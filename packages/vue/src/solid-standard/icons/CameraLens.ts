import { createVueIcon } from '../../createVueIcon';
import { CameraLens as iconData } from '@dga-icons/core/solid-standard';

const CameraLens = createVueIcon('camera-lens', iconData);

export default CameraLens;

import { createVueIcon } from '../../createVueIcon';
import { Terrace as iconData } from '@dga-icons/core/solid-standard';

const Terrace = createVueIcon('terrace', iconData);

export default Terrace;

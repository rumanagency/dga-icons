import { createVueIcon } from '../../createVueIcon';
import { Diameter as iconData } from '@dga-icons/core/solid-standard';

const Diameter = createVueIcon('diameter', iconData);

export default Diameter;

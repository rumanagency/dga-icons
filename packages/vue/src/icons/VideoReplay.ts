import { createVueIcon } from '../createVueIcon';
import { VideoReplay as iconData } from '@dga-icons/core';

const VideoReplay = createVueIcon('video-replay', iconData);

export default VideoReplay;

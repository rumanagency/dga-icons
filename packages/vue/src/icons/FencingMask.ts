import { createVueIcon } from '../createVueIcon';
import { FencingMask as iconData } from '@dga-icons/core';

const FencingMask = createVueIcon('fencing-mask', iconData);

export default FencingMask;

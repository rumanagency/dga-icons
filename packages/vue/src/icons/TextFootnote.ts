import { createVueIcon } from '../createVueIcon';
import { TextFootnote as iconData } from '@dga-icons/core';

const TextFootnote = createVueIcon('text-footnote', iconData);

export default TextFootnote;

import { createVueIcon } from '../createVueIcon';
import { BitcoinGraph as iconData } from '@dga-icons/core';

const BitcoinGraph = createVueIcon('bitcoin-graph', iconData);

export default BitcoinGraph;

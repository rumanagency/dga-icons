import { createVueIcon } from '../createVueIcon';
import { BitcoinSend as iconData } from '@dga-icons/core';

const BitcoinSend = createVueIcon('bitcoin-send', iconData);

export default BitcoinSend;

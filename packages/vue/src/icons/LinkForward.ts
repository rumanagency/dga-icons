import { createVueIcon } from '../createVueIcon';
import { LinkForward as iconData } from '@dga-icons/core';

const LinkForward = createVueIcon('link-forward', iconData);

export default LinkForward;

import { createVueIcon } from '../createVueIcon';
import { CameraAutomatically01 as iconData } from '@dga-icons/core';

const CameraAutomatically01 = createVueIcon('camera-automatically-01', iconData);

export default CameraAutomatically01;

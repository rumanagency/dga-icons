import { createVueIcon } from '../createVueIcon';
import { MenuRestaurant as iconData } from '@dga-icons/core';

const MenuRestaurant = createVueIcon('menu-restaurant', iconData);

export default MenuRestaurant;

import { createVueIcon } from '../createVueIcon';
import { ChartMinimum as iconData } from '@dga-icons/core';

const ChartMinimum = createVueIcon('chart-minimum', iconData);

export default ChartMinimum;

import { createVueIcon } from '../createVueIcon';
import { StoreVerified01 as iconData } from '@dga-icons/core';

const StoreVerified01 = createVueIcon('store-verified-01', iconData);

export default StoreVerified01;

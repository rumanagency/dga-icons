import { createVueIcon } from '../createVueIcon';
import { PizzaCutter as iconData } from '@dga-icons/core';

const PizzaCutter = createVueIcon('pizza-cutter', iconData);

export default PizzaCutter;

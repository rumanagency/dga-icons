import { createVueIcon } from '../createVueIcon';
import { IdNotVerified as iconData } from '@dga-icons/core';

const IdNotVerified = createVueIcon('id-not-verified', iconData);

export default IdNotVerified;

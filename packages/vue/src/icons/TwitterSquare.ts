import { createVueIcon } from '../createVueIcon';
import { TwitterSquare as iconData } from '@dga-icons/core';

const TwitterSquare = createVueIcon('twitter-square', iconData);

export default TwitterSquare;

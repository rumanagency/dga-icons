import { createVueIcon } from '../createVueIcon';
import { ZakatBagClosed as iconData } from '@dga-icons/core';

const ZakatBagClosed = createVueIcon('zakat-bag-closed', iconData);

export default ZakatBagClosed;

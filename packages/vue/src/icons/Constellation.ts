import { createVueIcon } from '../createVueIcon';
import { Constellation as iconData } from '@dga-icons/core';

const Constellation = createVueIcon('constellation', iconData);

export default Constellation;

import { createVueIcon } from '../createVueIcon';
import { LanguageCircle as iconData } from '@dga-icons/core';

const LanguageCircle = createVueIcon('language-circle', iconData);

export default LanguageCircle;

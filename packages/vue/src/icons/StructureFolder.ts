import { createVueIcon } from '../createVueIcon';
import { StructureFolder as iconData } from '@dga-icons/core';

const StructureFolder = createVueIcon('structure-folder', iconData);

export default StructureFolder;

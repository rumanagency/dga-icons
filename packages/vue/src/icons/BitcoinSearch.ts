import { createVueIcon } from '../createVueIcon';
import { BitcoinSearch as iconData } from '@dga-icons/core';

const BitcoinSearch = createVueIcon('bitcoin-search', iconData);

export default BitcoinSearch;

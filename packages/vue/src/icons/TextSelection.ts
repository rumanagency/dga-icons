import { createVueIcon } from '../createVueIcon';
import { TextSelection as iconData } from '@dga-icons/core';

const TextSelection = createVueIcon('text-selection', iconData);

export default TextSelection;

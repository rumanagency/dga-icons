import { createVueIcon } from '../createVueIcon';
import { MoreOrLessCircle as iconData } from '@dga-icons/core';

const MoreOrLessCircle = createVueIcon('more-or-less-circle', iconData);

export default MoreOrLessCircle;

import { createVueIcon } from '../createVueIcon';
import { DatabaseExport as iconData } from '@dga-icons/core';

const DatabaseExport = createVueIcon('database-export', iconData);

export default DatabaseExport;

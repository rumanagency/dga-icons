import { createVueIcon } from '../createVueIcon';
import { SearchMinus as iconData } from '@dga-icons/core';

const SearchMinus = createVueIcon('search-minus', iconData);

export default SearchMinus;

import { createVueIcon } from '../createVueIcon';
import { WifiFullSignal as iconData } from '@dga-icons/core';

const WifiFullSignal = createVueIcon('wifi-full-signal', iconData);

export default WifiFullSignal;

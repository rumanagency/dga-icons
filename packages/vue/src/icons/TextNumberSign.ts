import { createVueIcon } from '../createVueIcon';
import { TextNumberSign as iconData } from '@dga-icons/core';

const TextNumberSign = createVueIcon('text-number-sign', iconData);

export default TextNumberSign;

import { createVueIcon } from '../createVueIcon';
import { ServingFood as iconData } from '@dga-icons/core';

const ServingFood = createVueIcon('serving-food', iconData);

export default ServingFood;

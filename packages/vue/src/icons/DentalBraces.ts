import { createVueIcon } from '../createVueIcon';
import { DentalBraces as iconData } from '@dga-icons/core';

const DentalBraces = createVueIcon('dental-braces', iconData);

export default DentalBraces;

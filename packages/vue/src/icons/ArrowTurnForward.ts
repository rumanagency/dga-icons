import { createVueIcon } from '../createVueIcon';
import { ArrowTurnForward as iconData } from '@dga-icons/core';

const ArrowTurnForward = createVueIcon('arrow-turn-forward', iconData);

export default ArrowTurnForward;

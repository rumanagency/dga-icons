import { createVueIcon } from '../createVueIcon';
import { PreferenceVertical as iconData } from '@dga-icons/core';

const PreferenceVertical = createVueIcon('preference-vertical', iconData);

export default PreferenceVertical;

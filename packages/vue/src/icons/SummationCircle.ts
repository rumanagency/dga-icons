import { createVueIcon } from '../createVueIcon';
import { SummationCircle as iconData } from '@dga-icons/core';

const SummationCircle = createVueIcon('summation-circle', iconData);

export default SummationCircle;

import { createVueIcon } from '../createVueIcon';
import { AddressBook as iconData } from '@dga-icons/core';

const AddressBook = createVueIcon('address-book', iconData);

export default AddressBook;

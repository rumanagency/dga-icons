import { createVueIcon } from '../createVueIcon';
import { SecurityKeyUsb as iconData } from '@dga-icons/core';

const SecurityKeyUsb = createVueIcon('security-key-usb', iconData);

export default SecurityKeyUsb;

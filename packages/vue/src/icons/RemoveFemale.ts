import { createVueIcon } from '../createVueIcon';
import { RemoveFemale as iconData } from '@dga-icons/core';

const RemoveFemale = createVueIcon('remove-female', iconData);

export default RemoveFemale;

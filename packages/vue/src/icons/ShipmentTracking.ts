import { createVueIcon } from '../createVueIcon';
import { ShipmentTracking as iconData } from '@dga-icons/core';

const ShipmentTracking = createVueIcon('shipment-tracking', iconData);

export default ShipmentTracking;

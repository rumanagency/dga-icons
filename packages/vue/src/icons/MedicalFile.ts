import { createVueIcon } from '../createVueIcon';
import { MedicalFile as iconData } from '@dga-icons/core';

const MedicalFile = createVueIcon('medical-file', iconData);

export default MedicalFile;

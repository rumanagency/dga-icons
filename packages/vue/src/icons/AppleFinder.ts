import { createVueIcon } from '../createVueIcon';
import { AppleFinder as iconData } from '@dga-icons/core';

const AppleFinder = createVueIcon('apple-finder', iconData);

export default AppleFinder;

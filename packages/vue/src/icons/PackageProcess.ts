import { createVueIcon } from '../createVueIcon';
import { PackageProcess as iconData } from '@dga-icons/core';

const PackageProcess = createVueIcon('package-process', iconData);

export default PackageProcess;

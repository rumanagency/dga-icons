import { createVueIcon } from '../createVueIcon';
import { DeleteThrow as iconData } from '@dga-icons/core';

const DeleteThrow = createVueIcon('delete-throw', iconData);

export default DeleteThrow;

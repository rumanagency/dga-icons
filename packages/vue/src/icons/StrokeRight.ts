import { createVueIcon } from '../createVueIcon';
import { StrokeRight as iconData } from '@dga-icons/core';

const StrokeRight = createVueIcon('stroke-right', iconData);

export default StrokeRight;

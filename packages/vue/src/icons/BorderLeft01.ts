import { createVueIcon } from '../createVueIcon';
import { BorderLeft01 as iconData } from '@dga-icons/core';

const BorderLeft01 = createVueIcon('border-left-01', iconData);

export default BorderLeft01;

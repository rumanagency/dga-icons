import { createVueIcon } from '../createVueIcon';
import { TransitionTop as iconData } from '@dga-icons/core';

const TransitionTop = createVueIcon('transition-top', iconData);

export default TransitionTop;

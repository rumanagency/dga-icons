import { createVueIcon } from '../createVueIcon';
import { WebValidation as iconData } from '@dga-icons/core';

const WebValidation = createVueIcon('web-validation', iconData);

export default WebValidation;

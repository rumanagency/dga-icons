import { createVueIcon } from '../createVueIcon';
import { UsbConnected02 as iconData } from '@dga-icons/core';

const UsbConnected02 = createVueIcon('usb-connected-02', iconData);

export default UsbConnected02;

import { createVueIcon } from '../createVueIcon';
import { WifiLowSignal as iconData } from '@dga-icons/core';

const WifiLowSignal = createVueIcon('wifi-low-signal', iconData);

export default WifiLowSignal;

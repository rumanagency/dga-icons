import { createVueIcon } from '../createVueIcon';
import { TextKerning as iconData } from '@dga-icons/core';

const TextKerning = createVueIcon('text-kerning', iconData);

export default TextKerning;

import { createVueIcon } from '../createVueIcon';
import { TropicalStorm as iconData } from '@dga-icons/core';

const TropicalStorm = createVueIcon('tropical-storm', iconData);

export default TropicalStorm;

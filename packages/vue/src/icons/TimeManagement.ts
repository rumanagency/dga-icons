import { createVueIcon } from '../createVueIcon';
import { TimeManagement as iconData } from '@dga-icons/core';

const TimeManagement = createVueIcon('time-management', iconData);

export default TimeManagement;

import { createVueIcon } from '../createVueIcon';
import { TouchLocked04 as iconData } from '@dga-icons/core';

const TouchLocked04 = createVueIcon('touch-locked-04', iconData);

export default TouchLocked04;

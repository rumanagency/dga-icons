import { createVueIcon } from '../createVueIcon';
import { LaptopCloud as iconData } from '@dga-icons/core';

const LaptopCloud = createVueIcon('laptop-cloud', iconData);

export default LaptopCloud;

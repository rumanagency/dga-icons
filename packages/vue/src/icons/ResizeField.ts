import { createVueIcon } from '../createVueIcon';
import { ResizeField as iconData } from '@dga-icons/core';

const ResizeField = createVueIcon('resize-field', iconData);

export default ResizeField;

import { createVueIcon } from '../createVueIcon';
import { DatabaseAdd as iconData } from '@dga-icons/core';

const DatabaseAdd = createVueIcon('database-add', iconData);

export default DatabaseAdd;

import { createVueIcon } from '../createVueIcon';
import { CallInternal as iconData } from '@dga-icons/core';

const CallInternal = createVueIcon('call-internal', iconData);

export default CallInternal;

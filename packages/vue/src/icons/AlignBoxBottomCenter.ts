import { createVueIcon } from '../createVueIcon';
import { AlignBoxBottomCenter as iconData } from '@dga-icons/core';

const AlignBoxBottomCenter = createVueIcon('align-box-bottom-center', iconData);

export default AlignBoxBottomCenter;

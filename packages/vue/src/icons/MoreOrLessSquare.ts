import { createVueIcon } from '../createVueIcon';
import { MoreOrLessSquare as iconData } from '@dga-icons/core';

const MoreOrLessSquare = createVueIcon('more-or-less-square', iconData);

export default MoreOrLessSquare;

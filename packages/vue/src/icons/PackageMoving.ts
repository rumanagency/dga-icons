import { createVueIcon } from '../createVueIcon';
import { PackageMoving as iconData } from '@dga-icons/core';

const PackageMoving = createVueIcon('package-moving', iconData);

export default PackageMoving;

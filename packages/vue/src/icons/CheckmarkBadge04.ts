import { createVueIcon } from '../createVueIcon';
import { CheckmarkBadge04 as iconData } from '@dga-icons/core';

const CheckmarkBadge04 = createVueIcon('checkmark-badge-04', iconData);

export default CheckmarkBadge04;

import { createVueIcon } from '../createVueIcon';
import { HeartRemove as iconData } from '@dga-icons/core';

const HeartRemove = createVueIcon('heart-remove', iconData);

export default HeartRemove;

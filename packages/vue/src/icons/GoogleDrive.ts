import { createVueIcon } from '../createVueIcon';
import { GoogleDrive as iconData } from '@dga-icons/core';

const GoogleDrive = createVueIcon('google-drive', iconData);

export default GoogleDrive;

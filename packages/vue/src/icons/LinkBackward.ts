import { createVueIcon } from '../createVueIcon';
import { LinkBackward as iconData } from '@dga-icons/core';

const LinkBackward = createVueIcon('link-backward', iconData);

export default LinkBackward;

import { createVueIcon } from '../createVueIcon';
import { SwipeLeft05 as iconData } from '@dga-icons/core';

const SwipeLeft05 = createVueIcon('swipe-left-05', iconData);

export default SwipeLeft05;

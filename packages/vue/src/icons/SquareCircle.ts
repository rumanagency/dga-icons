import { createVueIcon } from '../createVueIcon';
import { SquareCircle as iconData } from '@dga-icons/core';

const SquareCircle = createVueIcon('square-circle', iconData);

export default SquareCircle;

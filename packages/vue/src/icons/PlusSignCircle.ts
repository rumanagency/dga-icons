import { createVueIcon } from '../createVueIcon';
import { PlusSignCircle as iconData } from '@dga-icons/core';

const PlusSignCircle = createVueIcon('plus-sign-circle', iconData);

export default PlusSignCircle;

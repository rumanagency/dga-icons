import { createVueIcon } from '../createVueIcon';
import { SummationSquare as iconData } from '@dga-icons/core';

const SummationSquare = createVueIcon('summation-square', iconData);

export default SummationSquare;

import { createVueIcon } from '../createVueIcon';
import { LockPassword as iconData } from '@dga-icons/core';

const LockPassword = createVueIcon('lock-password', iconData);

export default LockPassword;

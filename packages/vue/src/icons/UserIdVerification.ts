import { createVueIcon } from '../createVueIcon';
import { UserIdVerification as iconData } from '@dga-icons/core';

const UserIdVerification = createVueIcon('user-id-verification', iconData);

export default UserIdVerification;

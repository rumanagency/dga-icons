import { createVueIcon } from '../createVueIcon';
import { Conversation as iconData } from '@dga-icons/core';

const Conversation = createVueIcon('conversation', iconData);

export default Conversation;

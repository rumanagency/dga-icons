import { createVueIcon } from '../createVueIcon';
import { ArrowExpand as iconData } from '@dga-icons/core';

const ArrowExpand = createVueIcon('arrow-expand', iconData);

export default ArrowExpand;

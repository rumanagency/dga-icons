import { createVueIcon } from '../createVueIcon';
import { BitcoinSafe as iconData } from '@dga-icons/core';

const BitcoinSafe = createVueIcon('bitcoin-safe', iconData);

export default BitcoinSafe;

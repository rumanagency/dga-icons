import { createVueIcon } from '../createVueIcon';
import { ComputerDollar as iconData } from '@dga-icons/core';

const ComputerDollar = createVueIcon('computer-dollar', iconData);

export default ComputerDollar;

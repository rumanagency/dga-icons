import { createVueIcon } from '../createVueIcon';
import { FileAttachment as iconData } from '@dga-icons/core';

const FileAttachment = createVueIcon('file-attachment', iconData);

export default FileAttachment;

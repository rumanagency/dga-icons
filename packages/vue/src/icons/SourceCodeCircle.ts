import { createVueIcon } from '../createVueIcon';
import { SourceCodeCircle as iconData } from '@dga-icons/core';

const SourceCodeCircle = createVueIcon('source-code-circle', iconData);

export default SourceCodeCircle;

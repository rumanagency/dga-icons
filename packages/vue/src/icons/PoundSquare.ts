import { createVueIcon } from '../createVueIcon';
import { PoundSquare as iconData } from '@dga-icons/core';

const PoundSquare = createVueIcon('pound-square', iconData);

export default PoundSquare;

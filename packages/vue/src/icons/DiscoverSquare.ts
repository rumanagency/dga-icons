import { createVueIcon } from '../createVueIcon';
import { DiscoverSquare as iconData } from '@dga-icons/core';

const DiscoverSquare = createVueIcon('discover-square', iconData);

export default DiscoverSquare;

import { createVueIcon } from '../createVueIcon';
import { PoliceStation as iconData } from '@dga-icons/core';

const PoliceStation = createVueIcon('police-station', iconData);

export default PoliceStation;

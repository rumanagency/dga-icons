import { createVueIcon } from '../createVueIcon';
import { ParkingAreaSquare as iconData } from '@dga-icons/core';

const ParkingAreaSquare = createVueIcon('parking-area-square', iconData);

export default ParkingAreaSquare;

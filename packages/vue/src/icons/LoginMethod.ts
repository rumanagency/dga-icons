import { createVueIcon } from '../createVueIcon';
import { LoginMethod as iconData } from '@dga-icons/core';

const LoginMethod = createVueIcon('login-method', iconData);

export default LoginMethod;

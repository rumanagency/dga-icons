import { createVueIcon } from '../createVueIcon';
import { SecurityBlock as iconData } from '@dga-icons/core';

const SecurityBlock = createVueIcon('security-block', iconData);

export default SecurityBlock;

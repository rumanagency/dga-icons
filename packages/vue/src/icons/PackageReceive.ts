import { createVueIcon } from '../createVueIcon';
import { PackageReceive as iconData } from '@dga-icons/core';

const PackageReceive = createVueIcon('package-receive', iconData);

export default PackageReceive;

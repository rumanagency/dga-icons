import { createVueIcon } from '../createVueIcon';
import { PathfinderOutline as iconData } from '@dga-icons/core';

const PathfinderOutline = createVueIcon('pathfinder-outline', iconData);

export default PathfinderOutline;

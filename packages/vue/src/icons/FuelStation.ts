import { createVueIcon } from '../createVueIcon';
import { FuelStation as iconData } from '@dga-icons/core';

const FuelStation = createVueIcon('fuel-station', iconData);

export default FuelStation;

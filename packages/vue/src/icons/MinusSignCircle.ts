import { createVueIcon } from '../createVueIcon';
import { MinusSignCircle as iconData } from '@dga-icons/core';

const MinusSignCircle = createVueIcon('minus-sign-circle', iconData);

export default MinusSignCircle;

import { createVueIcon } from '../../createVueIcon';
import { Printer as iconData } from '@dga-icons/core/solid-rounded';

const Printer = createVueIcon('printer', iconData);

export default Printer;

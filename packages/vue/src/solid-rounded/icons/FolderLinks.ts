import { createVueIcon } from '../../createVueIcon';
import { FolderLinks as iconData } from '@dga-icons/core/solid-rounded';

const FolderLinks = createVueIcon('folder-links', iconData);

export default FolderLinks;

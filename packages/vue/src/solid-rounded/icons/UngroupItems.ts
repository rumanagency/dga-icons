import { createVueIcon } from '../../createVueIcon';
import { UngroupItems as iconData } from '@dga-icons/core/solid-rounded';

const UngroupItems = createVueIcon('ungroup-items', iconData);

export default UngroupItems;

import { createVueIcon } from '../../createVueIcon';
import { LoginMethod as iconData } from '@dga-icons/core/solid-rounded';

const LoginMethod = createVueIcon('login-method', iconData);

export default LoginMethod;

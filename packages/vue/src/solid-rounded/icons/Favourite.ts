import { createVueIcon } from '../../createVueIcon';
import { Favourite as iconData } from '@dga-icons/core/solid-rounded';

const Favourite = createVueIcon('favourite', iconData);

export default Favourite;

import { createVueIcon } from '../../createVueIcon';
import { Coinbase as iconData } from '@dga-icons/core/solid-rounded';

const Coinbase = createVueIcon('coinbase', iconData);

export default Coinbase;

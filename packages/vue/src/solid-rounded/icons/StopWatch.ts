import { createVueIcon } from '../../createVueIcon';
import { StopWatch as iconData } from '@dga-icons/core/solid-rounded';

const StopWatch = createVueIcon('stop-watch', iconData);

export default StopWatch;

import { createVueIcon } from '../../createVueIcon';
import { Contact as iconData } from '@dga-icons/core/solid-rounded';

const Contact = createVueIcon('contact', iconData);

export default Contact;

import { createVueIcon } from '../../createVueIcon';
import { Celsius as iconData } from '@dga-icons/core/solid-rounded';

const Celsius = createVueIcon('celsius', iconData);

export default Celsius;

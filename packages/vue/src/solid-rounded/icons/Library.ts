import { createVueIcon } from '../../createVueIcon';
import { Library as iconData } from '@dga-icons/core/solid-rounded';

const Library = createVueIcon('library', iconData);

export default Library;

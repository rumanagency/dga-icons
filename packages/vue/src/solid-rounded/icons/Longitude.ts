import { createVueIcon } from '../../createVueIcon';
import { Longitude as iconData } from '@dga-icons/core/solid-rounded';

const Longitude = createVueIcon('longitude', iconData);

export default Longitude;

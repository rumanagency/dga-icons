import { createVueIcon } from '../../createVueIcon';
import { FolderFavourite as iconData } from '@dga-icons/core/solid-rounded';

const FolderFavourite = createVueIcon('folder-favourite', iconData);

export default FolderFavourite;

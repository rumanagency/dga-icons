import { createVueIcon } from '../../createVueIcon';
import { Repository as iconData } from '@dga-icons/core/solid-rounded';

const Repository = createVueIcon('repository', iconData);

export default Repository;

import { createVueIcon } from '../../createVueIcon';
import { FilterEdit as iconData } from '@dga-icons/core/solid-rounded';

const FilterEdit = createVueIcon('filter-edit', iconData);

export default FilterEdit;

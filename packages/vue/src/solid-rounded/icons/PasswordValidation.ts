import { createVueIcon } from '../../createVueIcon';
import { PasswordValidation as iconData } from '@dga-icons/core/solid-rounded';

const PasswordValidation = createVueIcon('password-validation', iconData);

export default PasswordValidation;

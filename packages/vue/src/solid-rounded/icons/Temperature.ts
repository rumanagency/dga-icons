import { createVueIcon } from '../../createVueIcon';
import { Temperature as iconData } from '@dga-icons/core/solid-rounded';

const Temperature = createVueIcon('temperature', iconData);

export default Temperature;

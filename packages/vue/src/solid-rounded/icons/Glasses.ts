import { createVueIcon } from '../../createVueIcon';
import { Glasses as iconData } from '@dga-icons/core/solid-rounded';

const Glasses = createVueIcon('glasses', iconData);

export default Glasses;

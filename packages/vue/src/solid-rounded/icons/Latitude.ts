import { createVueIcon } from '../../createVueIcon';
import { Latitude as iconData } from '@dga-icons/core/solid-rounded';

const Latitude = createVueIcon('latitude', iconData);

export default Latitude;

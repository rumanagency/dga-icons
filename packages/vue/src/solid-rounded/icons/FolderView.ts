import { createVueIcon } from '../../createVueIcon';
import { FolderView as iconData } from '@dga-icons/core/solid-rounded';

const FolderView = createVueIcon('folder-view', iconData);

export default FolderView;

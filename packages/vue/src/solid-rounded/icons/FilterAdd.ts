import { createVueIcon } from '../../createVueIcon';
import { FilterAdd as iconData } from '@dga-icons/core/solid-rounded';

const FilterAdd = createVueIcon('filter-add', iconData);

export default FilterAdd;

import { createVueIcon } from '../../createVueIcon';
import { MosqueLocation as iconData } from '@dga-icons/core/solid-rounded';

const MosqueLocation = createVueIcon('mosque-location', iconData);

export default MosqueLocation;

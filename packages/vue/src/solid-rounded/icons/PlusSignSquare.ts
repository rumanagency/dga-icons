import { createVueIcon } from '../../createVueIcon';
import { PlusSignSquare as iconData } from '@dga-icons/core/solid-rounded';

const PlusSignSquare = createVueIcon('plus-sign-square', iconData);

export default PlusSignSquare;

import { createVueIcon } from '../../createVueIcon';
import { Jupiter as iconData } from '@dga-icons/core/solid-rounded';

const Jupiter = createVueIcon('jupiter', iconData);

export default Jupiter;

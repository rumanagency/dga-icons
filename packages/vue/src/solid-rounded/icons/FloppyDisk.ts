import { createVueIcon } from '../../createVueIcon';
import { FloppyDisk as iconData } from '@dga-icons/core/solid-rounded';

const FloppyDisk = createVueIcon('floppy-disk', iconData);

export default FloppyDisk;

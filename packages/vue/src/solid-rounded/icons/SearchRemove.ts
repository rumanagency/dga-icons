import { createVueIcon } from '../../createVueIcon';
import { SearchRemove as iconData } from '@dga-icons/core/solid-rounded';

const SearchRemove = createVueIcon('search-remove', iconData);

export default SearchRemove;

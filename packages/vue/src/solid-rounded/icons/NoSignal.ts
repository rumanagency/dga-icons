import { createVueIcon } from '../../createVueIcon';
import { NoSignal as iconData } from '@dga-icons/core/solid-rounded';

const NoSignal = createVueIcon('no-signal', iconData);

export default NoSignal;

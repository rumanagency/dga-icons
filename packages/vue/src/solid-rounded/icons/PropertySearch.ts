import { createVueIcon } from '../../createVueIcon';
import { PropertySearch as iconData } from '@dga-icons/core/solid-rounded';

const PropertySearch = createVueIcon('property-search', iconData);

export default PropertySearch;

import { createVueIcon } from '../../createVueIcon';
import { SourceCode as iconData } from '@dga-icons/core/solid-rounded';

const SourceCode = createVueIcon('source-code', iconData);

export default SourceCode;

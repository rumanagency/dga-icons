import { createVueIcon } from '../../createVueIcon';
import { DarkMode as iconData } from '@dga-icons/core/solid-rounded';

const DarkMode = createVueIcon('dark-mode', iconData);

export default DarkMode;

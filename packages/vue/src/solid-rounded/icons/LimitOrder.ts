import { createVueIcon } from '../../createVueIcon';
import { LimitOrder as iconData } from '@dga-icons/core/solid-rounded';

const LimitOrder = createVueIcon('limit-order', iconData);

export default LimitOrder;

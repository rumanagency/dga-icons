import { createVueIcon } from '../../createVueIcon';
import { GitFork as iconData } from '@dga-icons/core/solid-rounded';

const GitFork = createVueIcon('git-fork', iconData);

export default GitFork;

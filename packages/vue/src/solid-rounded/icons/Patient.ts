import { createVueIcon } from '../../createVueIcon';
import { Patient as iconData } from '@dga-icons/core/solid-rounded';

const Patient = createVueIcon('patient', iconData);

export default Patient;

import { createVueIcon } from '../../createVueIcon';
import { Factory as iconData } from '@dga-icons/core/solid-rounded';

const Factory = createVueIcon('factory', iconData);

export default Factory;

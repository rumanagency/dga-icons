import { createVueIcon } from '../../createVueIcon';
import { FileEdit as iconData } from '@dga-icons/core/solid-rounded';

const FileEdit = createVueIcon('file-edit', iconData);

export default FileEdit;

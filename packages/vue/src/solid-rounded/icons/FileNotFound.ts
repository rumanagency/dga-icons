import { createVueIcon } from '../../createVueIcon';
import { FileNotFound as iconData } from '@dga-icons/core/solid-rounded';

const FileNotFound = createVueIcon('file-not-found', iconData);

export default FileNotFound;

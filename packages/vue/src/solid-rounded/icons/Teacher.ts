import { createVueIcon } from '../../createVueIcon';
import { Teacher as iconData } from '@dga-icons/core/solid-rounded';

const Teacher = createVueIcon('teacher', iconData);

export default Teacher;

import { createVueIcon } from '../../createVueIcon';
import { PreferenceHorizontal as iconData } from '@dga-icons/core/solid-rounded';

const PreferenceHorizontal = createVueIcon('preference-horizontal', iconData);

export default PreferenceHorizontal;

import { createVueIcon } from '../../createVueIcon';
import { DatabaseSync as iconData } from '@dga-icons/core/solid-rounded';

const DatabaseSync = createVueIcon('database-sync', iconData);

export default DatabaseSync;

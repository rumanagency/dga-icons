import { createVueIcon } from '../../createVueIcon';
import { FolderOff as iconData } from '@dga-icons/core/solid-rounded';

const FolderOff = createVueIcon('folder-off', iconData);

export default FolderOff;

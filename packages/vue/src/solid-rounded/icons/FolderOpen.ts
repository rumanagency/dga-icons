import { createVueIcon } from '../../createVueIcon';
import { FolderOpen as iconData } from '@dga-icons/core/solid-rounded';

const FolderOpen = createVueIcon('folder-open', iconData);

export default FolderOpen;

import { createVueIcon } from '../../createVueIcon';
import { MinusSign as iconData } from '@dga-icons/core/solid-rounded';

const MinusSign = createVueIcon('minus-sign', iconData);

export default MinusSign;

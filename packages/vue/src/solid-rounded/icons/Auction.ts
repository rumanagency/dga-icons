import { createVueIcon } from '../../createVueIcon';
import { Auction as iconData } from '@dga-icons/core/solid-rounded';

const Auction = createVueIcon('auction', iconData);

export default Auction;

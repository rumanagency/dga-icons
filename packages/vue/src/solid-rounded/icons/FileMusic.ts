import { createVueIcon } from '../../createVueIcon';
import { FileMusic as iconData } from '@dga-icons/core/solid-rounded';

const FileMusic = createVueIcon('file-music', iconData);

export default FileMusic;

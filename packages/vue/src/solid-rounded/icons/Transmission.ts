import { createVueIcon } from '../../createVueIcon';
import { Transmission as iconData } from '@dga-icons/core/solid-rounded';

const Transmission = createVueIcon('transmission', iconData);

export default Transmission;

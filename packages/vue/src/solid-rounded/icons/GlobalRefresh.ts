import { createVueIcon } from '../../createVueIcon';
import { GlobalRefresh as iconData } from '@dga-icons/core/solid-rounded';

const GlobalRefresh = createVueIcon('global-refresh', iconData);

export default GlobalRefresh;

import { createVueIcon } from '../../createVueIcon';
import { Avalanche as iconData } from '@dga-icons/core/solid-rounded';

const Avalanche = createVueIcon('avalanche', iconData);

export default Avalanche;

import { createVueIcon } from '../../createVueIcon';
import { FullScreen as iconData } from '@dga-icons/core/solid-rounded';

const FullScreen = createVueIcon('full-screen', iconData);

export default FullScreen;

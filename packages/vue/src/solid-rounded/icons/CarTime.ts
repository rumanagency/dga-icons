import { createVueIcon } from '../../createVueIcon';
import { CarTime as iconData } from '@dga-icons/core/solid-rounded';

const CarTime = createVueIcon('car-time', iconData);

export default CarTime;

import { createVueIcon } from '../../createVueIcon';
import { Tractor as iconData } from '@dga-icons/core/solid-rounded';

const Tractor = createVueIcon('tractor', iconData);

export default Tractor;

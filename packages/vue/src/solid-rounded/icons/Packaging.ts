import { createVueIcon } from '../../createVueIcon';
import { Packaging as iconData } from '@dga-icons/core/solid-rounded';

const Packaging = createVueIcon('packaging', iconData);

export default Packaging;

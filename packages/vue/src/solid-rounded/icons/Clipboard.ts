import { createVueIcon } from '../../createVueIcon';
import { Clipboard as iconData } from '@dga-icons/core/solid-rounded';

const Clipboard = createVueIcon('clipboard', iconData);

export default Clipboard;

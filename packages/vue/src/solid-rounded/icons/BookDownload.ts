import { createVueIcon } from '../../createVueIcon';
import { BookDownload as iconData } from '@dga-icons/core/solid-rounded';

const BookDownload = createVueIcon('book-download', iconData);

export default BookDownload;

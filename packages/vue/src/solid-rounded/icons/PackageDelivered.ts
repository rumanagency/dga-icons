import { createVueIcon } from '../../createVueIcon';
import { PackageDelivered as iconData } from '@dga-icons/core/solid-rounded';

const PackageDelivered = createVueIcon('package-delivered', iconData);

export default PackageDelivered;

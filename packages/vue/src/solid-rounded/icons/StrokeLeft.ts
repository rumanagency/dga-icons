import { createVueIcon } from '../../createVueIcon';
import { StrokeLeft as iconData } from '@dga-icons/core/solid-rounded';

const StrokeLeft = createVueIcon('stroke-left', iconData);

export default StrokeLeft;

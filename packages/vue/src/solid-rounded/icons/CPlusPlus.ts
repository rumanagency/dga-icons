import { createVueIcon } from '../../createVueIcon';
import { CPlusPlus as iconData } from '@dga-icons/core/solid-rounded';

const CPlusPlus = createVueIcon('c++', iconData);

export default CPlusPlus;

import { createVueIcon } from '../../createVueIcon';
import { Diameter as iconData } from '@dga-icons/core/solid-rounded';

const Diameter = createVueIcon('diameter', iconData);

export default Diameter;

import { createVueIcon } from '../../createVueIcon';
import { PackageOpen as iconData } from '@dga-icons/core/solid-rounded';

const PackageOpen = createVueIcon('package-open', iconData);

export default PackageOpen;

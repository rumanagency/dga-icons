import { createVueIcon } from '../../createVueIcon';
import { CapProjecting as iconData } from '@dga-icons/core/solid-rounded';

const CapProjecting = createVueIcon('cap-projecting', iconData);

export default CapProjecting;

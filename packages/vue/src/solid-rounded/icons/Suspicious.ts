import { createVueIcon } from '../../createVueIcon';
import { Suspicious as iconData } from '@dga-icons/core/solid-rounded';

const Suspicious = createVueIcon('suspicious', iconData);

export default Suspicious;

import { createVueIcon } from '../../createVueIcon';
import { RotateClockwise as iconData } from '@dga-icons/core/solid-rounded';

const RotateClockwise = createVueIcon('rotate-clockwise', iconData);

export default RotateClockwise;

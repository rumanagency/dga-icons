import { createVueIcon } from '../../createVueIcon';
import { Percent as iconData } from '@dga-icons/core/solid-rounded';

const Percent = createVueIcon('percent', iconData);

export default Percent;

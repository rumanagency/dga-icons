import { createVueIcon } from '../../createVueIcon';
import { StraightEdge as iconData } from '@dga-icons/core/stroke-sharp';

const StraightEdge = createVueIcon('straight-edge', iconData);

export default StraightEdge;

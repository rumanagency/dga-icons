import { createVueIcon } from '../../createVueIcon';
import { Previous as iconData } from '@dga-icons/core/stroke-sharp';

const Previous = createVueIcon('previous', iconData);

export default Previous;

import { createVueIcon } from '../../createVueIcon';
import { FloppyDisk as iconData } from '@dga-icons/core/stroke-sharp';

const FloppyDisk = createVueIcon('floppy-disk', iconData);

export default FloppyDisk;

import { createVueIcon } from '../../createVueIcon';
import { CreditCard as iconData } from '@dga-icons/core/stroke-sharp';

const CreditCard = createVueIcon('credit-card', iconData);

export default CreditCard;

import { createVueIcon } from '../../createVueIcon';
import { CustomField as iconData } from '@dga-icons/core/stroke-sharp';

const CustomField = createVueIcon('custom-field', iconData);

export default CustomField;

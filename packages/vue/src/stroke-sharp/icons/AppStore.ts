import { createVueIcon } from '../../createVueIcon';
import { AppStore as iconData } from '@dga-icons/core/stroke-sharp';

const AppStore = createVueIcon('app-store', iconData);

export default AppStore;

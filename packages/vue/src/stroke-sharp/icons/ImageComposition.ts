import { createVueIcon } from '../../createVueIcon';
import { ImageComposition as iconData } from '@dga-icons/core/stroke-sharp';

const ImageComposition = createVueIcon('image-composition', iconData);

export default ImageComposition;

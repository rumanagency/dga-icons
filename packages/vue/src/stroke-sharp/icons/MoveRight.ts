import { createVueIcon } from '../../createVueIcon';
import { MoveRight as iconData } from '@dga-icons/core/stroke-sharp';

const MoveRight = createVueIcon('move-right', iconData);

export default MoveRight;

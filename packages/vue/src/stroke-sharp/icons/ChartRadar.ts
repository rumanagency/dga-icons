import { createVueIcon } from '../../createVueIcon';
import { ChartRadar as iconData } from '@dga-icons/core/stroke-sharp';

const ChartRadar = createVueIcon('chart-radar', iconData);

export default ChartRadar;

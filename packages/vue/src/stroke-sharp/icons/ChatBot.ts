import { createVueIcon } from '../../createVueIcon';
import { ChatBot as iconData } from '@dga-icons/core/stroke-sharp';

const ChatBot = createVueIcon('chat-bot', iconData);

export default ChatBot;

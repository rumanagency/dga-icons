import { createVueIcon } from '../../createVueIcon';
import { Constellation as iconData } from '@dga-icons/core/stroke-sharp';

const Constellation = createVueIcon('constellation', iconData);

export default Constellation;

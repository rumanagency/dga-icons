import { createVueIcon } from '../../createVueIcon';
import { Wireless as iconData } from '@dga-icons/core/stroke-sharp';

const Wireless = createVueIcon('wireless', iconData);

export default Wireless;

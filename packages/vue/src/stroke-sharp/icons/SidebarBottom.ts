import { createVueIcon } from '../../createVueIcon';
import { SidebarBottom as iconData } from '@dga-icons/core/stroke-sharp';

const SidebarBottom = createVueIcon('sidebar-bottom', iconData);

export default SidebarBottom;

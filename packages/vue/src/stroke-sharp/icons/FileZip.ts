import { createVueIcon } from '../../createVueIcon';
import { FileZip as iconData } from '@dga-icons/core/stroke-sharp';

const FileZip = createVueIcon('file-zip', iconData);

export default FileZip;

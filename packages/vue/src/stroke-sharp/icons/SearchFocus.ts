import { createVueIcon } from '../../createVueIcon';
import { SearchFocus as iconData } from '@dga-icons/core/stroke-sharp';

const SearchFocus = createVueIcon('search-focus', iconData);

export default SearchFocus;

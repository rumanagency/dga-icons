import { createVueIcon } from '../../createVueIcon';
import { MapsEditing as iconData } from '@dga-icons/core/stroke-sharp';

const MapsEditing = createVueIcon('maps-editing', iconData);

export default MapsEditing;

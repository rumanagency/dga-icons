import { createVueIcon } from '../../createVueIcon';
import { StudentCard as iconData } from '@dga-icons/core/stroke-sharp';

const StudentCard = createVueIcon('student-card', iconData);

export default StudentCard;

import { createVueIcon } from '../../createVueIcon';
import { Latitude as iconData } from '@dga-icons/core/stroke-sharp';

const Latitude = createVueIcon('latitude', iconData);

export default Latitude;

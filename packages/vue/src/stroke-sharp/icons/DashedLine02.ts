import { createVueIcon } from '../../createVueIcon';
import { DashedLine02 as iconData } from '@dga-icons/core/stroke-sharp';

const DashedLine02 = createVueIcon('dashed-line-02', iconData);

export default DashedLine02;

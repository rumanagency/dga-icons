import { createVueIcon } from '../../createVueIcon';
import { Hourglass as iconData } from '@dga-icons/core/stroke-sharp';

const Hourglass = createVueIcon('hourglass', iconData);

export default Hourglass;

import { createVueIcon } from '../../createVueIcon';
import { CPlusPlus as iconData } from '@dga-icons/core/stroke-sharp';

const CPlusPlus = createVueIcon('c++', iconData);

export default CPlusPlus;

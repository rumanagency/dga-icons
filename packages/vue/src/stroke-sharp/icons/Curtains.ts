import { createVueIcon } from '../../createVueIcon';
import { Curtains as iconData } from '@dga-icons/core/stroke-sharp';

const Curtains = createVueIcon('curtains', iconData);

export default Curtains;

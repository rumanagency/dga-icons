import { createVueIcon } from '../../createVueIcon';
import { Sparkles as iconData } from '@dga-icons/core/stroke-sharp';

const Sparkles = createVueIcon('sparkles', iconData);

export default Sparkles;

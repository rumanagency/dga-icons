import { createVueIcon } from '../../createVueIcon';
import { FolderDetails as iconData } from '@dga-icons/core/stroke-sharp';

const FolderDetails = createVueIcon('folder-details', iconData);

export default FolderDetails;

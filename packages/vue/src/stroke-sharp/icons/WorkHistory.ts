import { createVueIcon } from '../../createVueIcon';
import { WorkHistory as iconData } from '@dga-icons/core/stroke-sharp';

const WorkHistory = createVueIcon('work-history', iconData);

export default WorkHistory;

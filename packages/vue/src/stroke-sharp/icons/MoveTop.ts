import { createVueIcon } from '../../createVueIcon';
import { MoveTop as iconData } from '@dga-icons/core/stroke-sharp';

const MoveTop = createVueIcon('move-top', iconData);

export default MoveTop;

import { createVueIcon } from '../../createVueIcon';
import { LaptopSettings as iconData } from '@dga-icons/core/stroke-sharp';

const LaptopSettings = createVueIcon('laptop-settings', iconData);

export default LaptopSettings;

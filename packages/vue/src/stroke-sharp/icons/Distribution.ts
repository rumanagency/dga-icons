import { createVueIcon } from '../../createVueIcon';
import { Distribution as iconData } from '@dga-icons/core/stroke-sharp';

const Distribution = createVueIcon('distribution', iconData);

export default Distribution;

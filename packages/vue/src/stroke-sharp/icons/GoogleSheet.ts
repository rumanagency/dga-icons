import { createVueIcon } from '../../createVueIcon';
import { GoogleSheet as iconData } from '@dga-icons/core/stroke-sharp';

const GoogleSheet = createVueIcon('google-sheet', iconData);

export default GoogleSheet;

import { createVueIcon } from '../../createVueIcon';
import { FlipTop as iconData } from '@dga-icons/core/stroke-sharp';

const FlipTop = createVueIcon('flip-top', iconData);

export default FlipTop;

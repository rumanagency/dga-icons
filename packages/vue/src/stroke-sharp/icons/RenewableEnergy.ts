import { createVueIcon } from '../../createVueIcon';
import { RenewableEnergy as iconData } from '@dga-icons/core/stroke-sharp';

const RenewableEnergy = createVueIcon('renewable-energy', iconData);

export default RenewableEnergy;

import { createVueIcon } from '../../createVueIcon';
import { Submarine as iconData } from '@dga-icons/core/stroke-sharp';

const Submarine = createVueIcon('submarine', iconData);

export default Submarine;

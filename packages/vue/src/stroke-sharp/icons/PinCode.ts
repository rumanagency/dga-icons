import { createVueIcon } from '../../createVueIcon';
import { PinCode as iconData } from '@dga-icons/core/stroke-sharp';

const PinCode = createVueIcon('pin-code', iconData);

export default PinCode;

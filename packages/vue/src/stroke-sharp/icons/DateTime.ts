import { createVueIcon } from '../../createVueIcon';
import { DateTime as iconData } from '@dga-icons/core/stroke-sharp';

const DateTime = createVueIcon('date-time', iconData);

export default DateTime;

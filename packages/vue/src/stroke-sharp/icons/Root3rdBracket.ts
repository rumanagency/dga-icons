import { createVueIcon } from '../../createVueIcon';
import { Root3rdBracket as iconData } from '@dga-icons/core/stroke-sharp';

const Root3rdBracket = createVueIcon('root-3rd-bracket', iconData);

export default Root3rdBracket;

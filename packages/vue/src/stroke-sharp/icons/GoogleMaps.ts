import { createVueIcon } from '../../createVueIcon';
import { GoogleMaps as iconData } from '@dga-icons/core/stroke-sharp';

const GoogleMaps = createVueIcon('google-maps', iconData);

export default GoogleMaps;

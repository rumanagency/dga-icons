import { createVueIcon } from '../../createVueIcon';
import { MoonCloud as iconData } from '@dga-icons/core/stroke-sharp';

const MoonCloud = createVueIcon('moon-cloud', iconData);

export default MoonCloud;

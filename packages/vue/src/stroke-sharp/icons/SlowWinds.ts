import { createVueIcon } from '../../createVueIcon';
import { SlowWinds as iconData } from '@dga-icons/core/stroke-sharp';

const SlowWinds = createVueIcon('slow-winds', iconData);

export default SlowWinds;

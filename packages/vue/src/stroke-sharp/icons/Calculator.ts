import { createVueIcon } from '../../createVueIcon';
import { Calculator as iconData } from '@dga-icons/core/stroke-sharp';

const Calculator = createVueIcon('calculator', iconData);

export default Calculator;

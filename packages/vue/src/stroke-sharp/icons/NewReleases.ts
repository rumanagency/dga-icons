import { createVueIcon } from '../../createVueIcon';
import { NewReleases as iconData } from '@dga-icons/core/stroke-sharp';

const NewReleases = createVueIcon('new-releases', iconData);

export default NewReleases;

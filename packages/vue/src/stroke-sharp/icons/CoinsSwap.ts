import { createVueIcon } from '../../createVueIcon';
import { CoinsSwap as iconData } from '@dga-icons/core/stroke-sharp';

const CoinsSwap = createVueIcon('coins-swap', iconData);

export default CoinsSwap;

import { createVueIcon } from '../../createVueIcon';
import { HoldLocked01 as iconData } from '@dga-icons/core/stroke-sharp';

const HoldLocked01 = createVueIcon('hold-locked-01', iconData);

export default HoldLocked01;

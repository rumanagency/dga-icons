import { createVueIcon } from '../../createVueIcon';
import { ScrollVertical as iconData } from '@dga-icons/core/stroke-sharp';

const ScrollVertical = createVueIcon('scroll-vertical', iconData);

export default ScrollVertical;

import { createVueIcon } from '../../createVueIcon';
import { Acceleration as iconData } from '@dga-icons/core/stroke-sharp';

const Acceleration = createVueIcon('acceleration', iconData);

export default Acceleration;

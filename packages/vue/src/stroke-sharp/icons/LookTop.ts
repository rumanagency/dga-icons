import { createVueIcon } from '../../createVueIcon';
import { LookTop as iconData } from '@dga-icons/core/stroke-sharp';

const LookTop = createVueIcon('look-top', iconData);

export default LookTop;

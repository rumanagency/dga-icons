import { createVueIcon } from '../../createVueIcon';
import { GitMerge as iconData } from '@dga-icons/core/stroke-sharp';

const GitMerge = createVueIcon('git-merge', iconData);

export default GitMerge;

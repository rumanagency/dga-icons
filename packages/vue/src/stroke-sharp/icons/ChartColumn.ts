import { createVueIcon } from '../../createVueIcon';
import { ChartColumn as iconData } from '@dga-icons/core/stroke-sharp';

const ChartColumn = createVueIcon('chart-column', iconData);

export default ChartColumn;

import { createVueIcon } from '../../createVueIcon';
import { SourceCode as iconData } from '@dga-icons/core/stroke-sharp';

const SourceCode = createVueIcon('source-code', iconData);

export default SourceCode;

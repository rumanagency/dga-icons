import { createVueIcon } from '../../createVueIcon';
import { Petals02 as iconData } from '@dga-icons/core/stroke-sharp';

const Petals02 = createVueIcon('petals-02', iconData);

export default Petals02;

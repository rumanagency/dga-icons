import { createVueIcon } from '../../createVueIcon';
import { DatabaseImport as iconData } from '@dga-icons/core/stroke-sharp';

const DatabaseImport = createVueIcon('database-import', iconData);

export default DatabaseImport;

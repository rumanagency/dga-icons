import { createVueIcon } from '../../createVueIcon';
import { CloudDownload as iconData } from '@dga-icons/core/stroke-sharp';

const CloudDownload = createVueIcon('cloud-download', iconData);

export default CloudDownload;

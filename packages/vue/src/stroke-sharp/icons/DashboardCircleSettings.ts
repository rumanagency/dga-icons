import { createVueIcon } from '../../createVueIcon';
import { DashboardCircleSettings as iconData } from '@dga-icons/core/stroke-sharp';

const DashboardCircleSettings = createVueIcon('dashboard-circle-settings', iconData);

export default DashboardCircleSettings;

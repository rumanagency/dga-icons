import { createVueIcon } from '../../createVueIcon';
import { LaptopPhoneSync as iconData } from '@dga-icons/core/stroke-sharp';

const LaptopPhoneSync = createVueIcon('laptop-phone-sync', iconData);

export default LaptopPhoneSync;

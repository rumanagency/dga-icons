import { createVueIcon } from '../../createVueIcon';
import { UserFullView as iconData } from '@dga-icons/core/stroke-sharp';

const UserFullView = createVueIcon('user-full-view', iconData);

export default UserFullView;

import { createVueIcon } from '../../createVueIcon';
import { FileEuro as iconData } from '@dga-icons/core/stroke-sharp';

const FileEuro = createVueIcon('file-euro', iconData);

export default FileEuro;

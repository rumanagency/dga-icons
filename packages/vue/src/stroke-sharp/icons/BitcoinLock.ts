import { createVueIcon } from '../../createVueIcon';
import { BitcoinLock as iconData } from '@dga-icons/core/stroke-sharp';

const BitcoinLock = createVueIcon('bitcoin-lock', iconData);

export default BitcoinLock;

import { createVueIcon } from '../../createVueIcon';
import { SmsCode as iconData } from '@dga-icons/core/stroke-sharp';

const SmsCode = createVueIcon('sms-code', iconData);

export default SmsCode;

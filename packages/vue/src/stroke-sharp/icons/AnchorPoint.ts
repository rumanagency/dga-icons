import { createVueIcon } from '../../createVueIcon';
import { AnchorPoint as iconData } from '@dga-icons/core/stroke-sharp';

const AnchorPoint = createVueIcon('anchor-point', iconData);

export default AnchorPoint;

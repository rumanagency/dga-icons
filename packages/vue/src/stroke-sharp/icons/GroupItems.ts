import { createVueIcon } from '../../createVueIcon';
import { GroupItems as iconData } from '@dga-icons/core/stroke-sharp';

const GroupItems = createVueIcon('group-items', iconData);

export default GroupItems;

import { createVueIcon } from '../../createVueIcon';
import { StrokeTop as iconData } from '@dga-icons/core/stroke-sharp';

const StrokeTop = createVueIcon('stroke-top', iconData);

export default StrokeTop;

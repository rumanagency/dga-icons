import { createVueIcon } from '../../createVueIcon';
import { HoldPhone as iconData } from '@dga-icons/core/stroke-sharp';

const HoldPhone = createVueIcon('hold-phone', iconData);

export default HoldPhone;

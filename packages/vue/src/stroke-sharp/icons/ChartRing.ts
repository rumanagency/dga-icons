import { createVueIcon } from '../../createVueIcon';
import { ChartRing as iconData } from '@dga-icons/core/stroke-sharp';

const ChartRing = createVueIcon('chart-ring', iconData);

export default ChartRing;

import { createVueIcon } from '../../createVueIcon';
import { TokenCircle as iconData } from '@dga-icons/core/stroke-sharp';

const TokenCircle = createVueIcon('token-circle', iconData);

export default TokenCircle;

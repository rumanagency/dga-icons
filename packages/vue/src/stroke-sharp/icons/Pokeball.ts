import { createVueIcon } from '../../createVueIcon';
import { Pokeball as iconData } from '@dga-icons/core/stroke-sharp';

const Pokeball = createVueIcon('pokeball', iconData);

export default Pokeball;

import { createVueIcon } from '../../createVueIcon';
import { PlusSign as iconData } from '@dga-icons/core/stroke-sharp';

const PlusSign = createVueIcon('plus-sign', iconData);

export default PlusSign;

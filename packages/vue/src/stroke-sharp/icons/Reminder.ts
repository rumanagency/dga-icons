import { createVueIcon } from '../../createVueIcon';
import { Reminder as iconData } from '@dga-icons/core/stroke-sharp';

const Reminder = createVueIcon('reminder', iconData);

export default Reminder;

import { createVueIcon } from '../../createVueIcon';
import { TextStrikethrough as iconData } from '@dga-icons/core/stroke-sharp';

const TextStrikethrough = createVueIcon('text-strikethrough', iconData);

export default TextStrikethrough;

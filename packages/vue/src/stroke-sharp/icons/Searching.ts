import { createVueIcon } from '../../createVueIcon';
import { Searching as iconData } from '@dga-icons/core/stroke-sharp';

const Searching = createVueIcon('searching', iconData);

export default Searching;

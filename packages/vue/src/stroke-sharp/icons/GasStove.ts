import { createVueIcon } from '../../createVueIcon';
import { GasStove as iconData } from '@dga-icons/core/stroke-sharp';

const GasStove = createVueIcon('gas-stove', iconData);

export default GasStove;

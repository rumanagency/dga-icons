import { createVueIcon } from '../../createVueIcon';
import { CustomerService as iconData } from '@dga-icons/core/stroke-sharp';

const CustomerService = createVueIcon('customer-service', iconData);

export default CustomerService;

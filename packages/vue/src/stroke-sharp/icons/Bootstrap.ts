import { createVueIcon } from '../../createVueIcon';
import { Bootstrap as iconData } from '@dga-icons/core/stroke-sharp';

const Bootstrap = createVueIcon('bootstrap', iconData);

export default Bootstrap;

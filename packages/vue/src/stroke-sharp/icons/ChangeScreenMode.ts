import { createVueIcon } from '../../createVueIcon';
import { ChangeScreenMode as iconData } from '@dga-icons/core/stroke-sharp';

const ChangeScreenMode = createVueIcon('change-screen-mode', iconData);

export default ChangeScreenMode;

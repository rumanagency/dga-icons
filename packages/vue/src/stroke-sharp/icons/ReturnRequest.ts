import { createVueIcon } from '../../createVueIcon';
import { ReturnRequest as iconData } from '@dga-icons/core/stroke-sharp';

const ReturnRequest = createVueIcon('return-request', iconData);

export default ReturnRequest;

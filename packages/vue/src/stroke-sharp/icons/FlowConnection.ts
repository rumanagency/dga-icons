import { createVueIcon } from '../../createVueIcon';
import { FlowConnection as iconData } from '@dga-icons/core/stroke-sharp';

const FlowConnection = createVueIcon('flow-connection', iconData);

export default FlowConnection;

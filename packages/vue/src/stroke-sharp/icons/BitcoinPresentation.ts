import { createVueIcon } from '../../createVueIcon';
import { BitcoinPresentation as iconData } from '@dga-icons/core/stroke-sharp';

const BitcoinPresentation = createVueIcon('bitcoin-presentation', iconData);

export default BitcoinPresentation;

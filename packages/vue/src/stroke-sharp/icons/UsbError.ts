import { createVueIcon } from '../../createVueIcon';
import { UsbError as iconData } from '@dga-icons/core/stroke-sharp';

const UsbError = createVueIcon('usb-error', iconData);

export default UsbError;

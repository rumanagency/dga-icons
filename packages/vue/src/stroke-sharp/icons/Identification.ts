import { createVueIcon } from '../../createVueIcon';
import { Identification as iconData } from '@dga-icons/core/stroke-sharp';

const Identification = createVueIcon('identification', iconData);

export default Identification;

import { createVueIcon } from '../../createVueIcon';
import { UserSwitch as iconData } from '@dga-icons/core/stroke-sharp';

const UserSwitch = createVueIcon('user-switch', iconData);

export default UserSwitch;

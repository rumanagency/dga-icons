import { createVueIcon } from '../../createVueIcon';
import { Stationery as iconData } from '@dga-icons/core/stroke-sharp';

const Stationery = createVueIcon('stationery', iconData);

export default Stationery;

import { createVueIcon } from '../../createVueIcon';
import { ContentWriting as iconData } from '@dga-icons/core/stroke-sharp';

const ContentWriting = createVueIcon('content-writing', iconData);

export default ContentWriting;

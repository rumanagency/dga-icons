import { createVueIcon } from '../../createVueIcon';
import { MapsRefresh as iconData } from '@dga-icons/core/stroke-sharp';

const MapsRefresh = createVueIcon('maps-refresh', iconData);

export default MapsRefresh;

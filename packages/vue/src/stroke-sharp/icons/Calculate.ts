import { createVueIcon } from '../../createVueIcon';
import { Calculate as iconData } from '@dga-icons/core/stroke-sharp';

const Calculate = createVueIcon('calculate', iconData);

export default Calculate;

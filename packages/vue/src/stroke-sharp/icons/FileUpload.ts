import { createVueIcon } from '../../createVueIcon';
import { FileUpload as iconData } from '@dga-icons/core/stroke-sharp';

const FileUpload = createVueIcon('file-upload', iconData);

export default FileUpload;

import { createVueIcon } from '../../createVueIcon';
import { WifiCircle as iconData } from '@dga-icons/core/stroke-sharp';

const WifiCircle = createVueIcon('wifi-circle', iconData);

export default WifiCircle;

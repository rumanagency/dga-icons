import { createVueIcon } from '../../createVueIcon';
import { Whiteboard as iconData } from '@dga-icons/core/stroke-sharp';

const Whiteboard = createVueIcon('whiteboard', iconData);

export default Whiteboard;

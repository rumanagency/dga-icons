import { createVueIcon } from '../../createVueIcon';
import { ShutDown as iconData } from '@dga-icons/core/stroke-sharp';

const ShutDown = createVueIcon('shut-down', iconData);

export default ShutDown;

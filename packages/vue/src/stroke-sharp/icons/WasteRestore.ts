import { createVueIcon } from '../../createVueIcon';
import { WasteRestore as iconData } from '@dga-icons/core/stroke-sharp';

const WasteRestore = createVueIcon('waste-restore', iconData);

export default WasteRestore;

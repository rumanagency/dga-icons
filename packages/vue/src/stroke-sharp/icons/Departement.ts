import { createVueIcon } from '../../createVueIcon';
import { Departement as iconData } from '@dga-icons/core/stroke-sharp';

const Departement = createVueIcon('departement', iconData);

export default Departement;

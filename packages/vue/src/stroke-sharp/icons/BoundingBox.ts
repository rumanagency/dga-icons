import { createVueIcon } from '../../createVueIcon';
import { BoundingBox as iconData } from '@dga-icons/core/stroke-sharp';

const BoundingBox = createVueIcon('bounding-box', iconData);

export default BoundingBox;

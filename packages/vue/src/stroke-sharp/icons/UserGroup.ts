import { createVueIcon } from '../../createVueIcon';
import { UserGroup as iconData } from '@dga-icons/core/stroke-sharp';

const UserGroup = createVueIcon('user-group', iconData);

export default UserGroup;

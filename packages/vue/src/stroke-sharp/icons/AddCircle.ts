import { createVueIcon } from '../../createVueIcon';
import { AddCircle as iconData } from '@dga-icons/core/stroke-sharp';

const AddCircle = createVueIcon('add-circle', iconData);

export default AddCircle;

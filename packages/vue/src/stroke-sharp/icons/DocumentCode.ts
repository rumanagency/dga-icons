import { createVueIcon } from '../../createVueIcon';
import { DocumentCode as iconData } from '@dga-icons/core/stroke-sharp';

const DocumentCode = createVueIcon('document-code', iconData);

export default DocumentCode;

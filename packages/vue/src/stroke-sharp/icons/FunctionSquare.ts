import { createVueIcon } from '../../createVueIcon';
import { FunctionSquare as iconData } from '@dga-icons/core/stroke-sharp';

const FunctionSquare = createVueIcon('function-square', iconData);

export default FunctionSquare;

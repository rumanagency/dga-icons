import { createVueIcon } from '../../createVueIcon';
import { Telephone as iconData } from '@dga-icons/core/stroke-sharp';

const Telephone = createVueIcon('telephone', iconData);

export default Telephone;

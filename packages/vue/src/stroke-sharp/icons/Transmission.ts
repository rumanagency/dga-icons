import { createVueIcon } from '../../createVueIcon';
import { Transmission as iconData } from '@dga-icons/core/stroke-sharp';

const Transmission = createVueIcon('transmission', iconData);

export default Transmission;

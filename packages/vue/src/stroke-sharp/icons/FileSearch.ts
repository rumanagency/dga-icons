import { createVueIcon } from '../../createVueIcon';
import { FileSearch as iconData } from '@dga-icons/core/stroke-sharp';

const FileSearch = createVueIcon('file-search', iconData);

export default FileSearch;

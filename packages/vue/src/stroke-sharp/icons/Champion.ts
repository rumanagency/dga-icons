import { createVueIcon } from '../../createVueIcon';
import { Champion as iconData } from '@dga-icons/core/stroke-sharp';

const Champion = createVueIcon('champion', iconData);

export default Champion;

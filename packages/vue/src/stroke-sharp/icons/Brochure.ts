import { createVueIcon } from '../../createVueIcon';
import { Brochure as iconData } from '@dga-icons/core/stroke-sharp';

const Brochure = createVueIcon('brochure', iconData);

export default Brochure;

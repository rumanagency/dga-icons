import { createVueIcon } from '../../createVueIcon';
import { Wheelchair as iconData } from '@dga-icons/core/stroke-sharp';

const Wheelchair = createVueIcon('wheelchair', iconData);

export default Wheelchair;

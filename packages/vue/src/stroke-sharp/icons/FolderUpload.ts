import { createVueIcon } from '../../createVueIcon';
import { FolderUpload as iconData } from '@dga-icons/core/stroke-sharp';

const FolderUpload = createVueIcon('folder-upload', iconData);

export default FolderUpload;

import { createVueIcon } from '../../createVueIcon';
import { AiScheduling as iconData } from '@dga-icons/core/stroke-sharp';

const AiScheduling = createVueIcon('ai-scheduling', iconData);

export default AiScheduling;

import { createVueIcon } from '../../createVueIcon';
import { FullSignal as iconData } from '@dga-icons/core/stroke-sharp';

const FullSignal = createVueIcon('full-signal', iconData);

export default FullSignal;

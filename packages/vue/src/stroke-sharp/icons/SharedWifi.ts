import { createVueIcon } from '../../createVueIcon';
import { SharedWifi as iconData } from '@dga-icons/core/stroke-sharp';

const SharedWifi = createVueIcon('shared-wifi', iconData);

export default SharedWifi;

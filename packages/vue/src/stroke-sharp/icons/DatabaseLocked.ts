import { createVueIcon } from '../../createVueIcon';
import { DatabaseLocked as iconData } from '@dga-icons/core/stroke-sharp';

const DatabaseLocked = createVueIcon('database-locked', iconData);

export default DatabaseLocked;

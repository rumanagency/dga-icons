import { createVueIcon } from '../../createVueIcon';
import { AiSearch as iconData } from '@dga-icons/core/stroke-sharp';

const AiSearch = createVueIcon('ai-search', iconData);

export default AiSearch;

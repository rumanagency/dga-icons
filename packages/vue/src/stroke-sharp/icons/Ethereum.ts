import { createVueIcon } from '../../createVueIcon';
import { Ethereum as iconData } from '@dga-icons/core/stroke-sharp';

const Ethereum = createVueIcon('ethereum', iconData);

export default Ethereum;

import { createVueIcon } from '../../createVueIcon';
import { FilterMailCircle as iconData } from '@dga-icons/core/stroke-sharp';

const FilterMailCircle = createVueIcon('filter-mail-circle', iconData);

export default FilterMailCircle;

import { createVueIcon } from '../../createVueIcon';
import { AddToList as iconData } from '@dga-icons/core/stroke-sharp';

const AddToList = createVueIcon('add-to-list', iconData);

export default AddToList;

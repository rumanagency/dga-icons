import { createVueIcon } from '../../createVueIcon';
import { BitcoinTag as iconData } from '@dga-icons/core/stroke-sharp';

const BitcoinTag = createVueIcon('bitcoin-tag', iconData);

export default BitcoinTag;

import { createVueIcon } from '../../createVueIcon';
import { AirplaneMode as iconData } from '@dga-icons/core/stroke-sharp';

const AirplaneMode = createVueIcon('airplane-mode', iconData);

export default AirplaneMode;

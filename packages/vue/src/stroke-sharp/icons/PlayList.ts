import { createVueIcon } from '../../createVueIcon';
import { PlayList as iconData } from '@dga-icons/core/stroke-sharp';

const PlayList = createVueIcon('play-list', iconData);

export default PlayList;

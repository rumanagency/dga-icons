import { createVueIcon } from '../../createVueIcon';
import { Messenger as iconData } from '@dga-icons/core/stroke-sharp';

const Messenger = createVueIcon('messenger', iconData);

export default Messenger;

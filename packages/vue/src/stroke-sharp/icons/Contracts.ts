import { createVueIcon } from '../../createVueIcon';
import { Contracts as iconData } from '@dga-icons/core/stroke-sharp';

const Contracts = createVueIcon('contracts', iconData);

export default Contracts;

import { createVueIcon } from '../../createVueIcon';
import { WorkoutKicking as iconData } from '@dga-icons/core/stroke-sharp';

const WorkoutKicking = createVueIcon('workout-kicking', iconData);

export default WorkoutKicking;

import { createVueIcon } from '../../createVueIcon';
import { StopLossOrder as iconData } from '@dga-icons/core/stroke-sharp';

const StopLossOrder = createVueIcon('stop-loss-order', iconData);

export default StopLossOrder;

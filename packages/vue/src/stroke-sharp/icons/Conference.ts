import { createVueIcon } from '../../createVueIcon';
import { Conference as iconData } from '@dga-icons/core/stroke-sharp';

const Conference = createVueIcon('conference', iconData);

export default Conference;

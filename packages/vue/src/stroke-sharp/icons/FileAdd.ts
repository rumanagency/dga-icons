import { createVueIcon } from '../../createVueIcon';
import { FileAdd as iconData } from '@dga-icons/core/stroke-sharp';

const FileAdd = createVueIcon('file-add', iconData);

export default FileAdd;

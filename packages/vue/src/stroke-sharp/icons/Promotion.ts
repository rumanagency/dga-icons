import { createVueIcon } from '../../createVueIcon';
import { Promotion as iconData } from '@dga-icons/core/stroke-sharp';

const Promotion = createVueIcon('promotion', iconData);

export default Promotion;

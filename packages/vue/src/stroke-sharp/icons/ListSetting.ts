import { createVueIcon } from '../../createVueIcon';
import { ListSetting as iconData } from '@dga-icons/core/stroke-sharp';

const ListSetting = createVueIcon('list-setting', iconData);

export default ListSetting;

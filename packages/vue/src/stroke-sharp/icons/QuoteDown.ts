import { createVueIcon } from '../../createVueIcon';
import { QuoteDown as iconData } from '@dga-icons/core/stroke-sharp';

const QuoteDown = createVueIcon('quote-down', iconData);

export default QuoteDown;

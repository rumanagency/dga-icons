import { createVueIcon } from '../../createVueIcon';
import { LookLeft as iconData } from '@dga-icons/core/stroke-sharp';

const LookLeft = createVueIcon('look-left', iconData);

export default LookLeft;

import { createVueIcon } from '../../createVueIcon';
import { Transaction as iconData } from '@dga-icons/core/stroke-sharp';

const Transaction = createVueIcon('transaction', iconData);

export default Transaction;

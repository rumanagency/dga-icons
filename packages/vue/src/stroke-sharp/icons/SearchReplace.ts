import { createVueIcon } from '../../createVueIcon';
import { SearchReplace as iconData } from '@dga-icons/core/stroke-sharp';

const SearchReplace = createVueIcon('search-replace', iconData);

export default SearchReplace;

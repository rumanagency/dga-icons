import { createVueIcon } from '../../createVueIcon';
import { Registered as iconData } from '@dga-icons/core/stroke-sharp';

const Registered = createVueIcon('registered', iconData);

export default Registered;

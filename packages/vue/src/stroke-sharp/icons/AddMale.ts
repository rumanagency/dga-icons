import { createVueIcon } from '../../createVueIcon';
import { AddMale as iconData } from '@dga-icons/core/stroke-sharp';

const AddMale = createVueIcon('add-male', iconData);

export default AddMale;

import { createVueIcon } from '../../createVueIcon';
import { AlignSelection as iconData } from '@dga-icons/core/stroke-sharp';

const AlignSelection = createVueIcon('align-selection', iconData);

export default AlignSelection;

import { createVueIcon } from '../../createVueIcon';
import { Lighthouse as iconData } from '@dga-icons/core/stroke-sharp';

const Lighthouse = createVueIcon('lighthouse', iconData);

export default Lighthouse;

import { createVueIcon } from '../../createVueIcon';
import { RainDrop as iconData } from '@dga-icons/core/stroke-sharp';

const RainDrop = createVueIcon('rain-drop', iconData);

export default RainDrop;

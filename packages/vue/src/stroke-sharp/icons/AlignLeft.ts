import { createVueIcon } from '../../createVueIcon';
import { AlignLeft as iconData } from '@dga-icons/core/stroke-sharp';

const AlignLeft = createVueIcon('align-left', iconData);

export default AlignLeft;

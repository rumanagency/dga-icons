import { createVueIcon } from '../../createVueIcon';
import { HorizonalScrollPoint as iconData } from '@dga-icons/core/stroke-sharp';

const HorizonalScrollPoint = createVueIcon('horizonal-scroll-point', iconData);

export default HorizonalScrollPoint;

import { createVueIcon } from '../../createVueIcon';
import { JoinBevel as iconData } from '@dga-icons/core/stroke-sharp';

const JoinBevel = createVueIcon('join-bevel', iconData);

export default JoinBevel;

import { createVueIcon } from '../../createVueIcon';
import { NoteAdd as iconData } from '@dga-icons/core/stroke-sharp';

const NoteAdd = createVueIcon('note-add', iconData);

export default NoteAdd;

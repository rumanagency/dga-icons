import { createVueIcon } from '../../createVueIcon';
import { KeyGeneratorFob as iconData } from '@dga-icons/core/stroke-sharp';

const KeyGeneratorFob = createVueIcon('key-generator-fob', iconData);

export default KeyGeneratorFob;

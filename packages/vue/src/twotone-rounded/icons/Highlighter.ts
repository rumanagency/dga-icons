import { createVueIcon } from '../../createVueIcon';
import { Highlighter as iconData } from '@dga-icons/core/twotone-rounded';

const Highlighter = createVueIcon('highlighter', iconData);

export default Highlighter;

import { createVueIcon } from '../../createVueIcon';
import { PackageAdd as iconData } from '@dga-icons/core/twotone-rounded';

const PackageAdd = createVueIcon('package-add', iconData);

export default PackageAdd;

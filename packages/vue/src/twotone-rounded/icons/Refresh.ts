import { createVueIcon } from '../../createVueIcon';
import { Refresh as iconData } from '@dga-icons/core/twotone-rounded';

const Refresh = createVueIcon('refresh', iconData);

export default Refresh;

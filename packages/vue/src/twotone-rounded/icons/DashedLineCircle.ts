import { createVueIcon } from '../../createVueIcon';
import { DashedLineCircle as iconData } from '@dga-icons/core/twotone-rounded';

const DashedLineCircle = createVueIcon('dashed-line-circle', iconData);

export default DashedLineCircle;

import { createVueIcon } from '../../createVueIcon';
import { Waiters as iconData } from '@dga-icons/core/twotone-rounded';

const Waiters = createVueIcon('waiters', iconData);

export default Waiters;

import { createVueIcon } from '../../createVueIcon';
import { Shutterstock as iconData } from '@dga-icons/core/twotone-rounded';

const Shutterstock = createVueIcon('shutterstock', iconData);

export default Shutterstock;

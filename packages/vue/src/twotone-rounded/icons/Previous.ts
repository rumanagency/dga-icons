import { createVueIcon } from '../../createVueIcon';
import { Previous as iconData } from '@dga-icons/core/twotone-rounded';

const Previous = createVueIcon('previous', iconData);

export default Previous;

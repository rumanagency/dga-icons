import { createVueIcon } from '../../createVueIcon';
import { Brochure as iconData } from '@dga-icons/core/twotone-rounded';

const Brochure = createVueIcon('brochure', iconData);

export default Brochure;

import { createVueIcon } from '../../createVueIcon';
import { Thermometer as iconData } from '@dga-icons/core/twotone-rounded';

const Thermometer = createVueIcon('thermometer', iconData);

export default Thermometer;

import { createVueIcon } from '../../createVueIcon';
import { Ampoule as iconData } from '@dga-icons/core/twotone-rounded';

const Ampoule = createVueIcon('ampoule', iconData);

export default Ampoule;

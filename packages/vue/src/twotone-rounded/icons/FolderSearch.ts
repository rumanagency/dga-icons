import { createVueIcon } from '../../createVueIcon';
import { FolderSearch as iconData } from '@dga-icons/core/twotone-rounded';

const FolderSearch = createVueIcon('folder-search', iconData);

export default FolderSearch;

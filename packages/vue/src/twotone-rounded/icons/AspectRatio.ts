import { createVueIcon } from '../../createVueIcon';
import { AspectRatio as iconData } from '@dga-icons/core/twotone-rounded';

const AspectRatio = createVueIcon('aspect-ratio', iconData);

export default AspectRatio;

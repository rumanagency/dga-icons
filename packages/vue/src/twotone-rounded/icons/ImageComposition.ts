import { createVueIcon } from '../../createVueIcon';
import { ImageComposition as iconData } from '@dga-icons/core/twotone-rounded';

const ImageComposition = createVueIcon('image-composition', iconData);

export default ImageComposition;

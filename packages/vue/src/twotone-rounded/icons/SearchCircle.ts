import { createVueIcon } from '../../createVueIcon';
import { SearchCircle as iconData } from '@dga-icons/core/twotone-rounded';

const SearchCircle = createVueIcon('search-circle', iconData);

export default SearchCircle;

import { createVueIcon } from '../../createVueIcon';
import { Lighthouse as iconData } from '@dga-icons/core/twotone-rounded';

const Lighthouse = createVueIcon('lighthouse', iconData);

export default Lighthouse;

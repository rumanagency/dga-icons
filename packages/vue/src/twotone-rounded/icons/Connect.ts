import { createVueIcon } from '../../createVueIcon';
import { Connect as iconData } from '@dga-icons/core/twotone-rounded';

const Connect = createVueIcon('connect', iconData);

export default Connect;

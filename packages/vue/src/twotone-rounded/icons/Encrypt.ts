import { createVueIcon } from '../../createVueIcon';
import { Encrypt as iconData } from '@dga-icons/core/twotone-rounded';

const Encrypt = createVueIcon('encrypt', iconData);

export default Encrypt;

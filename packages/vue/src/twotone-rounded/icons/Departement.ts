import { createVueIcon } from '../../createVueIcon';
import { Departement as iconData } from '@dga-icons/core/twotone-rounded';

const Departement = createVueIcon('departement', iconData);

export default Departement;

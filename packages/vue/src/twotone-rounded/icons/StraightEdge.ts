import { createVueIcon } from '../../createVueIcon';
import { StraightEdge as iconData } from '@dga-icons/core/twotone-rounded';

const StraightEdge = createVueIcon('straight-edge', iconData);

export default StraightEdge;

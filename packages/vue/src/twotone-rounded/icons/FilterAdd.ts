import { createVueIcon } from '../../createVueIcon';
import { FilterAdd as iconData } from '@dga-icons/core/twotone-rounded';

const FilterAdd = createVueIcon('filter-add', iconData);

export default FilterAdd;

import { createVueIcon } from '../../createVueIcon';
import { SendingOrder as iconData } from '@dga-icons/core/twotone-rounded';

const SendingOrder = createVueIcon('sending-order', iconData);

export default SendingOrder;

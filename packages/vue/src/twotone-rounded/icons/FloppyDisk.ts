import { createVueIcon } from '../../createVueIcon';
import { FloppyDisk as iconData } from '@dga-icons/core/twotone-rounded';

const FloppyDisk = createVueIcon('floppy-disk', iconData);

export default FloppyDisk;

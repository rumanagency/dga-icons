import { createVueIcon } from '../../createVueIcon';
import { Command as iconData } from '@dga-icons/core/twotone-rounded';

const Command = createVueIcon('command', iconData);

export default Command;

import { createVueIcon } from '../../createVueIcon';
import { SeatSelector as iconData } from '@dga-icons/core/twotone-rounded';

const SeatSelector = createVueIcon('seat-selector', iconData);

export default SeatSelector;

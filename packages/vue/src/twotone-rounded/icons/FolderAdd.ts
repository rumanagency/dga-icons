import { createVueIcon } from '../../createVueIcon';
import { FolderAdd as iconData } from '@dga-icons/core/twotone-rounded';

const FolderAdd = createVueIcon('folder-add', iconData);

export default FolderAdd;

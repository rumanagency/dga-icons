import { createVueIcon } from '../../createVueIcon';
import { Blocked as iconData } from '@dga-icons/core/twotone-rounded';

const Blocked = createVueIcon('blocked', iconData);

export default Blocked;

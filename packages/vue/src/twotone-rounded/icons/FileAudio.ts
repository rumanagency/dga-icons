import { createVueIcon } from '../../createVueIcon';
import { FileAudio as iconData } from '@dga-icons/core/twotone-rounded';

const FileAudio = createVueIcon('file-audio', iconData);

export default FileAudio;

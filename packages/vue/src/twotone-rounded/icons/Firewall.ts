import { createVueIcon } from '../../createVueIcon';
import { Firewall as iconData } from '@dga-icons/core/twotone-rounded';

const Firewall = createVueIcon('firewall', iconData);

export default Firewall;

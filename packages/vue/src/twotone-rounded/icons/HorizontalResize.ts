import { createVueIcon } from '../../createVueIcon';
import { HorizontalResize as iconData } from '@dga-icons/core/twotone-rounded';

const HorizontalResize = createVueIcon('horizontal-resize', iconData);

export default HorizontalResize;

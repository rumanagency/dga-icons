import { createVueIcon } from '../../createVueIcon';
import { AlignLeft as iconData } from '@dga-icons/core/twotone-rounded';

const AlignLeft = createVueIcon('align-left', iconData);

export default AlignLeft;

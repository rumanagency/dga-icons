import { createVueIcon } from '../../createVueIcon';
import { Unavailable as iconData } from '@dga-icons/core/twotone-rounded';

const Unavailable = createVueIcon('unavailable', iconData);

export default Unavailable;

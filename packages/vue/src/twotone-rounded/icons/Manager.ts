import { createVueIcon } from '../../createVueIcon';
import { Manager as iconData } from '@dga-icons/core/twotone-rounded';

const Manager = createVueIcon('manager', iconData);

export default Manager;

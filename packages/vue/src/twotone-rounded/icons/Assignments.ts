import { createVueIcon } from '../../createVueIcon';
import { Assignments as iconData } from '@dga-icons/core/twotone-rounded';

const Assignments = createVueIcon('assignments', iconData);

export default Assignments;

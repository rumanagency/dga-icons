import { createVueIcon } from '../../createVueIcon';
import { UsbConnected01 as iconData } from '@dga-icons/core/twotone-rounded';

const UsbConnected01 = createVueIcon('usb-connected-01', iconData);

export default UsbConnected01;

import { createVueIcon } from '../../createVueIcon';
import { Package as iconData } from '@dga-icons/core/twotone-rounded';

const Package = createVueIcon('package', iconData);

export default Package;

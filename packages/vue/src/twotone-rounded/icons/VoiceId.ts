import { createVueIcon } from '../../createVueIcon';
import { VoiceId as iconData } from '@dga-icons/core/twotone-rounded';

const VoiceId = createVueIcon('voice-id', iconData);

export default VoiceId;

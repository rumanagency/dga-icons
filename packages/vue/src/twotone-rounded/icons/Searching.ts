import { createVueIcon } from '../../createVueIcon';
import { Searching as iconData } from '@dga-icons/core/twotone-rounded';

const Searching = createVueIcon('searching', iconData);

export default Searching;

import { createVueIcon } from '../../createVueIcon';
import { SecurityPassword as iconData } from '@dga-icons/core/twotone-rounded';

const SecurityPassword = createVueIcon('security-password', iconData);

export default SecurityPassword;

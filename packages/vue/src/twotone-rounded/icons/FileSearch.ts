import { createVueIcon } from '../../createVueIcon';
import { FileSearch as iconData } from '@dga-icons/core/twotone-rounded';

const FileSearch = createVueIcon('file-search', iconData);

export default FileSearch;

import { createVueIcon } from '../../createVueIcon';
import { FileValidation as iconData } from '@dga-icons/core/twotone-rounded';

const FileValidation = createVueIcon('file-validation', iconData);

export default FileValidation;

import { createVueIcon } from '../../createVueIcon';
import { BoundingBox as iconData } from '@dga-icons/core/twotone-rounded';

const BoundingBox = createVueIcon('bounding-box', iconData);

export default BoundingBox;

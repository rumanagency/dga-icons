import { createVueIcon } from '../../createVueIcon';
import { CpuSettings as iconData } from '@dga-icons/core/twotone-rounded';

const CpuSettings = createVueIcon('cpu-settings', iconData);

export default CpuSettings;

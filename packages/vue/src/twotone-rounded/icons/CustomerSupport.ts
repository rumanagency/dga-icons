import { createVueIcon } from '../../createVueIcon';
import { CustomerSupport as iconData } from '@dga-icons/core/twotone-rounded';

const CustomerSupport = createVueIcon('customer-support', iconData);

export default CustomerSupport;

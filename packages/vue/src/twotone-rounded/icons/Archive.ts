import { createVueIcon } from '../../createVueIcon';
import { Archive as iconData } from '@dga-icons/core/twotone-rounded';

const Archive = createVueIcon('archive', iconData);

export default Archive;

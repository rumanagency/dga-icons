import { createVueIcon } from '../../createVueIcon';
import { Temperature as iconData } from '@dga-icons/core/twotone-rounded';

const Temperature = createVueIcon('temperature', iconData);

export default Temperature;

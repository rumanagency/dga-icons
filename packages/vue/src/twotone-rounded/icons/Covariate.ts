import { createVueIcon } from '../../createVueIcon';
import { Covariate as iconData } from '@dga-icons/core/twotone-rounded';

const Covariate = createVueIcon('covariate', iconData);

export default Covariate;

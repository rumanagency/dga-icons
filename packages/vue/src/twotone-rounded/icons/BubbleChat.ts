import { createVueIcon } from '../../createVueIcon';
import { BubbleChat as iconData } from '@dga-icons/core/twotone-rounded';

const BubbleChat = createVueIcon('bubble-chat', iconData);

export default BubbleChat;

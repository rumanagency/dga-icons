import { createVueIcon } from '../../createVueIcon';
import { Tractor as iconData } from '@dga-icons/core/twotone-rounded';

const Tractor = createVueIcon('tractor', iconData);

export default Tractor;

import { createVueIcon } from '../../createVueIcon';
import { Factory as iconData } from '@dga-icons/core/twotone-rounded';

const Factory = createVueIcon('factory', iconData);

export default Factory;

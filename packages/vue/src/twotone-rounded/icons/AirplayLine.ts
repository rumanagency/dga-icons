import { createVueIcon } from '../../createVueIcon';
import { AirplayLine as iconData } from '@dga-icons/core/twotone-rounded';

const AirplayLine = createVueIcon('airplay-line', iconData);

export default AirplayLine;

import { createVueIcon } from '../../createVueIcon';
import { Ambulance as iconData } from '@dga-icons/core/twotone-rounded';

const Ambulance = createVueIcon('ambulance', iconData);

export default Ambulance;

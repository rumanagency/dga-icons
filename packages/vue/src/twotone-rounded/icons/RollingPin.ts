import { createVueIcon } from '../../createVueIcon';
import { RollingPin as iconData } from '@dga-icons/core/twotone-rounded';

const RollingPin = createVueIcon('rolling-pin', iconData);

export default RollingPin;

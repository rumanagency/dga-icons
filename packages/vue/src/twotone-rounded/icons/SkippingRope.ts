import { createVueIcon } from '../../createVueIcon';
import { SkippingRope as iconData } from '@dga-icons/core/twotone-rounded';

const SkippingRope = createVueIcon('skipping-rope', iconData);

export default SkippingRope;

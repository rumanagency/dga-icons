import { createVueIcon } from '../../createVueIcon';
import { Strategy as iconData } from '@dga-icons/core/twotone-rounded';

const Strategy = createVueIcon('strategy', iconData);

export default Strategy;

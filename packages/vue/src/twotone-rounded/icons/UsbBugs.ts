import { createVueIcon } from '../../createVueIcon';
import { UsbBugs as iconData } from '@dga-icons/core/twotone-rounded';

const UsbBugs = createVueIcon('usb-bugs', iconData);

export default UsbBugs;

import { createVueIcon } from '../../createVueIcon';
import { Curling as iconData } from '@dga-icons/core/twotone-rounded';

const Curling = createVueIcon('curling', iconData);

export default Curling;

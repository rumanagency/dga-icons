import { createVueIcon } from '../../createVueIcon';
import { FolderOpen as iconData } from '@dga-icons/core/twotone-rounded';

const FolderOpen = createVueIcon('folder-open', iconData);

export default FolderOpen;

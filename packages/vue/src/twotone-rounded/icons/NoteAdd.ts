import { createVueIcon } from '../../createVueIcon';
import { NoteAdd as iconData } from '@dga-icons/core/twotone-rounded';

const NoteAdd = createVueIcon('note-add', iconData);

export default NoteAdd;

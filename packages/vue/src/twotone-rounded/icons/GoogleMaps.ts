import { createVueIcon } from '../../createVueIcon';
import { GoogleMaps as iconData } from '@dga-icons/core/twotone-rounded';

const GoogleMaps = createVueIcon('google-maps', iconData);

export default GoogleMaps;

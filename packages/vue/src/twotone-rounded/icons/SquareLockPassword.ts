import { createVueIcon } from '../../createVueIcon';
import { SquareLockPassword as iconData } from '@dga-icons/core/twotone-rounded';

const SquareLockPassword = createVueIcon('square-lock-password', iconData);

export default SquareLockPassword;

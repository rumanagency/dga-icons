import { createVueIcon } from '../../createVueIcon';
import { Hangout as iconData } from '@dga-icons/core/twotone-rounded';

const Hangout = createVueIcon('hangout', iconData);

export default Hangout;

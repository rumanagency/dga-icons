import { createVueIcon } from '../../createVueIcon';
import { PinCode as iconData } from '@dga-icons/core/twotone-rounded';

const PinCode = createVueIcon('pin-code', iconData);

export default PinCode;

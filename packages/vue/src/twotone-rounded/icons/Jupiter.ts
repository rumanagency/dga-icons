import { createVueIcon } from '../../createVueIcon';
import { Jupiter as iconData } from '@dga-icons/core/twotone-rounded';

const Jupiter = createVueIcon('jupiter', iconData);

export default Jupiter;

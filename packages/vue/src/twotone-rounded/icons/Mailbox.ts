import { createVueIcon } from '../../createVueIcon';
import { Mailbox as iconData } from '@dga-icons/core/twotone-rounded';

const Mailbox = createVueIcon('mailbox', iconData);

export default Mailbox;

import { createVueIcon } from '../../createVueIcon';
import { Students as iconData } from '@dga-icons/core/twotone-rounded';

const Students = createVueIcon('students', iconData);

export default Students;

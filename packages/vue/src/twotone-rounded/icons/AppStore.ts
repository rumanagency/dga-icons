import { createVueIcon } from '../../createVueIcon';
import { AppStore as iconData } from '@dga-icons/core/twotone-rounded';

const AppStore = createVueIcon('app-store', iconData);

export default AppStore;

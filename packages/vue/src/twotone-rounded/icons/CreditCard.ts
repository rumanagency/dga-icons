import { createVueIcon } from '../../createVueIcon';
import { CreditCard as iconData } from '@dga-icons/core/twotone-rounded';

const CreditCard = createVueIcon('credit-card', iconData);

export default CreditCard;

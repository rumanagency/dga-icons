import { createVueIcon } from '../../createVueIcon';
import { ContainerTruck as iconData } from '@dga-icons/core/twotone-rounded';

const ContainerTruck = createVueIcon('container-truck', iconData);

export default ContainerTruck;

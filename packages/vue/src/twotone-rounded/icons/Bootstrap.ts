import { createVueIcon } from '../../createVueIcon';
import { Bootstrap as iconData } from '@dga-icons/core/twotone-rounded';

const Bootstrap = createVueIcon('bootstrap', iconData);

export default Bootstrap;

import { createVueIcon } from '../../createVueIcon';
import { GitCompare as iconData } from '@dga-icons/core/twotone-rounded';

const GitCompare = createVueIcon('git-compare', iconData);

export default GitCompare;

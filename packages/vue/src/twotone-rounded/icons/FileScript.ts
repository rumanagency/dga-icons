import { createVueIcon } from '../../createVueIcon';
import { FileScript as iconData } from '@dga-icons/core/twotone-rounded';

const FileScript = createVueIcon('file-script', iconData);

export default FileScript;

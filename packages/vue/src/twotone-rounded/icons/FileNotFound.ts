import { createVueIcon } from '../../createVueIcon';
import { FileNotFound as iconData } from '@dga-icons/core/twotone-rounded';

const FileNotFound = createVueIcon('file-not-found', iconData);

export default FileNotFound;

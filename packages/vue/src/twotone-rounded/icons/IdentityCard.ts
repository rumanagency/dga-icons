import { createVueIcon } from '../../createVueIcon';
import { IdentityCard as iconData } from '@dga-icons/core/twotone-rounded';

const IdentityCard = createVueIcon('identity-card', iconData);

export default IdentityCard;

import { createVueIcon } from '../../createVueIcon';
import { LowSignal as iconData } from '@dga-icons/core/twotone-rounded';

const LowSignal = createVueIcon('low-signal', iconData);

export default LowSignal;

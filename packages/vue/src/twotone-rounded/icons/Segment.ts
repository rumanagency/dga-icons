import { createVueIcon } from '../../createVueIcon';
import { Segment as iconData } from '@dga-icons/core/twotone-rounded';

const Segment = createVueIcon('segment', iconData);

export default Segment;

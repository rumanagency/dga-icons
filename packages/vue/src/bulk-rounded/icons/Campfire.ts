import { createVueIcon } from '../../createVueIcon';
import { Campfire as iconData } from '@dga-icons/core/bulk-rounded';

const Campfire = createVueIcon('campfire', iconData);

export default Campfire;

import { createVueIcon } from '../../createVueIcon';
import { MailOpen as iconData } from '@dga-icons/core/bulk-rounded';

const MailOpen = createVueIcon('mail-open', iconData);

export default MailOpen;

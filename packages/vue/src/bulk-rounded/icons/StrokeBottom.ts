import { createVueIcon } from '../../createVueIcon';
import { StrokeBottom as iconData } from '@dga-icons/core/bulk-rounded';

const StrokeBottom = createVueIcon('stroke-bottom', iconData);

export default StrokeBottom;

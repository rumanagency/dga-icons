import { createVueIcon } from '../../createVueIcon';
import { Mountain as iconData } from '@dga-icons/core/bulk-rounded';

const Mountain = createVueIcon('mountain', iconData);

export default Mountain;

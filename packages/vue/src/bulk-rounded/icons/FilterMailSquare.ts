import { createVueIcon } from '../../createVueIcon';
import { FilterMailSquare as iconData } from '@dga-icons/core/bulk-rounded';

const FilterMailSquare = createVueIcon('filter-mail-square', iconData);

export default FilterMailSquare;

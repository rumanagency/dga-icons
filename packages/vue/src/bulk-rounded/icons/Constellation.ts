import { createVueIcon } from '../../createVueIcon';
import { Constellation as iconData } from '@dga-icons/core/bulk-rounded';

const Constellation = createVueIcon('constellation', iconData);

export default Constellation;

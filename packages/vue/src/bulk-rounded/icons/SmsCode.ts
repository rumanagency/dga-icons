import { createVueIcon } from '../../createVueIcon';
import { SmsCode as iconData } from '@dga-icons/core/bulk-rounded';

const SmsCode = createVueIcon('sms-code', iconData);

export default SmsCode;

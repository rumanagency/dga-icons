import { createVueIcon } from '../../createVueIcon';
import { TrafficLight as iconData } from '@dga-icons/core/bulk-rounded';

const TrafficLight = createVueIcon('traffic-light', iconData);

export default TrafficLight;

import { createVueIcon } from '../../createVueIcon';
import { Fahrenheit as iconData } from '@dga-icons/core/bulk-rounded';

const Fahrenheit = createVueIcon('fahrenheit', iconData);

export default Fahrenheit;

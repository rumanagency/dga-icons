import { createVueIcon } from '../../createVueIcon';
import { PoliceBadge as iconData } from '@dga-icons/core/bulk-rounded';

const PoliceBadge = createVueIcon('police-badge', iconData);

export default PoliceBadge;

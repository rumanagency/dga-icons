import { createVueIcon } from '../../createVueIcon';
import { FolderView as iconData } from '@dga-icons/core/bulk-rounded';

const FolderView = createVueIcon('folder-view', iconData);

export default FolderView;

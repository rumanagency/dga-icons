import { createVueIcon } from '../../createVueIcon';
import { Libraries as iconData } from '@dga-icons/core/bulk-rounded';

const Libraries = createVueIcon('libraries', iconData);

export default Libraries;

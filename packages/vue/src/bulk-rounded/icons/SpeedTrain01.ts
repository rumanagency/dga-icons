import { createVueIcon } from '../../createVueIcon';
import { SpeedTrain01 as iconData } from '@dga-icons/core/bulk-rounded';

const SpeedTrain01 = createVueIcon('speed-train-01', iconData);

export default SpeedTrain01;

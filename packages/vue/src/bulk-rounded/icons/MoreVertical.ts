import { createVueIcon } from '../../createVueIcon';
import { MoreVertical as iconData } from '@dga-icons/core/bulk-rounded';

const MoreVertical = createVueIcon('more-vertical', iconData);

export default MoreVertical;

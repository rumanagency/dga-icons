import { createVueIcon } from '../../createVueIcon';
import { ArrowUpDown as iconData } from '@dga-icons/core/bulk-rounded';

const ArrowUpDown = createVueIcon('arrow-up-down', iconData);

export default ArrowUpDown;

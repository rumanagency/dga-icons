import { createVueIcon } from '../../createVueIcon';
import { FolderEdit as iconData } from '@dga-icons/core/bulk-rounded';

const FolderEdit = createVueIcon('folder-edit', iconData);

export default FolderEdit;

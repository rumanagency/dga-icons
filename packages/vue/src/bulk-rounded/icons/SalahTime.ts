import { createVueIcon } from '../../createVueIcon';
import { SalahTime as iconData } from '@dga-icons/core/bulk-rounded';

const SalahTime = createVueIcon('salah-time', iconData);

export default SalahTime;

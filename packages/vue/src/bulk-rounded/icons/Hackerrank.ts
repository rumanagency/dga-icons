import { createVueIcon } from '../../createVueIcon';
import { Hackerrank as iconData } from '@dga-icons/core/bulk-rounded';

const Hackerrank = createVueIcon('hackerrank', iconData);

export default Hackerrank;

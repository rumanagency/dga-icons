import { createVueIcon } from '../../createVueIcon';
import { CustomerSupport as iconData } from '@dga-icons/core/bulk-rounded';

const CustomerSupport = createVueIcon('customer-support', iconData);

export default CustomerSupport;

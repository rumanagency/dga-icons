import { createVueIcon } from '../../createVueIcon';
import { UserGroup as iconData } from '@dga-icons/core/bulk-rounded';

const UserGroup = createVueIcon('user-group', iconData);

export default UserGroup;

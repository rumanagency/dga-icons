import { createVueIcon } from '../../createVueIcon';
import { FloppyDisk as iconData } from '@dga-icons/core/bulk-rounded';

const FloppyDisk = createVueIcon('floppy-disk', iconData);

export default FloppyDisk;

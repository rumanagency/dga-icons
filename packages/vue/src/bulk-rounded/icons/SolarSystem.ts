import { createVueIcon } from '../../createVueIcon';
import { SolarSystem as iconData } from '@dga-icons/core/bulk-rounded';

const SolarSystem = createVueIcon('solar-system', iconData);

export default SolarSystem;

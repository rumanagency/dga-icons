import { createVueIcon } from '../../createVueIcon';
import { NoteEdit as iconData } from '@dga-icons/core/bulk-rounded';

const NoteEdit = createVueIcon('note-edit', iconData);

export default NoteEdit;

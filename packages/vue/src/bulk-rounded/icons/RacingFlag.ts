import { createVueIcon } from '../../createVueIcon';
import { RacingFlag as iconData } from '@dga-icons/core/bulk-rounded';

const RacingFlag = createVueIcon('racing-flag', iconData);

export default RacingFlag;

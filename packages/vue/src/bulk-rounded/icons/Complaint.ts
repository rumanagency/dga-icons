import { createVueIcon } from '../../createVueIcon';
import { Complaint as iconData } from '@dga-icons/core/bulk-rounded';

const Complaint = createVueIcon('complaint', iconData);

export default Complaint;

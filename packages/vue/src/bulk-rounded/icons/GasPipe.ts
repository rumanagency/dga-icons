import { createVueIcon } from '../../createVueIcon';
import { GasPipe as iconData } from '@dga-icons/core/bulk-rounded';

const GasPipe = createVueIcon('gas-pipe', iconData);

export default GasPipe;

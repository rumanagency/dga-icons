import { createVueIcon } from '../../createVueIcon';
import { FlimSlate as iconData } from '@dga-icons/core/bulk-rounded';

const FlimSlate = createVueIcon('flim-slate', iconData);

export default FlimSlate;

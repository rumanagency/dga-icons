import { createVueIcon } from '../../createVueIcon';
import { DatabaseSetting as iconData } from '@dga-icons/core/bulk-rounded';

const DatabaseSetting = createVueIcon('database-setting', iconData);

export default DatabaseSetting;

import { createVueIcon } from '../../createVueIcon';
import { Whatsapp as iconData } from '@dga-icons/core/bulk-rounded';

const Whatsapp = createVueIcon('whatsapp', iconData);

export default Whatsapp;

import { createVueIcon } from '../../createVueIcon';
import { CodeCircle as iconData } from '@dga-icons/core/bulk-rounded';

const CodeCircle = createVueIcon('code-circle', iconData);

export default CodeCircle;

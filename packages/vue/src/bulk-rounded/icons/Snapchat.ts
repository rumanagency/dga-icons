import { createVueIcon } from '../../createVueIcon';
import { Snapchat as iconData } from '@dga-icons/core/bulk-rounded';

const Snapchat = createVueIcon('snapchat', iconData);

export default Snapchat;

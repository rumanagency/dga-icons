import { createVueIcon } from '../../createVueIcon';
import { InsertTopImage as iconData } from '@dga-icons/core/bulk-rounded';

const InsertTopImage = createVueIcon('insert-top-image', iconData);

export default InsertTopImage;

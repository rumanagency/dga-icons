import { createVueIcon } from '../../createVueIcon';
import { CustomerService as iconData } from '@dga-icons/core/bulk-rounded';

const CustomerService = createVueIcon('customer-service', iconData);

export default CustomerService;

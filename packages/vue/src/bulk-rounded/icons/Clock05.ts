import { createVueIcon } from '../../createVueIcon';
import { Clock05 as iconData } from '@dga-icons/core/bulk-rounded';

const Clock05 = createVueIcon('clock-05', iconData);

export default Clock05;

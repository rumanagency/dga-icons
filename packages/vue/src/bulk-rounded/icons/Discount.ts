import { createVueIcon } from '../../createVueIcon';
import { Discount as iconData } from '@dga-icons/core/bulk-rounded';

const Discount = createVueIcon('discount', iconData);

export default Discount;

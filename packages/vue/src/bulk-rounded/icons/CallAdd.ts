import { createVueIcon } from '../../createVueIcon';
import { CallAdd as iconData } from '@dga-icons/core/bulk-rounded';

const CallAdd = createVueIcon('call-add', iconData);

export default CallAdd;

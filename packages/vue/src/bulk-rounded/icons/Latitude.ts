import { createVueIcon } from '../../createVueIcon';
import { Latitude as iconData } from '@dga-icons/core/bulk-rounded';

const Latitude = createVueIcon('latitude', iconData);

export default Latitude;

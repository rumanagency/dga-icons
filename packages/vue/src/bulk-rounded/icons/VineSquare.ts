import { createVueIcon } from '../../createVueIcon';
import { VineSquare as iconData } from '@dga-icons/core/bulk-rounded';

const VineSquare = createVueIcon('vine-square', iconData);

export default VineSquare;

import { createVueIcon } from '../../createVueIcon';
import { NewOffice as iconData } from '@dga-icons/core/bulk-rounded';

const NewOffice = createVueIcon('new-office', iconData);

export default NewOffice;

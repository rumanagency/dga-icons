import { createVueIcon } from '../../createVueIcon';
import { CameraVideo as iconData } from '@dga-icons/core/bulk-rounded';

const CameraVideo = createVueIcon('camera-video', iconData);

export default CameraVideo;

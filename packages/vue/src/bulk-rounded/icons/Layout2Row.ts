import { createVueIcon } from '../../createVueIcon';
import { Layout2Row as iconData } from '@dga-icons/core/bulk-rounded';

const Layout2Row = createVueIcon('layout-2-row', iconData);

export default Layout2Row;

import { createVueIcon } from '../../createVueIcon';
import { Identification as iconData } from '@dga-icons/core/bulk-rounded';

const Identification = createVueIcon('identification', iconData);

export default Identification;

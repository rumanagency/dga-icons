import { createVueIcon } from '../../createVueIcon';
import { BatteryEmpty as iconData } from '@dga-icons/core/bulk-rounded';

const BatteryEmpty = createVueIcon('battery-empty', iconData);

export default BatteryEmpty;

import { createVueIcon } from '../../createVueIcon';
import { Headphones as iconData } from '@dga-icons/core/bulk-rounded';

const Headphones = createVueIcon('headphones', iconData);

export default Headphones;

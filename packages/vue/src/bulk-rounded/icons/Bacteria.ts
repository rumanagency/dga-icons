import { createVueIcon } from '../../createVueIcon';
import { Bacteria as iconData } from '@dga-icons/core/bulk-rounded';

const Bacteria = createVueIcon('bacteria', iconData);

export default Bacteria;

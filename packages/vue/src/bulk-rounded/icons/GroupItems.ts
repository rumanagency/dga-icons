import { createVueIcon } from '../../createVueIcon';
import { GroupItems as iconData } from '@dga-icons/core/bulk-rounded';

const GroupItems = createVueIcon('group-items', iconData);

export default GroupItems;

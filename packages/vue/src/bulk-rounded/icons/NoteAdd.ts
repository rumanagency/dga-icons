import { createVueIcon } from '../../createVueIcon';
import { NoteAdd as iconData } from '@dga-icons/core/bulk-rounded';

const NoteAdd = createVueIcon('note-add', iconData);

export default NoteAdd;

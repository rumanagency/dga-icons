import { createVueIcon } from '../../createVueIcon';
import { MenuCircle as iconData } from '@dga-icons/core/bulk-rounded';

const MenuCircle = createVueIcon('menu-circle', iconData);

export default MenuCircle;

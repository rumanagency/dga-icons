import { createVueIcon } from '../../createVueIcon';
import { CloudServer as iconData } from '@dga-icons/core/bulk-rounded';

const CloudServer = createVueIcon('cloud-server', iconData);

export default CloudServer;

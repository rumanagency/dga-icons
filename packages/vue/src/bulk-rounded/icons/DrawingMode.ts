import { createVueIcon } from '../../createVueIcon';
import { DrawingMode as iconData } from '@dga-icons/core/bulk-rounded';

const DrawingMode = createVueIcon('drawing-mode', iconData);

export default DrawingMode;

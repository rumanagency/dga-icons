import { createVueIcon } from '../../createVueIcon';
import { Pipeline as iconData } from '@dga-icons/core/bulk-rounded';

const Pipeline = createVueIcon('pipeline', iconData);

export default Pipeline;

import { createVueIcon } from '../../createVueIcon';
import { TouchLocked01 as iconData } from '@dga-icons/core/bulk-rounded';

const TouchLocked01 = createVueIcon('touch-locked-01', iconData);

export default TouchLocked01;

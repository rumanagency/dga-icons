import { createVueIcon } from '../../createVueIcon';
import { Coinbase as iconData } from '@dga-icons/core/bulk-rounded';

const Coinbase = createVueIcon('coinbase', iconData);

export default Coinbase;

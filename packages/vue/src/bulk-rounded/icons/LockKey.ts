import { createVueIcon } from '../../createVueIcon';
import { LockKey as iconData } from '@dga-icons/core/bulk-rounded';

const LockKey = createVueIcon('lock-key', iconData);

export default LockKey;

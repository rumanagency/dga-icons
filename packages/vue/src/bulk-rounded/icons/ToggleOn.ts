import { createVueIcon } from '../../createVueIcon';
import { ToggleOn as iconData } from '@dga-icons/core/bulk-rounded';

const ToggleOn = createVueIcon('toggle-on', iconData);

export default ToggleOn;

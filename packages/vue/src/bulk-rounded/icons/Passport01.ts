import { createVueIcon } from '../../createVueIcon';
import { Passport01 as iconData } from '@dga-icons/core/bulk-rounded';

const Passport01 = createVueIcon('passport-01', iconData);

export default Passport01;

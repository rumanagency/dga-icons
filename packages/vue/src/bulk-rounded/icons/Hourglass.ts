import { createVueIcon } from '../../createVueIcon';
import { Hourglass as iconData } from '@dga-icons/core/bulk-rounded';

const Hourglass = createVueIcon('hourglass', iconData);

export default Hourglass;

import { createVueIcon } from '../../createVueIcon';
import { PowerService as iconData } from '@dga-icons/core/bulk-rounded';

const PowerService = createVueIcon('power-service', iconData);

export default PowerService;

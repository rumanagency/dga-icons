import { createVueIcon } from '../../createVueIcon';
import { BubbleChat as iconData } from '@dga-icons/core/bulk-rounded';

const BubbleChat = createVueIcon('bubble-chat', iconData);

export default BubbleChat;

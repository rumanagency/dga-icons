import { createVueIcon } from '../../createVueIcon';
import { Submarine as iconData } from '@dga-icons/core/bulk-rounded';

const Submarine = createVueIcon('submarine', iconData);

export default Submarine;

import { createVueIcon } from '../../createVueIcon';
import { FunctionCircle as iconData } from '@dga-icons/core/bulk-rounded';

const FunctionCircle = createVueIcon('function-circle', iconData);

export default FunctionCircle;

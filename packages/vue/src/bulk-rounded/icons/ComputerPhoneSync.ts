import { createVueIcon } from '../../createVueIcon';
import { ComputerPhoneSync as iconData } from '@dga-icons/core/bulk-rounded';

const ComputerPhoneSync = createVueIcon('computer-phone-sync', iconData);

export default ComputerPhoneSync;

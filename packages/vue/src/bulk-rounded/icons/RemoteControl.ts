import { createVueIcon } from '../../createVueIcon';
import { RemoteControl as iconData } from '@dga-icons/core/bulk-rounded';

const RemoteControl = createVueIcon('remote-control', iconData);

export default RemoteControl;

import { createVueIcon } from '../../createVueIcon';
import { BitcoinReceive as iconData } from '@dga-icons/core/bulk-rounded';

const BitcoinReceive = createVueIcon('bitcoin-receive', iconData);

export default BitcoinReceive;

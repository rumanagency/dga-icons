import { createVueIcon } from '../../createVueIcon';
import { Registered as iconData } from '@dga-icons/core/bulk-rounded';

const Registered = createVueIcon('registered', iconData);

export default Registered;

import { createVueIcon } from '../../createVueIcon';
import { Diameter as iconData } from '@dga-icons/core/bulk-rounded';

const Diameter = createVueIcon('diameter', iconData);

export default Diameter;

import { createVueIcon } from '../../createVueIcon';
import { ImageRotationClockwise as iconData } from '@dga-icons/core/bulk-rounded';

const ImageRotationClockwise = createVueIcon('image-rotation-clockwise', iconData);

export default ImageRotationClockwise;

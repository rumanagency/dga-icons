import { createVueIcon } from '../../createVueIcon';
import { LayerMask02 as iconData } from '@dga-icons/core/bulk-rounded';

const LayerMask02 = createVueIcon('layer-mask-02', iconData);

export default LayerMask02;

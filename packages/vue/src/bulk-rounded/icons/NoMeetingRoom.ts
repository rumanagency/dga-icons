import { createVueIcon } from '../../createVueIcon';
import { NoMeetingRoom as iconData } from '@dga-icons/core/bulk-rounded';

const NoMeetingRoom = createVueIcon('no-meeting-room', iconData);

export default NoMeetingRoom;

import { createVueIcon } from '../../createVueIcon';
import { StepOver as iconData } from '@dga-icons/core/bulk-rounded';

const StepOver = createVueIcon('step-over', iconData);

export default StepOver;

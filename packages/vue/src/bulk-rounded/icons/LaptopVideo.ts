import { createVueIcon } from '../../createVueIcon';
import { LaptopVideo as iconData } from '@dga-icons/core/bulk-rounded';

const LaptopVideo = createVueIcon('laptop-video', iconData);

export default LaptopVideo;

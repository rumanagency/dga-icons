import { createVueIcon } from '../../createVueIcon';
import { BookEdit as iconData } from '@dga-icons/core/bulk-rounded';

const BookEdit = createVueIcon('book-edit', iconData);

export default BookEdit;

import { createVueIcon } from '../../createVueIcon';
import { StopWatch as iconData } from '@dga-icons/core/bulk-rounded';

const StopWatch = createVueIcon('stop-watch', iconData);

export default StopWatch;

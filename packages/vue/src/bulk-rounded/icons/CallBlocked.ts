import { createVueIcon } from '../../createVueIcon';
import { CallBlocked as iconData } from '@dga-icons/core/bulk-rounded';

const CallBlocked = createVueIcon('call-blocked', iconData);

export default CallBlocked;

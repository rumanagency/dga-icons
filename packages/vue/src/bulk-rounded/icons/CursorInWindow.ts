import { createVueIcon } from '../../createVueIcon';
import { CursorInWindow as iconData } from '@dga-icons/core/bulk-rounded';

const CursorInWindow = createVueIcon('cursor-in-window', iconData);

export default CursorInWindow;

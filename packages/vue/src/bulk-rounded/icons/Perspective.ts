import { createVueIcon } from '../../createVueIcon';
import { Perspective as iconData } from '@dga-icons/core/bulk-rounded';

const Perspective = createVueIcon('perspective', iconData);

export default Perspective;

import { createVueIcon } from '../../createVueIcon';
import { Contracts as iconData } from '@dga-icons/core/bulk-rounded';

const Contracts = createVueIcon('contracts', iconData);

export default Contracts;

import { createVueIcon } from '../../createVueIcon';
import { LayoutLeft as iconData } from '@dga-icons/core/bulk-rounded';

const LayoutLeft = createVueIcon('layout-left', iconData);

export default LayoutLeft;

import { createVueIcon } from '../../createVueIcon';
import { Keyframe as iconData } from '@dga-icons/core/bulk-rounded';

const Keyframe = createVueIcon('keyframe', iconData);

export default Keyframe;

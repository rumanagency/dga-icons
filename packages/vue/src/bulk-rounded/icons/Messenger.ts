import { createVueIcon } from '../../createVueIcon';
import { Messenger as iconData } from '@dga-icons/core/bulk-rounded';

const Messenger = createVueIcon('messenger', iconData);

export default Messenger;

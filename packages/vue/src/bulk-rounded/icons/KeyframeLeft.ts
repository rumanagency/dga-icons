import { createVueIcon } from '../../createVueIcon';
import { KeyframeLeft as iconData } from '@dga-icons/core/bulk-rounded';

const KeyframeLeft = createVueIcon('keyframe-left', iconData);

export default KeyframeLeft;

import { createVueIcon } from '../../createVueIcon';
import { Brochure as iconData } from '@dga-icons/core/bulk-rounded';

const Brochure = createVueIcon('brochure', iconData);

export default Brochure;

import { createVueIcon } from '../../createVueIcon';
import { UserCircle as iconData } from '@dga-icons/core/bulk-rounded';

const UserCircle = createVueIcon('user-circle', iconData);

export default UserCircle;

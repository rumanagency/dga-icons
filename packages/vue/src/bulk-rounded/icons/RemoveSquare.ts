import { createVueIcon } from '../../createVueIcon';
import { RemoveSquare as iconData } from '@dga-icons/core/bulk-rounded';

const RemoveSquare = createVueIcon('remove-square', iconData);

export default RemoveSquare;

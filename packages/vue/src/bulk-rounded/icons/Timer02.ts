import { createVueIcon } from '../../createVueIcon';
import { Timer02 as iconData } from '@dga-icons/core/bulk-rounded';

const Timer02 = createVueIcon('timer-02', iconData);

export default Timer02;

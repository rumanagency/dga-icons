import { createVueIcon } from '../../createVueIcon';
import { Limitation as iconData } from '@dga-icons/core/bulk-rounded';

const Limitation = createVueIcon('limitation', iconData);

export default Limitation;

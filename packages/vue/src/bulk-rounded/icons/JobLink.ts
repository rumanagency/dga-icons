import { createVueIcon } from '../../createVueIcon';
import { JobLink as iconData } from '@dga-icons/core/bulk-rounded';

const JobLink = createVueIcon('job-link', iconData);

export default JobLink;

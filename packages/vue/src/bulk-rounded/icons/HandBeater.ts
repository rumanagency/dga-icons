import { createVueIcon } from '../../createVueIcon';
import { HandBeater as iconData } from '@dga-icons/core/bulk-rounded';

const HandBeater = createVueIcon('hand-beater', iconData);

export default HandBeater;

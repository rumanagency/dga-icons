import { createVueIcon } from '../../createVueIcon';
import { Microsoft as iconData } from '@dga-icons/core/bulk-rounded';

const Microsoft = createVueIcon('microsoft', iconData);

export default Microsoft;

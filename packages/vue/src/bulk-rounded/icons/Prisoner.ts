import { createVueIcon } from '../../createVueIcon';
import { Prisoner as iconData } from '@dga-icons/core/bulk-rounded';

const Prisoner = createVueIcon('prisoner', iconData);

export default Prisoner;

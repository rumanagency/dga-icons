import { createVueIcon } from '../../createVueIcon';
import { WindowsOld as iconData } from '@dga-icons/core/bulk-rounded';

const WindowsOld = createVueIcon('windows-old', iconData);

export default WindowsOld;

import { createVueIcon } from '../../createVueIcon';
import { Satellite as iconData } from '@dga-icons/core/bulk-rounded';

const Satellite = createVueIcon('satellite', iconData);

export default Satellite;

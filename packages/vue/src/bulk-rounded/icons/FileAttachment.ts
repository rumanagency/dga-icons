import { createVueIcon } from '../../createVueIcon';
import { FileAttachment as iconData } from '@dga-icons/core/bulk-rounded';

const FileAttachment = createVueIcon('file-attachment', iconData);

export default FileAttachment;

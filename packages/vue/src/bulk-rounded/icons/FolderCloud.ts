import { createVueIcon } from '../../createVueIcon';
import { FolderCloud as iconData } from '@dga-icons/core/bulk-rounded';

const FolderCloud = createVueIcon('folder-cloud', iconData);

export default FolderCloud;

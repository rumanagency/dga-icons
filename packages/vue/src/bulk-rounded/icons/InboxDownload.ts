import { createVueIcon } from '../../createVueIcon';
import { InboxDownload as iconData } from '@dga-icons/core/bulk-rounded';

const InboxDownload = createVueIcon('inbox-download', iconData);

export default InboxDownload;

import { createVueIcon } from '../../createVueIcon';
import { Chopsticks as iconData } from '@dga-icons/core/bulk-rounded';

const Chopsticks = createVueIcon('chopsticks', iconData);

export default Chopsticks;

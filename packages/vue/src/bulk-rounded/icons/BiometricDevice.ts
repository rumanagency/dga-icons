import { createVueIcon } from '../../createVueIcon';
import { BiometricDevice as iconData } from '@dga-icons/core/bulk-rounded';

const BiometricDevice = createVueIcon('biometric-device', iconData);

export default BiometricDevice;

import { createVueIcon } from '../../createVueIcon';
import { Security as iconData } from '@dga-icons/core/bulk-rounded';

const Security = createVueIcon('security', iconData);

export default Security;

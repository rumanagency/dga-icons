import { createVueIcon } from '../../createVueIcon';
import { Keyboard as iconData } from '@dga-icons/core/bulk-rounded';

const Keyboard = createVueIcon('keyboard', iconData);

export default Keyboard;

import { createVueIcon } from '../../createVueIcon';
import { ImageActualSize as iconData } from '@dga-icons/core/bulk-rounded';

const ImageActualSize = createVueIcon('image-actual-size', iconData);

export default ImageActualSize;

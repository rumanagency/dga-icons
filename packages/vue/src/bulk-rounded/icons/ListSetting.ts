import { createVueIcon } from '../../createVueIcon';
import { ListSetting as iconData } from '@dga-icons/core/bulk-rounded';

const ListSetting = createVueIcon('list-setting', iconData);

export default ListSetting;

import { createVueIcon } from '../../createVueIcon';
import { Searching as iconData } from '@dga-icons/core/bulk-rounded';

const Searching = createVueIcon('searching', iconData);

export default Searching;

import { createVueIcon } from '../../createVueIcon';
import { CovidInfo as iconData } from '@dga-icons/core/bulk-rounded';

const CovidInfo = createVueIcon('covid-info', iconData);

export default CovidInfo;

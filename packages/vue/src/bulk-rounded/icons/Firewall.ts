import { createVueIcon } from '../../createVueIcon';
import { Firewall as iconData } from '@dga-icons/core/bulk-rounded';

const Firewall = createVueIcon('firewall', iconData);

export default Firewall;

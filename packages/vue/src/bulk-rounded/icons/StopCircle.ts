import { createVueIcon } from '../../createVueIcon';
import { StopCircle as iconData } from '@dga-icons/core/bulk-rounded';

const StopCircle = createVueIcon('stop-circle', iconData);

export default StopCircle;

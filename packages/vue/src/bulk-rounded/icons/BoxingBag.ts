import { createVueIcon } from '../../createVueIcon';
import { BoxingBag as iconData } from '@dga-icons/core/bulk-rounded';

const BoxingBag = createVueIcon('boxing-bag', iconData);

export default BoxingBag;

import { createVueIcon } from '../../createVueIcon';
import { AlignTop as iconData } from '@dga-icons/core/bulk-rounded';

const AlignTop = createVueIcon('align-top', iconData);

export default AlignTop;

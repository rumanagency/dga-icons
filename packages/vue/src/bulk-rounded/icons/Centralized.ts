import { createVueIcon } from '../../createVueIcon';
import { Centralized as iconData } from '@dga-icons/core/bulk-rounded';

const Centralized = createVueIcon('centralized', iconData);

export default Centralized;

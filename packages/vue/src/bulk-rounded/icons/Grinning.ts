import { createVueIcon } from '../../createVueIcon';
import { Grinning as iconData } from '@dga-icons/core/bulk-rounded';

const Grinning = createVueIcon('grinning', iconData);

export default Grinning;

import { createVueIcon } from '../../createVueIcon';
import { NodeEdit as iconData } from '@dga-icons/core/bulk-rounded';

const NodeEdit = createVueIcon('node-edit', iconData);

export default NodeEdit;

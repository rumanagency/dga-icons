import { createVueIcon } from '../../createVueIcon';
import { Swimming as iconData } from '@dga-icons/core/bulk-rounded';

const Swimming = createVueIcon('swimming', iconData);

export default Swimming;

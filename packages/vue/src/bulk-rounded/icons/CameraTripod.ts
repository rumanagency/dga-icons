import { createVueIcon } from '../../createVueIcon';
import { CameraTripod as iconData } from '@dga-icons/core/bulk-rounded';

const CameraTripod = createVueIcon('camera-tripod', iconData);

export default CameraTripod;

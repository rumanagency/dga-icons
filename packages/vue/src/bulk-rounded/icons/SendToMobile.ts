import { createVueIcon } from '../../createVueIcon';
import { SendToMobile as iconData } from '@dga-icons/core/bulk-rounded';

const SendToMobile = createVueIcon('send-to-mobile', iconData);

export default SendToMobile;

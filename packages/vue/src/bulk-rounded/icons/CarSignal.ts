import { createVueIcon } from '../../createVueIcon';
import { CarSignal as iconData } from '@dga-icons/core/bulk-rounded';

const CarSignal = createVueIcon('car-signal', iconData);

export default CarSignal;

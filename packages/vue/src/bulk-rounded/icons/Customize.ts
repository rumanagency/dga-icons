import { createVueIcon } from '../../createVueIcon';
import { Customize as iconData } from '@dga-icons/core/bulk-rounded';

const Customize = createVueIcon('customize', iconData);

export default Customize;

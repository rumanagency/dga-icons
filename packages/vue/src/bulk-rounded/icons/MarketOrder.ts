import { createVueIcon } from '../../createVueIcon';
import { MarketOrder as iconData } from '@dga-icons/core/bulk-rounded';

const MarketOrder = createVueIcon('market-order', iconData);

export default MarketOrder;

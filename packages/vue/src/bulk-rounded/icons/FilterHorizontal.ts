import { createVueIcon } from '../../createVueIcon';
import { FilterHorizontal as iconData } from '@dga-icons/core/bulk-rounded';

const FilterHorizontal = createVueIcon('filter-horizontal', iconData);

export default FilterHorizontal;

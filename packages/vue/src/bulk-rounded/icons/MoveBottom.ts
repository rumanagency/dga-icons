import { createVueIcon } from '../../createVueIcon';
import { MoveBottom as iconData } from '@dga-icons/core/bulk-rounded';

const MoveBottom = createVueIcon('move-bottom', iconData);

export default MoveBottom;

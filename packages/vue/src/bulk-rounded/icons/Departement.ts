import { createVueIcon } from '../../createVueIcon';
import { Departement as iconData } from '@dga-icons/core/bulk-rounded';

const Departement = createVueIcon('departement', iconData);

export default Departement;

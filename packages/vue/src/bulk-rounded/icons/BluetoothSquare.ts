import { createVueIcon } from '../../createVueIcon';
import { BluetoothSquare as iconData } from '@dga-icons/core/bulk-rounded';

const BluetoothSquare = createVueIcon('bluetooth-square', iconData);

export default BluetoothSquare;

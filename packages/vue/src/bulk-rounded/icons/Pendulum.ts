import { createVueIcon } from '../../createVueIcon';
import { Pendulum as iconData } from '@dga-icons/core/bulk-rounded';

const Pendulum = createVueIcon('pendulum', iconData);

export default Pendulum;

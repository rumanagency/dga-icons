import { createVueIcon } from '../../createVueIcon';
import { FolderOpen as iconData } from '@dga-icons/core/bulk-rounded';

const FolderOpen = createVueIcon('folder-open', iconData);

export default FolderOpen;

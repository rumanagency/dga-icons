import { createVueIcon } from '../../createVueIcon';
import { Absolute as iconData } from '@dga-icons/core/bulk-rounded';

const Absolute = createVueIcon('absolute', iconData);

export default Absolute;

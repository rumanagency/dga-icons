import { createVueIcon } from '../../createVueIcon';
import { Layout07 as iconData } from '@dga-icons/core/bulk-rounded';

const Layout07 = createVueIcon('layout-07', iconData);

export default Layout07;

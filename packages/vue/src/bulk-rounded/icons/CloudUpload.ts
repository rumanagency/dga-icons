import { createVueIcon } from '../../createVueIcon';
import { CloudUpload as iconData } from '@dga-icons/core/bulk-rounded';

const CloudUpload = createVueIcon('cloud-upload', iconData);

export default CloudUpload;

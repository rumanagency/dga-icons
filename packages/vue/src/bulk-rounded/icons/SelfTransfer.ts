import { createVueIcon } from '../../createVueIcon';
import { SelfTransfer as iconData } from '@dga-icons/core/bulk-rounded';

const SelfTransfer = createVueIcon('self-transfer', iconData);

export default SelfTransfer;

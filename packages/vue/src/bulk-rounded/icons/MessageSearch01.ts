import { createVueIcon } from '../../createVueIcon';
import { MessageSearch01 as iconData } from '@dga-icons/core/bulk-rounded';

const MessageSearch01 = createVueIcon('message-search-01', iconData);

export default MessageSearch01;

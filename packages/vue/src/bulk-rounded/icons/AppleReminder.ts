import { createVueIcon } from '../../createVueIcon';
import { AppleReminder as iconData } from '@dga-icons/core/bulk-rounded';

const AppleReminder = createVueIcon('apple-reminder', iconData);

export default AppleReminder;

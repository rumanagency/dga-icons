import { createVueIcon } from '../../createVueIcon';
import { MenuSquare as iconData } from '@dga-icons/core/bulk-rounded';

const MenuSquare = createVueIcon('menu-square', iconData);

export default MenuSquare;

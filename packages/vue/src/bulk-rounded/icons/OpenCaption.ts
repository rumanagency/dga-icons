import { createVueIcon } from '../../createVueIcon';
import { OpenCaption as iconData } from '@dga-icons/core/bulk-rounded';

const OpenCaption = createVueIcon('open-caption', iconData);

export default OpenCaption;

import { createVueIcon } from '../../createVueIcon';
import { Champion as iconData } from '@dga-icons/core/bulk-rounded';

const Champion = createVueIcon('champion', iconData);

export default Champion;

import { createVueIcon } from '../../createVueIcon';
import { Layout06 as iconData } from '@dga-icons/core/bulk-rounded';

const Layout06 = createVueIcon('layout-06', iconData);

export default Layout06;

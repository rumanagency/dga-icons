import { createVueIcon } from '../../createVueIcon';
import { ChatBot as iconData } from '@dga-icons/core/bulk-rounded';

const ChatBot = createVueIcon('chat-bot', iconData);

export default ChatBot;

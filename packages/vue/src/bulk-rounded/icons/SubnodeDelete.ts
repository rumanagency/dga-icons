import { createVueIcon } from '../../createVueIcon';
import { SubnodeDelete as iconData } from '@dga-icons/core/bulk-rounded';

const SubnodeDelete = createVueIcon('subnode-delete', iconData);

export default SubnodeDelete;

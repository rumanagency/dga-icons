import { createVueIcon } from '../../createVueIcon';
import { Layers01 as iconData } from '@dga-icons/core/bulk-rounded';

const Layers01 = createVueIcon('layers-01', iconData);

export default Layers01;

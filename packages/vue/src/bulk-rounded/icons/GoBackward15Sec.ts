import { createVueIcon } from '../../createVueIcon';
import { GoBackward15Sec as iconData } from '@dga-icons/core/bulk-rounded';

const GoBackward15Sec = createVueIcon('go-backward-15-sec', iconData);

export default GoBackward15Sec;

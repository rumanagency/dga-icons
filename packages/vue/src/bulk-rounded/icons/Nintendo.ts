import { createVueIcon } from '../../createVueIcon';
import { Nintendo as iconData } from '@dga-icons/core/bulk-rounded';

const Nintendo = createVueIcon('nintendo', iconData);

export default Nintendo;

import { createVueIcon } from '../../createVueIcon';
import { WifiDisconnected02 as iconData } from '@dga-icons/core/bulk-rounded';

const WifiDisconnected02 = createVueIcon('wifi-disconnected-02', iconData);

export default WifiDisconnected02;

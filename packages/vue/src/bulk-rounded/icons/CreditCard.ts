import { createVueIcon } from '../../createVueIcon';
import { CreditCard as iconData } from '@dga-icons/core/bulk-rounded';

const CreditCard = createVueIcon('credit-card', iconData);

export default CreditCard;

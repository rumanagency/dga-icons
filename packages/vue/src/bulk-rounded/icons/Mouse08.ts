import { createVueIcon } from '../../createVueIcon';
import { Mouse08 as iconData } from '@dga-icons/core/bulk-rounded';

const Mouse08 = createVueIcon('mouse-08', iconData);

export default Mouse08;

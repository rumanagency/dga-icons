import { createVueIcon } from '../../createVueIcon';
import { WindSurf as iconData } from '@dga-icons/core/bulk-rounded';

const WindSurf = createVueIcon('wind-surf', iconData);

export default WindSurf;

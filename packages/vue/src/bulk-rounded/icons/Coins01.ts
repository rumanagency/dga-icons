import { createVueIcon } from '../../createVueIcon';
import { Coins01 as iconData } from '@dga-icons/core/bulk-rounded';

const Coins01 = createVueIcon('coins-01', iconData);

export default Coins01;

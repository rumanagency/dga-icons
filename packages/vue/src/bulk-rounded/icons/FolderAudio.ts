import { createVueIcon } from '../../createVueIcon';
import { FolderAudio as iconData } from '@dga-icons/core/bulk-rounded';

const FolderAudio = createVueIcon('folder-audio', iconData);

export default FolderAudio;

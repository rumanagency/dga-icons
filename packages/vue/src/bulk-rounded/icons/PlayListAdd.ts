import { createVueIcon } from '../../createVueIcon';
import { PlayListAdd as iconData } from '@dga-icons/core/bulk-rounded';

const PlayListAdd = createVueIcon('play-list-add', iconData);

export default PlayListAdd;

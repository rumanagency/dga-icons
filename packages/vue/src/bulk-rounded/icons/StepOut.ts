import { createVueIcon } from '../../createVueIcon';
import { StepOut as iconData } from '@dga-icons/core/bulk-rounded';

const StepOut = createVueIcon('step-out', iconData);

export default StepOut;

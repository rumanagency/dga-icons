import { createVueIcon } from '../../createVueIcon';
import { Calculate as iconData } from '@dga-icons/core/bulk-rounded';

const Calculate = createVueIcon('calculate', iconData);

export default Calculate;

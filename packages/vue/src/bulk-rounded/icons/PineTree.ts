import { createVueIcon } from '../../createVueIcon';
import { PineTree as iconData } from '@dga-icons/core/bulk-rounded';

const PineTree = createVueIcon('pine-tree', iconData);

export default PineTree;

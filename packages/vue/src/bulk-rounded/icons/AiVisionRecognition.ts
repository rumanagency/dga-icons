import { createVueIcon } from '../../createVueIcon';
import { AiVisionRecognition as iconData } from '@dga-icons/core/bulk-rounded';

const AiVisionRecognition = createVueIcon('ai-vision-recognition', iconData);

export default AiVisionRecognition;

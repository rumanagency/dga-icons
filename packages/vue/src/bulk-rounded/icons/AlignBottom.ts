import { createVueIcon } from '../../createVueIcon';
import { AlignBottom as iconData } from '@dga-icons/core/bulk-rounded';

const AlignBottom = createVueIcon('align-bottom', iconData);

export default AlignBottom;

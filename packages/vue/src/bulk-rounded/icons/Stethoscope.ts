import { createVueIcon } from '../../createVueIcon';
import { Stethoscope as iconData } from '@dga-icons/core/bulk-rounded';

const Stethoscope = createVueIcon('stethoscope', iconData);

export default Stethoscope;

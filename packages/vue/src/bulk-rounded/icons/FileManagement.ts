import { createVueIcon } from '../../createVueIcon';
import { FileManagement as iconData } from '@dga-icons/core/bulk-rounded';

const FileManagement = createVueIcon('file-management', iconData);

export default FileManagement;

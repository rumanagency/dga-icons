import { createVueIcon } from '../../createVueIcon';
import { Fireworks as iconData } from '@dga-icons/core/bulk-rounded';

const Fireworks = createVueIcon('fireworks', iconData);

export default Fireworks;

import { createVueIcon } from '../../createVueIcon';
import { Slideshare as iconData } from '@dga-icons/core/bulk-rounded';

const Slideshare = createVueIcon('slideshare', iconData);

export default Slideshare;

import { createVueIcon } from '../../createVueIcon';
import { WhatsappBusiness as iconData } from '@dga-icons/core/bulk-rounded';

const WhatsappBusiness = createVueIcon('whatsapp-business', iconData);

export default WhatsappBusiness;

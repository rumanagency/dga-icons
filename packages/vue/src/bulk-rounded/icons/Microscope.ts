import { createVueIcon } from '../../createVueIcon';
import { Microscope as iconData } from '@dga-icons/core/bulk-rounded';

const Microscope = createVueIcon('microscope', iconData);

export default Microscope;

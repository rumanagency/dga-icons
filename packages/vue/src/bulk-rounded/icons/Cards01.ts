import { createVueIcon } from '../../createVueIcon';
import { Cards01 as iconData } from '@dga-icons/core/bulk-rounded';

const Cards01 = createVueIcon('cards-01', iconData);

export default Cards01;

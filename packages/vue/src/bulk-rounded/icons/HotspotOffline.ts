import { createVueIcon } from '../../createVueIcon';
import { HotspotOffline as iconData } from '@dga-icons/core/bulk-rounded';

const HotspotOffline = createVueIcon('hotspot-offline', iconData);

export default HotspotOffline;

import { createVueIcon } from '../../createVueIcon';
import { PropertyNew as iconData } from '@dga-icons/core/bulk-rounded';

const PropertyNew = createVueIcon('property-new', iconData);

export default PropertyNew;

import { createVueIcon } from '../../createVueIcon';
import { JobSearch as iconData } from '@dga-icons/core/bulk-rounded';

const JobSearch = createVueIcon('job-search', iconData);

export default JobSearch;

import { createVueIcon } from '../../createVueIcon';
import { DatabaseSync01 as iconData } from '@dga-icons/core/bulk-rounded';

const DatabaseSync01 = createVueIcon('database-sync-01', iconData);

export default DatabaseSync01;

import { createVueIcon } from '../../createVueIcon';
import { PolyTank as iconData } from '@dga-icons/core/bulk-rounded';

const PolyTank = createVueIcon('poly-tank', iconData);

export default PolyTank;

import { createVueIcon } from '../../createVueIcon';
import { Sparkles as iconData } from '@dga-icons/core/bulk-rounded';

const Sparkles = createVueIcon('sparkles', iconData);

export default Sparkles;

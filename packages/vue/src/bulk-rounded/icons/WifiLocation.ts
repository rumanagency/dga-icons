import { createVueIcon } from '../../createVueIcon';
import { WifiLocation as iconData } from '@dga-icons/core/bulk-rounded';

const WifiLocation = createVueIcon('wifi-location', iconData);

export default WifiLocation;

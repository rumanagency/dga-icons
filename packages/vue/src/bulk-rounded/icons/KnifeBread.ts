import { createVueIcon } from '../../createVueIcon';
import { KnifeBread as iconData } from '@dga-icons/core/bulk-rounded';

const KnifeBread = createVueIcon('knife-bread', iconData);

export default KnifeBread;

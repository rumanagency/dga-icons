import { createVueIcon } from '../../createVueIcon';
import { TimeManagementCircle as iconData } from '@dga-icons/core/bulk-rounded';

const TimeManagementCircle = createVueIcon('time-management-circle', iconData);

export default TimeManagementCircle;

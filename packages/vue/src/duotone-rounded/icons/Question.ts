import { createVueIcon } from '../../createVueIcon';
import { Question as iconData } from '@dga-icons/core/duotone-rounded';

const Question = createVueIcon('question', iconData);

export default Question;

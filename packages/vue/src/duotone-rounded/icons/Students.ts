import { createVueIcon } from '../../createVueIcon';
import { Students as iconData } from '@dga-icons/core/duotone-rounded';

const Students = createVueIcon('students', iconData);

export default Students;

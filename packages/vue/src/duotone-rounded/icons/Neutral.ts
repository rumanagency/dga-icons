import { createVueIcon } from '../../createVueIcon';
import { Neutral as iconData } from '@dga-icons/core/duotone-rounded';

const Neutral = createVueIcon('neutral', iconData);

export default Neutral;

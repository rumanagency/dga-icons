import { createVueIcon } from '../../createVueIcon';
import { Pyramid as iconData } from '@dga-icons/core/duotone-rounded';

const Pyramid = createVueIcon('pyramid', iconData);

export default Pyramid;

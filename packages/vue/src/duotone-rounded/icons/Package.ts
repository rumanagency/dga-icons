import { createVueIcon } from '../../createVueIcon';
import { Package as iconData } from '@dga-icons/core/duotone-rounded';

const Package = createVueIcon('package', iconData);

export default Package;

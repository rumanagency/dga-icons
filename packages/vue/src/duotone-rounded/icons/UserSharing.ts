import { createVueIcon } from '../../createVueIcon';
import { UserSharing as iconData } from '@dga-icons/core/duotone-rounded';

const UserSharing = createVueIcon('user-sharing', iconData);

export default UserSharing;

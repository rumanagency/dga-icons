import { createVueIcon } from '../../createVueIcon';
import { Teacher as iconData } from '@dga-icons/core/duotone-rounded';

const Teacher = createVueIcon('teacher', iconData);

export default Teacher;

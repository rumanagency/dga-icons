import { createVueIcon } from '../../createVueIcon';
import { Departement as iconData } from '@dga-icons/core/duotone-rounded';

const Departement = createVueIcon('departement', iconData);

export default Departement;

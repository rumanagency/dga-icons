import { createVueIcon } from '../../createVueIcon';
import { SubnodeAdd as iconData } from '@dga-icons/core/duotone-rounded';

const SubnodeAdd = createVueIcon('subnode-add', iconData);

export default SubnodeAdd;

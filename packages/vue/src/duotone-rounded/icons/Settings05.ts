import { createVueIcon } from '../../createVueIcon';
import { Settings05 as iconData } from '@dga-icons/core/duotone-rounded';

const Settings05 = createVueIcon('settings-05', iconData);

export default Settings05;

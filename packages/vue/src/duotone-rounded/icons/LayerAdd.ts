import { createVueIcon } from '../../createVueIcon';
import { LayerAdd as iconData } from '@dga-icons/core/duotone-rounded';

const LayerAdd = createVueIcon('layer-add', iconData);

export default LayerAdd;

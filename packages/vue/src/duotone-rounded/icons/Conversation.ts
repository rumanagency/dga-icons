import { createVueIcon } from '../../createVueIcon';
import { Conversation as iconData } from '@dga-icons/core/duotone-rounded';

const Conversation = createVueIcon('conversation', iconData);

export default Conversation;

import { createVueIcon } from '../../createVueIcon';
import { FolderSearch as iconData } from '@dga-icons/core/duotone-rounded';

const FolderSearch = createVueIcon('folder-search', iconData);

export default FolderSearch;

import { createVueIcon } from '../../createVueIcon';
import { Shuffle as iconData } from '@dga-icons/core/duotone-rounded';

const Shuffle = createVueIcon('shuffle', iconData);

export default Shuffle;

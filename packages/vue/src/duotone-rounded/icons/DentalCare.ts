import { createVueIcon } from '../../createVueIcon';
import { DentalCare as iconData } from '@dga-icons/core/duotone-rounded';

const DentalCare = createVueIcon('dental-care', iconData);

export default DentalCare;

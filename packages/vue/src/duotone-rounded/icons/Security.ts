import { createVueIcon } from '../../createVueIcon';
import { Security as iconData } from '@dga-icons/core/duotone-rounded';

const Security = createVueIcon('security', iconData);

export default Security;

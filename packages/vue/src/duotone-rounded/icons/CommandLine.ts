import { createVueIcon } from '../../createVueIcon';
import { CommandLine as iconData } from '@dga-icons/core/duotone-rounded';

const CommandLine = createVueIcon('command-line', iconData);

export default CommandLine;

import { createVueIcon } from '../../createVueIcon';
import { Prisoner as iconData } from '@dga-icons/core/duotone-rounded';

const Prisoner = createVueIcon('prisoner', iconData);

export default Prisoner;

import { createVueIcon } from '../../createVueIcon';
import { Manager as iconData } from '@dga-icons/core/duotone-rounded';

const Manager = createVueIcon('manager', iconData);

export default Manager;

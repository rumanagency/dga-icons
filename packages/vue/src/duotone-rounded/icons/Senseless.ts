import { createVueIcon } from '../../createVueIcon';
import { Senseless as iconData } from '@dga-icons/core/duotone-rounded';

const Senseless = createVueIcon('senseless', iconData);

export default Senseless;

import { createVueIcon } from '../../createVueIcon';
import { LimitOrder as iconData } from '@dga-icons/core/duotone-rounded';

const LimitOrder = createVueIcon('limit-order', iconData);

export default LimitOrder;

import { createVueIcon } from '../../createVueIcon';
import { SecuredNetwork as iconData } from '@dga-icons/core/duotone-rounded';

const SecuredNetwork = createVueIcon('secured-network', iconData);

export default SecuredNetwork;

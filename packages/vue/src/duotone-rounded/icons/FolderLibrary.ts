import { createVueIcon } from '../../createVueIcon';
import { FolderLibrary as iconData } from '@dga-icons/core/duotone-rounded';

const FolderLibrary = createVueIcon('folder-library', iconData);

export default FolderLibrary;

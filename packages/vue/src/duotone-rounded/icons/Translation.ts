import { createVueIcon } from '../../createVueIcon';
import { Translation as iconData } from '@dga-icons/core/duotone-rounded';

const Translation = createVueIcon('translation', iconData);

export default Translation;

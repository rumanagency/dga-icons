import { createVueIcon } from '../../createVueIcon';
import { Command as iconData } from '@dga-icons/core/duotone-rounded';

const Command = createVueIcon('command', iconData);

export default Command;

import { createVueIcon } from '../../createVueIcon';
import { HorseSaddle as iconData } from '@dga-icons/core/duotone-rounded';

const HorseSaddle = createVueIcon('horse-saddle', iconData);

export default HorseSaddle;

import { createVueIcon } from '../../createVueIcon';
import { HeadsetConnected as iconData } from '@dga-icons/core/duotone-rounded';

const HeadsetConnected = createVueIcon('headset-connected', iconData);

export default HeadsetConnected;

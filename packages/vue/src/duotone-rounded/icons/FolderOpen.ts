import { createVueIcon } from '../../createVueIcon';
import { FolderOpen as iconData } from '@dga-icons/core/duotone-rounded';

const FolderOpen = createVueIcon('folder-open', iconData);

export default FolderOpen;

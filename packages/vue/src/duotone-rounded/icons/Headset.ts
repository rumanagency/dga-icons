import { createVueIcon } from '../../createVueIcon';
import { Headset as iconData } from '@dga-icons/core/duotone-rounded';

const Headset = createVueIcon('headset', iconData);

export default Headset;

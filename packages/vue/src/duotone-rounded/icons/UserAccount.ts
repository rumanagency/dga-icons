import { createVueIcon } from '../../createVueIcon';
import { UserAccount as iconData } from '@dga-icons/core/duotone-rounded';

const UserAccount = createVueIcon('user-account', iconData);

export default UserAccount;

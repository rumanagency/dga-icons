import { createVueIcon } from '../../createVueIcon';
import { TrafficLight as iconData } from '@dga-icons/core/duotone-rounded';

const TrafficLight = createVueIcon('traffic-light', iconData);

export default TrafficLight;

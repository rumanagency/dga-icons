import { createVueIcon } from '../../createVueIcon';
import { FileUnknown as iconData } from '@dga-icons/core/duotone-rounded';

const FileUnknown = createVueIcon('file-unknown', iconData);

export default FileUnknown;

import { createVueIcon } from '../../createVueIcon';
import { Wireless as iconData } from '@dga-icons/core/duotone-rounded';

const Wireless = createVueIcon('wireless', iconData);

export default Wireless;

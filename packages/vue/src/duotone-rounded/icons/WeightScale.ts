import { createVueIcon } from '../../createVueIcon';
import { WeightScale as iconData } from '@dga-icons/core/duotone-rounded';

const WeightScale = createVueIcon('weight-scale', iconData);

export default WeightScale;

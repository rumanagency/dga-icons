import { createVueIcon } from '../../createVueIcon';
import { Avalanche as iconData } from '@dga-icons/core/duotone-rounded';

const Avalanche = createVueIcon('avalanche', iconData);

export default Avalanche;

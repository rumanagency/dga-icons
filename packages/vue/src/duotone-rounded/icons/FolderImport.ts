import { createVueIcon } from '../../createVueIcon';
import { FolderImport as iconData } from '@dga-icons/core/duotone-rounded';

const FolderImport = createVueIcon('folder-import', iconData);

export default FolderImport;

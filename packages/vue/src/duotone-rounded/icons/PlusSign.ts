import { createVueIcon } from '../../createVueIcon';
import { PlusSign as iconData } from '@dga-icons/core/duotone-rounded';

const PlusSign = createVueIcon('plus-sign', iconData);

export default PlusSign;

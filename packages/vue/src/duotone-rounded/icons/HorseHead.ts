import { createVueIcon } from '../../createVueIcon';
import { HorseHead as iconData } from '@dga-icons/core/duotone-rounded';

const HorseHead = createVueIcon('horse-head', iconData);

export default HorseHead;

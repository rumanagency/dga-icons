import { createVueIcon } from '../../createVueIcon';
import { FileBookmark as iconData } from '@dga-icons/core/duotone-rounded';

const FileBookmark = createVueIcon('file-bookmark', iconData);

export default FileBookmark;

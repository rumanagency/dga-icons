import { createVueIcon } from '../../createVueIcon';
import { FileEdit as iconData } from '@dga-icons/core/duotone-rounded';

const FileEdit = createVueIcon('file-edit', iconData);

export default FileEdit;

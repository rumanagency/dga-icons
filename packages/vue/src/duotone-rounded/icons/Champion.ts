import { createVueIcon } from '../../createVueIcon';
import { Champion as iconData } from '@dga-icons/core/duotone-rounded';

const Champion = createVueIcon('champion', iconData);

export default Champion;

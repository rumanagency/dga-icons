import { createVueIcon } from '../../createVueIcon';
import { SidebarLeft as iconData } from '@dga-icons/core/duotone-rounded';

const SidebarLeft = createVueIcon('sidebar-left', iconData);

export default SidebarLeft;

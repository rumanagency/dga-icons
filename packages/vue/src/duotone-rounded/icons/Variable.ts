import { createVueIcon } from '../../createVueIcon';
import { Variable as iconData } from '@dga-icons/core/duotone-rounded';

const Variable = createVueIcon('variable', iconData);

export default Variable;

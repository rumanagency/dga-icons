import { createVueIcon } from '../../createVueIcon';
import { Alert01 as iconData } from '@dga-icons/core/duotone-rounded';

const Alert01 = createVueIcon('alert-01', iconData);

export default Alert01;

import { createVueIcon } from '../../createVueIcon';
import { Expander as iconData } from '@dga-icons/core/duotone-rounded';

const Expander = createVueIcon('expander', iconData);

export default Expander;

import { createVueIcon } from '../../createVueIcon';
import { Jupiter as iconData } from '@dga-icons/core/duotone-rounded';

const Jupiter = createVueIcon('jupiter', iconData);

export default Jupiter;

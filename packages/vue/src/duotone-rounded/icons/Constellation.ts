import { createVueIcon } from '../../createVueIcon';
import { Constellation as iconData } from '@dga-icons/core/duotone-rounded';

const Constellation = createVueIcon('constellation', iconData);

export default Constellation;

import { createVueIcon } from '../../createVueIcon';
import { Webhook as iconData } from '@dga-icons/core/duotone-rounded';

const Webhook = createVueIcon('webhook', iconData);

export default Webhook;

import { createVueIcon } from '../../createVueIcon';
import { FlipLeft as iconData } from '@dga-icons/core/duotone-rounded';

const FlipLeft = createVueIcon('flip-left', iconData);

export default FlipLeft;

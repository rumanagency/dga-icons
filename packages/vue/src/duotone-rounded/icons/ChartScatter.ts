import { createVueIcon } from '../../createVueIcon';
import { ChartScatter as iconData } from '@dga-icons/core/duotone-rounded';

const ChartScatter = createVueIcon('chart-scatter', iconData);

export default ChartScatter;

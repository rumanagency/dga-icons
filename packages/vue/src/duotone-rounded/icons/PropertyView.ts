import { createVueIcon } from '../../createVueIcon';
import { PropertyView as iconData } from '@dga-icons/core/duotone-rounded';

const PropertyView = createVueIcon('property-view', iconData);

export default PropertyView;

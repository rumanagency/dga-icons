import { createVueIcon } from '../../createVueIcon';
import { CovidInfo as iconData } from '@dga-icons/core/duotone-rounded';

const CovidInfo = createVueIcon('covid-info', iconData);

export default CovidInfo;

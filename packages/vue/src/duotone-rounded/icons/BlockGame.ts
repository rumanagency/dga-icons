import { createVueIcon } from '../../createVueIcon';
import { BlockGame as iconData } from '@dga-icons/core/duotone-rounded';

const BlockGame = createVueIcon('block-game', iconData);

export default BlockGame;

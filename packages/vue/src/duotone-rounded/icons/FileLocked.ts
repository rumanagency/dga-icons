import { createVueIcon } from '../../createVueIcon';
import { FileLocked as iconData } from '@dga-icons/core/duotone-rounded';

const FileLocked = createVueIcon('file-locked', iconData);

export default FileLocked;

import { createVueIcon } from '../../createVueIcon';
import { WifiLock as iconData } from '@dga-icons/core/duotone-rounded';

const WifiLock = createVueIcon('wifi-lock', iconData);

export default WifiLock;

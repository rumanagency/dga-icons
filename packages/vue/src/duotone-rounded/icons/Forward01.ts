import { createVueIcon } from '../../createVueIcon';
import { Forward01 as iconData } from '@dga-icons/core/duotone-rounded';

const Forward01 = createVueIcon('forward-01', iconData);

export default Forward01;

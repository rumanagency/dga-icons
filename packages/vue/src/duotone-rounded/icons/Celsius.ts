import { createVueIcon } from '../../createVueIcon';
import { Celsius as iconData } from '@dga-icons/core/duotone-rounded';

const Celsius = createVueIcon('celsius', iconData);

export default Celsius;

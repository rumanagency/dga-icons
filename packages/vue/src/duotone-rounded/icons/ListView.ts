import { createVueIcon } from '../../createVueIcon';
import { ListView as iconData } from '@dga-icons/core/duotone-rounded';

const ListView = createVueIcon('list-view', iconData);

export default ListView;

import { createVueIcon } from '../../createVueIcon';
import { Speaker as iconData } from '@dga-icons/core/duotone-rounded';

const Speaker = createVueIcon('speaker', iconData);

export default Speaker;

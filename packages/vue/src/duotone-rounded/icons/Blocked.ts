import { createVueIcon } from '../../createVueIcon';
import { Blocked as iconData } from '@dga-icons/core/duotone-rounded';

const Blocked = createVueIcon('blocked', iconData);

export default Blocked;

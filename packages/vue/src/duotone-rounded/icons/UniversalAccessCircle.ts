import { createVueIcon } from '../../createVueIcon';
import { UniversalAccessCircle as iconData } from '@dga-icons/core/duotone-rounded';

const UniversalAccessCircle = createVueIcon('universal-access-circle', iconData);

export default UniversalAccessCircle;

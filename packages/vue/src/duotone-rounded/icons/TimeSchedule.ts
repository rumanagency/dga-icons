import { createVueIcon } from '../../createVueIcon';
import { TimeSchedule as iconData } from '@dga-icons/core/duotone-rounded';

const TimeSchedule = createVueIcon('time-schedule', iconData);

export default TimeSchedule;

import { createVueIcon } from '../../createVueIcon';
import { PackageSearch as iconData } from '@dga-icons/core/duotone-rounded';

const PackageSearch = createVueIcon('package-search', iconData);

export default PackageSearch;

import { createVueIcon } from '../../createVueIcon';
import { CPlusPlus as iconData } from '@dga-icons/core/duotone-rounded';

const CPlusPlus = createVueIcon('c++', iconData);

export default CPlusPlus;

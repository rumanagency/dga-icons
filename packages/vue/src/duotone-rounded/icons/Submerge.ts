import { createVueIcon } from '../../createVueIcon';
import { Submerge as iconData } from '@dga-icons/core/duotone-rounded';

const Submerge = createVueIcon('submerge', iconData);

export default Submerge;

import { createVueIcon } from '../../createVueIcon';
import { IdVerified as iconData } from '@dga-icons/core/duotone-rounded';

const IdVerified = createVueIcon('id-verified', iconData);

export default IdVerified;

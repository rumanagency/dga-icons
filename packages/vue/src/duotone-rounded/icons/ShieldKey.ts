import { createVueIcon } from '../../createVueIcon';
import { ShieldKey as iconData } from '@dga-icons/core/duotone-rounded';

const ShieldKey = createVueIcon('shield-key', iconData);

export default ShieldKey;

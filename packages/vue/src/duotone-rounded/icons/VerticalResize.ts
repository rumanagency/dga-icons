import { createVueIcon } from '../../createVueIcon';
import { VerticalResize as iconData } from '@dga-icons/core/duotone-rounded';

const VerticalResize = createVueIcon('vertical-resize', iconData);

export default VerticalResize;

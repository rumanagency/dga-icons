import { createVueIcon } from '../../createVueIcon';
import { Lighthouse as iconData } from '@dga-icons/core/duotone-rounded';

const Lighthouse = createVueIcon('lighthouse', iconData);

export default Lighthouse;

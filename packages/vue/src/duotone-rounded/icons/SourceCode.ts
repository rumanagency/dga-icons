import { createVueIcon } from '../../createVueIcon';
import { SourceCode as iconData } from '@dga-icons/core/duotone-rounded';

const SourceCode = createVueIcon('source-code', iconData);

export default SourceCode;

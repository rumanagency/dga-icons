import { createVueIcon } from '../../createVueIcon';
import { Bootstrap as iconData } from '@dga-icons/core/duotone-rounded';

const Bootstrap = createVueIcon('bootstrap', iconData);

export default Bootstrap;

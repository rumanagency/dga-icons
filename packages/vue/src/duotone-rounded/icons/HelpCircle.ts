import { createVueIcon } from '../../createVueIcon';
import { HelpCircle as iconData } from '@dga-icons/core/duotone-rounded';

const HelpCircle = createVueIcon('help-circle', iconData);

export default HelpCircle;

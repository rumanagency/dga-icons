import { createVueIcon } from '../../createVueIcon';
import { Terrace as iconData } from '@dga-icons/core/duotone-rounded';

const Terrace = createVueIcon('terrace', iconData);

export default Terrace;

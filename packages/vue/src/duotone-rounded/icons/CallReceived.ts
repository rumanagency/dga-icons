import { createVueIcon } from '../../createVueIcon';
import { CallReceived as iconData } from '@dga-icons/core/duotone-rounded';

const CallReceived = createVueIcon('call-received', iconData);

export default CallReceived;

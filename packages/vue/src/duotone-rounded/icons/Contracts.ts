import { createVueIcon } from '../../createVueIcon';
import { Contracts as iconData } from '@dga-icons/core/duotone-rounded';

const Contracts = createVueIcon('contracts', iconData);

export default Contracts;

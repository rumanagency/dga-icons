import { createVueIcon } from '../../createVueIcon';
import { Database as iconData } from '@dga-icons/core/duotone-rounded';

const Database = createVueIcon('database', iconData);

export default Database;

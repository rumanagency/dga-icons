import { createVueIcon } from '../../createVueIcon';
import { Hangout as iconData } from '@dga-icons/core/duotone-rounded';

const Hangout = createVueIcon('hangout', iconData);

export default Hangout;

import { createVueIcon } from '../../createVueIcon';
import { FolderSync as iconData } from '@dga-icons/core/duotone-rounded';

const FolderSync = createVueIcon('folder-sync', iconData);

export default FolderSync;

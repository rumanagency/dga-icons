import { createVueIcon } from '../../createVueIcon';
import { Surprise as iconData } from '@dga-icons/core/duotone-rounded';

const Surprise = createVueIcon('surprise', iconData);

export default Surprise;

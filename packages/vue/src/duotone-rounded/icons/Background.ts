import { createVueIcon } from '../../createVueIcon';
import { Background as iconData } from '@dga-icons/core/duotone-rounded';

const Background = createVueIcon('background', iconData);

export default Background;

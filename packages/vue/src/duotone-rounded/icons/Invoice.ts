import { createVueIcon } from '../../createVueIcon';
import { Invoice as iconData } from '@dga-icons/core/duotone-rounded';

const Invoice = createVueIcon('invoice', iconData);

export default Invoice;

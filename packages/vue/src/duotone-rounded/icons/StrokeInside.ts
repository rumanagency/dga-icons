import { createVueIcon } from '../../createVueIcon';
import { StrokeInside as iconData } from '@dga-icons/core/duotone-rounded';

const StrokeInside = createVueIcon('stroke-inside', iconData);

export default StrokeInside;

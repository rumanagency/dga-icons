import { createVueIcon } from '../../createVueIcon';
import { ChatBot as iconData } from '@dga-icons/core/duotone-rounded';

const ChatBot = createVueIcon('chat-bot', iconData);

export default ChatBot;

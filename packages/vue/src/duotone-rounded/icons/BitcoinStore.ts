import { createVueIcon } from '../../createVueIcon';
import { BitcoinStore as iconData } from '@dga-icons/core/duotone-rounded';

const BitcoinStore = createVueIcon('bitcoin-store', iconData);

export default BitcoinStore;

import { createVueIcon } from '../../createVueIcon';
import { BubbleChat as iconData } from '@dga-icons/core/duotone-rounded';

const BubbleChat = createVueIcon('bubble-chat', iconData);

export default BubbleChat;

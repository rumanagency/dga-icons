import { createVueIcon } from '../../createVueIcon';
import { Matches as iconData } from '@dga-icons/core/duotone-rounded';

const Matches = createVueIcon('matches', iconData);

export default Matches;

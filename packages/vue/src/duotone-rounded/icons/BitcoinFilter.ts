import { createVueIcon } from '../../createVueIcon';
import { BitcoinFilter as iconData } from '@dga-icons/core/duotone-rounded';

const BitcoinFilter = createVueIcon('bitcoin-filter', iconData);

export default BitcoinFilter;

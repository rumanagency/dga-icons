import { createVueIcon } from '../../createVueIcon';
import { MoreHorizontal as iconData } from '@dga-icons/core/duotone-rounded';

const MoreHorizontal = createVueIcon('more-horizontal', iconData);

export default MoreHorizontal;

import { createVueIcon } from '../../createVueIcon';
import { AlarmClock as iconData } from '@dga-icons/core/duotone-rounded';

const AlarmClock = createVueIcon('alarm-clock', iconData);

export default AlarmClock;

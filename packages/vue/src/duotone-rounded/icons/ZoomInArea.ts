import { createVueIcon } from '../../createVueIcon';
import { ZoomInArea as iconData } from '@dga-icons/core/duotone-rounded';

const ZoomInArea = createVueIcon('zoom-in-area', iconData);

export default ZoomInArea;

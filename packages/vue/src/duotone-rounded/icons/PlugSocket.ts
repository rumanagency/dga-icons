import { createVueIcon } from '../../createVueIcon';
import { PlugSocket as iconData } from '@dga-icons/core/duotone-rounded';

const PlugSocket = createVueIcon('plug-socket', iconData);

export default PlugSocket;

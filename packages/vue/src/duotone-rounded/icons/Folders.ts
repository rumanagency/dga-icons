import { createVueIcon } from '../../createVueIcon';
import { Folders as iconData } from '@dga-icons/core/duotone-rounded';

const Folders = createVueIcon('folders', iconData);

export default Folders;

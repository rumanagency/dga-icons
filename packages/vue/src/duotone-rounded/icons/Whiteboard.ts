import { createVueIcon } from '../../createVueIcon';
import { Whiteboard as iconData } from '@dga-icons/core/duotone-rounded';

const Whiteboard = createVueIcon('whiteboard', iconData);

export default Whiteboard;

import { createVueIcon } from '../../createVueIcon';
import { FileAdd as iconData } from '@dga-icons/core/duotone-rounded';

const FileAdd = createVueIcon('file-add', iconData);

export default FileAdd;

import { createVueIcon } from '../../createVueIcon';
import { StopCircle as iconData } from '@dga-icons/core/duotone-rounded';

const StopCircle = createVueIcon('stop-circle', iconData);

export default StopCircle;

import { createVueIcon } from '../../createVueIcon';
import { UniversalAccess as iconData } from '@dga-icons/core/duotone-rounded';

const UniversalAccess = createVueIcon('universal-access', iconData);

export default UniversalAccess;

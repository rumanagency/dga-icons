import { createVueIcon } from '../../createVueIcon';
import { GitMerge as iconData } from '@dga-icons/core/duotone-rounded';

const GitMerge = createVueIcon('git-merge', iconData);

export default GitMerge;

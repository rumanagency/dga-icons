import { createVueIcon } from '../../createVueIcon';
import { FileVerified as iconData } from '@dga-icons/core/duotone-rounded';

const FileVerified = createVueIcon('file-verified', iconData);

export default FileVerified;

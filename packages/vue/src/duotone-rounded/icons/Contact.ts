import { createVueIcon } from '../../createVueIcon';
import { Contact as iconData } from '@dga-icons/core/duotone-rounded';

const Contact = createVueIcon('contact', iconData);

export default Contact;

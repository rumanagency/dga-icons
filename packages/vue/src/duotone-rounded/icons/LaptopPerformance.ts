import { createVueIcon } from '../../createVueIcon';
import { LaptopPerformance as iconData } from '@dga-icons/core/duotone-rounded';

const LaptopPerformance = createVueIcon('laptop-performance', iconData);

export default LaptopPerformance;

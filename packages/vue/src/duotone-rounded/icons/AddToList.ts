import { createVueIcon } from '../../createVueIcon';
import { AddToList as iconData } from '@dga-icons/core/duotone-rounded';

const AddToList = createVueIcon('add-to-list', iconData);

export default AddToList;

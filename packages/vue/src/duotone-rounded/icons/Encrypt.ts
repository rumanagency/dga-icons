import { createVueIcon } from '../../createVueIcon';
import { Encrypt as iconData } from '@dga-icons/core/duotone-rounded';

const Encrypt = createVueIcon('encrypt', iconData);

export default Encrypt;

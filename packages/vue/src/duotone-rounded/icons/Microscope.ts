import { createVueIcon } from '../../createVueIcon';
import { Microscope as iconData } from '@dga-icons/core/duotone-rounded';

const Microscope = createVueIcon('microscope', iconData);

export default Microscope;

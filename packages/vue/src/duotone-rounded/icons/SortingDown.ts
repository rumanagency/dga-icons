import { createVueIcon } from '../../createVueIcon';
import { SortingDown as iconData } from '@dga-icons/core/duotone-rounded';

const SortingDown = createVueIcon('sorting-down', iconData);

export default SortingDown;

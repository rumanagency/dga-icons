import { createVueIcon } from '../../createVueIcon';
import { FolderMinus as iconData } from '@dga-icons/core/duotone-rounded';

const FolderMinus = createVueIcon('folder-minus', iconData);

export default FolderMinus;

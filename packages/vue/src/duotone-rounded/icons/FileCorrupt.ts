import { createVueIcon } from '../../createVueIcon';
import { FileCorrupt as iconData } from '@dga-icons/core/duotone-rounded';

const FileCorrupt = createVueIcon('file-corrupt', iconData);

export default FileCorrupt;

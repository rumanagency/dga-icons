import { createVueIcon } from '../../createVueIcon';
import { Airport as iconData } from '@dga-icons/core/duotone-rounded';

const Airport = createVueIcon('airport', iconData);

export default Airport;

import { createVueIcon } from '../../createVueIcon';
import { AppStore as iconData } from '@dga-icons/core/duotone-rounded';

const AppStore = createVueIcon('app-store', iconData);

export default AppStore;

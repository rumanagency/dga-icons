import { createVueIcon } from '../../createVueIcon';
import { AlignBottom as iconData } from '@dga-icons/core/duotone-rounded';

const AlignBottom = createVueIcon('align-bottom', iconData);

export default AlignBottom;

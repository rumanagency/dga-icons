import { createVueIcon } from '../../createVueIcon';
import { Profile as iconData } from '@dga-icons/core/duotone-rounded';

const Profile = createVueIcon('profile', iconData);

export default Profile;

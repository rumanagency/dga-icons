import { createVueIcon } from '../../createVueIcon';
import { Messenger as iconData } from '@dga-icons/core/duotone-rounded';

const Messenger = createVueIcon('messenger', iconData);

export default Messenger;

import { createVueIcon } from '../../createVueIcon';
import { Feather as iconData } from '@dga-icons/core/duotone-rounded';

const Feather = createVueIcon('feather', iconData);

export default Feather;

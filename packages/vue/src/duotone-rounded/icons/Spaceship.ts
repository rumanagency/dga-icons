import { createVueIcon } from '../../createVueIcon';
import { Spaceship as iconData } from '@dga-icons/core/duotone-rounded';

const Spaceship = createVueIcon('spaceship', iconData);

export default Spaceship;

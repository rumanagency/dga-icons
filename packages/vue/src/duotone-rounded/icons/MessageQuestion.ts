import { createVueIcon } from '../../createVueIcon';
import { MessageQuestion as iconData } from '@dga-icons/core/duotone-rounded';

const MessageQuestion = createVueIcon('message-question', iconData);

export default MessageQuestion;

import { createVueIcon } from '../../createVueIcon';
import { AutoConversations as iconData } from '@dga-icons/core/duotone-rounded';

const AutoConversations = createVueIcon('auto-conversations', iconData);

export default AutoConversations;

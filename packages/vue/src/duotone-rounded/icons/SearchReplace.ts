import { createVueIcon } from '../../createVueIcon';
import { SearchReplace as iconData } from '@dga-icons/core/duotone-rounded';

const SearchReplace = createVueIcon('search-replace', iconData);

export default SearchReplace;

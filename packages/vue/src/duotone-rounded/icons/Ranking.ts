import { createVueIcon } from '../../createVueIcon';
import { Ranking as iconData } from '@dga-icons/core/duotone-rounded';

const Ranking = createVueIcon('ranking', iconData);

export default Ranking;

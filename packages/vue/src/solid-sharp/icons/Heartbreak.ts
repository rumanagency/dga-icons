import { createVueIcon } from '../../createVueIcon';
import { Heartbreak as iconData } from '@dga-icons/core/solid-sharp';

const Heartbreak = createVueIcon('heartbreak', iconData);

export default Heartbreak;

import { createVueIcon } from '../../createVueIcon';
import { DrawingMode as iconData } from '@dga-icons/core/solid-sharp';

const DrawingMode = createVueIcon('drawing-mode', iconData);

export default DrawingMode;

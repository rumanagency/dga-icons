import { createVueIcon } from '../../createVueIcon';
import { DatabaseRestore as iconData } from '@dga-icons/core/solid-sharp';

const DatabaseRestore = createVueIcon('database-restore', iconData);

export default DatabaseRestore;

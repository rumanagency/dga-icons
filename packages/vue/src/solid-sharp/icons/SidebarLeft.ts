import { createVueIcon } from '../../createVueIcon';
import { SidebarLeft as iconData } from '@dga-icons/core/solid-sharp';

const SidebarLeft = createVueIcon('sidebar-left', iconData);

export default SidebarLeft;

import { createVueIcon } from '../../createVueIcon';
import { SecurityPassword as iconData } from '@dga-icons/core/solid-sharp';

const SecurityPassword = createVueIcon('security-password', iconData);

export default SecurityPassword;

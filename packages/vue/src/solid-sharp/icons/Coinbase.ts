import { createVueIcon } from '../../createVueIcon';
import { Coinbase as iconData } from '@dga-icons/core/solid-sharp';

const Coinbase = createVueIcon('coinbase', iconData);

export default Coinbase;

import { createVueIcon } from '../../createVueIcon';
import { FileShredder as iconData } from '@dga-icons/core/solid-sharp';

const FileShredder = createVueIcon('file-shredder', iconData);

export default FileShredder;

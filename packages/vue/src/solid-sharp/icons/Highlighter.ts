import { createVueIcon } from '../../createVueIcon';
import { Highlighter as iconData } from '@dga-icons/core/solid-sharp';

const Highlighter = createVueIcon('highlighter', iconData);

export default Highlighter;

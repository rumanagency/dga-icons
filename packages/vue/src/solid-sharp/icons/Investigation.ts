import { createVueIcon } from '../../createVueIcon';
import { Investigation as iconData } from '@dga-icons/core/solid-sharp';

const Investigation = createVueIcon('investigation', iconData);

export default Investigation;

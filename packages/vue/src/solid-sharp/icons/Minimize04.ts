import { createVueIcon } from '../../createVueIcon';
import { Minimize04 as iconData } from '@dga-icons/core/solid-sharp';

const Minimize04 = createVueIcon('minimize-04', iconData);

export default Minimize04;

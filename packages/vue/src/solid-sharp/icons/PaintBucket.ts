import { createVueIcon } from '../../createVueIcon';
import { PaintBucket as iconData } from '@dga-icons/core/solid-sharp';

const PaintBucket = createVueIcon('paint-bucket', iconData);

export default PaintBucket;

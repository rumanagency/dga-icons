import { createVueIcon } from '../../createVueIcon';
import { FlipHorizontal as iconData } from '@dga-icons/core/solid-sharp';

const FlipHorizontal = createVueIcon('flip-horizontal', iconData);

export default FlipHorizontal;

import { createVueIcon } from '../../createVueIcon';
import { LockComputer as iconData } from '@dga-icons/core/solid-sharp';

const LockComputer = createVueIcon('lock-computer', iconData);

export default LockComputer;

import { createVueIcon } from '../../createVueIcon';
import { Prisoner as iconData } from '@dga-icons/core/solid-sharp';

const Prisoner = createVueIcon('prisoner', iconData);

export default Prisoner;

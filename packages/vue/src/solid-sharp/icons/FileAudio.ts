import { createVueIcon } from '../../createVueIcon';
import { FileAudio as iconData } from '@dga-icons/core/solid-sharp';

const FileAudio = createVueIcon('file-audio', iconData);

export default FileAudio;

import { createVueIcon } from '../../createVueIcon';
import { PackageAdd as iconData } from '@dga-icons/core/solid-sharp';

const PackageAdd = createVueIcon('package-add', iconData);

export default PackageAdd;

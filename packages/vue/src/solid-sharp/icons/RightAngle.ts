import { createVueIcon } from '../../createVueIcon';
import { RightAngle as iconData } from '@dga-icons/core/solid-sharp';

const RightAngle = createVueIcon('right-angle', iconData);

export default RightAngle;

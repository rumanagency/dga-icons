import { createVueIcon } from '../../createVueIcon';
import { Security as iconData } from '@dga-icons/core/solid-sharp';

const Security = createVueIcon('security', iconData);

export default Security;

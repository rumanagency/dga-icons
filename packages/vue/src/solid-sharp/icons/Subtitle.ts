import { createVueIcon } from '../../createVueIcon';
import { Subtitle as iconData } from '@dga-icons/core/solid-sharp';

const Subtitle = createVueIcon('subtitle', iconData);

export default Subtitle;

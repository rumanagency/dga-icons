import { createVueIcon } from '../../createVueIcon';
import { Brochure as iconData } from '@dga-icons/core/solid-sharp';

const Brochure = createVueIcon('brochure', iconData);

export default Brochure;

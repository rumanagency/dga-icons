import { createVueIcon } from '../../createVueIcon';
import { FileView as iconData } from '@dga-icons/core/solid-sharp';

const FileView = createVueIcon('file-view', iconData);

export default FileView;

import { createVueIcon } from '../../createVueIcon';
import { FilterEdit as iconData } from '@dga-icons/core/solid-sharp';

const FilterEdit = createVueIcon('filter-edit', iconData);

export default FilterEdit;

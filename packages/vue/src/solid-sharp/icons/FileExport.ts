import { createVueIcon } from '../../createVueIcon';
import { FileExport as iconData } from '@dga-icons/core/solid-sharp';

const FileExport = createVueIcon('file-export', iconData);

export default FileExport;

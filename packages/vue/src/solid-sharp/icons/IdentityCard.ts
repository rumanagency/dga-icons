import { createVueIcon } from '../../createVueIcon';
import { IdentityCard as iconData } from '@dga-icons/core/solid-sharp';

const IdentityCard = createVueIcon('identity-card', iconData);

export default IdentityCard;

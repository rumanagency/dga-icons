import { createVueIcon } from '../../createVueIcon';
import { PackageSearch as iconData } from '@dga-icons/core/solid-sharp';

const PackageSearch = createVueIcon('package-search', iconData);

export default PackageSearch;

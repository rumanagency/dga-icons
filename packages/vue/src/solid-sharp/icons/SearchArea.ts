import { createVueIcon } from '../../createVueIcon';
import { SearchArea as iconData } from '@dga-icons/core/solid-sharp';

const SearchArea = createVueIcon('search-area', iconData);

export default SearchArea;

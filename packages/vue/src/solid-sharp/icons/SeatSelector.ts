import { createVueIcon } from '../../createVueIcon';
import { SeatSelector as iconData } from '@dga-icons/core/solid-sharp';

const SeatSelector = createVueIcon('seat-selector', iconData);

export default SeatSelector;

import { createVueIcon } from '../../createVueIcon';
import { ToyTrain as iconData } from '@dga-icons/core/solid-sharp';

const ToyTrain = createVueIcon('toy-train', iconData);

export default ToyTrain;

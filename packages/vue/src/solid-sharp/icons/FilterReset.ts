import { createVueIcon } from '../../createVueIcon';
import { FilterReset as iconData } from '@dga-icons/core/solid-sharp';

const FilterReset = createVueIcon('filter-reset', iconData);

export default FilterReset;

import { createVueIcon } from '../../createVueIcon';
import { GroupItems as iconData } from '@dga-icons/core/solid-sharp';

const GroupItems = createVueIcon('group-items', iconData);

export default GroupItems;

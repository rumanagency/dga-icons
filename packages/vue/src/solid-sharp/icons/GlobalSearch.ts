import { createVueIcon } from '../../createVueIcon';
import { GlobalSearch as iconData } from '@dga-icons/core/solid-sharp';

const GlobalSearch = createVueIcon('global-search', iconData);

export default GlobalSearch;

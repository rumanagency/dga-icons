import { createVueIcon } from '../../createVueIcon';
import { CPlusPlus as iconData } from '@dga-icons/core/solid-sharp';

const CPlusPlus = createVueIcon('c++', iconData);

export default CPlusPlus;

import { createVueIcon } from '../../createVueIcon';
import { Temperature as iconData } from '@dga-icons/core/solid-sharp';

const Temperature = createVueIcon('temperature', iconData);

export default Temperature;

import { createVueIcon } from '../../createVueIcon';
import { AlignLeft as iconData } from '@dga-icons/core/solid-sharp';

const AlignLeft = createVueIcon('align-left', iconData);

export default AlignLeft;

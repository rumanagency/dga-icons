import { createVueIcon } from '../../createVueIcon';
import { SecurityCheck as iconData } from '@dga-icons/core/solid-sharp';

const SecurityCheck = createVueIcon('security-check', iconData);

export default SecurityCheck;

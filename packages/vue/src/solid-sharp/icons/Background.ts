import { createVueIcon } from '../../createVueIcon';
import { Background as iconData } from '@dga-icons/core/solid-sharp';

const Background = createVueIcon('background', iconData);

export default Background;

import { createVueIcon } from '../../createVueIcon';
import { Keyframe as iconData } from '@dga-icons/core/solid-sharp';

const Keyframe = createVueIcon('keyframe', iconData);

export default Keyframe;

import { createVueIcon } from '../../createVueIcon';
import { Firewall as iconData } from '@dga-icons/core/solid-sharp';

const Firewall = createVueIcon('firewall', iconData);

export default Firewall;

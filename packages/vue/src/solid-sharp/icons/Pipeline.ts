import { createVueIcon } from '../../createVueIcon';
import { Pipeline as iconData } from '@dga-icons/core/solid-sharp';

const Pipeline = createVueIcon('pipeline', iconData);

export default Pipeline;

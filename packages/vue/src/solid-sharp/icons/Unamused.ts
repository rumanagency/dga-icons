import { createVueIcon } from '../../createVueIcon';
import { Unamused as iconData } from '@dga-icons/core/solid-sharp';

const Unamused = createVueIcon('unamused', iconData);

export default Unamused;

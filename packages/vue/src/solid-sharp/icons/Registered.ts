import { createVueIcon } from '../../createVueIcon';
import { Registered as iconData } from '@dga-icons/core/solid-sharp';

const Registered = createVueIcon('registered', iconData);

export default Registered;

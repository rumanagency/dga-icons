import { createVueIcon } from '../../createVueIcon';
import { FolderOpen as iconData } from '@dga-icons/core/solid-sharp';

const FolderOpen = createVueIcon('folder-open', iconData);

export default FolderOpen;

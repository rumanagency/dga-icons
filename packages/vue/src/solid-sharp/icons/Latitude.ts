import { createVueIcon } from '../../createVueIcon';
import { Latitude as iconData } from '@dga-icons/core/solid-sharp';

const Latitude = createVueIcon('latitude', iconData);

export default Latitude;

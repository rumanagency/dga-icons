import { createVueIcon } from '../../createVueIcon';
import { HelpCircle as iconData } from '@dga-icons/core/solid-sharp';

const HelpCircle = createVueIcon('help-circle', iconData);

export default HelpCircle;

import { createVueIcon } from '../../createVueIcon';
import { FileSearch as iconData } from '@dga-icons/core/solid-sharp';

const FileSearch = createVueIcon('file-search', iconData);

export default FileSearch;

import { createVueIcon } from '../../createVueIcon';
import { Bluetooth as iconData } from '@dga-icons/core/solid-sharp';

const Bluetooth = createVueIcon('bluetooth', iconData);

export default Bluetooth;

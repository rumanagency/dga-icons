import { createVueIcon } from '../../createVueIcon';
import { LockSync01 as iconData } from '@dga-icons/core/solid-sharp';

const LockSync01 = createVueIcon('lock-sync-01', iconData);

export default LockSync01;

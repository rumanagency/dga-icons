import { createVueIcon } from '../../createVueIcon';
import { Translation as iconData } from '@dga-icons/core/solid-sharp';

const Translation = createVueIcon('translation', iconData);

export default Translation;

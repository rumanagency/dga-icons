import { createVueIcon } from '../../createVueIcon';
import { Distribution as iconData } from '@dga-icons/core/solid-sharp';

const Distribution = createVueIcon('distribution', iconData);

export default Distribution;

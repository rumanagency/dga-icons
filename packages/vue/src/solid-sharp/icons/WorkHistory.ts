import { createVueIcon } from '../../createVueIcon';
import { WorkHistory as iconData } from '@dga-icons/core/solid-sharp';

const WorkHistory = createVueIcon('work-history', iconData);

export default WorkHistory;

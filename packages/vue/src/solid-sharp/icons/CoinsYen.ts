import { createVueIcon } from '../../createVueIcon';
import { CoinsYen as iconData } from '@dga-icons/core/solid-sharp';

const CoinsYen = createVueIcon('coins-yen', iconData);

export default CoinsYen;

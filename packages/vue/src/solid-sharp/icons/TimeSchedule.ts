import { createVueIcon } from '../../createVueIcon';
import { TimeSchedule as iconData } from '@dga-icons/core/solid-sharp';

const TimeSchedule = createVueIcon('time-schedule', iconData);

export default TimeSchedule;

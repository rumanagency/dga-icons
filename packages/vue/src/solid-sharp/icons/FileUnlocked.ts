import { createVueIcon } from '../../createVueIcon';
import { FileUnlocked as iconData } from '@dga-icons/core/solid-sharp';

const FileUnlocked = createVueIcon('file-unlocked', iconData);

export default FileUnlocked;

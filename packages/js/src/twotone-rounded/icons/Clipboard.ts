import { createJsIcon } from '../../createJsIcon';
import { Clipboard as iconData } from '@dga-icons/core/twotone-rounded';

const Clipboard = createJsIcon('clipboard', iconData);

export default Clipboard;

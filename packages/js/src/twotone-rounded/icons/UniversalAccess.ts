import { createJsIcon } from '../../createJsIcon';
import { UniversalAccess as iconData } from '@dga-icons/core/twotone-rounded';

const UniversalAccess = createJsIcon('universal-access', iconData);

export default UniversalAccess;

import { createJsIcon } from '../../createJsIcon';
import { Security as iconData } from '@dga-icons/core/twotone-rounded';

const Security = createJsIcon('security', iconData);

export default Security;

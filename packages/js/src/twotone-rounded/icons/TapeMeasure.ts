import { createJsIcon } from '../../createJsIcon';
import { TapeMeasure as iconData } from '@dga-icons/core/twotone-rounded';

const TapeMeasure = createJsIcon('tape-measure', iconData);

export default TapeMeasure;

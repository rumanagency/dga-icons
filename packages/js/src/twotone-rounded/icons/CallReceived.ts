import { createJsIcon } from '../../createJsIcon';
import { CallReceived as iconData } from '@dga-icons/core/twotone-rounded';

const CallReceived = createJsIcon('call-received', iconData);

export default CallReceived;

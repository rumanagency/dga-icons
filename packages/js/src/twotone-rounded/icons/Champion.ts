import { createJsIcon } from '../../createJsIcon';
import { Champion as iconData } from '@dga-icons/core/twotone-rounded';

const Champion = createJsIcon('champion', iconData);

export default Champion;

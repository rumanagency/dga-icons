import { createJsIcon } from '../../createJsIcon';
import { CarSignal as iconData } from '@dga-icons/core/twotone-rounded';

const CarSignal = createJsIcon('car-signal', iconData);

export default CarSignal;

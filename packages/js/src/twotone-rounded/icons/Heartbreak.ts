import { createJsIcon } from '../../createJsIcon';
import { Heartbreak as iconData } from '@dga-icons/core/twotone-rounded';

const Heartbreak = createJsIcon('heartbreak', iconData);

export default Heartbreak;

import { createJsIcon } from '../../createJsIcon';
import { HorizonalScrollPoint as iconData } from '@dga-icons/core/twotone-rounded';

const HorizonalScrollPoint = createJsIcon('horizonal-scroll-point', iconData);

export default HorizonalScrollPoint;

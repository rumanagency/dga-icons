import { createJsIcon } from '../../createJsIcon';
import { UserSharing as iconData } from '@dga-icons/core/twotone-rounded';

const UserSharing = createJsIcon('user-sharing', iconData);

export default UserSharing;

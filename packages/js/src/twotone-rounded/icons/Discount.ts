import { createJsIcon } from '../../createJsIcon';
import { Discount as iconData } from '@dga-icons/core/twotone-rounded';

const Discount = createJsIcon('discount', iconData);

export default Discount;

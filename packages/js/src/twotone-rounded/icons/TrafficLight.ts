import { createJsIcon } from '../../createJsIcon';
import { TrafficLight as iconData } from '@dga-icons/core/twotone-rounded';

const TrafficLight = createJsIcon('traffic-light', iconData);

export default TrafficLight;

import { createJsIcon } from '../../createJsIcon';
import { SearchReplace as iconData } from '@dga-icons/core/twotone-rounded';

const SearchReplace = createJsIcon('search-replace', iconData);

export default SearchReplace;

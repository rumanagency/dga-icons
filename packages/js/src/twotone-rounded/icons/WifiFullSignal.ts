import { createJsIcon } from '../../createJsIcon';
import { WifiFullSignal as iconData } from '@dga-icons/core/twotone-rounded';

const WifiFullSignal = createJsIcon('wifi-full-signal', iconData);

export default WifiFullSignal;

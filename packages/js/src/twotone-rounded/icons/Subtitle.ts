import { createJsIcon } from '../../createJsIcon';
import { Subtitle as iconData } from '@dga-icons/core/twotone-rounded';

const Subtitle = createJsIcon('subtitle', iconData);

export default Subtitle;

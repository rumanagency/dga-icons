import { createJsIcon } from '../../createJsIcon';
import { Translation as iconData } from '@dga-icons/core/twotone-rounded';

const Translation = createJsIcon('translation', iconData);

export default Translation;

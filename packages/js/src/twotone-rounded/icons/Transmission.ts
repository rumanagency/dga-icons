import { createJsIcon } from '../../createJsIcon';
import { Transmission as iconData } from '@dga-icons/core/twotone-rounded';

const Transmission = createJsIcon('transmission', iconData);

export default Transmission;

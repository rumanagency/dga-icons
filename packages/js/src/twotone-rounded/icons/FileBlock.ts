import { createJsIcon } from '../../createJsIcon';
import { FileBlock as iconData } from '@dga-icons/core/twotone-rounded';

const FileBlock = createJsIcon('file-block', iconData);

export default FileBlock;

import { createJsIcon } from '../../createJsIcon';
import { SlowWinds as iconData } from '@dga-icons/core/twotone-rounded';

const SlowWinds = createJsIcon('slow-winds', iconData);

export default SlowWinds;

import { createJsIcon } from '../../createJsIcon';
import { Pipeline as iconData } from '@dga-icons/core/twotone-rounded';

const Pipeline = createJsIcon('pipeline', iconData);

export default Pipeline;

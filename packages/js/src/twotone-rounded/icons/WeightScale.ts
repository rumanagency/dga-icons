import { createJsIcon } from '../../createJsIcon';
import { WeightScale as iconData } from '@dga-icons/core/twotone-rounded';

const WeightScale = createJsIcon('weight-scale', iconData);

export default WeightScale;

import { createJsIcon } from '../../createJsIcon';
import { Background as iconData } from '@dga-icons/core/twotone-rounded';

const Background = createJsIcon('background', iconData);

export default Background;

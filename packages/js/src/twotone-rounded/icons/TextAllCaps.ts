import { createJsIcon } from '../../createJsIcon';
import { TextAllCaps as iconData } from '@dga-icons/core/twotone-rounded';

const TextAllCaps = createJsIcon('text-all-caps', iconData);

export default TextAllCaps;

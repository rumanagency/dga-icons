import { createJsIcon } from '../../createJsIcon';
import { ChatBot as iconData } from '@dga-icons/core/twotone-rounded';

const ChatBot = createJsIcon('chat-bot', iconData);

export default ChatBot;

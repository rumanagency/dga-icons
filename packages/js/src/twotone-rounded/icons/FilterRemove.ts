import { createJsIcon } from '../../createJsIcon';
import { FilterRemove as iconData } from '@dga-icons/core/twotone-rounded';

const FilterRemove = createJsIcon('filter-remove', iconData);

export default FilterRemove;

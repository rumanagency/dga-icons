import { createJsIcon } from '../../createJsIcon';
import { FlipHorizontal as iconData } from '@dga-icons/core/twotone-rounded';

const FlipHorizontal = createJsIcon('flip-horizontal', iconData);

export default FlipHorizontal;

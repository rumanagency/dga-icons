import { createJsIcon } from '../../createJsIcon';
import { FileUnlocked as iconData } from '@dga-icons/core/twotone-rounded';

const FileUnlocked = createJsIcon('file-unlocked', iconData);

export default FileUnlocked;

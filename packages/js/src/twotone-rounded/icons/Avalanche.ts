import { createJsIcon } from '../../createJsIcon';
import { Avalanche as iconData } from '@dga-icons/core/twotone-rounded';

const Avalanche = createJsIcon('avalanche', iconData);

export default Avalanche;

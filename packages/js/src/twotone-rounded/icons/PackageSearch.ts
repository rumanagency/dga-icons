import { createJsIcon } from '../../createJsIcon';
import { PackageSearch as iconData } from '@dga-icons/core/twotone-rounded';

const PackageSearch = createJsIcon('package-search', iconData);

export default PackageSearch;

import { createJsIcon } from '../../createJsIcon';
import { Chopsticks as iconData } from '@dga-icons/core/twotone-rounded';

const Chopsticks = createJsIcon('chopsticks', iconData);

export default Chopsticks;

import { createJsIcon } from '../../createJsIcon';
import { SubnodeAdd as iconData } from '@dga-icons/core/twotone-rounded';

const SubnodeAdd = createJsIcon('subnode-add', iconData);

export default SubnodeAdd;

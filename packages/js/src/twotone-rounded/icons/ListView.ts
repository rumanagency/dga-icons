import { createJsIcon } from '../../createJsIcon';
import { ListView as iconData } from '@dga-icons/core/twotone-rounded';

const ListView = createJsIcon('list-view', iconData);

export default ListView;

import { createJsIcon } from '../../createJsIcon';
import { CoinsSwap as iconData } from '@dga-icons/core/twotone-rounded';

const CoinsSwap = createJsIcon('coins-swap', iconData);

export default CoinsSwap;

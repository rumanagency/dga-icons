import { createJsIcon } from '../../createJsIcon';
import { Sparkles as iconData } from '@dga-icons/core/twotone-rounded';

const Sparkles = createJsIcon('sparkles', iconData);

export default Sparkles;

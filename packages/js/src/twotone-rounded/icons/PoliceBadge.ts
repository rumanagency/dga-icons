import { createJsIcon } from '../../createJsIcon';
import { PoliceBadge as iconData } from '@dga-icons/core/twotone-rounded';

const PoliceBadge = createJsIcon('police-badge', iconData);

export default PoliceBadge;

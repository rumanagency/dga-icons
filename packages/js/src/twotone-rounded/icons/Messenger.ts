import { createJsIcon } from '../../createJsIcon';
import { Messenger as iconData } from '@dga-icons/core/twotone-rounded';

const Messenger = createJsIcon('messenger', iconData);

export default Messenger;

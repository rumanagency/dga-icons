import { createJsIcon } from '../../createJsIcon';
import { FileLink as iconData } from '@dga-icons/core/twotone-rounded';

const FileLink = createJsIcon('file-link', iconData);

export default FileLink;

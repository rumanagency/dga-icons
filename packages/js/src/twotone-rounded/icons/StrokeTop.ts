import { createJsIcon } from '../../createJsIcon';
import { StrokeTop as iconData } from '@dga-icons/core/twotone-rounded';

const StrokeTop = createJsIcon('stroke-top', iconData);

export default StrokeTop;

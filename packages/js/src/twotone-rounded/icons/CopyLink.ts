import { createJsIcon } from '../../createJsIcon';
import { CopyLink as iconData } from '@dga-icons/core/twotone-rounded';

const CopyLink = createJsIcon('copy-link', iconData);

export default CopyLink;

import { createJsIcon } from '../../createJsIcon';
import { StrokeBottom as iconData } from '@dga-icons/core/twotone-rounded';

const StrokeBottom = createJsIcon('stroke-bottom', iconData);

export default StrokeBottom;

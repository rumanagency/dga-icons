import { createJsIcon } from '../../createJsIcon';
import { LockKey as iconData } from '@dga-icons/core/twotone-rounded';

const LockKey = createJsIcon('lock-key', iconData);

export default LockKey;

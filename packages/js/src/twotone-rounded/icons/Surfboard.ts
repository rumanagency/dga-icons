import { createJsIcon } from '../../createJsIcon';
import { Surfboard as iconData } from '@dga-icons/core/twotone-rounded';

const Surfboard = createJsIcon('surfboard', iconData);

export default Surfboard;

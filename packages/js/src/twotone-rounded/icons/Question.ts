import { createJsIcon } from '../../createJsIcon';
import { Question as iconData } from '@dga-icons/core/twotone-rounded';

const Question = createJsIcon('question', iconData);

export default Question;

import { createJsIcon } from '../../createJsIcon';
import { FolderOff as iconData } from '@dga-icons/core/twotone-rounded';

const FolderOff = createJsIcon('folder-off', iconData);

export default FolderOff;

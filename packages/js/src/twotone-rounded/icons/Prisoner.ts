import { createJsIcon } from '../../createJsIcon';
import { Prisoner as iconData } from '@dga-icons/core/twotone-rounded';

const Prisoner = createJsIcon('prisoner', iconData);

export default Prisoner;

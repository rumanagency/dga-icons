import { createJsIcon } from '../../createJsIcon';
import { GitPullRequest as iconData } from '@dga-icons/core/twotone-rounded';

const GitPullRequest = createJsIcon('git-pull-request', iconData);

export default GitPullRequest;

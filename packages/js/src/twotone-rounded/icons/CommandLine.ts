import { createJsIcon } from '../../createJsIcon';
import { CommandLine as iconData } from '@dga-icons/core/twotone-rounded';

const CommandLine = createJsIcon('command-line', iconData);

export default CommandLine;

import { createJsIcon } from '../../createJsIcon';
import { FileImport as iconData } from '@dga-icons/core/twotone-rounded';

const FileImport = createJsIcon('file-import', iconData);

export default FileImport;

import { createJsIcon } from '../../createJsIcon';
import { GitFork as iconData } from '@dga-icons/core/twotone-rounded';

const GitFork = createJsIcon('git-fork', iconData);

export default GitFork;

import { createJsIcon } from '../../createJsIcon';
import { InsertRowUp as iconData } from '@dga-icons/core/twotone-rounded';

const InsertRowUp = createJsIcon('insert-row-up', iconData);

export default InsertRowUp;

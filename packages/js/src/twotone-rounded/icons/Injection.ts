import { createJsIcon } from '../../createJsIcon';
import { Injection as iconData } from '@dga-icons/core/twotone-rounded';

const Injection = createJsIcon('injection', iconData);

export default Injection;

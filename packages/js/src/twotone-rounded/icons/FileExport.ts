import { createJsIcon } from '../../createJsIcon';
import { FileExport as iconData } from '@dga-icons/core/twotone-rounded';

const FileExport = createJsIcon('file-export', iconData);

export default FileExport;

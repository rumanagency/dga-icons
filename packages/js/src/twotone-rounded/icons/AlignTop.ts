import { createJsIcon } from '../../createJsIcon';
import { AlignTop as iconData } from '@dga-icons/core/twotone-rounded';

const AlignTop = createJsIcon('align-top', iconData);

export default AlignTop;

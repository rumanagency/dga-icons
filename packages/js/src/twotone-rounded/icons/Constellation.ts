import { createJsIcon } from '../../createJsIcon';
import { Constellation as iconData } from '@dga-icons/core/twotone-rounded';

const Constellation = createJsIcon('constellation', iconData);

export default Constellation;

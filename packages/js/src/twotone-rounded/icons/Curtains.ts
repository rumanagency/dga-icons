import { createJsIcon } from '../../createJsIcon';
import { Curtains as iconData } from '@dga-icons/core/twotone-rounded';

const Curtains = createJsIcon('curtains', iconData);

export default Curtains;

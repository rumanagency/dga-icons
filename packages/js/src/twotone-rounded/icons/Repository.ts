import { createJsIcon } from '../../createJsIcon';
import { Repository as iconData } from '@dga-icons/core/twotone-rounded';

const Repository = createJsIcon('repository', iconData);

export default Repository;

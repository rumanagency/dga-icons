import { createJsIcon } from '../../createJsIcon';
import { Conversation as iconData } from '@dga-icons/core/twotone-rounded';

const Conversation = createJsIcon('conversation', iconData);

export default Conversation;

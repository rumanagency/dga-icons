import { createJsIcon } from '../../createJsIcon';
import { LaptopPhoneSync as iconData } from '@dga-icons/core/twotone-rounded';

const LaptopPhoneSync = createJsIcon('laptop-phone-sync', iconData);

export default LaptopPhoneSync;

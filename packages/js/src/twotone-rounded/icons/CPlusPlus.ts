import { createJsIcon } from '../../createJsIcon';
import { CPlusPlus as iconData } from '@dga-icons/core/twotone-rounded';

const CPlusPlus = createJsIcon('c++', iconData);

export default CPlusPlus;

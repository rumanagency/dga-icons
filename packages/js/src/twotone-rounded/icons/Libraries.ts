import { createJsIcon } from '../../createJsIcon';
import { Libraries as iconData } from '@dga-icons/core/twotone-rounded';

const Libraries = createJsIcon('libraries', iconData);

export default Libraries;

import { createJsIcon } from '../../createJsIcon';
import { Alert01 as iconData } from '@dga-icons/core/twotone-rounded';

const Alert01 = createJsIcon('alert-01', iconData);

export default Alert01;

import { createJsIcon } from '../../createJsIcon';
import { TestTube as iconData } from '@dga-icons/core/twotone-rounded';

const TestTube = createJsIcon('test-tube', iconData);

export default TestTube;

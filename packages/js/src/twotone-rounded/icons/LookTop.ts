import { createJsIcon } from '../../createJsIcon';
import { LookTop as iconData } from '@dga-icons/core/twotone-rounded';

const LookTop = createJsIcon('look-top', iconData);

export default LookTop;

import { createJsIcon } from '../../createJsIcon';
import { AlignBottom as iconData } from '@dga-icons/core/twotone-rounded';

const AlignBottom = createJsIcon('align-bottom', iconData);

export default AlignBottom;

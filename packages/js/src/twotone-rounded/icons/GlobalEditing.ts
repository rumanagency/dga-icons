import { createJsIcon } from '../../createJsIcon';
import { GlobalEditing as iconData } from '@dga-icons/core/twotone-rounded';

const GlobalEditing = createJsIcon('global-editing', iconData);

export default GlobalEditing;

import { createJsIcon } from '../../createJsIcon';
import { Distribution as iconData } from '@dga-icons/core/twotone-rounded';

const Distribution = createJsIcon('distribution', iconData);

export default Distribution;

import { createJsIcon } from '../../createJsIcon';
import { WifiUnlock as iconData } from '@dga-icons/core/twotone-rounded';

const WifiUnlock = createJsIcon('wifi-unlock', iconData);

export default WifiUnlock;

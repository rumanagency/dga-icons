import { createJsIcon } from '../../createJsIcon';
import { SmsCode as iconData } from '@dga-icons/core/twotone-rounded';

const SmsCode = createJsIcon('sms-code', iconData);

export default SmsCode;

import { createJsIcon } from '../../createJsIcon';
import { FileEdit as iconData } from '@dga-icons/core/twotone-rounded';

const FileEdit = createJsIcon('file-edit', iconData);

export default FileEdit;

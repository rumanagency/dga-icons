import { createJsIcon } from '../../createJsIcon';
import { LaptopPerformance as iconData } from '@dga-icons/core/twotone-rounded';

const LaptopPerformance = createJsIcon('laptop-performance', iconData);

export default LaptopPerformance;

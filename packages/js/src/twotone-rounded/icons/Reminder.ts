import { createJsIcon } from '../../createJsIcon';
import { Reminder as iconData } from '@dga-icons/core/twotone-rounded';

const Reminder = createJsIcon('reminder', iconData);

export default Reminder;

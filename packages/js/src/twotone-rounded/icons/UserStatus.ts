import { createJsIcon } from '../../createJsIcon';
import { UserStatus as iconData } from '@dga-icons/core/twotone-rounded';

const UserStatus = createJsIcon('user-status', iconData);

export default UserStatus;

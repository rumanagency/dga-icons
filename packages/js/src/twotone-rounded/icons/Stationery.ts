import { createJsIcon } from '../../createJsIcon';
import { Stationery as iconData } from '@dga-icons/core/twotone-rounded';

const Stationery = createJsIcon('stationery', iconData);

export default Stationery;

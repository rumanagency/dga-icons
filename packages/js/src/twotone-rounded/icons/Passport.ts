import { createJsIcon } from '../../createJsIcon';
import { Passport as iconData } from '@dga-icons/core/twotone-rounded';

const Passport = createJsIcon('passport', iconData);

export default Passport;

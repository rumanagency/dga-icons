import { createJsIcon } from '../../createJsIcon';
import { SidebarLeft as iconData } from '@dga-icons/core/twotone-rounded';

const SidebarLeft = createJsIcon('sidebar-left', iconData);

export default SidebarLeft;

import { createJsIcon } from '../../createJsIcon';
import { BitcoinStore as iconData } from '@dga-icons/core/twotone-rounded';

const BitcoinStore = createJsIcon('bitcoin-store', iconData);

export default BitcoinStore;

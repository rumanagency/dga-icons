import { createJsIcon } from '../../createJsIcon';
import { HotTube as iconData } from '@dga-icons/core/twotone-rounded';

const HotTube = createJsIcon('hot-tube', iconData);

export default HotTube;

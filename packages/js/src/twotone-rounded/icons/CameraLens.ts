import { createJsIcon } from '../../createJsIcon';
import { CameraLens as iconData } from '@dga-icons/core/twotone-rounded';

const CameraLens = createJsIcon('camera-lens', iconData);

export default CameraLens;

import { createJsIcon } from '../../createJsIcon';
import { Keyframe as iconData } from '@dga-icons/core/twotone-rounded';

const Keyframe = createJsIcon('keyframe', iconData);

export default Keyframe;

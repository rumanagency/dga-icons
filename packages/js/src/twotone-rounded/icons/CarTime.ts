import { createJsIcon } from '../../createJsIcon';
import { CarTime as iconData } from '@dga-icons/core/twotone-rounded';

const CarTime = createJsIcon('car-time', iconData);

export default CarTime;

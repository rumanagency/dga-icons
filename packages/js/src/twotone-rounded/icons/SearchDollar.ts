import { createJsIcon } from '../../createJsIcon';
import { SearchDollar as iconData } from '@dga-icons/core/twotone-rounded';

const SearchDollar = createJsIcon('search-dollar', iconData);

export default SearchDollar;

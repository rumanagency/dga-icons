import { createJsIcon } from '../../createJsIcon';
import { Promotion as iconData } from '@dga-icons/core/twotone-rounded';

const Promotion = createJsIcon('promotion', iconData);

export default Promotion;

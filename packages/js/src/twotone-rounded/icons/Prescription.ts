import { createJsIcon } from '../../createJsIcon';
import { Prescription as iconData } from '@dga-icons/core/twotone-rounded';

const Prescription = createJsIcon('prescription', iconData);

export default Prescription;

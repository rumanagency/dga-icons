import { createJsIcon } from '../../createJsIcon';
import { FileAdd as iconData } from '@dga-icons/core/twotone-rounded';

const FileAdd = createJsIcon('file-add', iconData);

export default FileAdd;

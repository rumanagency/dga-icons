import { createJsIcon } from '../../createJsIcon';
import { Complaint as iconData } from '@dga-icons/core/twotone-rounded';

const Complaint = createJsIcon('complaint', iconData);

export default Complaint;

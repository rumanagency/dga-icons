import { createJsIcon } from '../../createJsIcon';
import { InsertPi as iconData } from '@dga-icons/core/twotone-rounded';

const InsertPi = createJsIcon('insert-pi', iconData);

export default InsertPi;

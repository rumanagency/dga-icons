import { createJsIcon } from '../../createJsIcon';
import { Whiteboard as iconData } from '@dga-icons/core/twotone-rounded';

const Whiteboard = createJsIcon('whiteboard', iconData);

export default Whiteboard;

import { createJsIcon } from '../../createJsIcon';
import { UsbConnected02 as iconData } from '@dga-icons/core/twotone-rounded';

const UsbConnected02 = createJsIcon('usb-connected-02', iconData);

export default UsbConnected02;

import { createJsIcon } from '../../createJsIcon';
import { LoginMethod as iconData } from '@dga-icons/core/twotone-rounded';

const LoginMethod = createJsIcon('login-method', iconData);

export default LoginMethod;

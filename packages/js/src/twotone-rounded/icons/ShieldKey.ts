import { createJsIcon } from '../../createJsIcon';
import { ShieldKey as iconData } from '@dga-icons/core/twotone-rounded';

const ShieldKey = createJsIcon('shield-key', iconData);

export default ShieldKey;

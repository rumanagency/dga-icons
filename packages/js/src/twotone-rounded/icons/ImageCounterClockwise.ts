import { createJsIcon } from '../../createJsIcon';
import { ImageCounterClockwise as iconData } from '@dga-icons/core/twotone-rounded';

const ImageCounterClockwise = createJsIcon('image-counter-clockwise', iconData);

export default ImageCounterClockwise;

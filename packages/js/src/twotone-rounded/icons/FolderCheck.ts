import { createJsIcon } from '../../createJsIcon';
import { FolderCheck as iconData } from '@dga-icons/core/twotone-rounded';

const FolderCheck = createJsIcon('folder-check', iconData);

export default FolderCheck;

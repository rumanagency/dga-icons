import { createJsIcon } from '../../createJsIcon';
import { WorkHistory as iconData } from '@dga-icons/core/twotone-rounded';

const WorkHistory = createJsIcon('work-history', iconData);

export default WorkHistory;

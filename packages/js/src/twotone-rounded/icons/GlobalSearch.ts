import { createJsIcon } from '../../createJsIcon';
import { GlobalSearch as iconData } from '@dga-icons/core/twotone-rounded';

const GlobalSearch = createJsIcon('global-search', iconData);

export default GlobalSearch;

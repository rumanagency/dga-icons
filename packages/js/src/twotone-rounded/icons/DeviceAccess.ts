import { createJsIcon } from '../../createJsIcon';
import { DeviceAccess as iconData } from '@dga-icons/core/twotone-rounded';

const DeviceAccess = createJsIcon('device-access', iconData);

export default DeviceAccess;

import { createJsIcon } from '../../createJsIcon';
import { Calculate as iconData } from '@dga-icons/core/twotone-rounded';

const Calculate = createJsIcon('calculate', iconData);

export default Calculate;

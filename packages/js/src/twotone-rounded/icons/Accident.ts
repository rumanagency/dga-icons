import { createJsIcon } from '../../createJsIcon';
import { Accident as iconData } from '@dga-icons/core/twotone-rounded';

const Accident = createJsIcon('accident', iconData);

export default Accident;

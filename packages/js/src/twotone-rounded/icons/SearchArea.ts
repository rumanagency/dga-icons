import { createJsIcon } from '../../createJsIcon';
import { SearchArea as iconData } from '@dga-icons/core/twotone-rounded';

const SearchArea = createJsIcon('search-area', iconData);

export default SearchArea;

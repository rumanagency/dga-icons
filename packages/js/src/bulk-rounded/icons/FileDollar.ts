import { createJsIcon } from '../../createJsIcon';
import { FileDollar as iconData } from '@dga-icons/core/bulk-rounded';

const FileDollar = createJsIcon('file-dollar', iconData);

export default FileDollar;

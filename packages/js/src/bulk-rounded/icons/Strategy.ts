import { createJsIcon } from '../../createJsIcon';
import { Strategy as iconData } from '@dga-icons/core/bulk-rounded';

const Strategy = createJsIcon('strategy', iconData);

export default Strategy;

import { createJsIcon } from '../../createJsIcon';
import { HardDrive as iconData } from '@dga-icons/core/bulk-rounded';

const HardDrive = createJsIcon('hard-drive', iconData);

export default HardDrive;

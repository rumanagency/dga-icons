import { createJsIcon } from '../../createJsIcon';
import { PackageSent as iconData } from '@dga-icons/core/bulk-rounded';

const PackageSent = createJsIcon('package-sent', iconData);

export default PackageSent;

import { createJsIcon } from '../../createJsIcon';
import { DistributeVerticalTop as iconData } from '@dga-icons/core/bulk-rounded';

const DistributeVerticalTop = createJsIcon('distribute-vertical-top', iconData);

export default DistributeVerticalTop;

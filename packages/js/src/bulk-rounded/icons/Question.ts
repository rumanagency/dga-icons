import { createJsIcon } from '../../createJsIcon';
import { Question as iconData } from '@dga-icons/core/bulk-rounded';

const Question = createJsIcon('question', iconData);

export default Question;

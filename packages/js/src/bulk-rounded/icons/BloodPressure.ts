import { createJsIcon } from '../../createJsIcon';
import { BloodPressure as iconData } from '@dga-icons/core/bulk-rounded';

const BloodPressure = createJsIcon('blood-pressure', iconData);

export default BloodPressure;

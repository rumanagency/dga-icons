import { createJsIcon } from '../../createJsIcon';
import { BlackHole as iconData } from '@dga-icons/core/bulk-rounded';

const BlackHole = createJsIcon('black-hole', iconData);

export default BlackHole;

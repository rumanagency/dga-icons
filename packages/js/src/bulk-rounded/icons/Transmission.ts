import { createJsIcon } from '../../createJsIcon';
import { Transmission as iconData } from '@dga-icons/core/bulk-rounded';

const Transmission = createJsIcon('transmission', iconData);

export default Transmission;

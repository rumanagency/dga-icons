import { createJsIcon } from '../../createJsIcon';
import { FileEdit as iconData } from '@dga-icons/core/bulk-rounded';

const FileEdit = createJsIcon('file-edit', iconData);

export default FileEdit;

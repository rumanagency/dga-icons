import { createJsIcon } from '../../createJsIcon';
import { AddressBook as iconData } from '@dga-icons/core/bulk-rounded';

const AddressBook = createJsIcon('address-book', iconData);

export default AddressBook;

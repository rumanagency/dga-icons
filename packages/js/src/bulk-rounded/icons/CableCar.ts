import { createJsIcon } from '../../createJsIcon';
import { CableCar as iconData } from '@dga-icons/core/bulk-rounded';

const CableCar = createJsIcon('cable-car', iconData);

export default CableCar;

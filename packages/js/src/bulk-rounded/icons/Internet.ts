import { createJsIcon } from '../../createJsIcon';
import { Internet as iconData } from '@dga-icons/core/bulk-rounded';

const Internet = createJsIcon('internet', iconData);

export default Internet;

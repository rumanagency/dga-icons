import { createJsIcon } from '../../createJsIcon';
import { Authorized as iconData } from '@dga-icons/core/bulk-rounded';

const Authorized = createJsIcon('authorized', iconData);

export default Authorized;

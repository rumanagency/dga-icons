import { createJsIcon } from '../../createJsIcon';
import { CustomField as iconData } from '@dga-icons/core/bulk-rounded';

const CustomField = createJsIcon('custom-field', iconData);

export default CustomField;

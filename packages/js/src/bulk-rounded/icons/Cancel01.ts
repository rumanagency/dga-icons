import { createJsIcon } from '../../createJsIcon';
import { Cancel01 as iconData } from '@dga-icons/core/bulk-rounded';

const Cancel01 = createJsIcon('cancel-01', iconData);

export default Cancel01;

import { createJsIcon } from '../../createJsIcon';
import { WasteRestore as iconData } from '@dga-icons/core/bulk-rounded';

const WasteRestore = createJsIcon('waste-restore', iconData);

export default WasteRestore;

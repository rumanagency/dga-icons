import { createJsIcon } from '../../createJsIcon';
import { BitcoinShield as iconData } from '@dga-icons/core/bulk-rounded';

const BitcoinShield = createJsIcon('bitcoin-shield', iconData);

export default BitcoinShield;

import { createJsIcon } from '../../createJsIcon';
import { CpuCharge as iconData } from '@dga-icons/core/bulk-rounded';

const CpuCharge = createJsIcon('cpu-charge', iconData);

export default CpuCharge;

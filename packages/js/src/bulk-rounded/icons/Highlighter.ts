import { createJsIcon } from '../../createJsIcon';
import { Highlighter as iconData } from '@dga-icons/core/bulk-rounded';

const Highlighter = createJsIcon('highlighter', iconData);

export default Highlighter;

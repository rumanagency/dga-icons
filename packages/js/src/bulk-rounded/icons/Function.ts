import { createJsIcon } from '../../createJsIcon';
import { Function as iconData } from '@dga-icons/core/bulk-rounded';

const Function = createJsIcon('function', iconData);

export default Function;

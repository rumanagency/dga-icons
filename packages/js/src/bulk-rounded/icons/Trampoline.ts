import { createJsIcon } from '../../createJsIcon';
import { Trampoline as iconData } from '@dga-icons/core/bulk-rounded';

const Trampoline = createJsIcon('trampoline', iconData);

export default Trampoline;

import { createJsIcon } from '../../createJsIcon';
import { WifiError01 as iconData } from '@dga-icons/core/bulk-rounded';

const WifiError01 = createJsIcon('wifi-error-01', iconData);

export default WifiError01;

import { createJsIcon } from '../../createJsIcon';
import { Notion02 as iconData } from '@dga-icons/core/bulk-rounded';

const Notion02 = createJsIcon('notion-02', iconData);

export default Notion02;

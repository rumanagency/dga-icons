import { createJsIcon } from '../../createJsIcon';
import { SolidLine01 as iconData } from '@dga-icons/core/bulk-rounded';

const SolidLine01 = createJsIcon('solid-line-01', iconData);

export default SolidLine01;

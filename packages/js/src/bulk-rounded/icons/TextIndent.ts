import { createJsIcon } from '../../createJsIcon';
import { TextIndent as iconData } from '@dga-icons/core/bulk-rounded';

const TextIndent = createJsIcon('text-indent', iconData);

export default TextIndent;

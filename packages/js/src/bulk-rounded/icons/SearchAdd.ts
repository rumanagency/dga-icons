import { createJsIcon } from '../../createJsIcon';
import { SearchAdd as iconData } from '@dga-icons/core/bulk-rounded';

const SearchAdd = createJsIcon('search-add', iconData);

export default SearchAdd;

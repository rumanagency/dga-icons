import { createJsIcon } from '../../createJsIcon';
import { CursorProgress03 as iconData } from '@dga-icons/core/bulk-rounded';

const CursorProgress03 = createJsIcon('cursor-progress-03', iconData);

export default CursorProgress03;

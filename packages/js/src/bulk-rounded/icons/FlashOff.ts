import { createJsIcon } from '../../createJsIcon';
import { FlashOff as iconData } from '@dga-icons/core/bulk-rounded';

const FlashOff = createJsIcon('flash-off', iconData);

export default FlashOff;

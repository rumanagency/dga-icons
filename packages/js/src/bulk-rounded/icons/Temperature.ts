import { createJsIcon } from '../../createJsIcon';
import { Temperature as iconData } from '@dga-icons/core/bulk-rounded';

const Temperature = createJsIcon('temperature', iconData);

export default Temperature;

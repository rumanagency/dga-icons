import { createJsIcon } from '../../createJsIcon';
import { SidebarTop as iconData } from '@dga-icons/core/bulk-rounded';

const SidebarTop = createJsIcon('sidebar-top', iconData);

export default SidebarTop;

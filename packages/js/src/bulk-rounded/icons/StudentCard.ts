import { createJsIcon } from '../../createJsIcon';
import { StudentCard as iconData } from '@dga-icons/core/bulk-rounded';

const StudentCard = createJsIcon('student-card', iconData);

export default StudentCard;

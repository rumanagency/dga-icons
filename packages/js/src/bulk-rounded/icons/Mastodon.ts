import { createJsIcon } from '../../createJsIcon';
import { Mastodon as iconData } from '@dga-icons/core/bulk-rounded';

const Mastodon = createJsIcon('mastodon', iconData);

export default Mastodon;

import { createJsIcon } from '../../createJsIcon';
import { Variable as iconData } from '@dga-icons/core/bulk-rounded';

const Variable = createJsIcon('variable', iconData);

export default Variable;

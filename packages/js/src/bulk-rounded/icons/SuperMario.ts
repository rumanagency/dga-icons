import { createJsIcon } from '../../createJsIcon';
import { SuperMario as iconData } from '@dga-icons/core/bulk-rounded';

const SuperMario = createJsIcon('super-mario', iconData);

export default SuperMario;

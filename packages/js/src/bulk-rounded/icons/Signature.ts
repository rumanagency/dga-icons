import { createJsIcon } from '../../createJsIcon';
import { Signature as iconData } from '@dga-icons/core/bulk-rounded';

const Signature = createJsIcon('signature', iconData);

export default Signature;

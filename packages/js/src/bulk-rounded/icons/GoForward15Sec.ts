import { createJsIcon } from '../../createJsIcon';
import { GoForward15Sec as iconData } from '@dga-icons/core/bulk-rounded';

const GoForward15Sec = createJsIcon('go-forward-15-sec', iconData);

export default GoForward15Sec;

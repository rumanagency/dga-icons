import { createJsIcon } from '../../createJsIcon';
import { Location01 as iconData } from '@dga-icons/core/bulk-rounded';

const Location01 = createJsIcon('location-01', iconData);

export default Location01;

import { createJsIcon } from '../../createJsIcon';
import { Spaceship as iconData } from '@dga-icons/core/bulk-rounded';

const Spaceship = createJsIcon('spaceship', iconData);

export default Spaceship;

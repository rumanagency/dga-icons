import { createJsIcon } from '../../createJsIcon';
import { ComputerAdd as iconData } from '@dga-icons/core/bulk-rounded';

const ComputerAdd = createJsIcon('computer-add', iconData);

export default ComputerAdd;

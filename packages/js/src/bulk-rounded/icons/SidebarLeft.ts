import { createJsIcon } from '../../createJsIcon';
import { SidebarLeft as iconData } from '@dga-icons/core/bulk-rounded';

const SidebarLeft = createJsIcon('sidebar-left', iconData);

export default SidebarLeft;

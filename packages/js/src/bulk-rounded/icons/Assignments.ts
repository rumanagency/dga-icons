import { createJsIcon } from '../../createJsIcon';
import { Assignments as iconData } from '@dga-icons/core/bulk-rounded';

const Assignments = createJsIcon('assignments', iconData);

export default Assignments;

import { createJsIcon } from '../../createJsIcon';
import { CallMinus as iconData } from '@dga-icons/core/bulk-rounded';

const CallMinus = createJsIcon('call-minus', iconData);

export default CallMinus;

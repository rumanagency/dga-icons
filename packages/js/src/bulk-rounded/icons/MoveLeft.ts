import { createJsIcon } from '../../createJsIcon';
import { MoveLeft as iconData } from '@dga-icons/core/bulk-rounded';

const MoveLeft = createJsIcon('move-left', iconData);

export default MoveLeft;

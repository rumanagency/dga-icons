import { createJsIcon } from '../../createJsIcon';
import { SoilTemperatureGlobal as iconData } from '@dga-icons/core/bulk-rounded';

const SoilTemperatureGlobal = createJsIcon('soil-temperature-global', iconData);

export default SoilTemperatureGlobal;

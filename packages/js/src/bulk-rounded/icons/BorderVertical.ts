import { createJsIcon } from '../../createJsIcon';
import { BorderVertical as iconData } from '@dga-icons/core/bulk-rounded';

const BorderVertical = createJsIcon('border-vertical', iconData);

export default BorderVertical;

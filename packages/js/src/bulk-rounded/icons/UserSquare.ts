import { createJsIcon } from '../../createJsIcon';
import { UserSquare as iconData } from '@dga-icons/core/bulk-rounded';

const UserSquare = createJsIcon('user-square', iconData);

export default UserSquare;

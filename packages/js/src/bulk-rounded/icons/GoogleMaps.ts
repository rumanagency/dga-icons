import { createJsIcon } from '../../createJsIcon';
import { GoogleMaps as iconData } from '@dga-icons/core/bulk-rounded';

const GoogleMaps = createJsIcon('google-maps', iconData);

export default GoogleMaps;

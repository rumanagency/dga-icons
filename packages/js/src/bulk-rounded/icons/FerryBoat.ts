import { createJsIcon } from '../../createJsIcon';
import { FerryBoat as iconData } from '@dga-icons/core/bulk-rounded';

const FerryBoat = createJsIcon('ferry-boat', iconData);

export default FerryBoat;

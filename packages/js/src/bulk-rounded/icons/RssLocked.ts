import { createJsIcon } from '../../createJsIcon';
import { RssLocked as iconData } from '@dga-icons/core/bulk-rounded';

const RssLocked = createJsIcon('rss-locked', iconData);

export default RssLocked;

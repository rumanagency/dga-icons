import { createJsIcon } from '../../createJsIcon';
import { CommandLine as iconData } from '@dga-icons/core/bulk-rounded';

const CommandLine = createJsIcon('command-line', iconData);

export default CommandLine;

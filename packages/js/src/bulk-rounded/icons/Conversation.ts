import { createJsIcon } from '../../createJsIcon';
import { Conversation as iconData } from '@dga-icons/core/bulk-rounded';

const Conversation = createJsIcon('conversation', iconData);

export default Conversation;

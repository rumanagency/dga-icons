import { createJsIcon } from '../../createJsIcon';
import { ShutDown as iconData } from '@dga-icons/core/bulk-rounded';

const ShutDown = createJsIcon('shut-down', iconData);

export default ShutDown;

import { createJsIcon } from '../../createJsIcon';
import { BorderTop01 as iconData } from '@dga-icons/core/bulk-rounded';

const BorderTop01 = createJsIcon('border-top-01', iconData);

export default BorderTop01;

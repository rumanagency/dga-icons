import { createJsIcon } from '../../createJsIcon';
import { FileImport as iconData } from '@dga-icons/core/bulk-rounded';

const FileImport = createJsIcon('file-import', iconData);

export default FileImport;

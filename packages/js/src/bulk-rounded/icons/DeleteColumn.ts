import { createJsIcon } from '../../createJsIcon';
import { DeleteColumn as iconData } from '@dga-icons/core/bulk-rounded';

const DeleteColumn = createJsIcon('delete-column', iconData);

export default DeleteColumn;

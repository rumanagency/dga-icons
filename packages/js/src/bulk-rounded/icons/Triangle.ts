import { createJsIcon } from '../../createJsIcon';
import { Triangle as iconData } from '@dga-icons/core/bulk-rounded';

const Triangle = createJsIcon('triangle', iconData);

export default Triangle;

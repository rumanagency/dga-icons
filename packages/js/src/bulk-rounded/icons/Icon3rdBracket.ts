import { createJsIcon } from '../../createJsIcon';
import { Icon3rdBracket as iconData } from '@dga-icons/core/bulk-rounded';

const Icon3rdBracket = createJsIcon('3rd-bracket', iconData);

export default Icon3rdBracket;

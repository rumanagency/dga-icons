import { createJsIcon } from '../../createJsIcon';
import { FolderSearch as iconData } from '@dga-icons/core/bulk-rounded';

const FolderSearch = createJsIcon('folder-search', iconData);

export default FolderSearch;

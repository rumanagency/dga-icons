import { createJsIcon } from '../../createJsIcon';
import { Bluetooth as iconData } from '@dga-icons/core/bulk-rounded';

const Bluetooth = createJsIcon('bluetooth', iconData);

export default Bluetooth;

import { createJsIcon } from '../../createJsIcon';
import { Flashlight as iconData } from '@dga-icons/core/bulk-rounded';

const Flashlight = createJsIcon('flashlight', iconData);

export default Flashlight;

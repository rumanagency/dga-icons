import { createJsIcon } from '../../createJsIcon';
import { Discount01 as iconData } from '@dga-icons/core/bulk-rounded';

const Discount01 = createJsIcon('discount-01', iconData);

export default Discount01;

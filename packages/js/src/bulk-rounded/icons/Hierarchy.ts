import { createJsIcon } from '../../createJsIcon';
import { Hierarchy as iconData } from '@dga-icons/core/bulk-rounded';

const Hierarchy = createJsIcon('hierarchy', iconData);

export default Hierarchy;

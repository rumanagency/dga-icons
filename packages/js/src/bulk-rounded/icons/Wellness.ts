import { createJsIcon } from '../../createJsIcon';
import { Wellness as iconData } from '@dga-icons/core/bulk-rounded';

const Wellness = createJsIcon('wellness', iconData);

export default Wellness;

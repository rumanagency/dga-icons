import { createJsIcon } from '../../createJsIcon';
import { GuestHouse as iconData } from '@dga-icons/core/bulk-rounded';

const GuestHouse = createJsIcon('guest-house', iconData);

export default GuestHouse;

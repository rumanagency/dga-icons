import { createJsIcon } from '../../createJsIcon';
import { Ethereum as iconData } from '@dga-icons/core/bulk-rounded';

const Ethereum = createJsIcon('ethereum', iconData);

export default Ethereum;

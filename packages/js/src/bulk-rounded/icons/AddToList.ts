import { createJsIcon } from '../../createJsIcon';
import { AddToList as iconData } from '@dga-icons/core/bulk-rounded';

const AddToList = createJsIcon('add-to-list', iconData);

export default AddToList;

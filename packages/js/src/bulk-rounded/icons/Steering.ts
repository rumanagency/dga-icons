import { createJsIcon } from '../../createJsIcon';
import { Steering as iconData } from '@dga-icons/core/bulk-rounded';

const Steering = createJsIcon('steering', iconData);

export default Steering;

import { createJsIcon } from '../../createJsIcon';
import { Dribbble as iconData } from '@dga-icons/core/bulk-rounded';

const Dribbble = createJsIcon('dribbble', iconData);

export default Dribbble;

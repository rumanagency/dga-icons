import { createJsIcon } from '../../createJsIcon';
import { ComputerProtection as iconData } from '@dga-icons/core/bulk-rounded';

const ComputerProtection = createJsIcon('computer-protection', iconData);

export default ComputerProtection;

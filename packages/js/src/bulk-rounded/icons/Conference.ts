import { createJsIcon } from '../../createJsIcon';
import { Conference as iconData } from '@dga-icons/core/bulk-rounded';

const Conference = createJsIcon('conference', iconData);

export default Conference;

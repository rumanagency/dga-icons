import { createJsIcon } from '../../createJsIcon';
import { WallLamp as iconData } from '@dga-icons/core/bulk-rounded';

const WallLamp = createJsIcon('wall-lamp', iconData);

export default WallLamp;

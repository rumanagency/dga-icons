import { createJsIcon } from '../../createJsIcon';
import { PackageProcess as iconData } from '@dga-icons/core/bulk-rounded';

const PackageProcess = createJsIcon('package-process', iconData);

export default PackageProcess;

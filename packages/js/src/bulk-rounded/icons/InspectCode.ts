import { createJsIcon } from '../../createJsIcon';
import { InspectCode as iconData } from '@dga-icons/core/bulk-rounded';

const InspectCode = createJsIcon('inspect-code', iconData);

export default InspectCode;

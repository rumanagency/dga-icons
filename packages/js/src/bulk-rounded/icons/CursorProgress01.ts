import { createJsIcon } from '../../createJsIcon';
import { CursorProgress01 as iconData } from '@dga-icons/core/bulk-rounded';

const CursorProgress01 = createJsIcon('cursor-progress-01', iconData);

export default CursorProgress01;

import { createJsIcon } from '../../createJsIcon';
import { Delete01 as iconData } from '@dga-icons/core/bulk-rounded';

const Delete01 = createJsIcon('delete-01', iconData);

export default Delete01;

import { createJsIcon } from '../../createJsIcon';
import { NoteRemove as iconData } from '@dga-icons/core/bulk-rounded';

const NoteRemove = createJsIcon('note-remove', iconData);

export default NoteRemove;

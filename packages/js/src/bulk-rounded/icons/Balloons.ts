import { createJsIcon } from '../../createJsIcon';
import { Balloons as iconData } from '@dga-icons/core/bulk-rounded';

const Balloons = createJsIcon('balloons', iconData);

export default Balloons;

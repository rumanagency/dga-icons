import { createJsIcon } from '../../createJsIcon';
import { BarChart as iconData } from '@dga-icons/core/bulk-rounded';

const BarChart = createJsIcon('bar-chart', iconData);

export default BarChart;

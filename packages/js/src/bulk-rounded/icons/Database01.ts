import { createJsIcon } from '../../createJsIcon';
import { Database01 as iconData } from '@dga-icons/core/bulk-rounded';

const Database01 = createJsIcon('database-01', iconData);

export default Database01;

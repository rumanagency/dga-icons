import { createJsIcon } from '../../createJsIcon';
import { SaveMoneyDollar as iconData } from '@dga-icons/core/bulk-rounded';

const SaveMoneyDollar = createJsIcon('save-money-dollar', iconData);

export default SaveMoneyDollar;

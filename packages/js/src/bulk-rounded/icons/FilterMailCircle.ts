import { createJsIcon } from '../../createJsIcon';
import { FilterMailCircle as iconData } from '@dga-icons/core/bulk-rounded';

const FilterMailCircle = createJsIcon('filter-mail-circle', iconData);

export default FilterMailCircle;

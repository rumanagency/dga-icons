import { createJsIcon } from '../../createJsIcon';
import { Clipboard as iconData } from '@dga-icons/core/bulk-rounded';

const Clipboard = createJsIcon('clipboard', iconData);

export default Clipboard;

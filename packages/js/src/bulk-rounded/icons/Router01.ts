import { createJsIcon } from '../../createJsIcon';
import { Router01 as iconData } from '@dga-icons/core/bulk-rounded';

const Router01 = createJsIcon('router-01', iconData);

export default Router01;

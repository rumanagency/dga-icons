import { createJsIcon } from '../../createJsIcon';
import { AllBookmark as iconData } from '@dga-icons/core/bulk-rounded';

const AllBookmark = createJsIcon('all-bookmark', iconData);

export default AllBookmark;

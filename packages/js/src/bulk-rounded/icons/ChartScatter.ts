import { createJsIcon } from '../../createJsIcon';
import { ChartScatter as iconData } from '@dga-icons/core/bulk-rounded';

const ChartScatter = createJsIcon('chart-scatter', iconData);

export default ChartScatter;

import { createJsIcon } from '../../createJsIcon';
import { FolderBlock as iconData } from '@dga-icons/core/bulk-rounded';

const FolderBlock = createJsIcon('folder-block', iconData);

export default FolderBlock;

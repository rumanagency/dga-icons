import { createJsIcon } from '../../createJsIcon';
import { TwitterSquare as iconData } from '@dga-icons/core/bulk-rounded';

const TwitterSquare = createJsIcon('twitter-square', iconData);

export default TwitterSquare;

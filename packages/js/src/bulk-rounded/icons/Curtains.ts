import { createJsIcon } from '../../createJsIcon';
import { Curtains as iconData } from '@dga-icons/core/bulk-rounded';

const Curtains = createJsIcon('curtains', iconData);

export default Curtains;

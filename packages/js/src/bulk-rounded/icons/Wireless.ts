import { createJsIcon } from '../../createJsIcon';
import { Wireless as iconData } from '@dga-icons/core/bulk-rounded';

const Wireless = createJsIcon('wireless', iconData);

export default Wireless;

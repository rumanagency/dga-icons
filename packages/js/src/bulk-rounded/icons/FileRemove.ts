import { createJsIcon } from '../../createJsIcon';
import { FileRemove as iconData } from '@dga-icons/core/bulk-rounded';

const FileRemove = createJsIcon('file-remove', iconData);

export default FileRemove;

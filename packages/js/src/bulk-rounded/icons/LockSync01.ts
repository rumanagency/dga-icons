import { createJsIcon } from '../../createJsIcon';
import { LockSync01 as iconData } from '@dga-icons/core/bulk-rounded';

const LockSync01 = createJsIcon('lock-sync-01', iconData);

export default LockSync01;

import { createJsIcon } from '../../createJsIcon';
import { TextSelection as iconData } from '@dga-icons/core/bulk-rounded';

const TextSelection = createJsIcon('text-selection', iconData);

export default TextSelection;

import { createJsIcon } from '../../createJsIcon';
import { FileMusic as iconData } from '@dga-icons/core/bulk-rounded';

const FileMusic = createJsIcon('file-music', iconData);

export default FileMusic;

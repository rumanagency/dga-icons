import { createJsIcon } from '../../createJsIcon';
import { SlidersVertical as iconData } from '@dga-icons/core/bulk-rounded';

const SlidersVertical = createJsIcon('sliders-vertical', iconData);

export default SlidersVertical;

import { createJsIcon } from '../../createJsIcon';
import { FolderVideo as iconData } from '@dga-icons/core/bulk-rounded';

const FolderVideo = createJsIcon('folder-video', iconData);

export default FolderVideo;

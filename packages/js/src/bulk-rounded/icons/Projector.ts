import { createJsIcon } from '../../createJsIcon';
import { Projector as iconData } from '@dga-icons/core/bulk-rounded';

const Projector = createJsIcon('projector', iconData);

export default Projector;

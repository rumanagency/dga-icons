import { createJsIcon } from '../../createJsIcon';
import { PlayList as iconData } from '@dga-icons/core/bulk-rounded';

const PlayList = createJsIcon('play-list', iconData);

export default PlayList;

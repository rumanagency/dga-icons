import { createJsIcon } from '../../createJsIcon';
import { FileExport as iconData } from '@dga-icons/core/bulk-rounded';

const FileExport = createJsIcon('file-export', iconData);

export default FileExport;

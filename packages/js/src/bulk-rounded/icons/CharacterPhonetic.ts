import { createJsIcon } from '../../createJsIcon';
import { CharacterPhonetic as iconData } from '@dga-icons/core/bulk-rounded';

const CharacterPhonetic = createJsIcon('character-phonetic', iconData);

export default CharacterPhonetic;

import { createJsIcon } from '../../createJsIcon';
import { SpeechToText as iconData } from '@dga-icons/core/bulk-rounded';

const SpeechToText = createJsIcon('speech-to-text', iconData);

export default SpeechToText;

import { createJsIcon } from '../../createJsIcon';
import { Marketing as iconData } from '@dga-icons/core/bulk-rounded';

const Marketing = createJsIcon('marketing', iconData);

export default Marketing;

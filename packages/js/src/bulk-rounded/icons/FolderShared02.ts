import { createJsIcon } from '../../createJsIcon';
import { FolderShared02 as iconData } from '@dga-icons/core/bulk-rounded';

const FolderShared02 = createJsIcon('folder-shared-02', iconData);

export default FolderShared02;

import { createJsIcon } from '../../createJsIcon';
import { LaptopIssue as iconData } from '@dga-icons/core/bulk-rounded';

const LaptopIssue = createJsIcon('laptop-issue', iconData);

export default LaptopIssue;

import { createJsIcon } from '../../createJsIcon';
import { PathfinderExclude as iconData } from '@dga-icons/core/bulk-rounded';

const PathfinderExclude = createJsIcon('pathfinder-exclude', iconData);

export default PathfinderExclude;

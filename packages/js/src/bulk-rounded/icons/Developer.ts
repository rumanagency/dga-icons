import { createJsIcon } from '../../createJsIcon';
import { Developer as iconData } from '@dga-icons/core/bulk-rounded';

const Developer = createJsIcon('developer', iconData);

export default Developer;

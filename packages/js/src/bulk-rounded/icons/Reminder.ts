import { createJsIcon } from '../../createJsIcon';
import { Reminder as iconData } from '@dga-icons/core/bulk-rounded';

const Reminder = createJsIcon('reminder', iconData);

export default Reminder;

import { createJsIcon } from '../../createJsIcon';
import { ScratchCard as iconData } from '@dga-icons/core/bulk-rounded';

const ScratchCard = createJsIcon('scratch-card', iconData);

export default ScratchCard;

import { createJsIcon } from '../../createJsIcon';
import { ArrowDataTransferHorizontal as iconData } from '@dga-icons/core/bulk-rounded';

const ArrowDataTransferHorizontal = createJsIcon('arrow-data-transfer-horizontal', iconData);

export default ArrowDataTransferHorizontal;

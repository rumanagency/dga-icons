import { createJsIcon } from '../../createJsIcon';
import { SecurityCheck as iconData } from '@dga-icons/core/bulk-rounded';

const SecurityCheck = createJsIcon('security-check', iconData);

export default SecurityCheck;

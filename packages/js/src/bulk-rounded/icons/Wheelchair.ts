import { createJsIcon } from '../../createJsIcon';
import { Wheelchair as iconData } from '@dga-icons/core/bulk-rounded';

const Wheelchair = createJsIcon('wheelchair', iconData);

export default Wheelchair;

import { createJsIcon } from '../../createJsIcon';
import { ImageDownload as iconData } from '@dga-icons/core/bulk-rounded';

const ImageDownload = createJsIcon('image-download', iconData);

export default ImageDownload;

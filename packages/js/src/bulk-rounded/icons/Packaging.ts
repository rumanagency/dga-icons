import { createJsIcon } from '../../createJsIcon';
import { Packaging as iconData } from '@dga-icons/core/bulk-rounded';

const Packaging = createJsIcon('packaging', iconData);

export default Packaging;

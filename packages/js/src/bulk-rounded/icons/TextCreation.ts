import { createJsIcon } from '../../createJsIcon';
import { TextCreation as iconData } from '@dga-icons/core/bulk-rounded';

const TextCreation = createJsIcon('text-creation', iconData);

export default TextCreation;

import { createJsIcon } from '../../createJsIcon';
import { LookRight as iconData } from '@dga-icons/core/bulk-rounded';

const LookRight = createJsIcon('look-right', iconData);

export default LookRight;

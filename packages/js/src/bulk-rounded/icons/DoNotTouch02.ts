import { createJsIcon } from '../../createJsIcon';
import { DoNotTouch02 as iconData } from '@dga-icons/core/bulk-rounded';

const DoNotTouch02 = createJsIcon('do-not-touch-02', iconData);

export default DoNotTouch02;

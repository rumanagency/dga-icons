import { createJsIcon } from '../../createJsIcon';
import { TouchLocked04 as iconData } from '@dga-icons/core/bulk-rounded';

const TouchLocked04 = createJsIcon('touch-locked-04', iconData);

export default TouchLocked04;

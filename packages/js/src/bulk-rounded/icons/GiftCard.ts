import { createJsIcon } from '../../createJsIcon';
import { GiftCard as iconData } from '@dga-icons/core/bulk-rounded';

const GiftCard = createJsIcon('gift-card', iconData);

export default GiftCard;

import { createJsIcon } from '../../createJsIcon';
import { PackageMoving as iconData } from '@dga-icons/core/bulk-rounded';

const PackageMoving = createJsIcon('package-moving', iconData);

export default PackageMoving;

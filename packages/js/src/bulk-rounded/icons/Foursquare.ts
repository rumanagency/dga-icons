import { createJsIcon } from '../../createJsIcon';
import { Foursquare as iconData } from '@dga-icons/core/bulk-rounded';

const Foursquare = createJsIcon('foursquare', iconData);

export default Foursquare;

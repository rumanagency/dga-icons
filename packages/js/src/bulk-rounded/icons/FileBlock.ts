import { createJsIcon } from '../../createJsIcon';
import { FileBlock as iconData } from '@dga-icons/core/bulk-rounded';

const FileBlock = createJsIcon('file-block', iconData);

export default FileBlock;

import { createJsIcon } from '../../createJsIcon';
import { WifiDisconnected01 as iconData } from '@dga-icons/core/bulk-rounded';

const WifiDisconnected01 = createJsIcon('wifi-disconnected-01', iconData);

export default WifiDisconnected01;

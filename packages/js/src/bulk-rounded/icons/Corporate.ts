import { createJsIcon } from '../../createJsIcon';
import { Corporate as iconData } from '@dga-icons/core/bulk-rounded';

const Corporate = createJsIcon('corporate', iconData);

export default Corporate;

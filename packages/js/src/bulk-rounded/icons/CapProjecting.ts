import { createJsIcon } from '../../createJsIcon';
import { CapProjecting as iconData } from '@dga-icons/core/bulk-rounded';

const CapProjecting = createJsIcon('cap-projecting', iconData);

export default CapProjecting;

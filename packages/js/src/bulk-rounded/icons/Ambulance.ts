import { createJsIcon } from '../../createJsIcon';
import { Ambulance as iconData } from '@dga-icons/core/bulk-rounded';

const Ambulance = createJsIcon('ambulance', iconData);

export default Ambulance;

import { createJsIcon } from '../../createJsIcon';
import { MenuCollapse as iconData } from '@dga-icons/core/bulk-rounded';

const MenuCollapse = createJsIcon('menu-collapse', iconData);

export default MenuCollapse;

import { createJsIcon } from '../../createJsIcon';
import { Router02 as iconData } from '@dga-icons/core/bulk-rounded';

const Router02 = createJsIcon('router-02', iconData);

export default Router02;

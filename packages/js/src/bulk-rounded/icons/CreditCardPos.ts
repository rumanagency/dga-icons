import { createJsIcon } from '../../createJsIcon';
import { CreditCardPos as iconData } from '@dga-icons/core/bulk-rounded';

const CreditCardPos = createJsIcon('credit-card-pos', iconData);

export default CreditCardPos;

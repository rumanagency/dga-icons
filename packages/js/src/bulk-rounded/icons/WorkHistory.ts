import { createJsIcon } from '../../createJsIcon';
import { WorkHistory as iconData } from '@dga-icons/core/bulk-rounded';

const WorkHistory = createJsIcon('work-history', iconData);

export default WorkHistory;

import { createJsIcon } from '../../createJsIcon';
import { Students as iconData } from '@dga-icons/core/bulk-rounded';

const Students = createJsIcon('students', iconData);

export default Students;

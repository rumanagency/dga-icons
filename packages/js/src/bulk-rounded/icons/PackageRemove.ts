import { createJsIcon } from '../../createJsIcon';
import { PackageRemove as iconData } from '@dga-icons/core/bulk-rounded';

const PackageRemove = createJsIcon('package-remove', iconData);

export default PackageRemove;

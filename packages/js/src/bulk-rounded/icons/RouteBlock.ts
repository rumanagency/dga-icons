import { createJsIcon } from '../../createJsIcon';
import { RouteBlock as iconData } from '@dga-icons/core/bulk-rounded';

const RouteBlock = createJsIcon('route-block', iconData);

export default RouteBlock;

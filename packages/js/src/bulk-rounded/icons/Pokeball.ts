import { createJsIcon } from '../../createJsIcon';
import { Pokeball as iconData } from '@dga-icons/core/bulk-rounded';

const Pokeball = createJsIcon('pokeball', iconData);

export default Pokeball;

import { createJsIcon } from '../../createJsIcon';
import { Database as iconData } from '@dga-icons/core/bulk-rounded';

const Database = createJsIcon('database', iconData);

export default Database;

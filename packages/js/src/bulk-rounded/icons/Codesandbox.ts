import { createJsIcon } from '../../createJsIcon';
import { Codesandbox as iconData } from '@dga-icons/core/bulk-rounded';

const Codesandbox = createJsIcon('codesandbox', iconData);

export default Codesandbox;

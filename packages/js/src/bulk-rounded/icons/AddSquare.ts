import { createJsIcon } from '../../createJsIcon';
import { AddSquare as iconData } from '@dga-icons/core/bulk-rounded';

const AddSquare = createJsIcon('add-square', iconData);

export default AddSquare;

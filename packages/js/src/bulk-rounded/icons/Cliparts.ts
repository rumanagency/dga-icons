import { createJsIcon } from '../../createJsIcon';
import { Cliparts as iconData } from '@dga-icons/core/bulk-rounded';

const Cliparts = createJsIcon('cliparts', iconData);

export default Cliparts;

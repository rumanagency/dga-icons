import { createJsIcon } from '../../createJsIcon';
import { LeftAngle as iconData } from '@dga-icons/core/bulk-rounded';

const LeftAngle = createJsIcon('left-angle', iconData);

export default LeftAngle;

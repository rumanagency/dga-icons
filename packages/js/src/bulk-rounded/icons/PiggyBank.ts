import { createJsIcon } from '../../createJsIcon';
import { PiggyBank as iconData } from '@dga-icons/core/bulk-rounded';

const PiggyBank = createJsIcon('piggy-bank', iconData);

export default PiggyBank;

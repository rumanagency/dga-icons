import { createJsIcon } from '../../createJsIcon';
import { Notification01 as iconData } from '@dga-icons/core/bulk-rounded';

const Notification01 = createJsIcon('notification-01', iconData);

export default Notification01;

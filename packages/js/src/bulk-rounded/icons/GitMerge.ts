import { createJsIcon } from '../../createJsIcon';
import { GitMerge as iconData } from '@dga-icons/core/bulk-rounded';

const GitMerge = createJsIcon('git-merge', iconData);

export default GitMerge;

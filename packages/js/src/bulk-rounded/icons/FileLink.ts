import { createJsIcon } from '../../createJsIcon';
import { FileLink as iconData } from '@dga-icons/core/bulk-rounded';

const FileLink = createJsIcon('file-link', iconData);

export default FileLink;

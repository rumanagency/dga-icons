import { createJsIcon } from '../../createJsIcon';
import { DentalTooth as iconData } from '@dga-icons/core/bulk-rounded';

const DentalTooth = createJsIcon('dental-tooth', iconData);

export default DentalTooth;

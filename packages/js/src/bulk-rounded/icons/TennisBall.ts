import { createJsIcon } from '../../createJsIcon';
import { TennisBall as iconData } from '@dga-icons/core/bulk-rounded';

const TennisBall = createJsIcon('tennis-ball', iconData);

export default TennisBall;

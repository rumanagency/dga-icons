import { createJsIcon } from '../../createJsIcon';
import { ScrollVertical as iconData } from '@dga-icons/core/bulk-rounded';

const ScrollVertical = createJsIcon('scroll-vertical', iconData);

export default ScrollVertical;

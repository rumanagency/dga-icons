import { createJsIcon } from '../../createJsIcon';
import { FlipVertical as iconData } from '@dga-icons/core/bulk-rounded';

const FlipVertical = createJsIcon('flip-vertical', iconData);

export default FlipVertical;

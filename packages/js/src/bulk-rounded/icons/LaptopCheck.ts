import { createJsIcon } from '../../createJsIcon';
import { LaptopCheck as iconData } from '@dga-icons/core/bulk-rounded';

const LaptopCheck = createJsIcon('laptop-check', iconData);

export default LaptopCheck;

import { createJsIcon } from '../../createJsIcon';
import { EditTable as iconData } from '@dga-icons/core/bulk-rounded';

const EditTable = createJsIcon('edit-table', iconData);

export default EditTable;

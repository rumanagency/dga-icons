import { createJsIcon } from '../../createJsIcon';
import { BitcoinKey as iconData } from '@dga-icons/core/bulk-rounded';

const BitcoinKey = createJsIcon('bitcoin-key', iconData);

export default BitcoinKey;

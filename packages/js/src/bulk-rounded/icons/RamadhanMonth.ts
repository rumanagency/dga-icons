import { createJsIcon } from '../../createJsIcon';
import { RamadhanMonth as iconData } from '@dga-icons/core/bulk-rounded';

const RamadhanMonth = createJsIcon('ramadhan-month', iconData);

export default RamadhanMonth;

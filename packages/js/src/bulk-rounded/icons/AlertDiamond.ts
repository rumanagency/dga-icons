import { createJsIcon } from '../../createJsIcon';
import { AlertDiamond as iconData } from '@dga-icons/core/bulk-rounded';

const AlertDiamond = createJsIcon('alert-diamond', iconData);

export default AlertDiamond;

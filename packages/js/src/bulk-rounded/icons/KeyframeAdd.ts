import { createJsIcon } from '../../createJsIcon';
import { KeyframeAdd as iconData } from '@dga-icons/core/bulk-rounded';

const KeyframeAdd = createJsIcon('keyframe-add', iconData);

export default KeyframeAdd;

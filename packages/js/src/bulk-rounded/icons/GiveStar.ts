import { createJsIcon } from '../../createJsIcon';
import { GiveStar as iconData } from '@dga-icons/core/bulk-rounded';

const GiveStar = createJsIcon('give-star', iconData);

export default GiveStar;

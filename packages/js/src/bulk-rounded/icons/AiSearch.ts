import { createJsIcon } from '../../createJsIcon';
import { AiSearch as iconData } from '@dga-icons/core/bulk-rounded';

const AiSearch = createJsIcon('ai-search', iconData);

export default AiSearch;

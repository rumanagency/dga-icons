import { createJsIcon } from '../../createJsIcon';
import { FolderPin as iconData } from '@dga-icons/core/bulk-rounded';

const FolderPin = createJsIcon('folder-pin', iconData);

export default FolderPin;

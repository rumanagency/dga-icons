import { createJsIcon } from '../../createJsIcon';
import { SourceCode as iconData } from '@dga-icons/core/bulk-rounded';

const SourceCode = createJsIcon('source-code', iconData);

export default SourceCode;

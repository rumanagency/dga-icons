import { createJsIcon } from '../../createJsIcon';
import { SolarPower as iconData } from '@dga-icons/core/bulk-rounded';

const SolarPower = createJsIcon('solar-power', iconData);

export default SolarPower;

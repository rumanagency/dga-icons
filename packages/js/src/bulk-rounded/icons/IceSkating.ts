import { createJsIcon } from '../../createJsIcon';
import { IceSkating as iconData } from '@dga-icons/core/bulk-rounded';

const IceSkating = createJsIcon('ice-skating', iconData);

export default IceSkating;

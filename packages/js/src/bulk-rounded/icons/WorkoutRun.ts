import { createJsIcon } from '../../createJsIcon';
import { WorkoutRun as iconData } from '@dga-icons/core/bulk-rounded';

const WorkoutRun = createJsIcon('workout-run', iconData);

export default WorkoutRun;

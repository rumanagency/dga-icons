import { createJsIcon } from '../../createJsIcon';
import { Baseball as iconData } from '@dga-icons/core/bulk-rounded';

const Baseball = createJsIcon('baseball', iconData);

export default Baseball;

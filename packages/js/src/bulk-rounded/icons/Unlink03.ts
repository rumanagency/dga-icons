import { createJsIcon } from '../../createJsIcon';
import { Unlink03 as iconData } from '@dga-icons/core/bulk-rounded';

const Unlink03 = createJsIcon('unlink-03', iconData);

export default Unlink03;

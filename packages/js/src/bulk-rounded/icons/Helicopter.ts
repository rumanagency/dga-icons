import { createJsIcon } from '../../createJsIcon';
import { Helicopter as iconData } from '@dga-icons/core/bulk-rounded';

const Helicopter = createJsIcon('helicopter', iconData);

export default Helicopter;

import { createJsIcon } from '../../createJsIcon';
import { AlignSelection as iconData } from '@dga-icons/core/bulk-rounded';

const AlignSelection = createJsIcon('align-selection', iconData);

export default AlignSelection;

import { createJsIcon } from '../../createJsIcon';
import { Register as iconData } from '@dga-icons/core/bulk-rounded';

const Register = createJsIcon('register', iconData);

export default Register;

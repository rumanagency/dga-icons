import { createJsIcon } from '../../createJsIcon';
import { FilePaste as iconData } from '@dga-icons/core/bulk-rounded';

const FilePaste = createJsIcon('file-paste', iconData);

export default FilePaste;

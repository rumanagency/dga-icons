import { createJsIcon } from '../../createJsIcon';
import { CheckmarkBadge04 as iconData } from '@dga-icons/core/bulk-rounded';

const CheckmarkBadge04 = createJsIcon('checkmark-badge-04', iconData);

export default CheckmarkBadge04;

import { createJsIcon } from '../../createJsIcon';
import { Structure01 as iconData } from '@dga-icons/core/bulk-rounded';

const Structure01 = createJsIcon('structure-01', iconData);

export default Structure01;

import { createJsIcon } from '../../createJsIcon';
import { CoinsPound as iconData } from '@dga-icons/core/bulk-rounded';

const CoinsPound = createJsIcon('coins-pound', iconData);

export default CoinsPound;

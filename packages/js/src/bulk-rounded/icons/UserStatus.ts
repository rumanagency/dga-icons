import { createJsIcon } from '../../createJsIcon';
import { UserStatus as iconData } from '@dga-icons/core/bulk-rounded';

const UserStatus = createJsIcon('user-status', iconData);

export default UserStatus;

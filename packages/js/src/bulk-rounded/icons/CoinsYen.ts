import { createJsIcon } from '../../createJsIcon';
import { CoinsYen as iconData } from '@dga-icons/core/bulk-rounded';

const CoinsYen = createJsIcon('coins-yen', iconData);

export default CoinsYen;

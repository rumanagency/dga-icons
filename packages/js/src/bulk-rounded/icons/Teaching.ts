import { createJsIcon } from '../../createJsIcon';
import { Teaching as iconData } from '@dga-icons/core/bulk-rounded';

const Teaching = createJsIcon('teaching', iconData);

export default Teaching;

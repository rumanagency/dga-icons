import { createJsIcon } from '../../createJsIcon';
import { AlignRight as iconData } from '@dga-icons/core/bulk-rounded';

const AlignRight = createJsIcon('align-right', iconData);

export default AlignRight;

import { createJsIcon } from '../../createJsIcon';
import { WorkoutBattleRopes as iconData } from '@dga-icons/core/bulk-rounded';

const WorkoutBattleRopes = createJsIcon('workout-battle-ropes', iconData);

export default WorkoutBattleRopes;

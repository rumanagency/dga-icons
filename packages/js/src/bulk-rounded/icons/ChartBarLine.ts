import { createJsIcon } from '../../createJsIcon';
import { ChartBarLine as iconData } from '@dga-icons/core/bulk-rounded';

const ChartBarLine = createJsIcon('chart-bar-line', iconData);

export default ChartBarLine;

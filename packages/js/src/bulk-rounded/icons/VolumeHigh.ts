import { createJsIcon } from '../../createJsIcon';
import { VolumeHigh as iconData } from '@dga-icons/core/bulk-rounded';

const VolumeHigh = createJsIcon('volume-high', iconData);

export default VolumeHigh;

import { createJsIcon } from '../../createJsIcon';
import { Rotate360 as iconData } from '@dga-icons/core/bulk-rounded';

const Rotate360 = createJsIcon('rotate-360', iconData);

export default Rotate360;

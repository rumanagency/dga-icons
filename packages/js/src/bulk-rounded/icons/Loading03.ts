import { createJsIcon } from '../../createJsIcon';
import { Loading03 as iconData } from '@dga-icons/core/bulk-rounded';

const Loading03 = createJsIcon('loading-03', iconData);

export default Loading03;

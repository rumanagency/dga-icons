import { createJsIcon } from '../../createJsIcon';
import { SquareLock01 as iconData } from '@dga-icons/core/bulk-rounded';

const SquareLock01 = createJsIcon('square-lock-01', iconData);

export default SquareLock01;

import { createJsIcon } from '../../createJsIcon';
import { FileView as iconData } from '@dga-icons/core/bulk-rounded';

const FileView = createJsIcon('file-view', iconData);

export default FileView;

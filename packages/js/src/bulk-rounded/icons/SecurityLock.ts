import { createJsIcon } from '../../createJsIcon';
import { SecurityLock as iconData } from '@dga-icons/core/bulk-rounded';

const SecurityLock = createJsIcon('security-lock', iconData);

export default SecurityLock;

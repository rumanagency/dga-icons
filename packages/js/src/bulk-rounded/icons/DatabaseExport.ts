import { createJsIcon } from '../../createJsIcon';
import { DatabaseExport as iconData } from '@dga-icons/core/bulk-rounded';

const DatabaseExport = createJsIcon('database-export', iconData);

export default DatabaseExport;

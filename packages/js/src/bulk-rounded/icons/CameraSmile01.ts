import { createJsIcon } from '../../createJsIcon';
import { CameraSmile01 as iconData } from '@dga-icons/core/bulk-rounded';

const CameraSmile01 = createJsIcon('camera-smile-01', iconData);

export default CameraSmile01;

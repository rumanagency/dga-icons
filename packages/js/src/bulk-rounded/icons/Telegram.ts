import { createJsIcon } from '../../createJsIcon';
import { Telegram as iconData } from '@dga-icons/core/bulk-rounded';

const Telegram = createJsIcon('telegram', iconData);

export default Telegram;

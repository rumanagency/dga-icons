import { createJsIcon } from '../../createJsIcon';
import { StraightEdge as iconData } from '@dga-icons/core/bulk-rounded';

const StraightEdge = createJsIcon('straight-edge', iconData);

export default StraightEdge;

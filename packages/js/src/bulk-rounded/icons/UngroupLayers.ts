import { createJsIcon } from '../../createJsIcon';
import { UngroupLayers as iconData } from '@dga-icons/core/bulk-rounded';

const UngroupLayers = createJsIcon('ungroup-layers', iconData);

export default UngroupLayers;

import { createJsIcon } from '../../createJsIcon';
import { Subtitle as iconData } from '@dga-icons/core/bulk-rounded';

const Subtitle = createJsIcon('subtitle', iconData);

export default Subtitle;

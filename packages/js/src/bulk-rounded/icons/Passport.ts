import { createJsIcon } from '../../createJsIcon';
import { Passport as iconData } from '@dga-icons/core/bulk-rounded';

const Passport = createJsIcon('passport', iconData);

export default Passport;

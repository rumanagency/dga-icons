import { createJsIcon } from '../../createJsIcon';
import { Artboard as iconData } from '@dga-icons/core/bulk-rounded';

const Artboard = createJsIcon('artboard', iconData);

export default Artboard;

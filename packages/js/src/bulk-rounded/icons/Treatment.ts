import { createJsIcon } from '../../createJsIcon';
import { Treatment as iconData } from '@dga-icons/core/bulk-rounded';

const Treatment = createJsIcon('treatment', iconData);

export default Treatment;

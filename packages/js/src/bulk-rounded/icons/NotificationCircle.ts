import { createJsIcon } from '../../createJsIcon';
import { NotificationCircle as iconData } from '@dga-icons/core/bulk-rounded';

const NotificationCircle = createJsIcon('notification-circle', iconData);

export default NotificationCircle;

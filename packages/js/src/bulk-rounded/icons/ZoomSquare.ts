import { createJsIcon } from '../../createJsIcon';
import { ZoomSquare as iconData } from '@dga-icons/core/bulk-rounded';

const ZoomSquare = createJsIcon('zoom-square', iconData);

export default ZoomSquare;

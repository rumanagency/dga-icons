import { createJsIcon } from '../../createJsIcon';
import { Compass01 as iconData } from '@dga-icons/core/bulk-rounded';

const Compass01 = createJsIcon('compass-01', iconData);

export default Compass01;

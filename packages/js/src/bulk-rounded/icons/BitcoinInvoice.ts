import { createJsIcon } from '../../createJsIcon';
import { BitcoinInvoice as iconData } from '@dga-icons/core/bulk-rounded';

const BitcoinInvoice = createJsIcon('bitcoin-invoice', iconData);

export default BitcoinInvoice;

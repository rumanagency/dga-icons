import { createJsIcon } from '../../createJsIcon';
import { Folder01 as iconData } from '@dga-icons/core/bulk-rounded';

const Folder01 = createJsIcon('folder-01', iconData);

export default Folder01;

import { createJsIcon } from '../../createJsIcon';
import { BorderInner as iconData } from '@dga-icons/core/bulk-rounded';

const BorderInner = createJsIcon('border-inner', iconData);

export default BorderInner;

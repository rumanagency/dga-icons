import { createJsIcon } from '../../createJsIcon';
import { ChessPawn as iconData } from '@dga-icons/core/bulk-rounded';

const ChessPawn = createJsIcon('chess-pawn', iconData);

export default ChessPawn;

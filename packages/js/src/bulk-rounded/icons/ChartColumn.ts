import { createJsIcon } from '../../createJsIcon';
import { ChartColumn as iconData } from '@dga-icons/core/bulk-rounded';

const ChartColumn = createJsIcon('chart-column', iconData);

export default ChartColumn;

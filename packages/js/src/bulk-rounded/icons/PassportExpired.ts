import { createJsIcon } from '../../createJsIcon';
import { PassportExpired as iconData } from '@dga-icons/core/bulk-rounded';

const PassportExpired = createJsIcon('passport-expired', iconData);

export default PassportExpired;

import { createJsIcon } from '../../createJsIcon';
import { Favourite as iconData } from '@dga-icons/core/bulk-rounded';

const Favourite = createJsIcon('favourite', iconData);

export default Favourite;

import { createJsIcon } from '../../createJsIcon';
import { ImageComposition as iconData } from '@dga-icons/core/bulk-rounded';

const ImageComposition = createJsIcon('image-composition', iconData);

export default ImageComposition;

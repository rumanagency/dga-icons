import { createJsIcon } from '../../createJsIcon';
import { Profile02 as iconData } from '@dga-icons/core/bulk-rounded';

const Profile02 = createJsIcon('profile-02', iconData);

export default Profile02;

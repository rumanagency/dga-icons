import { createJsIcon } from '../../createJsIcon';
import { GitCompare as iconData } from '@dga-icons/core/bulk-rounded';

const GitCompare = createJsIcon('git-compare', iconData);

export default GitCompare;

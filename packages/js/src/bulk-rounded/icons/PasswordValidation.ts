import { createJsIcon } from '../../createJsIcon';
import { PasswordValidation as iconData } from '@dga-icons/core/bulk-rounded';

const PasswordValidation = createJsIcon('password-validation', iconData);

export default PasswordValidation;

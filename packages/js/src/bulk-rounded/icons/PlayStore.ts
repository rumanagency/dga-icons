import { createJsIcon } from '../../createJsIcon';
import { PlayStore as iconData } from '@dga-icons/core/bulk-rounded';

const PlayStore = createJsIcon('play-store', iconData);

export default PlayStore;

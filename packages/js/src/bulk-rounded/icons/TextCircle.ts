import { createJsIcon } from '../../createJsIcon';
import { TextCircle as iconData } from '@dga-icons/core/bulk-rounded';

const TextCircle = createJsIcon('text-circle', iconData);

export default TextCircle;

import { createJsIcon } from '../../createJsIcon';
import { TextWrap as iconData } from '@dga-icons/core/bulk-rounded';

const TextWrap = createJsIcon('text-wrap', iconData);

export default TextWrap;

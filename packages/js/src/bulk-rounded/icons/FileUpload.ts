import { createJsIcon } from '../../createJsIcon';
import { FileUpload as iconData } from '@dga-icons/core/bulk-rounded';

const FileUpload = createJsIcon('file-upload', iconData);

export default FileUpload;

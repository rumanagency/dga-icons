import { createJsIcon } from '../../createJsIcon';
import { SemiTruck as iconData } from '@dga-icons/core/bulk-rounded';

const SemiTruck = createJsIcon('semi-truck', iconData);

export default SemiTruck;

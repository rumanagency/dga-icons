import { createJsIcon } from '../../createJsIcon';
import { PenToolAdd as iconData } from '@dga-icons/core/bulk-rounded';

const PenToolAdd = createJsIcon('pen-tool-add', iconData);

export default PenToolAdd;

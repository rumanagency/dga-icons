import { createJsIcon } from '../../createJsIcon';
import { PlusSignCircle as iconData } from '@dga-icons/core/bulk-rounded';

const PlusSignCircle = createJsIcon('plus-sign-circle', iconData);

export default PlusSignCircle;

import { createJsIcon } from '../../createJsIcon';
import { University as iconData } from '@dga-icons/core/bulk-rounded';

const University = createJsIcon('university', iconData);

export default University;

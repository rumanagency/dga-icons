import { createJsIcon } from '../../createJsIcon';
import { Accident as iconData } from '@dga-icons/core/bulk-rounded';

const Accident = createJsIcon('accident', iconData);

export default Accident;

import { createJsIcon } from '../../createJsIcon';
import { GitBranch as iconData } from '@dga-icons/core/bulk-rounded';

const GitBranch = createJsIcon('git-branch', iconData);

export default GitBranch;

import { createJsIcon } from '../../createJsIcon';
import { PoolTable as iconData } from '@dga-icons/core/bulk-rounded';

const PoolTable = createJsIcon('pool-table', iconData);

export default PoolTable;

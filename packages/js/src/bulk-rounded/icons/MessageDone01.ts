import { createJsIcon } from '../../createJsIcon';
import { MessageDone01 as iconData } from '@dga-icons/core/bulk-rounded';

const MessageDone01 = createJsIcon('message-done-01', iconData);

export default MessageDone01;

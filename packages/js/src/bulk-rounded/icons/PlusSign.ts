import { createJsIcon } from '../../createJsIcon';
import { PlusSign as iconData } from '@dga-icons/core/bulk-rounded';

const PlusSign = createJsIcon('plus-sign', iconData);

export default PlusSign;

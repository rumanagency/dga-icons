import { createJsIcon } from '../../createJsIcon';
import { Humidity as iconData } from '@dga-icons/core/bulk-rounded';

const Humidity = createJsIcon('humidity', iconData);

export default Humidity;

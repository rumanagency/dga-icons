import { createJsIcon } from '../../createJsIcon';
import { AiSmartwatch as iconData } from '@dga-icons/core/bulk-rounded';

const AiSmartwatch = createJsIcon('ai-smartwatch', iconData);

export default AiSmartwatch;

import { createJsIcon } from '../../createJsIcon';
import { ImageUpload as iconData } from '@dga-icons/core/bulk-rounded';

const ImageUpload = createJsIcon('image-upload', iconData);

export default ImageUpload;

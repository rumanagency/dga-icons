import { createJsIcon } from '../../createJsIcon';
import { Catalogue as iconData } from '@dga-icons/core/bulk-rounded';

const Catalogue = createJsIcon('catalogue', iconData);

export default Catalogue;

import { createJsIcon } from '../../createJsIcon';
import { Layout04 as iconData } from '@dga-icons/core/bulk-rounded';

const Layout04 = createJsIcon('layout-04', iconData);

export default Layout04;

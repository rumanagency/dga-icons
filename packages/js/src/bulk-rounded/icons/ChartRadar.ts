import { createJsIcon } from '../../createJsIcon';
import { ChartRadar as iconData } from '@dga-icons/core/bulk-rounded';

const ChartRadar = createJsIcon('chart-radar', iconData);

export default ChartRadar;

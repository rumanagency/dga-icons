import { createJsIcon } from '../../createJsIcon';
import { LaptopAdd as iconData } from '@dga-icons/core/bulk-rounded';

const LaptopAdd = createJsIcon('laptop-add', iconData);

export default LaptopAdd;

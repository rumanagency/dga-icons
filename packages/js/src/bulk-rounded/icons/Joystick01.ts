import { createJsIcon } from '../../createJsIcon';
import { Joystick01 as iconData } from '@dga-icons/core/bulk-rounded';

const Joystick01 = createJsIcon('joystick-01', iconData);

export default Joystick01;

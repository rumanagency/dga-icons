import { createJsIcon } from '../../createJsIcon';
import { Configuration02 as iconData } from '@dga-icons/core/bulk-rounded';

const Configuration02 = createJsIcon('configuration-02', iconData);

export default Configuration02;

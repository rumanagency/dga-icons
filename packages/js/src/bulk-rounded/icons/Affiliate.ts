import { createJsIcon } from '../../createJsIcon';
import { Affiliate as iconData } from '@dga-icons/core/bulk-rounded';

const Affiliate = createJsIcon('affiliate', iconData);

export default Affiliate;

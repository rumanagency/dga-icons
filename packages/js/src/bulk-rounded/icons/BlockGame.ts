import { createJsIcon } from '../../createJsIcon';
import { BlockGame as iconData } from '@dga-icons/core/bulk-rounded';

const BlockGame = createJsIcon('block-game', iconData);

export default BlockGame;

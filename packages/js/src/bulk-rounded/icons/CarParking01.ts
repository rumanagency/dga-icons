import { createJsIcon } from '../../createJsIcon';
import { CarParking01 as iconData } from '@dga-icons/core/bulk-rounded';

const CarParking01 = createJsIcon('car-parking-01', iconData);

export default CarParking01;

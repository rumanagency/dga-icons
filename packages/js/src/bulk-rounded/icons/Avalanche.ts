import { createJsIcon } from '../../createJsIcon';
import { Avalanche as iconData } from '@dga-icons/core/bulk-rounded';

const Avalanche = createJsIcon('avalanche', iconData);

export default Avalanche;

import { createJsIcon } from '../../createJsIcon';
import { CameraAutomatically01 as iconData } from '@dga-icons/core/bulk-rounded';

const CameraAutomatically01 = createJsIcon('camera-automatically-01', iconData);

export default CameraAutomatically01;

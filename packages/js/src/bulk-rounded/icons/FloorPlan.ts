import { createJsIcon } from '../../createJsIcon';
import { FloorPlan as iconData } from '@dga-icons/core/bulk-rounded';

const FloorPlan = createJsIcon('floor-plan', iconData);

export default FloorPlan;

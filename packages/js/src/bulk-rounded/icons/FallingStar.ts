import { createJsIcon } from '../../createJsIcon';
import { FallingStar as iconData } from '@dga-icons/core/bulk-rounded';

const FallingStar = createJsIcon('falling-star', iconData);

export default FallingStar;

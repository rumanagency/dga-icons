import { createJsIcon } from '../../createJsIcon';
import { CheckmarkBadge03 as iconData } from '@dga-icons/core/bulk-rounded';

const CheckmarkBadge03 = createJsIcon('checkmark-badge-03', iconData);

export default CheckmarkBadge03;

import { createJsIcon } from '../../createJsIcon';
import { BloodType as iconData } from '@dga-icons/core/bulk-rounded';

const BloodType = createJsIcon('blood-type', iconData);

export default BloodType;

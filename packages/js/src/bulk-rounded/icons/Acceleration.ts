import { createJsIcon } from '../../createJsIcon';
import { Acceleration as iconData } from '@dga-icons/core/bulk-rounded';

const Acceleration = createJsIcon('acceleration', iconData);

export default Acceleration;

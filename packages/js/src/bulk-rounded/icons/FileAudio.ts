import { createJsIcon } from '../../createJsIcon';
import { FileAudio as iconData } from '@dga-icons/core/bulk-rounded';

const FileAudio = createJsIcon('file-audio', iconData);

export default FileAudio;

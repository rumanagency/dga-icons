import { createJsIcon } from '../../createJsIcon';
import { PreferenceVertical as iconData } from '@dga-icons/core/stroke-standard';

const PreferenceVertical = createJsIcon('preference-vertical', iconData);

export default PreferenceVertical;

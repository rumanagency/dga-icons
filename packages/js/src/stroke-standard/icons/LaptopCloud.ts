import { createJsIcon } from '../../createJsIcon';
import { LaptopCloud as iconData } from '@dga-icons/core/stroke-standard';

const LaptopCloud = createJsIcon('laptop-cloud', iconData);

export default LaptopCloud;

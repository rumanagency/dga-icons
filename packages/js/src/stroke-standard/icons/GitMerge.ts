import { createJsIcon } from '../../createJsIcon';
import { GitMerge as iconData } from '@dga-icons/core/stroke-standard';

const GitMerge = createJsIcon('git-merge', iconData);

export default GitMerge;

import { createJsIcon } from '../../createJsIcon';
import { CallPaused as iconData } from '@dga-icons/core/stroke-standard';

const CallPaused = createJsIcon('call-paused', iconData);

export default CallPaused;

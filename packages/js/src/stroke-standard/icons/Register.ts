import { createJsIcon } from '../../createJsIcon';
import { Register as iconData } from '@dga-icons/core/stroke-standard';

const Register = createJsIcon('register', iconData);

export default Register;

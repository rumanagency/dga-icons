import { createJsIcon } from '../../createJsIcon';
import { StopWatch as iconData } from '@dga-icons/core/stroke-standard';

const StopWatch = createJsIcon('stop-watch', iconData);

export default StopWatch;

import { createJsIcon } from '../../createJsIcon';
import { Telegram as iconData } from '@dga-icons/core/stroke-standard';

const Telegram = createJsIcon('telegram', iconData);

export default Telegram;

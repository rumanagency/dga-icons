import { createJsIcon } from '../../createJsIcon';
import { FileFavourite as iconData } from '@dga-icons/core/stroke-standard';

const FileFavourite = createJsIcon('file-favourite', iconData);

export default FileFavourite;

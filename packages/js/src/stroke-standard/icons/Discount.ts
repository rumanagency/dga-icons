import { createJsIcon } from '../../createJsIcon';
import { Discount as iconData } from '@dga-icons/core/stroke-standard';

const Discount = createJsIcon('discount', iconData);

export default Discount;

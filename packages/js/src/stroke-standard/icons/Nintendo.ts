import { createJsIcon } from '../../createJsIcon';
import { Nintendo as iconData } from '@dga-icons/core/stroke-standard';

const Nintendo = createJsIcon('nintendo', iconData);

export default Nintendo;

import { createJsIcon } from '../../createJsIcon';
import { DocumentAttachment as iconData } from '@dga-icons/core/stroke-standard';

const DocumentAttachment = createJsIcon('document-attachment', iconData);

export default DocumentAttachment;

import { createJsIcon } from '../../createJsIcon';
import { NewReleases as iconData } from '@dga-icons/core/stroke-standard';

const NewReleases = createJsIcon('new-releases', iconData);

export default NewReleases;

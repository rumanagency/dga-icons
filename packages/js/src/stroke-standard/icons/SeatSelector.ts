import { createJsIcon } from '../../createJsIcon';
import { SeatSelector as iconData } from '@dga-icons/core/stroke-standard';

const SeatSelector = createJsIcon('seat-selector', iconData);

export default SeatSelector;

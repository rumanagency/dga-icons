import { createJsIcon } from '../../createJsIcon';
import { Triangle as iconData } from '@dga-icons/core/stroke-standard';

const Triangle = createJsIcon('triangle', iconData);

export default Triangle;

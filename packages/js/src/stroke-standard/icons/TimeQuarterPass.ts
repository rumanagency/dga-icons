import { createJsIcon } from '../../createJsIcon';
import { TimeQuarterPass as iconData } from '@dga-icons/core/stroke-standard';

const TimeQuarterPass = createJsIcon('time-quarter-pass', iconData);

export default TimeQuarterPass;

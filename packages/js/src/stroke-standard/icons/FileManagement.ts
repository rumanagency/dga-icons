import { createJsIcon } from '../../createJsIcon';
import { FileManagement as iconData } from '@dga-icons/core/stroke-standard';

const FileManagement = createJsIcon('file-management', iconData);

export default FileManagement;

import { createJsIcon } from '../../createJsIcon';
import { FolderSecurity as iconData } from '@dga-icons/core/stroke-standard';

const FolderSecurity = createJsIcon('folder-security', iconData);

export default FolderSecurity;

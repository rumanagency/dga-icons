import { createJsIcon } from '../../createJsIcon';
import { AppleFinder as iconData } from '@dga-icons/core/stroke-standard';

const AppleFinder = createJsIcon('apple-finder', iconData);

export default AppleFinder;

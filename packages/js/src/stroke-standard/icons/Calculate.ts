import { createJsIcon } from '../../createJsIcon';
import { Calculate as iconData } from '@dga-icons/core/stroke-standard';

const Calculate = createJsIcon('calculate', iconData);

export default Calculate;

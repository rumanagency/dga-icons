import { createJsIcon } from '../../createJsIcon';
import { GasStove as iconData } from '@dga-icons/core/stroke-standard';

const GasStove = createJsIcon('gas-stove', iconData);

export default GasStove;

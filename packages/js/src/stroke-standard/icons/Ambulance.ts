import { createJsIcon } from '../../createJsIcon';
import { Ambulance as iconData } from '@dga-icons/core/stroke-standard';

const Ambulance = createJsIcon('ambulance', iconData);

export default Ambulance;

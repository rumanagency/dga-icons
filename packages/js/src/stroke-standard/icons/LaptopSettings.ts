import { createJsIcon } from '../../createJsIcon';
import { LaptopSettings as iconData } from '@dga-icons/core/stroke-standard';

const LaptopSettings = createJsIcon('laptop-settings', iconData);

export default LaptopSettings;

import { createJsIcon } from '../../createJsIcon';
import { CallUnlocked as iconData } from '@dga-icons/core/stroke-standard';

const CallUnlocked = createJsIcon('call-unlocked', iconData);

export default CallUnlocked;

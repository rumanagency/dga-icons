import { createJsIcon } from '../../createJsIcon';
import { EaseOutControlPoint as iconData } from '@dga-icons/core/stroke-standard';

const EaseOutControlPoint = createJsIcon('ease-out-control-point', iconData);

export default EaseOutControlPoint;

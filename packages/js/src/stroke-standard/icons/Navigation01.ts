import { createJsIcon } from '../../createJsIcon';
import { Navigation01 as iconData } from '@dga-icons/core/stroke-standard';

const Navigation01 = createJsIcon('navigation-01', iconData);

export default Navigation01;

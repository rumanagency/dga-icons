import { createJsIcon } from '../../createJsIcon';
import { FileAudio as iconData } from '@dga-icons/core/stroke-standard';

const FileAudio = createJsIcon('file-audio', iconData);

export default FileAudio;

import { createJsIcon } from '../../createJsIcon';
import { Payoneer as iconData } from '@dga-icons/core/stroke-standard';

const Payoneer = createJsIcon('payoneer', iconData);

export default Payoneer;

import { createJsIcon } from '../../createJsIcon';
import { ForgotPassword as iconData } from '@dga-icons/core/stroke-standard';

const ForgotPassword = createJsIcon('forgot-password', iconData);

export default ForgotPassword;

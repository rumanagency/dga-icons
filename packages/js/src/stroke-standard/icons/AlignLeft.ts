import { createJsIcon } from '../../createJsIcon';
import { AlignLeft as iconData } from '@dga-icons/core/stroke-standard';

const AlignLeft = createJsIcon('align-left', iconData);

export default AlignLeft;

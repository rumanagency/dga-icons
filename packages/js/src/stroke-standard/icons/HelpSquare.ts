import { createJsIcon } from '../../createJsIcon';
import { HelpSquare as iconData } from '@dga-icons/core/stroke-standard';

const HelpSquare = createJsIcon('help-square', iconData);

export default HelpSquare;

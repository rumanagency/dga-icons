import { createJsIcon } from '../../createJsIcon';
import { RotateLeft05 as iconData } from '@dga-icons/core/stroke-standard';

const RotateLeft05 = createJsIcon('rotate-left-05', iconData);

export default RotateLeft05;

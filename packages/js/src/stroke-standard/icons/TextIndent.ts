import { createJsIcon } from '../../createJsIcon';
import { TextIndent as iconData } from '@dga-icons/core/stroke-standard';

const TextIndent = createJsIcon('text-indent', iconData);

export default TextIndent;

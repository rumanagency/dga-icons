import { createJsIcon } from '../../createJsIcon';
import { CovidInfo as iconData } from '@dga-icons/core/stroke-standard';

const CovidInfo = createJsIcon('covid-info', iconData);

export default CovidInfo;

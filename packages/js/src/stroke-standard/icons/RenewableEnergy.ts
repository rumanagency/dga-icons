import { createJsIcon } from '../../createJsIcon';
import { RenewableEnergy as iconData } from '@dga-icons/core/stroke-standard';

const RenewableEnergy = createJsIcon('renewable-energy', iconData);

export default RenewableEnergy;

import { createJsIcon } from '../../createJsIcon';
import { YenSend as iconData } from '@dga-icons/core/stroke-standard';

const YenSend = createJsIcon('yen-send', iconData);

export default YenSend;

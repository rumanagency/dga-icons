import { createJsIcon } from '../../createJsIcon';
import { RoadWayside as iconData } from '@dga-icons/core/stroke-standard';

const RoadWayside = createJsIcon('road-wayside', iconData);

export default RoadWayside;

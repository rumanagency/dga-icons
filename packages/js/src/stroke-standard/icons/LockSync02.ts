import { createJsIcon } from '../../createJsIcon';
import { LockSync02 as iconData } from '@dga-icons/core/stroke-standard';

const LockSync02 = createJsIcon('lock-sync-02', iconData);

export default LockSync02;

import { createJsIcon } from '../../createJsIcon';
import { Bootstrap as iconData } from '@dga-icons/core/stroke-standard';

const Bootstrap = createJsIcon('bootstrap', iconData);

export default Bootstrap;

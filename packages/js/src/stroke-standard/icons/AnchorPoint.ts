import { createJsIcon } from '../../createJsIcon';
import { AnchorPoint as iconData } from '@dga-icons/core/stroke-standard';

const AnchorPoint = createJsIcon('anchor-point', iconData);

export default AnchorPoint;

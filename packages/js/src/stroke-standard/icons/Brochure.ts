import { createJsIcon } from '../../createJsIcon';
import { Brochure as iconData } from '@dga-icons/core/stroke-standard';

const Brochure = createJsIcon('brochure', iconData);

export default Brochure;

import { createJsIcon } from '../../createJsIcon';
import { Complaint as iconData } from '@dga-icons/core/stroke-standard';

const Complaint = createJsIcon('complaint', iconData);

export default Complaint;

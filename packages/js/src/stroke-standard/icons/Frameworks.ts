import { createJsIcon } from '../../createJsIcon';
import { Frameworks as iconData } from '@dga-icons/core/stroke-standard';

const Frameworks = createJsIcon('frameworks', iconData);

export default Frameworks;

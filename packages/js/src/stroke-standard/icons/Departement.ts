import { createJsIcon } from '../../createJsIcon';
import { Departement as iconData } from '@dga-icons/core/stroke-standard';

const Departement = createJsIcon('departement', iconData);

export default Departement;

import { createJsIcon } from '../../createJsIcon';
import { FolderSync as iconData } from '@dga-icons/core/stroke-standard';

const FolderSync = createJsIcon('folder-sync', iconData);

export default FolderSync;

import { createJsIcon } from '../../createJsIcon';
import { Telephone as iconData } from '@dga-icons/core/stroke-standard';

const Telephone = createJsIcon('telephone', iconData);

export default Telephone;

import { createJsIcon } from '../../createJsIcon';
import { WifiConnected02 as iconData } from '@dga-icons/core/stroke-standard';

const WifiConnected02 = createJsIcon('wifi-connected-02', iconData);

export default WifiConnected02;

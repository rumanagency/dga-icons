import { createJsIcon } from '../../createJsIcon';
import { Acceleration as iconData } from '@dga-icons/core/stroke-standard';

const Acceleration = createJsIcon('acceleration', iconData);

export default Acceleration;

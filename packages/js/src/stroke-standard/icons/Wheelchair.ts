import { createJsIcon } from '../../createJsIcon';
import { Wheelchair as iconData } from '@dga-icons/core/stroke-standard';

const Wheelchair = createJsIcon('wheelchair', iconData);

export default Wheelchair;

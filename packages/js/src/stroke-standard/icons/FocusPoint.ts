import { createJsIcon } from '../../createJsIcon';
import { FocusPoint as iconData } from '@dga-icons/core/stroke-standard';

const FocusPoint = createJsIcon('focus-point', iconData);

export default FocusPoint;

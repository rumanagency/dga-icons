import { createJsIcon } from '../../createJsIcon';
import { Humidity as iconData } from '@dga-icons/core/stroke-standard';

const Humidity = createJsIcon('humidity', iconData);

export default Humidity;

import { createJsIcon } from '../../createJsIcon';
import { AspectRatio as iconData } from '@dga-icons/core/stroke-standard';

const AspectRatio = createJsIcon('aspect-ratio', iconData);

export default AspectRatio;

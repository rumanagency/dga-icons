import { createJsIcon } from '../../createJsIcon';
import { BitcoinTag as iconData } from '@dga-icons/core/stroke-standard';

const BitcoinTag = createJsIcon('bitcoin-tag', iconData);

export default BitcoinTag;

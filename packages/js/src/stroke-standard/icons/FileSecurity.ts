import { createJsIcon } from '../../createJsIcon';
import { FileSecurity as iconData } from '@dga-icons/core/stroke-standard';

const FileSecurity = createJsIcon('file-security', iconData);

export default FileSecurity;

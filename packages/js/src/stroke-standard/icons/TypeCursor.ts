import { createJsIcon } from '../../createJsIcon';
import { TypeCursor as iconData } from '@dga-icons/core/stroke-standard';

const TypeCursor = createJsIcon('type-cursor', iconData);

export default TypeCursor;

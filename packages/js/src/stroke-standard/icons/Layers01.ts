import { createJsIcon } from '../../createJsIcon';
import { Layers01 as iconData } from '@dga-icons/core/stroke-standard';

const Layers01 = createJsIcon('layers-01', iconData);

export default Layers01;

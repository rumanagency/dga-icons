import { createJsIcon } from '../../createJsIcon';
import { RotateBottomLeft as iconData } from '@dga-icons/core/stroke-standard';

const RotateBottomLeft = createJsIcon('rotate-bottom-left', iconData);

export default RotateBottomLeft;

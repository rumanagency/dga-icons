import { createJsIcon } from '../../createJsIcon';
import { DateTime as iconData } from '@dga-icons/core/stroke-standard';

const DateTime = createJsIcon('date-time', iconData);

export default DateTime;

import { createJsIcon } from '../../createJsIcon';
import { Suspicious as iconData } from '@dga-icons/core/stroke-standard';

const Suspicious = createJsIcon('suspicious', iconData);

export default Suspicious;

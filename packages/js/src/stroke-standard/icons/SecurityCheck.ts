import { createJsIcon } from '../../createJsIcon';
import { SecurityCheck as iconData } from '@dga-icons/core/stroke-standard';

const SecurityCheck = createJsIcon('security-check', iconData);

export default SecurityCheck;

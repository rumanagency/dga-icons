import { createJsIcon } from '../../createJsIcon';
import { ScooterElectric as iconData } from '@dga-icons/core/stroke-standard';

const ScooterElectric = createJsIcon('scooter-electric', iconData);

export default ScooterElectric;

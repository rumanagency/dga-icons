import { createJsIcon } from '../../createJsIcon';
import { MenuCollapse as iconData } from '@dga-icons/core/stroke-standard';

const MenuCollapse = createJsIcon('menu-collapse', iconData);

export default MenuCollapse;

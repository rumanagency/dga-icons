import { createJsIcon } from '../../createJsIcon';
import { TextSubscript as iconData } from '@dga-icons/core/stroke-standard';

const TextSubscript = createJsIcon('text-subscript', iconData);

export default TextSubscript;

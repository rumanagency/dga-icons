import { createJsIcon } from '../../createJsIcon';
import { MinusSign as iconData } from '@dga-icons/core/stroke-standard';

const MinusSign = createJsIcon('minus-sign', iconData);

export default MinusSign;

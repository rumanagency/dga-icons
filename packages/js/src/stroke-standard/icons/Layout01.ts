import { createJsIcon } from '../../createJsIcon';
import { Layout01 as iconData } from '@dga-icons/core/stroke-standard';

const Layout01 = createJsIcon('layout-01', iconData);

export default Layout01;

import { createJsIcon } from '../../createJsIcon';
import { ShieldEnergy as iconData } from '@dga-icons/core/stroke-standard';

const ShieldEnergy = createJsIcon('shield-energy', iconData);

export default ShieldEnergy;

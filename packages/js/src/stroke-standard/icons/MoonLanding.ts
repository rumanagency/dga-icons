import { createJsIcon } from '../../createJsIcon';
import { MoonLanding as iconData } from '@dga-icons/core/stroke-standard';

const MoonLanding = createJsIcon('moon-landing', iconData);

export default MoonLanding;

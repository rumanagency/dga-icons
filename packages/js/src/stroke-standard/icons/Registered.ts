import { createJsIcon } from '../../createJsIcon';
import { Registered as iconData } from '@dga-icons/core/stroke-standard';

const Registered = createJsIcon('registered', iconData);

export default Registered;

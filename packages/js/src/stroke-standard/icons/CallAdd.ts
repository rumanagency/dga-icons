import { createJsIcon } from '../../createJsIcon';
import { CallAdd as iconData } from '@dga-icons/core/stroke-standard';

const CallAdd = createJsIcon('call-add', iconData);

export default CallAdd;

import { createJsIcon } from '../../createJsIcon';
import { Coins01 as iconData } from '@dga-icons/core/stroke-standard';

const Coins01 = createJsIcon('coins-01', iconData);

export default Coins01;

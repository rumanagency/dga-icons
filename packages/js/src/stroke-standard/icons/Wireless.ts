import { createJsIcon } from '../../createJsIcon';
import { Wireless as iconData } from '@dga-icons/core/stroke-standard';

const Wireless = createJsIcon('wireless', iconData);

export default Wireless;

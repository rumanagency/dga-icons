import { createJsIcon } from '../../createJsIcon';
import { Whiteboard as iconData } from '@dga-icons/core/stroke-standard';

const Whiteboard = createJsIcon('whiteboard', iconData);

export default Whiteboard;

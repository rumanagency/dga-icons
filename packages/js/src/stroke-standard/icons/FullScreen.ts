import { createJsIcon } from '../../createJsIcon';
import { FullScreen as iconData } from '@dga-icons/core/stroke-standard';

const FullScreen = createJsIcon('full-screen', iconData);

export default FullScreen;

import { createJsIcon } from '../../createJsIcon';
import { Reminder as iconData } from '@dga-icons/core/stroke-standard';

const Reminder = createJsIcon('reminder', iconData);

export default Reminder;

import { createJsIcon } from '../../createJsIcon';
import { FolderMinus as iconData } from '@dga-icons/core/stroke-standard';

const FolderMinus = createJsIcon('folder-minus', iconData);

export default FolderMinus;

import { createJsIcon } from '../../createJsIcon';
import { FileView as iconData } from '@dga-icons/core/stroke-standard';

const FileView = createJsIcon('file-view', iconData);

export default FileView;

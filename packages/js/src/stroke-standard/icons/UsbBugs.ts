import { createJsIcon } from '../../createJsIcon';
import { UsbBugs as iconData } from '@dga-icons/core/stroke-standard';

const UsbBugs = createJsIcon('usb-bugs', iconData);

export default UsbBugs;

import { createJsIcon } from '../../createJsIcon';
import { PropertyNew as iconData } from '@dga-icons/core/stroke-standard';

const PropertyNew = createJsIcon('property-new', iconData);

export default PropertyNew;

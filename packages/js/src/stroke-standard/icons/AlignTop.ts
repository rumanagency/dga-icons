import { createJsIcon } from '../../createJsIcon';
import { AlignTop as iconData } from '@dga-icons/core/stroke-standard';

const AlignTop = createJsIcon('align-top', iconData);

export default AlignTop;

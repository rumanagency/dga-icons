import { createJsIcon } from '../../createJsIcon';
import { WorkoutWarmUp as iconData } from '@dga-icons/core/stroke-standard';

const WorkoutWarmUp = createJsIcon('workout-warm-up', iconData);

export default WorkoutWarmUp;

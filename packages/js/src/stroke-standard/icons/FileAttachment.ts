import { createJsIcon } from '../../createJsIcon';
import { FileAttachment as iconData } from '@dga-icons/core/stroke-standard';

const FileAttachment = createJsIcon('file-attachment', iconData);

export default FileAttachment;

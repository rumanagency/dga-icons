import { createJsIcon } from '../../createJsIcon';
import { FolderSearch as iconData } from '@dga-icons/core/stroke-standard';

const FolderSearch = createJsIcon('folder-search', iconData);

export default FolderSearch;

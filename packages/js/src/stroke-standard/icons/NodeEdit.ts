import { createJsIcon } from '../../createJsIcon';
import { NodeEdit as iconData } from '@dga-icons/core/stroke-standard';

const NodeEdit = createJsIcon('node-edit', iconData);

export default NodeEdit;

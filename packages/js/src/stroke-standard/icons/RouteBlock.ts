import { createJsIcon } from '../../createJsIcon';
import { RouteBlock as iconData } from '@dga-icons/core/stroke-standard';

const RouteBlock = createJsIcon('route-block', iconData);

export default RouteBlock;

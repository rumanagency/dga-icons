import { createJsIcon } from '../../createJsIcon';
import { FolderEdit as iconData } from '@dga-icons/core/stroke-standard';

const FolderEdit = createJsIcon('folder-edit', iconData);

export default FolderEdit;

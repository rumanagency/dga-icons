import { createJsIcon } from '../../createJsIcon';
import { ComputerCloud as iconData } from '@dga-icons/core/stroke-standard';

const ComputerCloud = createJsIcon('computer-cloud', iconData);

export default ComputerCloud;

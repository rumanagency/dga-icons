import { createJsIcon } from '../../createJsIcon';
import { FileZip as iconData } from '@dga-icons/core/stroke-standard';

const FileZip = createJsIcon('file-zip', iconData);

export default FileZip;

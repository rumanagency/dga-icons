import { createJsIcon } from '../../createJsIcon';
import { Calculator as iconData } from '@dga-icons/core/stroke-standard';

const Calculator = createJsIcon('calculator', iconData);

export default Calculator;

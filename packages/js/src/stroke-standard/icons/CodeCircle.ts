import { createJsIcon } from '../../createJsIcon';
import { CodeCircle as iconData } from '@dga-icons/core/stroke-standard';

const CodeCircle = createJsIcon('code-circle', iconData);

export default CodeCircle;

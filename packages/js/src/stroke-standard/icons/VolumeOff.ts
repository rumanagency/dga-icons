import { createJsIcon } from '../../createJsIcon';
import { VolumeOff as iconData } from '@dga-icons/core/stroke-standard';

const VolumeOff = createJsIcon('volume-off', iconData);

export default VolumeOff;

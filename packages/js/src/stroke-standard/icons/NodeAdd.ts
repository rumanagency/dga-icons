import { createJsIcon } from '../../createJsIcon';
import { NodeAdd as iconData } from '@dga-icons/core/stroke-standard';

const NodeAdd = createJsIcon('node-add', iconData);

export default NodeAdd;

import { createJsIcon } from '../../createJsIcon';
import { Developer as iconData } from '@dga-icons/core/stroke-standard';

const Developer = createJsIcon('developer', iconData);

export default Developer;

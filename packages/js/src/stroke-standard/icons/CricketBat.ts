import { createJsIcon } from '../../createJsIcon';
import { CricketBat as iconData } from '@dga-icons/core/stroke-standard';

const CricketBat = createJsIcon('cricket-bat', iconData);

export default CricketBat;

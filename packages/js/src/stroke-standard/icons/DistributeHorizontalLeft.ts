import { createJsIcon } from '../../createJsIcon';
import { DistributeHorizontalLeft as iconData } from '@dga-icons/core/stroke-standard';

const DistributeHorizontalLeft = createJsIcon('distribute-horizontal-left', iconData);

export default DistributeHorizontalLeft;

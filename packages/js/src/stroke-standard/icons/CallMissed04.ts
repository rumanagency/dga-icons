import { createJsIcon } from '../../createJsIcon';
import { CallMissed04 as iconData } from '@dga-icons/core/stroke-standard';

const CallMissed04 = createJsIcon('call-missed-04', iconData);

export default CallMissed04;

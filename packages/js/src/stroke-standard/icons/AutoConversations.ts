import { createJsIcon } from '../../createJsIcon';
import { AutoConversations as iconData } from '@dga-icons/core/stroke-standard';

const AutoConversations = createJsIcon('auto-conversations', iconData);

export default AutoConversations;

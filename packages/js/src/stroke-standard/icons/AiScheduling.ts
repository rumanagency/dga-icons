import { createJsIcon } from '../../createJsIcon';
import { AiScheduling as iconData } from '@dga-icons/core/stroke-standard';

const AiScheduling = createJsIcon('ai-scheduling', iconData);

export default AiScheduling;

import { createJsIcon } from '../../createJsIcon';
import { ContentWriting as iconData } from '@dga-icons/core/stroke-standard';

const ContentWriting = createJsIcon('content-writing', iconData);

export default ContentWriting;

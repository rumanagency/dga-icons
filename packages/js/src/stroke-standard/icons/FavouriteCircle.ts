import { createJsIcon } from '../../createJsIcon';
import { FavouriteCircle as iconData } from '@dga-icons/core/stroke-standard';

const FavouriteCircle = createJsIcon('favourite-circle', iconData);

export default FavouriteCircle;

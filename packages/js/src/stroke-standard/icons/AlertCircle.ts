import { createJsIcon } from '../../createJsIcon';
import { AlertCircle as iconData } from '@dga-icons/core/stroke-standard';

const AlertCircle = createJsIcon('alert-circle', iconData);

export default AlertCircle;

import { createJsIcon } from '../../createJsIcon';
import { BitcoinLock as iconData } from '@dga-icons/core/stroke-standard';

const BitcoinLock = createJsIcon('bitcoin-lock', iconData);

export default BitcoinLock;

import { createJsIcon } from '../../createJsIcon';
import { Passport as iconData } from '@dga-icons/core/stroke-standard';

const Passport = createJsIcon('passport', iconData);

export default Passport;

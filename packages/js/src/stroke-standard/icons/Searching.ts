import { createJsIcon } from '../../createJsIcon';
import { Searching as iconData } from '@dga-icons/core/stroke-standard';

const Searching = createJsIcon('searching', iconData);

export default Searching;

import { createJsIcon } from '../../createJsIcon';
import { Affiliate as iconData } from '@dga-icons/core/stroke-standard';

const Affiliate = createJsIcon('affiliate', iconData);

export default Affiliate;

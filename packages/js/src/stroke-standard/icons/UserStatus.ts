import { createJsIcon } from '../../createJsIcon';
import { UserStatus as iconData } from '@dga-icons/core/stroke-standard';

const UserStatus = createJsIcon('user-status', iconData);

export default UserStatus;

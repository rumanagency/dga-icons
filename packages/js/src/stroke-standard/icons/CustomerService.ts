import { createJsIcon } from '../../createJsIcon';
import { CustomerService as iconData } from '@dga-icons/core/stroke-standard';

const CustomerService = createJsIcon('customer-service', iconData);

export default CustomerService;

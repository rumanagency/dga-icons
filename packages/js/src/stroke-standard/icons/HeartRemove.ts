import { createJsIcon } from '../../createJsIcon';
import { HeartRemove as iconData } from '@dga-icons/core/stroke-standard';

const HeartRemove = createJsIcon('heart-remove', iconData);

export default HeartRemove;

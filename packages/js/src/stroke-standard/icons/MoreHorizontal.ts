import { createJsIcon } from '../../createJsIcon';
import { MoreHorizontal as iconData } from '@dga-icons/core/stroke-standard';

const MoreHorizontal = createJsIcon('more-horizontal', iconData);

export default MoreHorizontal;

import { createJsIcon } from '../../createJsIcon';
import { StraightEdge as iconData } from '@dga-icons/core/stroke-standard';

const StraightEdge = createJsIcon('straight-edge', iconData);

export default StraightEdge;

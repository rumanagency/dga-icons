import { createJsIcon } from '../../createJsIcon';
import { BalanceScale as iconData } from '@dga-icons/core/stroke-standard';

const BalanceScale = createJsIcon('balance-scale', iconData);

export default BalanceScale;

import { createJsIcon } from '../../createJsIcon';
import { AppleReminder as iconData } from '@dga-icons/core/stroke-standard';

const AppleReminder = createJsIcon('apple-reminder', iconData);

export default AppleReminder;

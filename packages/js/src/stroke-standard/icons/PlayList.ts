import { createJsIcon } from '../../createJsIcon';
import { PlayList as iconData } from '@dga-icons/core/stroke-standard';

const PlayList = createJsIcon('play-list', iconData);

export default PlayList;

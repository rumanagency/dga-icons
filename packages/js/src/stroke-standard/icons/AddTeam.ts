import { createJsIcon } from '../../createJsIcon';
import { AddTeam as iconData } from '@dga-icons/core/stroke-standard';

const AddTeam = createJsIcon('add-team', iconData);

export default AddTeam;

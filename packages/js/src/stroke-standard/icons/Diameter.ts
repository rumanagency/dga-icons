import { createJsIcon } from '../../createJsIcon';
import { Diameter as iconData } from '@dga-icons/core/stroke-standard';

const Diameter = createJsIcon('diameter', iconData);

export default Diameter;

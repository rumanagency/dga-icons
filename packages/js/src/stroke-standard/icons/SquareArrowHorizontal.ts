import { createJsIcon } from '../../createJsIcon';
import { SquareArrowHorizontal as iconData } from '@dga-icons/core/stroke-standard';

const SquareArrowHorizontal = createJsIcon('square-arrow-horizontal', iconData);

export default SquareArrowHorizontal;

import { createJsIcon } from '../../createJsIcon';
import { ShippingCenter as iconData } from '@dga-icons/core/stroke-standard';

const ShippingCenter = createJsIcon('shipping-center', iconData);

export default ShippingCenter;

import { createJsIcon } from '../../createJsIcon';
import { VideoReplay as iconData } from '@dga-icons/core/stroke-standard';

const VideoReplay = createJsIcon('video-replay', iconData);

export default VideoReplay;

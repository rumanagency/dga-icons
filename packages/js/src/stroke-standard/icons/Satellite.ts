import { createJsIcon } from '../../createJsIcon';
import { Satellite as iconData } from '@dga-icons/core/stroke-standard';

const Satellite = createJsIcon('satellite', iconData);

export default Satellite;

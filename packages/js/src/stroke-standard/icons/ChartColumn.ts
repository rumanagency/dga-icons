import { createJsIcon } from '../../createJsIcon';
import { ChartColumn as iconData } from '@dga-icons/core/stroke-standard';

const ChartColumn = createJsIcon('chart-column', iconData);

export default ChartColumn;

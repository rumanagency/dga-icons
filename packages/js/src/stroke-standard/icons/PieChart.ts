import { createJsIcon } from '../../createJsIcon';
import { PieChart as iconData } from '@dga-icons/core/stroke-standard';

const PieChart = createJsIcon('pie-chart', iconData);

export default PieChart;

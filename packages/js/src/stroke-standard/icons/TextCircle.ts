import { createJsIcon } from '../../createJsIcon';
import { TextCircle as iconData } from '@dga-icons/core/stroke-standard';

const TextCircle = createJsIcon('text-circle', iconData);

export default TextCircle;

import { createJsIcon } from '../../createJsIcon';
import { SpeechToText as iconData } from '@dga-icons/core/stroke-standard';

const SpeechToText = createJsIcon('speech-to-text', iconData);

export default SpeechToText;

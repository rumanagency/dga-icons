import { createJsIcon } from '../../createJsIcon';
import { BluetoothNotConnected as iconData } from '@dga-icons/core/stroke-standard';

const BluetoothNotConnected = createJsIcon('bluetooth-not-connected', iconData);

export default BluetoothNotConnected;

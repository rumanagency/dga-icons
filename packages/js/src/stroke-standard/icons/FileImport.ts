import { createJsIcon } from '../../createJsIcon';
import { FileImport as iconData } from '@dga-icons/core/stroke-standard';

const FileImport = createJsIcon('file-import', iconData);

export default FileImport;

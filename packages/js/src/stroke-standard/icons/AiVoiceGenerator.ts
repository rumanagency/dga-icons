import { createJsIcon } from '../../createJsIcon';
import { AiVoiceGenerator as iconData } from '@dga-icons/core/stroke-standard';

const AiVoiceGenerator = createJsIcon('ai-voice-generator', iconData);

export default AiVoiceGenerator;

import { createJsIcon } from '../../createJsIcon';
import { Flashlight as iconData } from '@dga-icons/core/stroke-standard';

const Flashlight = createJsIcon('flashlight', iconData);

export default Flashlight;

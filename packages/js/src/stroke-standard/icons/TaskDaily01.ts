import { createJsIcon } from '../../createJsIcon';
import { TaskDaily01 as iconData } from '@dga-icons/core/stroke-standard';

const TaskDaily01 = createJsIcon('task-daily-01', iconData);

export default TaskDaily01;

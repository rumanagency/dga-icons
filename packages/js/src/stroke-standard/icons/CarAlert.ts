import { createJsIcon } from '../../createJsIcon';
import { CarAlert as iconData } from '@dga-icons/core/stroke-standard';

const CarAlert = createJsIcon('car-alert', iconData);

export default CarAlert;

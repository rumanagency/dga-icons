import { createJsIcon } from '../../createJsIcon';
import { FolderFavourite as iconData } from '@dga-icons/core/stroke-standard';

const FolderFavourite = createJsIcon('folder-favourite', iconData);

export default FolderFavourite;

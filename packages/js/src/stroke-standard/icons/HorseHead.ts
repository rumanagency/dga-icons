import { createJsIcon } from '../../createJsIcon';
import { HorseHead as iconData } from '@dga-icons/core/stroke-standard';

const HorseHead = createJsIcon('horse-head', iconData);

export default HorseHead;

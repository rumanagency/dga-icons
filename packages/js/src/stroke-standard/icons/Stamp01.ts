import { createJsIcon } from '../../createJsIcon';
import { Stamp01 as iconData } from '@dga-icons/core/stroke-standard';

const Stamp01 = createJsIcon('stamp-01', iconData);

export default Stamp01;

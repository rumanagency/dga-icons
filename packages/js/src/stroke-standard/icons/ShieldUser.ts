import { createJsIcon } from '../../createJsIcon';
import { ShieldUser as iconData } from '@dga-icons/core/stroke-standard';

const ShieldUser = createJsIcon('shield-user', iconData);

export default ShieldUser;

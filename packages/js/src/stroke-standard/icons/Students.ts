import { createJsIcon } from '../../createJsIcon';
import { Students as iconData } from '@dga-icons/core/stroke-standard';

const Students = createJsIcon('students', iconData);

export default Students;

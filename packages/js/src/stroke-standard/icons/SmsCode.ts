import { createJsIcon } from '../../createJsIcon';
import { SmsCode as iconData } from '@dga-icons/core/stroke-standard';

const SmsCode = createJsIcon('sms-code', iconData);

export default SmsCode;

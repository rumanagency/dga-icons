import { createJsIcon } from '../../createJsIcon';
import { Prescription as iconData } from '@dga-icons/core/stroke-standard';

const Prescription = createJsIcon('prescription', iconData);

export default Prescription;

import { createJsIcon } from '../../createJsIcon';
import { DentalBraces as iconData } from '@dga-icons/core/stroke-standard';

const DentalBraces = createJsIcon('dental-braces', iconData);

export default DentalBraces;

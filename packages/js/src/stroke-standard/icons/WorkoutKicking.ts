import { createJsIcon } from '../../createJsIcon';
import { WorkoutKicking as iconData } from '@dga-icons/core/stroke-standard';

const WorkoutKicking = createJsIcon('workout-kicking', iconData);

export default WorkoutKicking;

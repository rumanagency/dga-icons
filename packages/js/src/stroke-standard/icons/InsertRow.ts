import { createJsIcon } from '../../createJsIcon';
import { InsertRow as iconData } from '@dga-icons/core/stroke-standard';

const InsertRow = createJsIcon('insert-row', iconData);

export default InsertRow;

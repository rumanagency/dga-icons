import { createJsIcon } from '../../createJsIcon';
import { CursorInWindow as iconData } from '@dga-icons/core/stroke-standard';

const CursorInWindow = createJsIcon('cursor-in-window', iconData);

export default CursorInWindow;

import { createJsIcon } from '../../createJsIcon';
import { FileEdit as iconData } from '@dga-icons/core/stroke-standard';

const FileEdit = createJsIcon('file-edit', iconData);

export default FileEdit;

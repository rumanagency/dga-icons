import { createJsIcon } from '../../createJsIcon';
import { Touchpad04 as iconData } from '@dga-icons/core/stroke-standard';

const Touchpad04 = createJsIcon('touchpad-04', iconData);

export default Touchpad04;

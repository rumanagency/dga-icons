import { createJsIcon } from '../../createJsIcon';
import { Security as iconData } from '@dga-icons/core/stroke-standard';

const Security = createJsIcon('security', iconData);

export default Security;

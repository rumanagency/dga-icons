import { createJsIcon } from '../../createJsIcon';
import { Steering as iconData } from '@dga-icons/core/stroke-standard';

const Steering = createJsIcon('steering', iconData);

export default Steering;

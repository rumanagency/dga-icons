import { createJsIcon } from '../../createJsIcon';
import { SecurityKeyUsb as iconData } from '@dga-icons/core/stroke-standard';

const SecurityKeyUsb = createJsIcon('security-key-usb', iconData);

export default SecurityKeyUsb;

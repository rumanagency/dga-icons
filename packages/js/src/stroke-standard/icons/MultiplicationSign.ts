import { createJsIcon } from '../../createJsIcon';
import { MultiplicationSign as iconData } from '@dga-icons/core/stroke-standard';

const MultiplicationSign = createJsIcon('multiplication-sign', iconData);

export default MultiplicationSign;

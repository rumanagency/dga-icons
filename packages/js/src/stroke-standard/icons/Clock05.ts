import { createJsIcon } from '../../createJsIcon';
import { Clock05 as iconData } from '@dga-icons/core/stroke-standard';

const Clock05 = createJsIcon('clock-05', iconData);

export default Clock05;

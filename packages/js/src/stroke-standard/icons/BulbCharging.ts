import { createJsIcon } from '../../createJsIcon';
import { BulbCharging as iconData } from '@dga-icons/core/stroke-standard';

const BulbCharging = createJsIcon('bulb-charging', iconData);

export default BulbCharging;

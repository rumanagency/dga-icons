import { createJsIcon } from '../../createJsIcon';
import { TropicalStorm as iconData } from '@dga-icons/core/stroke-standard';

const TropicalStorm = createJsIcon('tropical-storm', iconData);

export default TropicalStorm;

import { createJsIcon } from '../../createJsIcon';
import { Customize as iconData } from '@dga-icons/core/stroke-standard';

const Customize = createJsIcon('customize', iconData);

export default Customize;

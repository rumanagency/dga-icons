import { createJsIcon } from '../../createJsIcon';
import { PisaTower as iconData } from '@dga-icons/core/stroke-standard';

const PisaTower = createJsIcon('pisa-tower', iconData);

export default PisaTower;

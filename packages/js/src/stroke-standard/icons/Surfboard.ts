import { createJsIcon } from '../../createJsIcon';
import { Surfboard as iconData } from '@dga-icons/core/stroke-standard';

const Surfboard = createJsIcon('surfboard', iconData);

export default Surfboard;

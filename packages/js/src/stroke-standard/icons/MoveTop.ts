import { createJsIcon } from '../../createJsIcon';
import { MoveTop as iconData } from '@dga-icons/core/stroke-standard';

const MoveTop = createJsIcon('move-top', iconData);

export default MoveTop;

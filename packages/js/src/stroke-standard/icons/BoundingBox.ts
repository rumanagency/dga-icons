import { createJsIcon } from '../../createJsIcon';
import { BoundingBox as iconData } from '@dga-icons/core/stroke-standard';

const BoundingBox = createJsIcon('bounding-box', iconData);

export default BoundingBox;

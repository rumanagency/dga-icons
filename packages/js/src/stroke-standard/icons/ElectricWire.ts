import { createJsIcon } from '../../createJsIcon';
import { ElectricWire as iconData } from '@dga-icons/core/stroke-standard';

const ElectricWire = createJsIcon('electric-wire', iconData);

export default ElectricWire;

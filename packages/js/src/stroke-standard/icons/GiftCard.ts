import { createJsIcon } from '../../createJsIcon';
import { GiftCard as iconData } from '@dga-icons/core/stroke-standard';

const GiftCard = createJsIcon('gift-card', iconData);

export default GiftCard;

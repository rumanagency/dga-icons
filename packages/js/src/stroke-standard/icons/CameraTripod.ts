import { createJsIcon } from '../../createJsIcon';
import { CameraTripod as iconData } from '@dga-icons/core/stroke-standard';

const CameraTripod = createJsIcon('camera-tripod', iconData);

export default CameraTripod;

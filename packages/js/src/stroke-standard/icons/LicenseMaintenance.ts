import { createJsIcon } from '../../createJsIcon';
import { LicenseMaintenance as iconData } from '@dga-icons/core/stroke-standard';

const LicenseMaintenance = createJsIcon('license-maintenance', iconData);

export default LicenseMaintenance;

import { createJsIcon } from '../../createJsIcon';
import { PinCode as iconData } from '@dga-icons/core/stroke-standard';

const PinCode = createJsIcon('pin-code', iconData);

export default PinCode;

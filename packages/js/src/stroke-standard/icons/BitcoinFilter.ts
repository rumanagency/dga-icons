import { createJsIcon } from '../../createJsIcon';
import { BitcoinFilter as iconData } from '@dga-icons/core/stroke-standard';

const BitcoinFilter = createJsIcon('bitcoin-filter', iconData);

export default BitcoinFilter;

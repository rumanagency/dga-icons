import { createJsIcon } from '../../createJsIcon';
import { Certificate02 as iconData } from '@dga-icons/core/stroke-standard';

const Certificate02 = createJsIcon('certificate-02', iconData);

export default Certificate02;

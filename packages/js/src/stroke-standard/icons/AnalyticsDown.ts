import { createJsIcon } from '../../createJsIcon';
import { AnalyticsDown as iconData } from '@dga-icons/core/stroke-standard';

const AnalyticsDown = createJsIcon('analytics-down', iconData);

export default AnalyticsDown;

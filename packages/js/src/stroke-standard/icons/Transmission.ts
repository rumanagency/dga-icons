import { createJsIcon } from '../../createJsIcon';
import { Transmission as iconData } from '@dga-icons/core/stroke-standard';

const Transmission = createJsIcon('transmission', iconData);

export default Transmission;

import { createJsIcon } from '../../createJsIcon';
import { BitcoinWithdraw as iconData } from '@dga-icons/core/stroke-standard';

const BitcoinWithdraw = createJsIcon('bitcoin-withdraw', iconData);

export default BitcoinWithdraw;

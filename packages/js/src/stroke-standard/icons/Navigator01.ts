import { createJsIcon } from '../../createJsIcon';
import { Navigator01 as iconData } from '@dga-icons/core/stroke-standard';

const Navigator01 = createJsIcon('navigator-01', iconData);

export default Navigator01;

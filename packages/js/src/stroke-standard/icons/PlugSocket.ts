import { createJsIcon } from '../../createJsIcon';
import { PlugSocket as iconData } from '@dga-icons/core/stroke-standard';

const PlugSocket = createJsIcon('plug-socket', iconData);

export default PlugSocket;

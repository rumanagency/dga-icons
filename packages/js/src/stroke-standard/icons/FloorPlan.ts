import { createJsIcon } from '../../createJsIcon';
import { FloorPlan as iconData } from '@dga-icons/core/stroke-standard';

const FloorPlan = createJsIcon('floor-plan', iconData);

export default FloorPlan;

import { createJsIcon } from '../../createJsIcon';
import { NoteAdd as iconData } from '@dga-icons/core/stroke-standard';

const NoteAdd = createJsIcon('note-add', iconData);

export default NoteAdd;

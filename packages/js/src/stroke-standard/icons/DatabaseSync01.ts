import { createJsIcon } from '../../createJsIcon';
import { DatabaseSync01 as iconData } from '@dga-icons/core/stroke-standard';

const DatabaseSync01 = createJsIcon('database-sync-01', iconData);

export default DatabaseSync01;

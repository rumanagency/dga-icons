import { createJsIcon } from '../../createJsIcon';
import { GlobalRefresh as iconData } from '@dga-icons/core/stroke-standard';

const GlobalRefresh = createJsIcon('global-refresh', iconData);

export default GlobalRefresh;

import { createJsIcon } from '../../createJsIcon';
import { Whatsapp as iconData } from '@dga-icons/core/stroke-standard';

const Whatsapp = createJsIcon('whatsapp', iconData);

export default Whatsapp;

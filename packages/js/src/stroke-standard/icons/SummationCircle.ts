import { createJsIcon } from '../../createJsIcon';
import { SummationCircle as iconData } from '@dga-icons/core/stroke-standard';

const SummationCircle = createJsIcon('summation-circle', iconData);

export default SummationCircle;

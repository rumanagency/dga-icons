import { createJsIcon } from '../../createJsIcon';
import { CheckList as iconData } from '@dga-icons/core/stroke-standard';

const CheckList = createJsIcon('check-list', iconData);

export default CheckList;

import { createJsIcon } from '../../createJsIcon';
import { MinusSignCircle as iconData } from '@dga-icons/core/stroke-standard';

const MinusSignCircle = createJsIcon('minus-sign-circle', iconData);

export default MinusSignCircle;

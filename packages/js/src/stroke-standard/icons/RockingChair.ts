import { createJsIcon } from '../../createJsIcon';
import { RockingChair as iconData } from '@dga-icons/core/stroke-standard';

const RockingChair = createJsIcon('rocking-chair', iconData);

export default RockingChair;

import { createJsIcon } from '../../createJsIcon';
import { PlayStore as iconData } from '@dga-icons/core/stroke-standard';

const PlayStore = createJsIcon('play-store', iconData);

export default PlayStore;

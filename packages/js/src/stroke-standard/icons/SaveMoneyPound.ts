import { createJsIcon } from '../../createJsIcon';
import { SaveMoneyPound as iconData } from '@dga-icons/core/stroke-standard';

const SaveMoneyPound = createJsIcon('save-money-pound', iconData);

export default SaveMoneyPound;

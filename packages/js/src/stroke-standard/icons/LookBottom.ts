import { createJsIcon } from '../../createJsIcon';
import { LookBottom as iconData } from '@dga-icons/core/stroke-standard';

const LookBottom = createJsIcon('look-bottom', iconData);

export default LookBottom;

import { createJsIcon } from '../../createJsIcon';
import { LookLeft as iconData } from '@dga-icons/core/stroke-standard';

const LookLeft = createJsIcon('look-left', iconData);

export default LookLeft;

import { createJsIcon } from '../../createJsIcon';
import { RemoveFemale as iconData } from '@dga-icons/core/stroke-standard';

const RemoveFemale = createJsIcon('remove-female', iconData);

export default RemoveFemale;

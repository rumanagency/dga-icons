import { createJsIcon } from '../../createJsIcon';
import { Balloons as iconData } from '@dga-icons/core/stroke-standard';

const Balloons = createJsIcon('balloons', iconData);

export default Balloons;

import { createJsIcon } from '../../createJsIcon';
import { Share07 as iconData } from '@dga-icons/core/stroke-standard';

const Share07 = createJsIcon('share-07', iconData);

export default Share07;

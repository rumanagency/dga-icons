import { createJsIcon } from '../../createJsIcon';
import { Covariate as iconData } from '@dga-icons/core/stroke-standard';

const Covariate = createJsIcon('covariate', iconData);

export default Covariate;

import { createJsIcon } from '../../createJsIcon';
import { PaintBucket as iconData } from '@dga-icons/core/stroke-standard';

const PaintBucket = createJsIcon('paint-bucket', iconData);

export default PaintBucket;

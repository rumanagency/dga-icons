import { createJsIcon } from '../../createJsIcon';
import { Garlands as iconData } from '@dga-icons/core/stroke-standard';

const Garlands = createJsIcon('garlands', iconData);

export default Garlands;

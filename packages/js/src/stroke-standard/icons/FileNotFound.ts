import { createJsIcon } from '../../createJsIcon';
import { FileNotFound as iconData } from '@dga-icons/core/stroke-standard';

const FileNotFound = createJsIcon('file-not-found', iconData);

export default FileNotFound;

import { createJsIcon } from '../../createJsIcon';
import { Setup02 as iconData } from '@dga-icons/core/stroke-standard';

const Setup02 = createJsIcon('setup-02', iconData);

export default Setup02;

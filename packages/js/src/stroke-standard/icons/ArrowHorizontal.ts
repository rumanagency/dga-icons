import { createJsIcon } from '../../createJsIcon';
import { ArrowHorizontal as iconData } from '@dga-icons/core/stroke-standard';

const ArrowHorizontal = createJsIcon('arrow-horizontal', iconData);

export default ArrowHorizontal;

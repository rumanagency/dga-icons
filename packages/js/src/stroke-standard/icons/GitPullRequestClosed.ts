import { createJsIcon } from '../../createJsIcon';
import { GitPullRequestClosed as iconData } from '@dga-icons/core/stroke-standard';

const GitPullRequestClosed = createJsIcon('git-pull-request-closed', iconData);

export default GitPullRequestClosed;

import { createJsIcon } from '../../createJsIcon';
import { InsertRowDown as iconData } from '@dga-icons/core/stroke-standard';

const InsertRowDown = createJsIcon('insert-row-down', iconData);

export default InsertRowDown;

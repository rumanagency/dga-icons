import { createJsIcon } from '../../createJsIcon';
import { PiggyBank as iconData } from '@dga-icons/core/stroke-standard';

const PiggyBank = createJsIcon('piggy-bank', iconData);

export default PiggyBank;

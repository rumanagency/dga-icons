import { createJsIcon } from '../../createJsIcon';
import { AlarmClock as iconData } from '@dga-icons/core/stroke-standard';

const AlarmClock = createJsIcon('alarm-clock', iconData);

export default AlarmClock;

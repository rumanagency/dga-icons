import { createJsIcon } from '../../createJsIcon';
import { FlowCircle as iconData } from '@dga-icons/core/stroke-standard';

const FlowCircle = createJsIcon('flow-circle', iconData);

export default FlowCircle;

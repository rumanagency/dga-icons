import { createJsIcon } from '../../createJsIcon';
import { Avalanche as iconData } from '@dga-icons/core/stroke-standard';

const Avalanche = createJsIcon('avalanche', iconData);

export default Avalanche;

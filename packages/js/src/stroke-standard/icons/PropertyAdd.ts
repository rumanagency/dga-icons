import { createJsIcon } from '../../createJsIcon';
import { PropertyAdd as iconData } from '@dga-icons/core/stroke-standard';

const PropertyAdd = createJsIcon('property-add', iconData);

export default PropertyAdd;

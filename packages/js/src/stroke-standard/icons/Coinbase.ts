import { createJsIcon } from '../../createJsIcon';
import { Coinbase as iconData } from '@dga-icons/core/stroke-standard';

const Coinbase = createJsIcon('coinbase', iconData);

export default Coinbase;

import { createJsIcon } from '../../createJsIcon';
import { Loading01 as iconData } from '@dga-icons/core/stroke-standard';

const Loading01 = createJsIcon('loading-01', iconData);

export default Loading01;

import { createJsIcon } from '../../createJsIcon';
import { Eraser01 as iconData } from '@dga-icons/core/stroke-standard';

const Eraser01 = createJsIcon('eraser-01', iconData);

export default Eraser01;

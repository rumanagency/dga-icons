import { createJsIcon } from '../../createJsIcon';
import { CloudSlowWind as iconData } from '@dga-icons/core/stroke-standard';

const CloudSlowWind = createJsIcon('cloud-slow-wind', iconData);

export default CloudSlowWind;

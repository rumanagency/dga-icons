import { createJsIcon } from '../../createJsIcon';
import { FilePin as iconData } from '@dga-icons/core/stroke-standard';

const FilePin = createJsIcon('file-pin', iconData);

export default FilePin;

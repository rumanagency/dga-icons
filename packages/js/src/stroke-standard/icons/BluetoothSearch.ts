import { createJsIcon } from '../../createJsIcon';
import { BluetoothSearch as iconData } from '@dga-icons/core/stroke-standard';

const BluetoothSearch = createJsIcon('bluetooth-search', iconData);

export default BluetoothSearch;

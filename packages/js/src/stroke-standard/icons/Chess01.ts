import { createJsIcon } from '../../createJsIcon';
import { Chess01 as iconData } from '@dga-icons/core/stroke-standard';

const Chess01 = createJsIcon('chess-01', iconData);

export default Chess01;

import { createJsIcon } from '../../createJsIcon';
import { PauseCircle as iconData } from '@dga-icons/core/stroke-standard';

const PauseCircle = createJsIcon('pause-circle', iconData);

export default PauseCircle;

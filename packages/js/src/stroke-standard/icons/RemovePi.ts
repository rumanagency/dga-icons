import { createJsIcon } from '../../createJsIcon';
import { RemovePi as iconData } from '@dga-icons/core/stroke-standard';

const RemovePi = createJsIcon('remove-pi', iconData);

export default RemovePi;

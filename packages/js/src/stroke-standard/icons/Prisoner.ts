import { createJsIcon } from '../../createJsIcon';
import { Prisoner as iconData } from '@dga-icons/core/stroke-standard';

const Prisoner = createJsIcon('prisoner', iconData);

export default Prisoner;

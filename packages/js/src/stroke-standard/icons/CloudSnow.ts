import { createJsIcon } from '../../createJsIcon';
import { CloudSnow as iconData } from '@dga-icons/core/stroke-standard';

const CloudSnow = createJsIcon('cloud-snow', iconData);

export default CloudSnow;

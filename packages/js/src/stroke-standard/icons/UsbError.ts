import { createJsIcon } from '../../createJsIcon';
import { UsbError as iconData } from '@dga-icons/core/stroke-standard';

const UsbError = createJsIcon('usb-error', iconData);

export default UsbError;

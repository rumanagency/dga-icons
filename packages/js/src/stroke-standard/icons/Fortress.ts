import { createJsIcon } from '../../createJsIcon';
import { Fortress as iconData } from '@dga-icons/core/stroke-standard';

const Fortress = createJsIcon('fortress', iconData);

export default Fortress;

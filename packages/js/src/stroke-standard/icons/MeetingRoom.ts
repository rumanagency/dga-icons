import { createJsIcon } from '../../createJsIcon';
import { MeetingRoom as iconData } from '@dga-icons/core/stroke-standard';

const MeetingRoom = createJsIcon('meeting-room', iconData);

export default MeetingRoom;

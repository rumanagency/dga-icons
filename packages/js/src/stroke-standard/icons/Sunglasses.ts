import { createJsIcon } from '../../createJsIcon';
import { Sunglasses as iconData } from '@dga-icons/core/stroke-standard';

const Sunglasses = createJsIcon('sunglasses', iconData);

export default Sunglasses;

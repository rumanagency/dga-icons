import { createJsIcon } from '../../createJsIcon';
import { DarkMode as iconData } from '@dga-icons/core/stroke-standard';

const DarkMode = createJsIcon('dark-mode', iconData);

export default DarkMode;

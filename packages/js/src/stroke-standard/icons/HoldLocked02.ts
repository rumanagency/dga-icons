import { createJsIcon } from '../../createJsIcon';
import { HoldLocked02 as iconData } from '@dga-icons/core/stroke-standard';

const HoldLocked02 = createJsIcon('hold-locked-02', iconData);

export default HoldLocked02;

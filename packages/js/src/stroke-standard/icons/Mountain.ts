import { createJsIcon } from '../../createJsIcon';
import { Mountain as iconData } from '@dga-icons/core/stroke-standard';

const Mountain = createJsIcon('mountain', iconData);

export default Mountain;

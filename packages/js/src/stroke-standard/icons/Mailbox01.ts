import { createJsIcon } from '../../createJsIcon';
import { Mailbox01 as iconData } from '@dga-icons/core/stroke-standard';

const Mailbox01 = createJsIcon('mailbox-01', iconData);

export default Mailbox01;

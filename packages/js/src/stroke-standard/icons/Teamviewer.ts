import { createJsIcon } from '../../createJsIcon';
import { Teamviewer as iconData } from '@dga-icons/core/stroke-standard';

const Teamviewer = createJsIcon('teamviewer', iconData);

export default Teamviewer;

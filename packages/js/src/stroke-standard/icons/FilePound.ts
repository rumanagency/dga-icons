import { createJsIcon } from '../../createJsIcon';
import { FilePound as iconData } from '@dga-icons/core/stroke-standard';

const FilePound = createJsIcon('file-pound', iconData);

export default FilePound;

import { createJsIcon } from '../../createJsIcon';
import { LayerMask02 as iconData } from '@dga-icons/core/stroke-standard';

const LayerMask02 = createJsIcon('layer-mask-02', iconData);

export default LayerMask02;

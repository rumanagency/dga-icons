import { createJsIcon } from '../../createJsIcon';
import { PoliceCar as iconData } from '@dga-icons/core/stroke-standard';

const PoliceCar = createJsIcon('police-car', iconData);

export default PoliceCar;

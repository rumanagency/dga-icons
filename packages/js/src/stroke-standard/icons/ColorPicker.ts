import { createJsIcon } from '../../createJsIcon';
import { ColorPicker as iconData } from '@dga-icons/core/stroke-standard';

const ColorPicker = createJsIcon('color-picker', iconData);

export default ColorPicker;

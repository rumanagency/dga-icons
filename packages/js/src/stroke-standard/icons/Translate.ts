import { createJsIcon } from '../../createJsIcon';
import { Translate as iconData } from '@dga-icons/core/stroke-standard';

const Translate = createJsIcon('translate', iconData);

export default Translate;

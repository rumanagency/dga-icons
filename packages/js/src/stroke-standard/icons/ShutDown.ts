import { createJsIcon } from '../../createJsIcon';
import { ShutDown as iconData } from '@dga-icons/core/stroke-standard';

const ShutDown = createJsIcon('shut-down', iconData);

export default ShutDown;

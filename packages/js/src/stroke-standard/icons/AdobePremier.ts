import { createJsIcon } from '../../createJsIcon';
import { AdobePremier as iconData } from '@dga-icons/core/stroke-standard';

const AdobePremier = createJsIcon('adobe-premier', iconData);

export default AdobePremier;

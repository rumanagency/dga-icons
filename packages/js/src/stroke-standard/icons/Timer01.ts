import { createJsIcon } from '../../createJsIcon';
import { Timer01 as iconData } from '@dga-icons/core/stroke-standard';

const Timer01 = createJsIcon('timer-01', iconData);

export default Timer01;

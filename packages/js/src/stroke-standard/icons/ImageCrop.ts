import { createJsIcon } from '../../createJsIcon';
import { ImageCrop as iconData } from '@dga-icons/core/stroke-standard';

const ImageCrop = createJsIcon('image-crop', iconData);

export default ImageCrop;

import { createJsIcon } from '../../createJsIcon';
import { Icon3dMove as iconData } from '@dga-icons/core/stroke-standard';

const Icon3dMove = createJsIcon('3d-move', iconData);

export default Icon3dMove;

import { createJsIcon } from '../../createJsIcon';
import { Displeased as iconData } from '@dga-icons/core/stroke-standard';

const Displeased = createJsIcon('displeased', iconData);

export default Displeased;

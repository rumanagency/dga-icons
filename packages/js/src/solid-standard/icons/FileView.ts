import { createJsIcon } from '../../createJsIcon';
import { FileView as iconData } from '@dga-icons/core/solid-standard';

const FileView = createJsIcon('file-view', iconData);

export default FileView;

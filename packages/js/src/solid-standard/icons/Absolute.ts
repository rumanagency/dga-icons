import { createJsIcon } from '../../createJsIcon';
import { Absolute as iconData } from '@dga-icons/core/solid-standard';

const Absolute = createJsIcon('absolute', iconData);

export default Absolute;

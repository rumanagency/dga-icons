import { createJsIcon } from '../../createJsIcon';
import { UserMultiple as iconData } from '@dga-icons/core/solid-standard';

const UserMultiple = createJsIcon('user-multiple', iconData);

export default UserMultiple;

import { createJsIcon } from '../../createJsIcon';
import { Packaging as iconData } from '@dga-icons/core/solid-standard';

const Packaging = createJsIcon('packaging', iconData);

export default Packaging;

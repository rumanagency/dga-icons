import { createJsIcon } from '../../createJsIcon';
import { Subtitle as iconData } from '@dga-icons/core/solid-standard';

const Subtitle = createJsIcon('subtitle', iconData);

export default Subtitle;

import { createJsIcon } from '../../createJsIcon';
import { SidebarLeft as iconData } from '@dga-icons/core/solid-standard';

const SidebarLeft = createJsIcon('sidebar-left', iconData);

export default SidebarLeft;

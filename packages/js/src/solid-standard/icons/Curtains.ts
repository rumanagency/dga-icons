import { createJsIcon } from '../../createJsIcon';
import { Curtains as iconData } from '@dga-icons/core/solid-standard';

const Curtains = createJsIcon('curtains', iconData);

export default Curtains;

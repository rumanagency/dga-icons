import { createJsIcon } from '../../createJsIcon';
import { Constellation as iconData } from '@dga-icons/core/solid-standard';

const Constellation = createJsIcon('constellation', iconData);

export default Constellation;

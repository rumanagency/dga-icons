import { createJsIcon } from '../../createJsIcon';
import { SkippingRope as iconData } from '@dga-icons/core/solid-standard';

const SkippingRope = createJsIcon('skipping-rope', iconData);

export default SkippingRope;

import { createJsIcon } from '../../createJsIcon';
import { StepOver as iconData } from '@dga-icons/core/solid-standard';

const StepOver = createJsIcon('step-over', iconData);

export default StepOver;

import { createJsIcon } from '../../createJsIcon';
import { HorizontalResize as iconData } from '@dga-icons/core/solid-standard';

const HorizontalResize = createJsIcon('horizontal-resize', iconData);

export default HorizontalResize;

import { createJsIcon } from '../../createJsIcon';
import { TimeSchedule as iconData } from '@dga-icons/core/solid-standard';

const TimeSchedule = createJsIcon('time-schedule', iconData);

export default TimeSchedule;

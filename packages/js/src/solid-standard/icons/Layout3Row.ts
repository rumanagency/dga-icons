import { createJsIcon } from '../../createJsIcon';
import { Layout3Row as iconData } from '@dga-icons/core/solid-standard';

const Layout3Row = createJsIcon('layout-3-row', iconData);

export default Layout3Row;

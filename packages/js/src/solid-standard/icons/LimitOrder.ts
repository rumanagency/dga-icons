import { createJsIcon } from '../../createJsIcon';
import { LimitOrder as iconData } from '@dga-icons/core/solid-standard';

const LimitOrder = createJsIcon('limit-order', iconData);

export default LimitOrder;

import { createJsIcon } from '../../createJsIcon';
import { PaintBucket as iconData } from '@dga-icons/core/solid-standard';

const PaintBucket = createJsIcon('paint-bucket', iconData);

export default PaintBucket;

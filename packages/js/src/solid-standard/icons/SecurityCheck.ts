import { createJsIcon } from '../../createJsIcon';
import { SecurityCheck as iconData } from '@dga-icons/core/solid-standard';

const SecurityCheck = createJsIcon('security-check', iconData);

export default SecurityCheck;

import { createJsIcon } from '../../createJsIcon';
import { Upload05 as iconData } from '@dga-icons/core/solid-standard';

const Upload05 = createJsIcon('upload-05', iconData);

export default Upload05;

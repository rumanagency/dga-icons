import { createJsIcon } from '../../createJsIcon';
import { Ambulance as iconData } from '@dga-icons/core/solid-standard';

const Ambulance = createJsIcon('ambulance', iconData);

export default Ambulance;

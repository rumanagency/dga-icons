import { createJsIcon } from '../../createJsIcon';
import { Whatsapp as iconData } from '@dga-icons/core/solid-standard';

const Whatsapp = createJsIcon('whatsapp', iconData);

export default Whatsapp;

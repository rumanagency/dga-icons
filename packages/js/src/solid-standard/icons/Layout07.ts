import { createJsIcon } from '../../createJsIcon';
import { Layout07 as iconData } from '@dga-icons/core/solid-standard';

const Layout07 = createJsIcon('layout-07', iconData);

export default Layout07;

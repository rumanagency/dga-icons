import { createJsIcon } from '../../createJsIcon';
import { Conversation as iconData } from '@dga-icons/core/solid-standard';

const Conversation = createJsIcon('conversation', iconData);

export default Conversation;

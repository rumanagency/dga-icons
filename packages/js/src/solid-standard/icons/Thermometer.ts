import { createJsIcon } from '../../createJsIcon';
import { Thermometer as iconData } from '@dga-icons/core/solid-standard';

const Thermometer = createJsIcon('thermometer', iconData);

export default Thermometer;

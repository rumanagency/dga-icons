import { createJsIcon } from '../../createJsIcon';
import { SearchRemove as iconData } from '@dga-icons/core/solid-standard';

const SearchRemove = createJsIcon('search-remove', iconData);

export default SearchRemove;

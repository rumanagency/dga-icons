import { createJsIcon } from '../../createJsIcon';
import { ChartScatter as iconData } from '@dga-icons/core/solid-standard';

const ChartScatter = createJsIcon('chart-scatter', iconData);

export default ChartScatter;

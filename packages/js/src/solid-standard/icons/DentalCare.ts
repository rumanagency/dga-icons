import { createJsIcon } from '../../createJsIcon';
import { DentalCare as iconData } from '@dga-icons/core/solid-standard';

const DentalCare = createJsIcon('dental-care', iconData);

export default DentalCare;

import { createJsIcon } from '../../createJsIcon';
import { PoundSend as iconData } from '@dga-icons/core/solid-standard';

const PoundSend = createJsIcon('pound-send', iconData);

export default PoundSend;

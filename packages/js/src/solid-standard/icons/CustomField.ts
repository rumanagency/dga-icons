import { createJsIcon } from '../../createJsIcon';
import { CustomField as iconData } from '@dga-icons/core/solid-standard';

const CustomField = createJsIcon('custom-field', iconData);

export default CustomField;

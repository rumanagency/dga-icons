import { createJsIcon } from '../../createJsIcon';
import { Hourglass as iconData } from '@dga-icons/core/solid-standard';

const Hourglass = createJsIcon('hourglass', iconData);

export default Hourglass;

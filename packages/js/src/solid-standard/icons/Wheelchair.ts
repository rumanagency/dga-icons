import { createJsIcon } from '../../createJsIcon';
import { Wheelchair as iconData } from '@dga-icons/core/solid-standard';

const Wheelchair = createJsIcon('wheelchair', iconData);

export default Wheelchair;

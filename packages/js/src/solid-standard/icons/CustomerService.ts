import { createJsIcon } from '../../createJsIcon';
import { CustomerService as iconData } from '@dga-icons/core/solid-standard';

const CustomerService = createJsIcon('customer-service', iconData);

export default CustomerService;

import { createJsIcon } from '../../createJsIcon';
import { NodeMoveUp as iconData } from '@dga-icons/core/solid-standard';

const NodeMoveUp = createJsIcon('node-move-up', iconData);

export default NodeMoveUp;

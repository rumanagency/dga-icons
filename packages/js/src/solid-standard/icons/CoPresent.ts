import { createJsIcon } from '../../createJsIcon';
import { CoPresent as iconData } from '@dga-icons/core/solid-standard';

const CoPresent = createJsIcon('co-present', iconData);

export default CoPresent;

import { createJsIcon } from '../../createJsIcon';
import { TransitionTop as iconData } from '@dga-icons/core/solid-standard';

const TransitionTop = createJsIcon('transition-top', iconData);

export default TransitionTop;

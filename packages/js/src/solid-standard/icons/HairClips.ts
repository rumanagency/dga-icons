import { createJsIcon } from '../../createJsIcon';
import { HairClips as iconData } from '@dga-icons/core/solid-standard';

const HairClips = createJsIcon('hair-clips', iconData);

export default HairClips;

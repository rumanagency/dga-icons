import { createJsIcon } from '../../createJsIcon';
import { UniversalAccessCircle as iconData } from '@dga-icons/core/solid-standard';

const UniversalAccessCircle = createJsIcon('universal-access-circle', iconData);

export default UniversalAccessCircle;

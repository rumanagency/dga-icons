import { createJsIcon } from '../../createJsIcon';
import { FlimSlate as iconData } from '@dga-icons/core/solid-standard';

const FlimSlate = createJsIcon('flim-slate', iconData);

export default FlimSlate;

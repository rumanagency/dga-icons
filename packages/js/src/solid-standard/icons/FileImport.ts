import { createJsIcon } from '../../createJsIcon';
import { FileImport as iconData } from '@dga-icons/core/solid-standard';

const FileImport = createJsIcon('file-import', iconData);

export default FileImport;

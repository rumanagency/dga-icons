import { createJsIcon } from '../../createJsIcon';
import { FilterAdd as iconData } from '@dga-icons/core/solid-standard';

const FilterAdd = createJsIcon('filter-add', iconData);

export default FilterAdd;

import { createJsIcon } from '../../createJsIcon';
import { PackageAdd as iconData } from '@dga-icons/core/solid-standard';

const PackageAdd = createJsIcon('package-add', iconData);

export default PackageAdd;

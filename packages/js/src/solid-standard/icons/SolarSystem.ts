import { createJsIcon } from '../../createJsIcon';
import { SolarSystem as iconData } from '@dga-icons/core/solid-standard';

const SolarSystem = createJsIcon('solar-system', iconData);

export default SolarSystem;

import { createJsIcon } from '../../createJsIcon';
import { CommandLine as iconData } from '@dga-icons/core/solid-standard';

const CommandLine = createJsIcon('command-line', iconData);

export default CommandLine;

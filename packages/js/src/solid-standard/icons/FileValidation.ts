import { createJsIcon } from '../../createJsIcon';
import { FileValidation as iconData } from '@dga-icons/core/solid-standard';

const FileValidation = createJsIcon('file-validation', iconData);

export default FileValidation;

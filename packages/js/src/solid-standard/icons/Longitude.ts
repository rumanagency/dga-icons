import { createJsIcon } from '../../createJsIcon';
import { Longitude as iconData } from '@dga-icons/core/solid-standard';

const Longitude = createJsIcon('longitude', iconData);

export default Longitude;

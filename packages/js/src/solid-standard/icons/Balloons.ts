import { createJsIcon } from '../../createJsIcon';
import { Balloons as iconData } from '@dga-icons/core/solid-standard';

const Balloons = createJsIcon('balloons', iconData);

export default Balloons;

import { createJsIcon } from '../../createJsIcon';
import { Senseless as iconData } from '@dga-icons/core/solid-standard';

const Senseless = createJsIcon('senseless', iconData);

export default Senseless;

import { createJsIcon } from '../../createJsIcon';
import { Keyboard as iconData } from '@dga-icons/core/solid-standard';

const Keyboard = createJsIcon('keyboard', iconData);

export default Keyboard;

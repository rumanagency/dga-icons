import { createJsIcon } from '../../createJsIcon';
import { Lighthouse as iconData } from '@dga-icons/core/solid-standard';

const Lighthouse = createJsIcon('lighthouse', iconData);

export default Lighthouse;

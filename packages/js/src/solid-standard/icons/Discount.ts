import { createJsIcon } from '../../createJsIcon';
import { Discount as iconData } from '@dga-icons/core/solid-standard';

const Discount = createJsIcon('discount', iconData);

export default Discount;

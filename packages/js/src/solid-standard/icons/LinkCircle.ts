import { createJsIcon } from '../../createJsIcon';
import { LinkCircle as iconData } from '@dga-icons/core/solid-standard';

const LinkCircle = createJsIcon('link-circle', iconData);

export default LinkCircle;

import { createJsIcon } from '../../createJsIcon';
import { StrokeCenter as iconData } from '@dga-icons/core/solid-standard';

const StrokeCenter = createJsIcon('stroke-center', iconData);

export default StrokeCenter;

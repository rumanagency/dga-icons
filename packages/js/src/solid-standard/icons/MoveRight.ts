import { createJsIcon } from '../../createJsIcon';
import { MoveRight as iconData } from '@dga-icons/core/solid-standard';

const MoveRight = createJsIcon('move-right', iconData);

export default MoveRight;

import { createJsIcon } from '../../createJsIcon';
import { AlignBottom as iconData } from '@dga-icons/core/solid-standard';

const AlignBottom = createJsIcon('align-bottom', iconData);

export default AlignBottom;

import { createJsIcon } from '../../createJsIcon';
import { ImageActualSize as iconData } from '@dga-icons/core/solid-standard';

const ImageActualSize = createJsIcon('image-actual-size', iconData);

export default ImageActualSize;

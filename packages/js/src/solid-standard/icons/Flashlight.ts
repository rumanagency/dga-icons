import { createJsIcon } from '../../createJsIcon';
import { Flashlight as iconData } from '@dga-icons/core/solid-standard';

const Flashlight = createJsIcon('flashlight', iconData);

export default Flashlight;

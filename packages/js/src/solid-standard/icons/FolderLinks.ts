import { createJsIcon } from '../../createJsIcon';
import { FolderLinks as iconData } from '@dga-icons/core/solid-standard';

const FolderLinks = createJsIcon('folder-links', iconData);

export default FolderLinks;

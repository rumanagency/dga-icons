import { createJsIcon } from '../../createJsIcon';
import { Campfire as iconData } from '@dga-icons/core/solid-standard';

const Campfire = createJsIcon('campfire', iconData);

export default Campfire;

import { createJsIcon } from '../../createJsIcon';
import { EaseInControlPoint as iconData } from '@dga-icons/core/solid-standard';

const EaseInControlPoint = createJsIcon('ease-in-control-point', iconData);

export default EaseInControlPoint;

import { createJsIcon } from '../../createJsIcon';
import { Accident as iconData } from '@dga-icons/core/solid-standard';

const Accident = createJsIcon('accident', iconData);

export default Accident;

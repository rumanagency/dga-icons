import { createJsIcon } from '../../createJsIcon';
import { TestTube as iconData } from '@dga-icons/core/solid-standard';

const TestTube = createJsIcon('test-tube', iconData);

export default TestTube;

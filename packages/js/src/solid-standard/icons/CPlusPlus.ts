import { createJsIcon } from '../../createJsIcon';
import { CPlusPlus as iconData } from '@dga-icons/core/solid-standard';

const CPlusPlus = createJsIcon('c++', iconData);

export default CPlusPlus;

import { createJsIcon } from '../../createJsIcon';
import { TargetDollar as iconData } from '@dga-icons/core/solid-standard';

const TargetDollar = createJsIcon('target-dollar', iconData);

export default TargetDollar;

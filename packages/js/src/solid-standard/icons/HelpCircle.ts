import { createJsIcon } from '../../createJsIcon';
import { HelpCircle as iconData } from '@dga-icons/core/solid-standard';

const HelpCircle = createJsIcon('help-circle', iconData);

export default HelpCircle;

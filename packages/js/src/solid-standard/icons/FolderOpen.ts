import { createJsIcon } from '../../createJsIcon';
import { FolderOpen as iconData } from '@dga-icons/core/solid-standard';

const FolderOpen = createJsIcon('folder-open', iconData);

export default FolderOpen;

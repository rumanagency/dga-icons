import { createJsIcon } from '../../createJsIcon';
import { AddressBook as iconData } from '@dga-icons/core/solid-standard';

const AddressBook = createJsIcon('address-book', iconData);

export default AddressBook;

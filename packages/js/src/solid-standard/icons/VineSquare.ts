import { createJsIcon } from '../../createJsIcon';
import { VineSquare as iconData } from '@dga-icons/core/solid-standard';

const VineSquare = createJsIcon('vine-square', iconData);

export default VineSquare;

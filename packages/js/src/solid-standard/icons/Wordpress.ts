import { createJsIcon } from '../../createJsIcon';
import { Wordpress as iconData } from '@dga-icons/core/solid-standard';

const Wordpress = createJsIcon('wordpress', iconData);

export default Wordpress;

import { createJsIcon } from '../../createJsIcon';
import { Submerge as iconData } from '@dga-icons/core/solid-standard';

const Submerge = createJsIcon('submerge', iconData);

export default Submerge;

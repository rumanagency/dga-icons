import { createJsIcon } from '../../createJsIcon';
import { Pentagon as iconData } from '@dga-icons/core/solid-standard';

const Pentagon = createJsIcon('pentagon', iconData);

export default Pentagon;

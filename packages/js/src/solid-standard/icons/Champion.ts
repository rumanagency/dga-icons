import { createJsIcon } from '../../createJsIcon';
import { Champion as iconData } from '@dga-icons/core/solid-standard';

const Champion = createJsIcon('champion', iconData);

export default Champion;

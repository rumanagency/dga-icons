import { createJsIcon } from '../../createJsIcon';
import { Question as iconData } from '@dga-icons/core/solid-standard';

const Question = createJsIcon('question', iconData);

export default Question;

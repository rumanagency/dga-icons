import { createJsIcon } from '../../createJsIcon';
import { FilterReset as iconData } from '@dga-icons/core/solid-standard';

const FilterReset = createJsIcon('filter-reset', iconData);

export default FilterReset;

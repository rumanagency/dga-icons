import { createJsIcon } from '../../createJsIcon';
import { Pipeline as iconData } from '@dga-icons/core/solid-standard';

const Pipeline = createJsIcon('pipeline', iconData);

export default Pipeline;

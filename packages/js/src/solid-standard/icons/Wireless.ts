import { createJsIcon } from '../../createJsIcon';
import { Wireless as iconData } from '@dga-icons/core/solid-standard';

const Wireless = createJsIcon('wireless', iconData);

export default Wireless;

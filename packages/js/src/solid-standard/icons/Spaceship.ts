import { createJsIcon } from '../../createJsIcon';
import { Spaceship as iconData } from '@dga-icons/core/solid-standard';

const Spaceship = createJsIcon('spaceship', iconData);

export default Spaceship;

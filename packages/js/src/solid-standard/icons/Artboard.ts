import { createJsIcon } from '../../createJsIcon';
import { Artboard as iconData } from '@dga-icons/core/solid-standard';

const Artboard = createJsIcon('artboard', iconData);

export default Artboard;

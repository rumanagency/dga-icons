import { createJsIcon } from '../../createJsIcon';
import { Security as iconData } from '@dga-icons/core/solid-standard';

const Security = createJsIcon('security', iconData);

export default Security;

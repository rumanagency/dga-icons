import { createJsIcon } from '../../createJsIcon';
import { MarketOrder as iconData } from '@dga-icons/core/solid-standard';

const MarketOrder = createJsIcon('market-order', iconData);

export default MarketOrder;

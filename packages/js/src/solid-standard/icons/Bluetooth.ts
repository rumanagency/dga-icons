import { createJsIcon } from '../../createJsIcon';
import { Bluetooth as iconData } from '@dga-icons/core/solid-standard';

const Bluetooth = createJsIcon('bluetooth', iconData);

export default Bluetooth;

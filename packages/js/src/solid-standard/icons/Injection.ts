import { createJsIcon } from '../../createJsIcon';
import { Injection as iconData } from '@dga-icons/core/solid-standard';

const Injection = createJsIcon('injection', iconData);

export default Injection;

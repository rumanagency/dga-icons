import { createJsIcon } from '../../createJsIcon';
import { Transaction as iconData } from '@dga-icons/core/solid-standard';

const Transaction = createJsIcon('transaction', iconData);

export default Transaction;

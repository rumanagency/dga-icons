import { createJsIcon } from '../../createJsIcon';
import { Fahrenheit as iconData } from '@dga-icons/core/solid-standard';

const Fahrenheit = createJsIcon('fahrenheit', iconData);

export default Fahrenheit;

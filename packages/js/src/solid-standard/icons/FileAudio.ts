import { createJsIcon } from '../../createJsIcon';
import { FileAudio as iconData } from '@dga-icons/core/solid-standard';

const FileAudio = createJsIcon('file-audio', iconData);

export default FileAudio;

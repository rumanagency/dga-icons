import { createJsIcon } from '../../createJsIcon';
import { NewOffice as iconData } from '@dga-icons/core/solid-standard';

const NewOffice = createJsIcon('new-office', iconData);

export default NewOffice;

import { createJsIcon } from '../../createJsIcon';
import { Translation as iconData } from '@dga-icons/core/solid-standard';

const Translation = createJsIcon('translation', iconData);

export default Translation;

import { createJsIcon } from '../../createJsIcon';
import { Coinbase as iconData } from '@dga-icons/core/solid-standard';

const Coinbase = createJsIcon('coinbase', iconData);

export default Coinbase;

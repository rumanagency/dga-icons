import { createJsIcon } from '../../createJsIcon';
import { Steering as iconData } from '@dga-icons/core/solid-standard';

const Steering = createJsIcon('steering', iconData);

export default Steering;

import { createJsIcon } from '../../createJsIcon';
import { WifiLowSignal as iconData } from '@dga-icons/core/solid-standard';

const WifiLowSignal = createJsIcon('wifi-low-signal', iconData);

export default WifiLowSignal;

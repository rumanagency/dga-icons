import { createJsIcon } from '../../createJsIcon';
import { Signature as iconData } from '@dga-icons/core/solid-standard';

const Signature = createJsIcon('signature', iconData);

export default Signature;

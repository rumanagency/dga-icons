import { createJsIcon } from '../../createJsIcon';
import { Promotion as iconData } from '@dga-icons/core/solid-standard';

const Promotion = createJsIcon('promotion', iconData);

export default Promotion;

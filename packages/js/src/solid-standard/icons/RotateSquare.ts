import { createJsIcon } from '../../createJsIcon';
import { RotateSquare as iconData } from '@dga-icons/core/solid-standard';

const RotateSquare = createJsIcon('rotate-square', iconData);

export default RotateSquare;

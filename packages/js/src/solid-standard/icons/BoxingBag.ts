import { createJsIcon } from '../../createJsIcon';
import { BoxingBag as iconData } from '@dga-icons/core/solid-standard';

const BoxingBag = createJsIcon('boxing-bag', iconData);

export default BoxingBag;

import { createJsIcon } from '../../createJsIcon';
import { Calculator as iconData } from '@dga-icons/core/solid-sharp';

const Calculator = createJsIcon('calculator', iconData);

export default Calculator;

import { createJsIcon } from '../../createJsIcon';
import { CommandLine as iconData } from '@dga-icons/core/solid-sharp';

const CommandLine = createJsIcon('command-line', iconData);

export default CommandLine;

import { createJsIcon } from '../../createJsIcon';
import { ShipmentTracking as iconData } from '@dga-icons/core/solid-sharp';

const ShipmentTracking = createJsIcon('shipment-tracking', iconData);

export default ShipmentTracking;

import { createJsIcon } from '../../createJsIcon';
import { TextKerning as iconData } from '@dga-icons/core/solid-sharp';

const TextKerning = createJsIcon('text-kerning', iconData);

export default TextKerning;

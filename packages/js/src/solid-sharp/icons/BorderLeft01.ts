import { createJsIcon } from '../../createJsIcon';
import { BorderLeft01 as iconData } from '@dga-icons/core/solid-sharp';

const BorderLeft01 = createJsIcon('border-left-01', iconData);

export default BorderLeft01;

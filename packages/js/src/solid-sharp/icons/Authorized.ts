import { createJsIcon } from '../../createJsIcon';
import { Authorized as iconData } from '@dga-icons/core/solid-sharp';

const Authorized = createJsIcon('authorized', iconData);

export default Authorized;

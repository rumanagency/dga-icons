import { createJsIcon } from '../../createJsIcon';
import { Validation as iconData } from '@dga-icons/core/solid-sharp';

const Validation = createJsIcon('validation', iconData);

export default Validation;

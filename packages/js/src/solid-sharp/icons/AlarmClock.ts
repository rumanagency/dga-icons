import { createJsIcon } from '../../createJsIcon';
import { AlarmClock as iconData } from '@dga-icons/core/solid-sharp';

const AlarmClock = createJsIcon('alarm-clock', iconData);

export default AlarmClock;

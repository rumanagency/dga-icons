import { createJsIcon } from '../../createJsIcon';
import { FloppyDisk as iconData } from '@dga-icons/core/solid-sharp';

const FloppyDisk = createJsIcon('floppy-disk', iconData);

export default FloppyDisk;

import { createJsIcon } from '../../createJsIcon';
import { FolderImport as iconData } from '@dga-icons/core/solid-sharp';

const FolderImport = createJsIcon('folder-import', iconData);

export default FolderImport;

import { createJsIcon } from '../../createJsIcon';
import { Libraries as iconData } from '@dga-icons/core/solid-sharp';

const Libraries = createJsIcon('libraries', iconData);

export default Libraries;

import { createJsIcon } from '../../createJsIcon';
import { StructureFail as iconData } from '@dga-icons/core/solid-sharp';

const StructureFail = createJsIcon('structure-fail', iconData);

export default StructureFail;

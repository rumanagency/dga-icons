import { createJsIcon } from '../../createJsIcon';
import { ChartRadar as iconData } from '@dga-icons/core/solid-sharp';

const ChartRadar = createJsIcon('chart-radar', iconData);

export default ChartRadar;

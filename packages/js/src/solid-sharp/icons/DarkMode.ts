import { createJsIcon } from '../../createJsIcon';
import { DarkMode as iconData } from '@dga-icons/core/solid-sharp';

const DarkMode = createJsIcon('dark-mode', iconData);

export default DarkMode;

import { createJsIcon } from '../../createJsIcon';
import { Clipboard as iconData } from '@dga-icons/core/solid-sharp';

const Clipboard = createJsIcon('clipboard', iconData);

export default Clipboard;

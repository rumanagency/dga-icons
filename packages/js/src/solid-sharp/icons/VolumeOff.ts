import { createJsIcon } from '../../createJsIcon';
import { VolumeOff as iconData } from '@dga-icons/core/solid-sharp';

const VolumeOff = createJsIcon('volume-off', iconData);

export default VolumeOff;

import { createJsIcon } from '../../createJsIcon';
import { LicensePin as iconData } from '@dga-icons/core/solid-sharp';

const LicensePin = createJsIcon('license-pin', iconData);

export default LicensePin;

import { createJsIcon } from '../../createJsIcon';
import { LoginMethod as iconData } from '@dga-icons/core/solid-sharp';

const LoginMethod = createJsIcon('login-method', iconData);

export default LoginMethod;

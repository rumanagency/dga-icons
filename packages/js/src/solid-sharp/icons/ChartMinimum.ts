import { createJsIcon } from '../../createJsIcon';
import { ChartMinimum as iconData } from '@dga-icons/core/solid-sharp';

const ChartMinimum = createJsIcon('chart-minimum', iconData);

export default ChartMinimum;

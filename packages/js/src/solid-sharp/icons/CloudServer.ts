import { createJsIcon } from '../../createJsIcon';
import { CloudServer as iconData } from '@dga-icons/core/solid-sharp';

const CloudServer = createJsIcon('cloud-server', iconData);

export default CloudServer;

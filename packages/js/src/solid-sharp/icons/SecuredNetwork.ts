import { createJsIcon } from '../../createJsIcon';
import { SecuredNetwork as iconData } from '@dga-icons/core/solid-sharp';

const SecuredNetwork = createJsIcon('secured-network', iconData);

export default SecuredNetwork;

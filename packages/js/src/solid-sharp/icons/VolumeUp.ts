import { createJsIcon } from '../../createJsIcon';
import { VolumeUp as iconData } from '@dga-icons/core/solid-sharp';

const VolumeUp = createJsIcon('volume-up', iconData);

export default VolumeUp;

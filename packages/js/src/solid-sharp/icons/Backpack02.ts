import { createJsIcon } from '../../createJsIcon';
import { Backpack02 as iconData } from '@dga-icons/core/solid-sharp';

const Backpack02 = createJsIcon('backpack-02', iconData);

export default Backpack02;

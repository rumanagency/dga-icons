import { createJsIcon } from '../../createJsIcon';
import { Departement as iconData } from '@dga-icons/core/solid-sharp';

const Departement = createJsIcon('departement', iconData);

export default Departement;

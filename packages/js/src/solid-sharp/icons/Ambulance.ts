import { createJsIcon } from '../../createJsIcon';
import { Ambulance as iconData } from '@dga-icons/core/solid-sharp';

const Ambulance = createJsIcon('ambulance', iconData);

export default Ambulance;

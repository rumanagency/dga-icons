import { createJsIcon } from '../../createJsIcon';
import { FlipPhone as iconData } from '@dga-icons/core/solid-sharp';

const FlipPhone = createJsIcon('flip-phone', iconData);

export default FlipPhone;

import { createJsIcon } from '../../createJsIcon';
import { Anonymous as iconData } from '@dga-icons/core/solid-sharp';

const Anonymous = createJsIcon('anonymous', iconData);

export default Anonymous;

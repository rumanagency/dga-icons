import { createJsIcon } from '../../createJsIcon';
import { GitPullRequest as iconData } from '@dga-icons/core/solid-sharp';

const GitPullRequest = createJsIcon('git-pull-request', iconData);

export default GitPullRequest;

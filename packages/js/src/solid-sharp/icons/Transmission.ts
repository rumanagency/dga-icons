import { createJsIcon } from '../../createJsIcon';
import { Transmission as iconData } from '@dga-icons/core/solid-sharp';

const Transmission = createJsIcon('transmission', iconData);

export default Transmission;

import { createJsIcon } from '../../createJsIcon';
import { FullSignal as iconData } from '@dga-icons/core/solid-sharp';

const FullSignal = createJsIcon('full-signal', iconData);

export default FullSignal;

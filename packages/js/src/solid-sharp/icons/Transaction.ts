import { createJsIcon } from '../../createJsIcon';
import { Transaction as iconData } from '@dga-icons/core/solid-sharp';

const Transaction = createJsIcon('transaction', iconData);

export default Transaction;

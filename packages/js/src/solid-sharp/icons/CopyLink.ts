import { createJsIcon } from '../../createJsIcon';
import { CopyLink as iconData } from '@dga-icons/core/solid-sharp';

const CopyLink = createJsIcon('copy-link', iconData);

export default CopyLink;

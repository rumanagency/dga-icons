import { createJsIcon } from '../../createJsIcon';
import { LayerAdd as iconData } from '@dga-icons/core/solid-sharp';

const LayerAdd = createJsIcon('layer-add', iconData);

export default LayerAdd;

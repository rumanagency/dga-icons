import { createJsIcon } from '../../createJsIcon';
import { Catalogue as iconData } from '@dga-icons/core/solid-sharp';

const Catalogue = createJsIcon('catalogue', iconData);

export default Catalogue;

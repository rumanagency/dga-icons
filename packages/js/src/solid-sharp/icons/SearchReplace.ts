import { createJsIcon } from '../../createJsIcon';
import { SearchReplace as iconData } from '@dga-icons/core/solid-sharp';

const SearchReplace = createJsIcon('search-replace', iconData);

export default SearchReplace;

import { createJsIcon } from '../../createJsIcon';
import { HoldLocked01 as iconData } from '@dga-icons/core/solid-sharp';

const HoldLocked01 = createJsIcon('hold-locked-01', iconData);

export default HoldLocked01;

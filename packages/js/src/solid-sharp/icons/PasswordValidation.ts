import { createJsIcon } from '../../createJsIcon';
import { PasswordValidation as iconData } from '@dga-icons/core/solid-sharp';

const PasswordValidation = createJsIcon('password-validation', iconData);

export default PasswordValidation;

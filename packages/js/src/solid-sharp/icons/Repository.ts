import { createJsIcon } from '../../createJsIcon';
import { Repository as iconData } from '@dga-icons/core/solid-sharp';

const Repository = createJsIcon('repository', iconData);

export default Repository;

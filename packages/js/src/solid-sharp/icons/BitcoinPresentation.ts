import { createJsIcon } from '../../createJsIcon';
import { BitcoinPresentation as iconData } from '@dga-icons/core/solid-sharp';

const BitcoinPresentation = createJsIcon('bitcoin-presentation', iconData);

export default BitcoinPresentation;

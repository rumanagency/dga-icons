import { createJsIcon } from '../../createJsIcon';
import { BitcoinSafe as iconData } from '@dga-icons/core/solid-sharp';

const BitcoinSafe = createJsIcon('bitcoin-safe', iconData);

export default BitcoinSafe;

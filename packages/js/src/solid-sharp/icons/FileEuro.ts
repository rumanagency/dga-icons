import { createJsIcon } from '../../createJsIcon';
import { FileEuro as iconData } from '@dga-icons/core/solid-sharp';

const FileEuro = createJsIcon('file-euro', iconData);

export default FileEuro;

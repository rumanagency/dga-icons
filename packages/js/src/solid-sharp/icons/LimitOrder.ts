import { createJsIcon } from '../../createJsIcon';
import { LimitOrder as iconData } from '@dga-icons/core/solid-sharp';

const LimitOrder = createJsIcon('limit-order', iconData);

export default LimitOrder;

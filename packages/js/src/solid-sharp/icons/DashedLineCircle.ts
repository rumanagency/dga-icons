import { createJsIcon } from '../../createJsIcon';
import { DashedLineCircle as iconData } from '@dga-icons/core/solid-sharp';

const DashedLineCircle = createJsIcon('dashed-line-circle', iconData);

export default DashedLineCircle;

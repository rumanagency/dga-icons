import { createJsIcon } from '../../createJsIcon';
import { TouchLocked01 as iconData } from '@dga-icons/core/solid-sharp';

const TouchLocked01 = createJsIcon('touch-locked-01', iconData);

export default TouchLocked01;

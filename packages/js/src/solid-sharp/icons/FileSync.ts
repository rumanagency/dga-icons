import { createJsIcon } from '../../createJsIcon';
import { FileSync as iconData } from '@dga-icons/core/solid-sharp';

const FileSync = createJsIcon('file-sync', iconData);

export default FileSync;

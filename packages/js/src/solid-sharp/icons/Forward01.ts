import { createJsIcon } from '../../createJsIcon';
import { Forward01 as iconData } from '@dga-icons/core/solid-sharp';

const Forward01 = createJsIcon('forward-01', iconData);

export default Forward01;

import { createJsIcon } from '../../createJsIcon';
import { SelfTransfer as iconData } from '@dga-icons/core/solid-sharp';

const SelfTransfer = createJsIcon('self-transfer', iconData);

export default SelfTransfer;

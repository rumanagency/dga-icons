import { createJsIcon } from '../../createJsIcon';
import { FileCloud as iconData } from '@dga-icons/core/solid-sharp';

const FileCloud = createJsIcon('file-cloud', iconData);

export default FileCloud;

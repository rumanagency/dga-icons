import { createJsIcon } from '../../createJsIcon';
import { MinusSignSquare as iconData } from '@dga-icons/core/solid-sharp';

const MinusSignSquare = createJsIcon('minus-sign-square', iconData);

export default MinusSignSquare;

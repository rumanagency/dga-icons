import { createJsIcon } from '../../createJsIcon';
import { PreferenceHorizontal as iconData } from '@dga-icons/core/solid-sharp';

const PreferenceHorizontal = createJsIcon('preference-horizontal', iconData);

export default PreferenceHorizontal;

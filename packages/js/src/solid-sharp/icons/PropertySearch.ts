import { createJsIcon } from '../../createJsIcon';
import { PropertySearch as iconData } from '@dga-icons/core/solid-sharp';

const PropertySearch = createJsIcon('property-search', iconData);

export default PropertySearch;

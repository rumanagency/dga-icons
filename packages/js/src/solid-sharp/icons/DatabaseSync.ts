import { createJsIcon } from '../../createJsIcon';
import { DatabaseSync as iconData } from '@dga-icons/core/solid-sharp';

const DatabaseSync = createJsIcon('database-sync', iconData);

export default DatabaseSync;

import { createJsIcon } from '../../createJsIcon';
import { FileUnknown as iconData } from '@dga-icons/core/solid-sharp';

const FileUnknown = createJsIcon('file-unknown', iconData);

export default FileUnknown;

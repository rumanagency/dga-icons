import { createJsIcon } from '../../createJsIcon';
import { Promotion as iconData } from '@dga-icons/core/solid-sharp';

const Promotion = createJsIcon('promotion', iconData);

export default Promotion;

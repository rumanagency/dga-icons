import { createJsIcon } from '../../createJsIcon';
import { Complaint as iconData } from '@dga-icons/core/solid-sharp';

const Complaint = createJsIcon('complaint', iconData);

export default Complaint;

import { createJsIcon } from '../../createJsIcon';
import { RotateClockwise as iconData } from '@dga-icons/core/solid-sharp';

const RotateClockwise = createJsIcon('rotate-clockwise', iconData);

export default RotateClockwise;

import { createJsIcon } from '../../createJsIcon';
import { Assignments as iconData } from '@dga-icons/core/solid-sharp';

const Assignments = createJsIcon('assignments', iconData);

export default Assignments;

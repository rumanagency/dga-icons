import { createJsIcon } from '../../createJsIcon';
import { MessageBlocked as iconData } from '@dga-icons/core/solid-sharp';

const MessageBlocked = createJsIcon('message-blocked', iconData);

export default MessageBlocked;

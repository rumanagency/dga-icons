import { createJsIcon } from '../../createJsIcon';
import { Avalanche as iconData } from '@dga-icons/core/solid-sharp';

const Avalanche = createJsIcon('avalanche', iconData);

export default Avalanche;

import { createJsIcon } from '../../createJsIcon';
import { Lighthouse as iconData } from '@dga-icons/core/solid-sharp';

const Lighthouse = createJsIcon('lighthouse', iconData);

export default Lighthouse;

import { createJsIcon } from '../../createJsIcon';
import { CallInternal as iconData } from '@dga-icons/core/solid-sharp';

const CallInternal = createJsIcon('call-internal', iconData);

export default CallInternal;

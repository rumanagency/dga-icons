import { createJsIcon } from '../../createJsIcon';
import { VerticalResize as iconData } from '@dga-icons/core/solid-sharp';

const VerticalResize = createJsIcon('vertical-resize', iconData);

export default VerticalResize;

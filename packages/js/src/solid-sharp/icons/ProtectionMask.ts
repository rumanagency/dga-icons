import { createJsIcon } from '../../createJsIcon';
import { ProtectionMask as iconData } from '@dga-icons/core/solid-sharp';

const ProtectionMask = createJsIcon('protection-mask', iconData);

export default ProtectionMask;

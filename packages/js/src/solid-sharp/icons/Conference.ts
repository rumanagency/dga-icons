import { createJsIcon } from '../../createJsIcon';
import { Conference as iconData } from '@dga-icons/core/solid-sharp';

const Conference = createJsIcon('conference', iconData);

export default Conference;

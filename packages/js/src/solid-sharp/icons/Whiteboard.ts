import { createJsIcon } from '../../createJsIcon';
import { Whiteboard as iconData } from '@dga-icons/core/solid-sharp';

const Whiteboard = createJsIcon('whiteboard', iconData);

export default Whiteboard;

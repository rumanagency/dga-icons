import { createJsIcon } from '../../createJsIcon';
import { ArrowTurnBackward as iconData } from '@dga-icons/core/solid-sharp';

const ArrowTurnBackward = createJsIcon('arrow-turn-backward', iconData);

export default ArrowTurnBackward;

import { createJsIcon } from '../../createJsIcon';
import { FileLink as iconData } from '@dga-icons/core/solid-sharp';

const FileLink = createJsIcon('file-link', iconData);

export default FileLink;

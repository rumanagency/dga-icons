import { createJsIcon } from '../../createJsIcon';
import { ImageRotationClockwise as iconData } from '@dga-icons/core/solid-sharp';

const ImageRotationClockwise = createJsIcon('image-rotation-clockwise', iconData);

export default ImageRotationClockwise;

import { createJsIcon } from '../../createJsIcon';
import { ContentWriting as iconData } from '@dga-icons/core/solid-sharp';

const ContentWriting = createJsIcon('content-writing', iconData);

export default ContentWriting;

import { createJsIcon } from '../../createJsIcon';
import { JoggerPants as iconData } from '@dga-icons/core/duotone-rounded';

const JoggerPants = createJsIcon('jogger-pants', iconData);

export default JoggerPants;

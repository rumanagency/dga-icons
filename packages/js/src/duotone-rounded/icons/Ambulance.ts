import { createJsIcon } from '../../createJsIcon';
import { Ambulance as iconData } from '@dga-icons/core/duotone-rounded';

const Ambulance = createJsIcon('ambulance', iconData);

export default Ambulance;

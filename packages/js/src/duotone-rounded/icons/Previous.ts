import { createJsIcon } from '../../createJsIcon';
import { Previous as iconData } from '@dga-icons/core/duotone-rounded';

const Previous = createJsIcon('previous', iconData);

export default Previous;

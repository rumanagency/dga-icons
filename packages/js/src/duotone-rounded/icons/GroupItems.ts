import { createJsIcon } from '../../createJsIcon';
import { GroupItems as iconData } from '@dga-icons/core/duotone-rounded';

const GroupItems = createJsIcon('group-items', iconData);

export default GroupItems;

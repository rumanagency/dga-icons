import { createJsIcon } from '../../createJsIcon';
import { Bluetooth as iconData } from '@dga-icons/core/duotone-rounded';

const Bluetooth = createJsIcon('bluetooth', iconData);

export default Bluetooth;

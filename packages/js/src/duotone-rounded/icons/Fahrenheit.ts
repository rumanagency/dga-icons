import { createJsIcon } from '../../createJsIcon';
import { Fahrenheit as iconData } from '@dga-icons/core/duotone-rounded';

const Fahrenheit = createJsIcon('fahrenheit', iconData);

export default Fahrenheit;

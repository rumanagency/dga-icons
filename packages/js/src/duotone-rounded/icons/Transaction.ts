import { createJsIcon } from '../../createJsIcon';
import { Transaction as iconData } from '@dga-icons/core/duotone-rounded';

const Transaction = createJsIcon('transaction', iconData);

export default Transaction;

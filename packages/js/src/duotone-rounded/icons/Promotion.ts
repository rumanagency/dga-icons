import { createJsIcon } from '../../createJsIcon';
import { Promotion as iconData } from '@dga-icons/core/duotone-rounded';

const Promotion = createJsIcon('promotion', iconData);

export default Promotion;

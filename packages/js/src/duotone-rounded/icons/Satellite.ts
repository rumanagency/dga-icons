import { createJsIcon } from '../../createJsIcon';
import { Satellite as iconData } from '@dga-icons/core/duotone-rounded';

const Satellite = createJsIcon('satellite', iconData);

export default Satellite;

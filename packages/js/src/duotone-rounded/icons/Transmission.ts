import { createJsIcon } from '../../createJsIcon';
import { Transmission as iconData } from '@dga-icons/core/duotone-rounded';

const Transmission = createJsIcon('transmission', iconData);

export default Transmission;

import { createJsIcon } from '../../createJsIcon';
import { TimeQuarter as iconData } from '@dga-icons/core/duotone-rounded';

const TimeQuarter = createJsIcon('time-quarter', iconData);

export default TimeQuarter;

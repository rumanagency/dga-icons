import { createJsIcon } from '../../createJsIcon';
import { ChartUp as iconData } from '@dga-icons/core/duotone-rounded';

const ChartUp = createJsIcon('chart-up', iconData);

export default ChartUp;

import { createJsIcon } from '../../createJsIcon';
import { FolderDownload as iconData } from '@dga-icons/core/duotone-rounded';

const FolderDownload = createJsIcon('folder-download', iconData);

export default FolderDownload;

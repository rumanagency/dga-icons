import { createJsIcon } from '../../createJsIcon';
import { SidebarBottom as iconData } from '@dga-icons/core/duotone-rounded';

const SidebarBottom = createJsIcon('sidebar-bottom', iconData);

export default SidebarBottom;

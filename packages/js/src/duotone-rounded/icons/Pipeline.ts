import { createJsIcon } from '../../createJsIcon';
import { Pipeline as iconData } from '@dga-icons/core/duotone-rounded';

const Pipeline = createJsIcon('pipeline', iconData);

export default Pipeline;

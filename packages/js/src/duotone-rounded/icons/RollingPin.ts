import { createJsIcon } from '../../createJsIcon';
import { RollingPin as iconData } from '@dga-icons/core/duotone-rounded';

const RollingPin = createJsIcon('rolling-pin', iconData);

export default RollingPin;

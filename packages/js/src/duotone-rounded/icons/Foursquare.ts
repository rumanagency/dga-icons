import { createJsIcon } from '../../createJsIcon';
import { Foursquare as iconData } from '@dga-icons/core/duotone-rounded';

const Foursquare = createJsIcon('foursquare', iconData);

export default Foursquare;

import { createJsIcon } from '../../createJsIcon';
import { TankTop as iconData } from '@dga-icons/core/duotone-rounded';

const TankTop = createJsIcon('tank-top', iconData);

export default TankTop;

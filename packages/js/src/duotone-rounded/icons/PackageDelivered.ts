import { createJsIcon } from '../../createJsIcon';
import { PackageDelivered as iconData } from '@dga-icons/core/duotone-rounded';

const PackageDelivered = createJsIcon('package-delivered', iconData);

export default PackageDelivered;

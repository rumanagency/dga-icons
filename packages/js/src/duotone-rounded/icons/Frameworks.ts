import { createJsIcon } from '../../createJsIcon';
import { Frameworks as iconData } from '@dga-icons/core/duotone-rounded';

const Frameworks = createJsIcon('frameworks', iconData);

export default Frameworks;

import { createJsIcon } from '../../createJsIcon';
import { LanguageCircle as iconData } from '@dga-icons/core/duotone-rounded';

const LanguageCircle = createJsIcon('language-circle', iconData);

export default LanguageCircle;

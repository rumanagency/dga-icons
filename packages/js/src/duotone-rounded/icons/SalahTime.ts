import { createJsIcon } from '../../createJsIcon';
import { SalahTime as iconData } from '@dga-icons/core/duotone-rounded';

const SalahTime = createJsIcon('salah-time', iconData);

export default SalahTime;

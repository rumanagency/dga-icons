import { createJsIcon } from '../../createJsIcon';
import { DatabaseAdd as iconData } from '@dga-icons/core/duotone-rounded';

const DatabaseAdd = createJsIcon('database-add', iconData);

export default DatabaseAdd;

import { createJsIcon } from '../../createJsIcon';
import { FolderView as iconData } from '@dga-icons/core/duotone-rounded';

const FolderView = createJsIcon('folder-view', iconData);

export default FolderView;

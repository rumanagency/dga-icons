import { createJsIcon } from '../../createJsIcon';
import { Packaging as iconData } from '@dga-icons/core/duotone-rounded';

const Packaging = createJsIcon('packaging', iconData);

export default Packaging;

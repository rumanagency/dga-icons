import { createJsIcon } from '../../createJsIcon';
import { SortingUp as iconData } from '@dga-icons/core/duotone-rounded';

const SortingUp = createJsIcon('sorting-up', iconData);

export default SortingUp;

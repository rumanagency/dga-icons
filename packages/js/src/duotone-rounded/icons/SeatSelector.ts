import { createJsIcon } from '../../createJsIcon';
import { SeatSelector as iconData } from '@dga-icons/core/duotone-rounded';

const SeatSelector = createJsIcon('seat-selector', iconData);

export default SeatSelector;

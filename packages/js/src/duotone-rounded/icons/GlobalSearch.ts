import { createJsIcon } from '../../createJsIcon';
import { GlobalSearch as iconData } from '@dga-icons/core/duotone-rounded';

const GlobalSearch = createJsIcon('global-search', iconData);

export default GlobalSearch;

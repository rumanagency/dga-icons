import { createJsIcon } from '../../createJsIcon';
import { CallDisabled as iconData } from '@dga-icons/core/duotone-rounded';

const CallDisabled = createJsIcon('call-disabled', iconData);

export default CallDisabled;

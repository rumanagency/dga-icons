import { createJsIcon } from '../../createJsIcon';
import { ZakatBagClosed as iconData } from '@dga-icons/core/duotone-rounded';

const ZakatBagClosed = createJsIcon('zakat-bag-closed', iconData);

export default ZakatBagClosed;

import { createJsIcon } from '../../createJsIcon';
import { GasPipe as iconData } from '@dga-icons/core/duotone-rounded';

const GasPipe = createJsIcon('gas-pipe', iconData);

export default GasPipe;

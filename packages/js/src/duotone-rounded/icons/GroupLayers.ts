import { createJsIcon } from '../../createJsIcon';
import { GroupLayers as iconData } from '@dga-icons/core/duotone-rounded';

const GroupLayers = createJsIcon('group-layers', iconData);

export default GroupLayers;

import { createJsIcon } from '../../createJsIcon';
import { PoundReceive as iconData } from '@dga-icons/core/duotone-rounded';

const PoundReceive = createJsIcon('pound-receive', iconData);

export default PoundReceive;

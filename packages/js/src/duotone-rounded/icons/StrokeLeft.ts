import { createJsIcon } from '../../createJsIcon';
import { StrokeLeft as iconData } from '@dga-icons/core/duotone-rounded';

const StrokeLeft = createJsIcon('stroke-left', iconData);

export default StrokeLeft;

import { createJsIcon } from '../../createJsIcon';
import { ArrowExpand as iconData } from '@dga-icons/core/duotone-rounded';

const ArrowExpand = createJsIcon('arrow-expand', iconData);

export default ArrowExpand;

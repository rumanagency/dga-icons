import { createJsIcon } from '../../createJsIcon';
import { Hackerrank as iconData } from '@dga-icons/core/duotone-rounded';

const Hackerrank = createJsIcon('hackerrank', iconData);

export default Hackerrank;

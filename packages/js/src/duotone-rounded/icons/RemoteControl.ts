import { createJsIcon } from '../../createJsIcon';
import { RemoteControl as iconData } from '@dga-icons/core/duotone-rounded';

const RemoteControl = createJsIcon('remote-control', iconData);

export default RemoteControl;

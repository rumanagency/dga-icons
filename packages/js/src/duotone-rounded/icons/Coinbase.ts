import { createJsIcon } from '../../createJsIcon';
import { Coinbase as iconData } from '@dga-icons/core/duotone-rounded';

const Coinbase = createJsIcon('coinbase', iconData);

export default Coinbase;

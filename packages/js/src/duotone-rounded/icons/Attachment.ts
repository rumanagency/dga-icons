import { createJsIcon } from '../../createJsIcon';
import { Attachment as iconData } from '@dga-icons/core/duotone-rounded';

const Attachment = createJsIcon('attachment', iconData);

export default Attachment;

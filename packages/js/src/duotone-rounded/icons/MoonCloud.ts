import { createJsIcon } from '../../createJsIcon';
import { MoonCloud as iconData } from '@dga-icons/core/duotone-rounded';

const MoonCloud = createJsIcon('moon-cloud', iconData);

export default MoonCloud;

import { createJsIcon } from '../../createJsIcon';
import { Discount as iconData } from '@dga-icons/core/duotone-rounded';

const Discount = createJsIcon('discount', iconData);

export default Discount;

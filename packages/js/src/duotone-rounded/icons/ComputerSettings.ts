import { createJsIcon } from '../../createJsIcon';
import { ComputerSettings as iconData } from '@dga-icons/core/duotone-rounded';

const ComputerSettings = createJsIcon('computer-settings', iconData);

export default ComputerSettings;

import { createJsIcon } from '../../createJsIcon';
import { BitcoinCloud as iconData } from '@dga-icons/core/duotone-rounded';

const BitcoinCloud = createJsIcon('bitcoin-cloud', iconData);

export default BitcoinCloud;

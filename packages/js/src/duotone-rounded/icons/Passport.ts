import { createJsIcon } from '../../createJsIcon';
import { Passport as iconData } from '@dga-icons/core/duotone-rounded';

const Passport = createJsIcon('passport', iconData);

export default Passport;

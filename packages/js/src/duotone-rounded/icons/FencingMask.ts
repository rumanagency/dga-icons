import { createJsIcon } from '../../createJsIcon';
import { FencingMask as iconData } from '@dga-icons/core/duotone-rounded';

const FencingMask = createJsIcon('fencing-mask', iconData);

export default FencingMask;

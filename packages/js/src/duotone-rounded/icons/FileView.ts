import { createJsIcon } from '../../createJsIcon';
import { FileView as iconData } from '@dga-icons/core/duotone-rounded';

const FileView = createJsIcon('file-view', iconData);

export default FileView;

import { createJsIcon } from '../../createJsIcon';
import { PiSquare as iconData } from '@dga-icons/core/duotone-rounded';

const PiSquare = createJsIcon('pi-square', iconData);

export default PiSquare;

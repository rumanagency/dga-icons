import { createJsIcon } from '../../createJsIcon';
import { CheckList as iconData } from '@dga-icons/core/duotone-rounded';

const CheckList = createJsIcon('check-list', iconData);

export default CheckList;

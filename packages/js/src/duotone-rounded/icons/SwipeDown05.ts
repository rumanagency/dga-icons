import { createJsIcon } from '../../createJsIcon';
import { SwipeDown05 as iconData } from '@dga-icons/core/duotone-rounded';

const SwipeDown05 = createJsIcon('swipe-down-05', iconData);

export default SwipeDown05;

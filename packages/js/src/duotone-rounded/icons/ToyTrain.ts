import { createJsIcon } from '../../createJsIcon';
import { ToyTrain as iconData } from '@dga-icons/core/duotone-rounded';

const ToyTrain = createJsIcon('toy-train', iconData);

export default ToyTrain;

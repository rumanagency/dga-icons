import { createJsIcon } from '../../createJsIcon';
import { DashboardCircleRemove as iconData } from '@dga-icons/core/duotone-rounded';

const DashboardCircleRemove = createJsIcon('dashboard-circle-remove', iconData);

export default DashboardCircleRemove;

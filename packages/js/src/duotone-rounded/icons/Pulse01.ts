import { createJsIcon } from '../../createJsIcon';
import { Pulse01 as iconData } from '@dga-icons/core/duotone-rounded';

const Pulse01 = createJsIcon('pulse-01', iconData);

export default Pulse01;

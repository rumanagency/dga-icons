import { createJsIcon } from '../../createJsIcon';
import { TimeManagementCircle as iconData } from '@dga-icons/core/duotone-rounded';

const TimeManagementCircle = createJsIcon('time-management-circle', iconData);

export default TimeManagementCircle;

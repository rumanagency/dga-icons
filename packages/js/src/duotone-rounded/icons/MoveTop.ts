import { createJsIcon } from '../../createJsIcon';
import { MoveTop as iconData } from '@dga-icons/core/duotone-rounded';

const MoveTop = createJsIcon('move-top', iconData);

export default MoveTop;

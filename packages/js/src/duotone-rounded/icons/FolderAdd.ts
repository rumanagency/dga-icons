import { createJsIcon } from '../../createJsIcon';
import { FolderAdd as iconData } from '@dga-icons/core/duotone-rounded';

const FolderAdd = createJsIcon('folder-add', iconData);

export default FolderAdd;

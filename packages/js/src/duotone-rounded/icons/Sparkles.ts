import { createJsIcon } from '../../createJsIcon';
import { Sparkles as iconData } from '@dga-icons/core/duotone-rounded';

const Sparkles = createJsIcon('sparkles', iconData);

export default Sparkles;

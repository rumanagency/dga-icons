import { createJsIcon } from '../../createJsIcon';
import { PayByCheck as iconData } from '@dga-icons/core/duotone-rounded';

const PayByCheck = createJsIcon('pay-by-check', iconData);

export default PayByCheck;

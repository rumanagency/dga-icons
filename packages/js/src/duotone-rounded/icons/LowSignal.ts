import { createJsIcon } from '../../createJsIcon';
import { LowSignal as iconData } from '@dga-icons/core/duotone-rounded';

const LowSignal = createJsIcon('low-signal', iconData);

export default LowSignal;

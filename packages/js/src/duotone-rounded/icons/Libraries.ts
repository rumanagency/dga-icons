import { createJsIcon } from '../../createJsIcon';
import { Libraries as iconData } from '@dga-icons/core/duotone-rounded';

const Libraries = createJsIcon('libraries', iconData);

export default Libraries;

import { createJsIcon } from '../../createJsIcon';
import { IdentityCard as iconData } from '@dga-icons/core/duotone-rounded';

const IdentityCard = createJsIcon('identity-card', iconData);

export default IdentityCard;

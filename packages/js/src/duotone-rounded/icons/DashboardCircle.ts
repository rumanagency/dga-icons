import { createJsIcon } from '../../createJsIcon';
import { DashboardCircle as iconData } from '@dga-icons/core/duotone-rounded';

const DashboardCircle = createJsIcon('dashboard-circle', iconData);

export default DashboardCircle;

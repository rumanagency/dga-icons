import { createJsIcon } from '../../createJsIcon';
import { FileShredder as iconData } from '@dga-icons/core/duotone-rounded';

const FileShredder = createJsIcon('file-shredder', iconData);

export default FileShredder;

import { createJsIcon } from '../../createJsIcon';
import { Mouse20 as iconData } from '@dga-icons/core/duotone-rounded';

const Mouse20 = createJsIcon('mouse-20', iconData);

export default Mouse20;

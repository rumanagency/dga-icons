import { createJsIcon } from '../../createJsIcon';
import { Searching as iconData } from '@dga-icons/core/duotone-rounded';

const Searching = createJsIcon('searching', iconData);

export default Searching;

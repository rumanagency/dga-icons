import { createJsIcon } from '../../createJsIcon';
import { MaximizeScreen as iconData } from '@dga-icons/core/duotone-rounded';

const MaximizeScreen = createJsIcon('maximize-screen', iconData);

export default MaximizeScreen;

import { createJsIcon } from '../../createJsIcon';
import { CoinsEuro as iconData } from '@dga-icons/core/duotone-rounded';

const CoinsEuro = createJsIcon('coins-euro', iconData);

export default CoinsEuro;

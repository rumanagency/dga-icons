import { createJsIcon } from '../../createJsIcon';
import { PoundSquare as iconData } from '@dga-icons/core/duotone-rounded';

const PoundSquare = createJsIcon('pound-square', iconData);

export default PoundSquare;

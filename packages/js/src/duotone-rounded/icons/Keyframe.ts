import { createJsIcon } from '../../createJsIcon';
import { Keyframe as iconData } from '@dga-icons/core/duotone-rounded';

const Keyframe = createJsIcon('keyframe', iconData);

export default Keyframe;

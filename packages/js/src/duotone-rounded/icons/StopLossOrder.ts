import { createJsIcon } from '../../createJsIcon';
import { StopLossOrder as iconData } from '@dga-icons/core/duotone-rounded';

const StopLossOrder = createJsIcon('stop-loss-order', iconData);

export default StopLossOrder;

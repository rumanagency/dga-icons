import { createJsIcon } from '../../createJsIcon';
import { DrawingMode as iconData } from '@dga-icons/core/duotone-rounded';

const DrawingMode = createJsIcon('drawing-mode', iconData);

export default DrawingMode;

import { createJsIcon } from '../../createJsIcon';
import { VoiceId as iconData } from '@dga-icons/core/duotone-rounded';

const VoiceId = createJsIcon('voice-id', iconData);

export default VoiceId;

import { createJsIcon } from '../../createJsIcon';
import { JobSearch as iconData } from '@dga-icons/core/duotone-rounded';

const JobSearch = createJsIcon('job-search', iconData);

export default JobSearch;

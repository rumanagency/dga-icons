import { createJsIcon } from '../../createJsIcon';
import { FolderCloud as iconData } from '@dga-icons/core/duotone-rounded';

const FolderCloud = createJsIcon('folder-cloud', iconData);

export default FolderCloud;

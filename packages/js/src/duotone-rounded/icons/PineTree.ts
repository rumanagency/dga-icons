import { createJsIcon } from '../../createJsIcon';
import { PineTree as iconData } from '@dga-icons/core/duotone-rounded';

const PineTree = createJsIcon('pine-tree', iconData);

export default PineTree;

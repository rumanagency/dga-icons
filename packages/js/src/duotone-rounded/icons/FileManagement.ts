import { createJsIcon } from '../../createJsIcon';
import { FileManagement as iconData } from '@dga-icons/core/duotone-rounded';

const FileManagement = createJsIcon('file-management', iconData);

export default FileManagement;

import { createJsIcon } from '../../createJsIcon';
import { Firewall as iconData } from '@dga-icons/core/duotone-rounded';

const Firewall = createJsIcon('firewall', iconData);

export default Firewall;

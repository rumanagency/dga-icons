import { createJsIcon } from '../../createJsIcon';
import { UserGroup as iconData } from '@dga-icons/core/duotone-rounded';

const UserGroup = createJsIcon('user-group', iconData);

export default UserGroup;

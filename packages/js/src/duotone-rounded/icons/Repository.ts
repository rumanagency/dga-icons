import { createJsIcon } from '../../createJsIcon';
import { Repository as iconData } from '@dga-icons/core/duotone-rounded';

const Repository = createJsIcon('repository', iconData);

export default Repository;

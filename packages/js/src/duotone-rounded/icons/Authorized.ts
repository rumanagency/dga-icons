import { createJsIcon } from '../../createJsIcon';
import { Authorized as iconData } from '@dga-icons/core/duotone-rounded';

const Authorized = createJsIcon('authorized', iconData);

export default Authorized;

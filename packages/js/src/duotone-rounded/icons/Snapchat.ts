import { createJsIcon } from '../../createJsIcon';
import { Snapchat as iconData } from '@dga-icons/core/duotone-rounded';

const Snapchat = createJsIcon('snapchat', iconData);

export default Snapchat;

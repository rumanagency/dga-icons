import { createJsIcon } from '../../createJsIcon';
import { Highlighter as iconData } from '@dga-icons/core/duotone-rounded';

const Highlighter = createJsIcon('highlighter', iconData);

export default Highlighter;

import { createJsIcon } from '../../createJsIcon';
import { Acceleration as iconData } from '@dga-icons/core/duotone-rounded';

const Acceleration = createJsIcon('acceleration', iconData);

export default Acceleration;

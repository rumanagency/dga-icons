import { createJsIcon } from '../../createJsIcon';
import { Shellfish as iconData } from '@dga-icons/core/duotone-rounded';

const Shellfish = createJsIcon('shellfish', iconData);

export default Shellfish;

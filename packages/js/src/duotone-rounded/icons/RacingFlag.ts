import { createJsIcon } from '../../createJsIcon';
import { RacingFlag as iconData } from '@dga-icons/core/duotone-rounded';

const RacingFlag = createJsIcon('racing-flag', iconData);

export default RacingFlag;

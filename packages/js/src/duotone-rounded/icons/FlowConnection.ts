import { createJsIcon } from '../../createJsIcon';
import { FlowConnection as iconData } from '@dga-icons/core/duotone-rounded';

const FlowConnection = createJsIcon('flow-connection', iconData);

export default FlowConnection;

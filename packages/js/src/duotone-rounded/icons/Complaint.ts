import { createJsIcon } from '../../createJsIcon';
import { Complaint as iconData } from '@dga-icons/core/duotone-rounded';

const Complaint = createJsIcon('complaint', iconData);

export default Complaint;

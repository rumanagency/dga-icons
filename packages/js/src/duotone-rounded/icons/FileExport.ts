import { createJsIcon } from '../../createJsIcon';
import { FileExport as iconData } from '@dga-icons/core/duotone-rounded';

const FileExport = createJsIcon('file-export', iconData);

export default FileExport;

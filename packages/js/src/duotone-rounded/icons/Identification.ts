import { createJsIcon } from '../../createJsIcon';
import { Identification as iconData } from '@dga-icons/core/duotone-rounded';

const Identification = createJsIcon('identification', iconData);

export default Identification;

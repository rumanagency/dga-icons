import { createJsIcon } from '../../createJsIcon';
import { SoftwareUninstall as iconData } from '@dga-icons/core/duotone-rounded';

const SoftwareUninstall = createJsIcon('software-uninstall', iconData);

export default SoftwareUninstall;

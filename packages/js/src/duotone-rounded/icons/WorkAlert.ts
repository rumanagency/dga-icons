import { createJsIcon } from '../../createJsIcon';
import { WorkAlert as iconData } from '@dga-icons/core/duotone-rounded';

const WorkAlert = createJsIcon('work-alert', iconData);

export default WorkAlert;

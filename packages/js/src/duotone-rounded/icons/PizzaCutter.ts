import { createJsIcon } from '../../createJsIcon';
import { PizzaCutter as iconData } from '@dga-icons/core/duotone-rounded';

const PizzaCutter = createJsIcon('pizza-cutter', iconData);

export default PizzaCutter;

import { createJsIcon } from '../../createJsIcon';
import { Grinning as iconData } from '@dga-icons/core/duotone-rounded';

const Grinning = createJsIcon('grinning', iconData);

export default Grinning;

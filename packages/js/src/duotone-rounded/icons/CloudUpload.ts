import { createJsIcon } from '../../createJsIcon';
import { CloudUpload as iconData } from '@dga-icons/core/duotone-rounded';

const CloudUpload = createJsIcon('cloud-upload', iconData);

export default CloudUpload;

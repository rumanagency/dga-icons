import { createJsIcon } from '../../createJsIcon';
import { GiftCard as iconData } from '@dga-icons/core/duotone-rounded';

const GiftCard = createJsIcon('gift-card', iconData);

export default GiftCard;

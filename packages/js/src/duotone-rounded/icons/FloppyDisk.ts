import { createJsIcon } from '../../createJsIcon';
import { FloppyDisk as iconData } from '@dga-icons/core/duotone-rounded';

const FloppyDisk = createJsIcon('floppy-disk', iconData);

export default FloppyDisk;

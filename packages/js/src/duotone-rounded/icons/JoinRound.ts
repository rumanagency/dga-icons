import { createJsIcon } from '../../createJsIcon';
import { JoinRound as iconData } from '@dga-icons/core/duotone-rounded';

const JoinRound = createJsIcon('join-round', iconData);

export default JoinRound;

import { createJsIcon } from '../../createJsIcon';
import { FilterEdit as iconData } from '@dga-icons/core/duotone-rounded';

const FilterEdit = createJsIcon('filter-edit', iconData);

export default FilterEdit;

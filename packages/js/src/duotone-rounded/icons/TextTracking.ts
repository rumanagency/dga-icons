import { createJsIcon } from '../../createJsIcon';
import { TextTracking as iconData } from '@dga-icons/core/duotone-rounded';

const TextTracking = createJsIcon('text-tracking', iconData);

export default TextTracking;

import { createJsIcon } from '../../createJsIcon';
import { Assignments as iconData } from '@dga-icons/core/duotone-rounded';

const Assignments = createJsIcon('assignments', iconData);

export default Assignments;

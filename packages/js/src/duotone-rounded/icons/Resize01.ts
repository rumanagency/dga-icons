import { createJsIcon } from '../../createJsIcon';
import { Resize01 as iconData } from '@dga-icons/core/duotone-rounded';

const Resize01 = createJsIcon('resize-01', iconData);

export default Resize01;

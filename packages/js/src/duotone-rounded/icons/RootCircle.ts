import { createJsIcon } from '../../createJsIcon';
import { RootCircle as iconData } from '@dga-icons/core/duotone-rounded';

const RootCircle = createJsIcon('root-circle', iconData);

export default RootCircle;

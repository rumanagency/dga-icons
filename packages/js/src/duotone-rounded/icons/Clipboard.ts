import { createJsIcon } from '../../createJsIcon';
import { Clipboard as iconData } from '@dga-icons/core/duotone-rounded';

const Clipboard = createJsIcon('clipboard', iconData);

export default Clipboard;

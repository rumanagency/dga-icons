import { createJsIcon } from '../../createJsIcon';
import { FlipRight as iconData } from '@dga-icons/core/duotone-rounded';

const FlipRight = createJsIcon('flip-right', iconData);

export default FlipRight;

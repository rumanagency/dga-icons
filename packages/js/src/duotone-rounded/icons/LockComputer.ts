import { createJsIcon } from '../../createJsIcon';
import { LockComputer as iconData } from '@dga-icons/core/duotone-rounded';

const LockComputer = createJsIcon('lock-computer', iconData);

export default LockComputer;

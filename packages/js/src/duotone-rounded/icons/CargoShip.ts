import { createJsIcon } from '../../createJsIcon';
import { CargoShip as iconData } from '@dga-icons/core/duotone-rounded';

const CargoShip = createJsIcon('cargo-ship', iconData);

export default CargoShip;

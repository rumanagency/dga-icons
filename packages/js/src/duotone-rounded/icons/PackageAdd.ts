import { createJsIcon } from '../../createJsIcon';
import { PackageAdd as iconData } from '@dga-icons/core/duotone-rounded';

const PackageAdd = createJsIcon('package-add', iconData);

export default PackageAdd;

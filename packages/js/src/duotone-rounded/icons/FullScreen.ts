import { createJsIcon } from '../../createJsIcon';
import { FullScreen as iconData } from '@dga-icons/core/duotone-rounded';

const FullScreen = createJsIcon('full-screen', iconData);

export default FullScreen;

import { createJsIcon } from '../../createJsIcon';
import { Ethereum as iconData } from '@dga-icons/core/duotone-rounded';

const Ethereum = createJsIcon('ethereum', iconData);

export default Ethereum;

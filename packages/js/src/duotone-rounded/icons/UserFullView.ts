import { createJsIcon } from '../../createJsIcon';
import { UserFullView as iconData } from '@dga-icons/core/duotone-rounded';

const UserFullView = createJsIcon('user-full-view', iconData);

export default UserFullView;

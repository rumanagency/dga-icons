import { createJsIcon } from '../../createJsIcon';
import { AirplayLine as iconData } from '@dga-icons/core/duotone-rounded';

const AirplayLine = createJsIcon('airplay-line', iconData);

export default AirplayLine;

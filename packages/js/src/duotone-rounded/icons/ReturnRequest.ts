import { createJsIcon } from '../../createJsIcon';
import { ReturnRequest as iconData } from '@dga-icons/core/duotone-rounded';

const ReturnRequest = createJsIcon('return-request', iconData);

export default ReturnRequest;

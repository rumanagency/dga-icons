import { createJsIcon } from '../../createJsIcon';
import { Brochure as iconData } from '@dga-icons/core/duotone-rounded';

const Brochure = createJsIcon('brochure', iconData);

export default Brochure;

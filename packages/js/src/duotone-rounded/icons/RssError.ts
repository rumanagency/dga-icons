import { createJsIcon } from '../../createJsIcon';
import { RssError as iconData } from '@dga-icons/core/duotone-rounded';

const RssError = createJsIcon('rss-error', iconData);

export default RssError;

import { createJsIcon } from '../../createJsIcon';
import { Latitude as iconData } from '@dga-icons/core/duotone-rounded';

const Latitude = createJsIcon('latitude', iconData);

export default Latitude;

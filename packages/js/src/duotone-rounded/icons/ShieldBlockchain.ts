import { createJsIcon } from '../../createJsIcon';
import { ShieldBlockchain as iconData } from '@dga-icons/core/duotone-rounded';

const ShieldBlockchain = createJsIcon('shield-blockchain', iconData);

export default ShieldBlockchain;

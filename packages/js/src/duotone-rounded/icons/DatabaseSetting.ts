import { createJsIcon } from '../../createJsIcon';
import { DatabaseSetting as iconData } from '@dga-icons/core/duotone-rounded';

const DatabaseSetting = createJsIcon('database-setting', iconData);

export default DatabaseSetting;

import { createJsIcon } from '../../createJsIcon';
import { LoginMethod as iconData } from '@dga-icons/core/duotone-rounded';

const LoginMethod = createJsIcon('login-method', iconData);

export default LoginMethod;

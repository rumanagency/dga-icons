import { createJsIcon } from '../../createJsIcon';
import { CodeSquare as iconData } from '@dga-icons/core/duotone-rounded';

const CodeSquare = createJsIcon('code-square', iconData);

export default CodeSquare;

import { createJsIcon } from '../../createJsIcon';
import { FolderDetails as iconData } from '@dga-icons/core/duotone-rounded';

const FolderDetails = createJsIcon('folder-details', iconData);

export default FolderDetails;

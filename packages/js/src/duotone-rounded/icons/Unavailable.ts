import { createJsIcon } from '../../createJsIcon';
import { Unavailable as iconData } from '@dga-icons/core/duotone-rounded';

const Unavailable = createJsIcon('unavailable', iconData);

export default Unavailable;

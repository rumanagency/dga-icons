import { createJsIcon } from '../../createJsIcon';
import { Catalogue as iconData } from '@dga-icons/core/duotone-rounded';

const Catalogue = createJsIcon('catalogue', iconData);

export default Catalogue;

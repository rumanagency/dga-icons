import { createJsIcon } from '../../createJsIcon';
import { MenuCollapse as iconData } from '@dga-icons/core/duotone-rounded';

const MenuCollapse = createJsIcon('menu-collapse', iconData);

export default MenuCollapse;

import { createJsIcon } from '../../createJsIcon';
import { NoteEdit as iconData } from '@dga-icons/core/duotone-rounded';

const NoteEdit = createJsIcon('note-edit', iconData);

export default NoteEdit;

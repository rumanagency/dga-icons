import { createJsIcon } from '../../createJsIcon';
import { PinCode as iconData } from '@dga-icons/core/duotone-rounded';

const PinCode = createJsIcon('pin-code', iconData);

export default PinCode;

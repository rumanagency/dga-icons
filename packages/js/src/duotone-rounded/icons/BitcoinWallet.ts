import { createJsIcon } from '../../createJsIcon';
import { BitcoinWallet as iconData } from '@dga-icons/core/duotone-rounded';

const BitcoinWallet = createJsIcon('bitcoin-wallet', iconData);

export default BitcoinWallet;

import { createJsIcon } from '../../createJsIcon';
import { CallLocked as iconData } from '@dga-icons/core/duotone-rounded';

const CallLocked = createJsIcon('call-locked', iconData);

export default CallLocked;

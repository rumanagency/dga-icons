import { createJsIcon } from '../../createJsIcon';
import { LicenseNo as iconData } from '@dga-icons/core/duotone-rounded';

const LicenseNo = createJsIcon('license-no', iconData);

export default LicenseNo;

import { createJsIcon } from '../../createJsIcon';
import { DatabaseExport as iconData } from '@dga-icons/core/duotone-rounded';

const DatabaseExport = createJsIcon('database-export', iconData);

export default DatabaseExport;

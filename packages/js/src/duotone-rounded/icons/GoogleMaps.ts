import { createJsIcon } from '../../createJsIcon';
import { GoogleMaps as iconData } from '@dga-icons/core/duotone-rounded';

const GoogleMaps = createJsIcon('google-maps', iconData);

export default GoogleMaps;

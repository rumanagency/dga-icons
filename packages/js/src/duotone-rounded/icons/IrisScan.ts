import { createJsIcon } from '../../createJsIcon';
import { IrisScan as iconData } from '@dga-icons/core/duotone-rounded';

const IrisScan = createJsIcon('iris-scan', iconData);

export default IrisScan;

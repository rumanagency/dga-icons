import { createJsIcon } from '../../createJsIcon';
import { GoogleSheet as iconData } from '@dga-icons/core/duotone-rounded';

const GoogleSheet = createJsIcon('google-sheet', iconData);

export default GoogleSheet;

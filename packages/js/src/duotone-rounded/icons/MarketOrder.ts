import { createJsIcon } from '../../createJsIcon';
import { MarketOrder as iconData } from '@dga-icons/core/duotone-rounded';

const MarketOrder = createJsIcon('market-order', iconData);

export default MarketOrder;

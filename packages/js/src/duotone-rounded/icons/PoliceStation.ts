import { createJsIcon } from '../../createJsIcon';
import { PoliceStation as iconData } from '@dga-icons/core/duotone-rounded';

const PoliceStation = createJsIcon('police-station', iconData);

export default PoliceStation;

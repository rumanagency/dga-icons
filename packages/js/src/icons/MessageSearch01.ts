import { createJsIcon } from '../createJsIcon';
import { MessageSearch01 as iconData } from '@dga-icons/core';

const MessageSearch01 = createJsIcon('message-search-01', iconData);

export default MessageSearch01;

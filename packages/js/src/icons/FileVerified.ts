import { createJsIcon } from '../createJsIcon';
import { FileVerified as iconData } from '@dga-icons/core';

const FileVerified = createJsIcon('file-verified', iconData);

export default FileVerified;

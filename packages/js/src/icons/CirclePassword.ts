import { createJsIcon } from '../createJsIcon';
import { CirclePassword as iconData } from '@dga-icons/core';

const CirclePassword = createJsIcon('circle-password', iconData);

export default CirclePassword;

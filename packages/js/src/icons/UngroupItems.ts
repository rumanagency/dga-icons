import { createJsIcon } from '../createJsIcon';
import { UngroupItems as iconData } from '@dga-icons/core';

const UngroupItems = createJsIcon('ungroup-items', iconData);

export default UngroupItems;

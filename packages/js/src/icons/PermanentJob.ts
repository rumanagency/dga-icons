import { createJsIcon } from '../createJsIcon';
import { PermanentJob as iconData } from '@dga-icons/core';

const PermanentJob = createJsIcon('permanent-job', iconData);

export default PermanentJob;

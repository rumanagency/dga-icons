import { createJsIcon } from '../createJsIcon';
import { SearchSquare as iconData } from '@dga-icons/core';

const SearchSquare = createJsIcon('search-square', iconData);

export default SearchSquare;

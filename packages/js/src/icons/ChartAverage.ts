import { createJsIcon } from '../createJsIcon';
import { ChartAverage as iconData } from '@dga-icons/core';

const ChartAverage = createJsIcon('chart-average', iconData);

export default ChartAverage;

import { createJsIcon } from '../createJsIcon';
import { BitcoinWallet as iconData } from '@dga-icons/core';

const BitcoinWallet = createJsIcon('bitcoin-wallet', iconData);

export default BitcoinWallet;

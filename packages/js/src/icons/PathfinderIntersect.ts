import { createJsIcon } from '../createJsIcon';
import { PathfinderIntersect as iconData } from '@dga-icons/core';

const PathfinderIntersect = createJsIcon('pathfinder-intersect', iconData);

export default PathfinderIntersect;

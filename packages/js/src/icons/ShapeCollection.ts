import { createJsIcon } from '../createJsIcon';
import { ShapeCollection as iconData } from '@dga-icons/core';

const ShapeCollection = createJsIcon('shape-collection', iconData);

export default ShapeCollection;

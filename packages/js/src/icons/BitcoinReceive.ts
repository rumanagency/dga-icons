import { createJsIcon } from '../createJsIcon';
import { BitcoinReceive as iconData } from '@dga-icons/core';

const BitcoinReceive = createJsIcon('bitcoin-receive', iconData);

export default BitcoinReceive;

import { createJsIcon } from '../createJsIcon';
import { DashboardCircleSettings as iconData } from '@dga-icons/core';

const DashboardCircleSettings = createJsIcon('dashboard-circle-settings', iconData);

export default DashboardCircleSettings;

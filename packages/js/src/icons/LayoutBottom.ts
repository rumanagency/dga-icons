import { createJsIcon } from '../createJsIcon';
import { LayoutBottom as iconData } from '@dga-icons/core';

const LayoutBottom = createJsIcon('layout-bottom', iconData);

export default LayoutBottom;

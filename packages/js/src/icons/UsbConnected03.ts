import { createJsIcon } from '../createJsIcon';
import { UsbConnected03 as iconData } from '@dga-icons/core';

const UsbConnected03 = createJsIcon('usb-connected-03', iconData);

export default UsbConnected03;

import { createJsIcon } from '../createJsIcon';
import { SecurityValidation as iconData } from '@dga-icons/core';

const SecurityValidation = createJsIcon('security-validation', iconData);

export default SecurityValidation;

import { createJsIcon } from '../createJsIcon';
import { ClosedCaptionAlt as iconData } from '@dga-icons/core';

const ClosedCaptionAlt = createJsIcon('closed-caption-alt', iconData);

export default ClosedCaptionAlt;

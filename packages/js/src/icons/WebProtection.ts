import { createJsIcon } from '../createJsIcon';
import { WebProtection as iconData } from '@dga-icons/core';

const WebProtection = createJsIcon('web-protection', iconData);

export default WebProtection;

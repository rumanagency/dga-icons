import { createJsIcon } from '../createJsIcon';
import { TrafficLight as iconData } from '@dga-icons/core';

const TrafficLight = createJsIcon('traffic-light', iconData);

export default TrafficLight;

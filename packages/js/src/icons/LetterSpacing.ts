import { createJsIcon } from '../createJsIcon';
import { LetterSpacing as iconData } from '@dga-icons/core';

const LetterSpacing = createJsIcon('letter-spacing', iconData);

export default LetterSpacing;

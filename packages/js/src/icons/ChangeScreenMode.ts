import { createJsIcon } from '../createJsIcon';
import { ChangeScreenMode as iconData } from '@dga-icons/core';

const ChangeScreenMode = createJsIcon('change-screen-mode', iconData);

export default ChangeScreenMode;

import { createJsIcon } from '../createJsIcon';
import { ShipmentTracking as iconData } from '@dga-icons/core';

const ShipmentTracking = createJsIcon('shipment-tracking', iconData);

export default ShipmentTracking;

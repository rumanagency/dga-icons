import { createJsIcon } from '../createJsIcon';
import { ResourcesRemove as iconData } from '@dga-icons/core';

const ResourcesRemove = createJsIcon('resources-remove', iconData);

export default ResourcesRemove;

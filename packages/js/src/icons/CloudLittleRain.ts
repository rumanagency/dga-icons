import { createJsIcon } from '../createJsIcon';
import { CloudLittleRain as iconData } from '@dga-icons/core';

const CloudLittleRain = createJsIcon('cloud-little-rain', iconData);

export default CloudLittleRain;

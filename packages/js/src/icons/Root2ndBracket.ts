import { createJsIcon } from '../createJsIcon';
import { Root2ndBracket as iconData } from '@dga-icons/core';

const Root2ndBracket = createJsIcon('root-2nd-bracket', iconData);

export default Root2ndBracket;

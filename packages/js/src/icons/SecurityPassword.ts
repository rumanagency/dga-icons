import { createJsIcon } from '../createJsIcon';
import { SecurityPassword as iconData } from '@dga-icons/core';

const SecurityPassword = createJsIcon('security-password', iconData);

export default SecurityPassword;

import { createJsIcon } from '../createJsIcon';
import { HotspotOffline as iconData } from '@dga-icons/core';

const HotspotOffline = createJsIcon('hotspot-offline', iconData);

export default HotspotOffline;

import { createJsIcon } from '../createJsIcon';
import { GraduateFemale as iconData } from '@dga-icons/core';

const GraduateFemale = createJsIcon('graduate-female', iconData);

export default GraduateFemale;

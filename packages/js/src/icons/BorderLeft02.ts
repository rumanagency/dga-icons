import { createJsIcon } from '../createJsIcon';
import { BorderLeft02 as iconData } from '@dga-icons/core';

const BorderLeft02 = createJsIcon('border-left-02', iconData);

export default BorderLeft02;

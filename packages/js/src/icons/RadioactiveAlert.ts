import { createJsIcon } from '../createJsIcon';
import { RadioactiveAlert as iconData } from '@dga-icons/core';

const RadioactiveAlert = createJsIcon('radioactive-alert', iconData);

export default RadioactiveAlert;

import { createJsIcon } from '../createJsIcon';
import { CloudDownload as iconData } from '@dga-icons/core';

const CloudDownload = createJsIcon('cloud-download', iconData);

export default CloudDownload;

import { createJsIcon } from '../createJsIcon';
import { MobileProtection as iconData } from '@dga-icons/core';

const MobileProtection = createJsIcon('mobile-protection', iconData);

export default MobileProtection;

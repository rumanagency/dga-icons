import { createJsIcon } from '../createJsIcon';
import { BitcoinCircle as iconData } from '@dga-icons/core';

const BitcoinCircle = createJsIcon('bitcoin-circle', iconData);

export default BitcoinCircle;

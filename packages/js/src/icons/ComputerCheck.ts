import { createJsIcon } from '../createJsIcon';
import { ComputerCheck as iconData } from '@dga-icons/core';

const ComputerCheck = createJsIcon('computer-check', iconData);

export default ComputerCheck;

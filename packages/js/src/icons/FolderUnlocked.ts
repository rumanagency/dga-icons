import { createJsIcon } from '../createJsIcon';
import { FolderUnlocked as iconData } from '@dga-icons/core';

const FolderUnlocked = createJsIcon('folder-unlocked', iconData);

export default FolderUnlocked;

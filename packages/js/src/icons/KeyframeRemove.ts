import { createJsIcon } from '../createJsIcon';
import { KeyframeRemove as iconData } from '@dga-icons/core';

const KeyframeRemove = createJsIcon('keyframe-remove', iconData);

export default KeyframeRemove;

import { createJsIcon } from '../createJsIcon';
import { BitcoinCreditCard as iconData } from '@dga-icons/core';

const BitcoinCreditCard = createJsIcon('bitcoin-credit-card', iconData);

export default BitcoinCreditCard;

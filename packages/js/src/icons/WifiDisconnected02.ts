import { createJsIcon } from '../createJsIcon';
import { WifiDisconnected02 as iconData } from '@dga-icons/core';

const WifiDisconnected02 = createJsIcon('wifi-disconnected-02', iconData);

export default WifiDisconnected02;

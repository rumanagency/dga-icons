import { createJsIcon } from '../createJsIcon';
import { PathfinderMerge as iconData } from '@dga-icons/core';

const PathfinderMerge = createJsIcon('pathfinder-merge', iconData);

export default PathfinderMerge;

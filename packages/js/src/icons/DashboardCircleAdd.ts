import { createJsIcon } from '../createJsIcon';
import { DashboardCircleAdd as iconData } from '@dga-icons/core';

const DashboardCircleAdd = createJsIcon('dashboard-circle-add', iconData);

export default DashboardCircleAdd;

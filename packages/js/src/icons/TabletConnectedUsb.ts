import { createJsIcon } from '../createJsIcon';
import { TabletConnectedUsb as iconData } from '@dga-icons/core';

const TabletConnectedUsb = createJsIcon('tablet-connected-usb', iconData);

export default TabletConnectedUsb;

import { createJsIcon } from '../createJsIcon';
import { PowerSocket01 as iconData } from '@dga-icons/core';

const PowerSocket01 = createJsIcon('power-socket-01', iconData);

export default PowerSocket01;

import { createJsIcon } from '../createJsIcon';
import { DatabaseRestore as iconData } from '@dga-icons/core';

const DatabaseRestore = createJsIcon('database-restore', iconData);

export default DatabaseRestore;

import { createJsIcon } from '../createJsIcon';
import { PlusSignSquare as iconData } from '@dga-icons/core';

const PlusSignSquare = createJsIcon('plus-sign-square', iconData);

export default PlusSignSquare;

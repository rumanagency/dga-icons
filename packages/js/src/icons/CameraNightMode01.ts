import { createJsIcon } from '../createJsIcon';
import { CameraNightMode01 as iconData } from '@dga-icons/core';

const CameraNightMode01 = createJsIcon('camera-night-mode-01', iconData);

export default CameraNightMode01;

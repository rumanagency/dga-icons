import { createJsIcon } from '../createJsIcon';
import { FolderUnknown as iconData } from '@dga-icons/core';

const FolderUnknown = createJsIcon('folder-unknown', iconData);

export default FolderUnknown;

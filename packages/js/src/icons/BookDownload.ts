import { createJsIcon } from '../createJsIcon';
import { BookDownload as iconData } from '@dga-icons/core';

const BookDownload = createJsIcon('book-download', iconData);

export default BookDownload;

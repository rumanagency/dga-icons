import { createJsIcon } from '../createJsIcon';
import { WhatsappBusiness as iconData } from '@dga-icons/core';

const WhatsappBusiness = createJsIcon('whatsapp-business', iconData);

export default WhatsappBusiness;

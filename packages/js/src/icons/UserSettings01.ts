import { createJsIcon } from '../createJsIcon';
import { UserSettings01 as iconData } from '@dga-icons/core';

const UserSettings01 = createJsIcon('user-settings-01', iconData);

export default UserSettings01;

import { createJsIcon } from '../createJsIcon';
import { VintageClock as iconData } from '@dga-icons/core';

const VintageClock = createJsIcon('vintage-clock', iconData);

export default VintageClock;

import { createJsIcon } from '../createJsIcon';
import { ComputerRemove as iconData } from '@dga-icons/core';

const ComputerRemove = createJsIcon('computer-remove', iconData);

export default ComputerRemove;

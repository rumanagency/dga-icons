import { createJsIcon } from '../createJsIcon';
import { SustainableEnergy as iconData } from '@dga-icons/core';

const SustainableEnergy = createJsIcon('sustainable-energy', iconData);

export default SustainableEnergy;

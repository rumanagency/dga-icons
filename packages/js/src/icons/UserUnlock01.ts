import { createJsIcon } from '../createJsIcon';
import { UserUnlock01 as iconData } from '@dga-icons/core';

const UserUnlock01 = createJsIcon('user-unlock-01', iconData);

export default UserUnlock01;

import { createJsIcon } from '../createJsIcon';
import { FileDownload as iconData } from '@dga-icons/core';

const FileDownload = createJsIcon('file-download', iconData);

export default FileDownload;

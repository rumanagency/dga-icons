import { createJsIcon } from '../createJsIcon';
import { TimeManagement as iconData } from '@dga-icons/core';

const TimeManagement = createJsIcon('time-management', iconData);

export default TimeManagement;

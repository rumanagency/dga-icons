import { createJsIcon } from '../createJsIcon';
import { DistributeHorizontalRight as iconData } from '@dga-icons/core';

const DistributeHorizontalRight = createJsIcon('distribute-horizontal-right', iconData);

export default DistributeHorizontalRight;

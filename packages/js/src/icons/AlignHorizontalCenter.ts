import { createJsIcon } from '../createJsIcon';
import { AlignHorizontalCenter as iconData } from '@dga-icons/core';

const AlignHorizontalCenter = createJsIcon('align-horizontal-center', iconData);

export default AlignHorizontalCenter;

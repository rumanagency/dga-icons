import { createJsIcon } from '../createJsIcon';
import { GoForward30Sec as iconData } from '@dga-icons/core';

const GoForward30Sec = createJsIcon('go-forward-30-sec', iconData);

export default GoForward30Sec;

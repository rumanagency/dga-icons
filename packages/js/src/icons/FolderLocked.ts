import { createJsIcon } from '../createJsIcon';
import { FolderLocked as iconData } from '@dga-icons/core';

const FolderLocked = createJsIcon('folder-locked', iconData);

export default FolderLocked;

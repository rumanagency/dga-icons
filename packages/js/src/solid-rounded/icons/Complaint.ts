import { createJsIcon } from '../../createJsIcon';
import { Complaint as iconData } from '@dga-icons/core/solid-rounded';

const Complaint = createJsIcon('complaint', iconData);

export default Complaint;

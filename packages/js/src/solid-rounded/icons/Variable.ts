import { createJsIcon } from '../../createJsIcon';
import { Variable as iconData } from '@dga-icons/core/solid-rounded';

const Variable = createJsIcon('variable', iconData);

export default Variable;

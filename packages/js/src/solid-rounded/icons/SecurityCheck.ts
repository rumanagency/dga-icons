import { createJsIcon } from '../../createJsIcon';
import { SecurityCheck as iconData } from '@dga-icons/core/solid-rounded';

const SecurityCheck = createJsIcon('security-check', iconData);

export default SecurityCheck;

import { createJsIcon } from '../../createJsIcon';
import { MessageQuestion as iconData } from '@dga-icons/core/solid-rounded';

const MessageQuestion = createJsIcon('message-question', iconData);

export default MessageQuestion;

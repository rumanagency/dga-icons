import { createJsIcon } from '../../createJsIcon';
import { BitcoinCpu as iconData } from '@dga-icons/core/solid-rounded';

const BitcoinCpu = createJsIcon('bitcoin-cpu', iconData);

export default BitcoinCpu;

import { createJsIcon } from '../../createJsIcon';
import { CpuSettings as iconData } from '@dga-icons/core/solid-rounded';

const CpuSettings = createJsIcon('cpu-settings', iconData);

export default CpuSettings;

import { createJsIcon } from '../../createJsIcon';
import { Contracts as iconData } from '@dga-icons/core/solid-rounded';

const Contracts = createJsIcon('contracts', iconData);

export default Contracts;

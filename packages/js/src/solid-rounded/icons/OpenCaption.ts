import { createJsIcon } from '../../createJsIcon';
import { OpenCaption as iconData } from '@dga-icons/core/solid-rounded';

const OpenCaption = createJsIcon('open-caption', iconData);

export default OpenCaption;

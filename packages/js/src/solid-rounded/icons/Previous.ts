import { createJsIcon } from '../../createJsIcon';
import { Previous as iconData } from '@dga-icons/core/solid-rounded';

const Previous = createJsIcon('previous', iconData);

export default Previous;

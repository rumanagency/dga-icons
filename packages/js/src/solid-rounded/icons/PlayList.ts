import { createJsIcon } from '../../createJsIcon';
import { PlayList as iconData } from '@dga-icons/core/solid-rounded';

const PlayList = createJsIcon('play-list', iconData);

export default PlayList;

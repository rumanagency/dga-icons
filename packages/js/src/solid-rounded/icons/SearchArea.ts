import { createJsIcon } from '../../createJsIcon';
import { SearchArea as iconData } from '@dga-icons/core/solid-rounded';

const SearchArea = createJsIcon('search-area', iconData);

export default SearchArea;

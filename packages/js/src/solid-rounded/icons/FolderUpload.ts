import { createJsIcon } from '../../createJsIcon';
import { FolderUpload as iconData } from '@dga-icons/core/solid-rounded';

const FolderUpload = createJsIcon('folder-upload', iconData);

export default FolderUpload;

import { createJsIcon } from '../../createJsIcon';
import { Database as iconData } from '@dga-icons/core/solid-rounded';

const Database = createJsIcon('database', iconData);

export default Database;

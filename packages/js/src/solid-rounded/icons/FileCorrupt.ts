import { createJsIcon } from '../../createJsIcon';
import { FileCorrupt as iconData } from '@dga-icons/core/solid-rounded';

const FileCorrupt = createJsIcon('file-corrupt', iconData);

export default FileCorrupt;

import { createJsIcon } from '../../createJsIcon';
import { WebValidation as iconData } from '@dga-icons/core/solid-rounded';

const WebValidation = createJsIcon('web-validation', iconData);

export default WebValidation;

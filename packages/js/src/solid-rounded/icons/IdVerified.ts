import { createJsIcon } from '../../createJsIcon';
import { IdVerified as iconData } from '@dga-icons/core/solid-rounded';

const IdVerified = createJsIcon('id-verified', iconData);

export default IdVerified;

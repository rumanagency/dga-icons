import { createJsIcon } from '../../createJsIcon';
import { Artboard as iconData } from '@dga-icons/core/solid-rounded';

const Artboard = createJsIcon('artboard', iconData);

export default Artboard;

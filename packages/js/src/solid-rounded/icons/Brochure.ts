import { createJsIcon } from '../../createJsIcon';
import { Brochure as iconData } from '@dga-icons/core/solid-rounded';

const Brochure = createJsIcon('brochure', iconData);

export default Brochure;

import { createJsIcon } from '../../createJsIcon';
import { TimeHalfPass as iconData } from '@dga-icons/core/solid-rounded';

const TimeHalfPass = createJsIcon('time-half-pass', iconData);

export default TimeHalfPass;

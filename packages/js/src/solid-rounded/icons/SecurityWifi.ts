import { createJsIcon } from '../../createJsIcon';
import { SecurityWifi as iconData } from '@dga-icons/core/solid-rounded';

const SecurityWifi = createJsIcon('security-wifi', iconData);

export default SecurityWifi;

import { createJsIcon } from '../../createJsIcon';
import { Petals02 as iconData } from '@dga-icons/core/solid-rounded';

const Petals02 = createJsIcon('petals-02', iconData);

export default Petals02;

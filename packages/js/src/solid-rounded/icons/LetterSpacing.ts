import { createJsIcon } from '../../createJsIcon';
import { LetterSpacing as iconData } from '@dga-icons/core/solid-rounded';

const LetterSpacing = createJsIcon('letter-spacing', iconData);

export default LetterSpacing;

import { createJsIcon } from '../../createJsIcon';
import { BrokenBone as iconData } from '@dga-icons/core/solid-rounded';

const BrokenBone = createJsIcon('broken-bone', iconData);

export default BrokenBone;

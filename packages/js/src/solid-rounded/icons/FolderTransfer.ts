import { createJsIcon } from '../../createJsIcon';
import { FolderTransfer as iconData } from '@dga-icons/core/solid-rounded';

const FolderTransfer = createJsIcon('folder-transfer', iconData);

export default FolderTransfer;

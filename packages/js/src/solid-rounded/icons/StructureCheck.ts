import { createJsIcon } from '../../createJsIcon';
import { StructureCheck as iconData } from '@dga-icons/core/solid-rounded';

const StructureCheck = createJsIcon('structure-check', iconData);

export default StructureCheck;

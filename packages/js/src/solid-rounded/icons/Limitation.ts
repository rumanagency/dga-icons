import { createJsIcon } from '../../createJsIcon';
import { Limitation as iconData } from '@dga-icons/core/solid-rounded';

const Limitation = createJsIcon('limitation', iconData);

export default Limitation;

import { createJsIcon } from '../../createJsIcon';
import { Departement as iconData } from '@dga-icons/core/solid-rounded';

const Departement = createJsIcon('departement', iconData);

export default Departement;

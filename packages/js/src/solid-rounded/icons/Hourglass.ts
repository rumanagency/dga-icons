import { createJsIcon } from '../../createJsIcon';
import { Hourglass as iconData } from '@dga-icons/core/solid-rounded';

const Hourglass = createJsIcon('hourglass', iconData);

export default Hourglass;

import { createJsIcon } from '../../createJsIcon';
import { LaptopRemove as iconData } from '@dga-icons/core/solid-rounded';

const LaptopRemove = createJsIcon('laptop-remove', iconData);

export default LaptopRemove;

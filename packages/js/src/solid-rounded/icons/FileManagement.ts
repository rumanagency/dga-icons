import { createJsIcon } from '../../createJsIcon';
import { FileManagement as iconData } from '@dga-icons/core/solid-rounded';

const FileManagement = createJsIcon('file-management', iconData);

export default FileManagement;

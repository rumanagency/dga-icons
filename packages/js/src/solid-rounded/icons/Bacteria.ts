import { createJsIcon } from '../../createJsIcon';
import { Bacteria as iconData } from '@dga-icons/core/solid-rounded';

const Bacteria = createJsIcon('bacteria', iconData);

export default Bacteria;

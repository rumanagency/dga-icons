import { createJsIcon } from '../../createJsIcon';
import { Signature as iconData } from '@dga-icons/core/solid-rounded';

const Signature = createJsIcon('signature', iconData);

export default Signature;

import { createJsIcon } from '../../createJsIcon';
import { Triangle as iconData } from '@dga-icons/core/solid-rounded';

const Triangle = createJsIcon('triangle', iconData);

export default Triangle;

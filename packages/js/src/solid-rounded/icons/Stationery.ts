import { createJsIcon } from '../../createJsIcon';
import { Stationery as iconData } from '@dga-icons/core/solid-rounded';

const Stationery = createJsIcon('stationery', iconData);

export default Stationery;

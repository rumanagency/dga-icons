import { createJsIcon } from '../../createJsIcon';
import { FolderAudio as iconData } from '@dga-icons/core/solid-rounded';

const FolderAudio = createJsIcon('folder-audio', iconData);

export default FolderAudio;

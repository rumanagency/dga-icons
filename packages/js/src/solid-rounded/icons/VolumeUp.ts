import { createJsIcon } from '../../createJsIcon';
import { VolumeUp as iconData } from '@dga-icons/core/solid-rounded';

const VolumeUp = createJsIcon('volume-up', iconData);

export default VolumeUp;

import { createJsIcon } from '../../createJsIcon';
import { MasterCard as iconData } from '@dga-icons/core/solid-rounded';

const MasterCard = createJsIcon('master-card', iconData);

export default MasterCard;

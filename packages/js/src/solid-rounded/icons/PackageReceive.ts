import { createJsIcon } from '../../createJsIcon';
import { PackageReceive as iconData } from '@dga-icons/core/solid-rounded';

const PackageReceive = createJsIcon('package-receive', iconData);

export default PackageReceive;

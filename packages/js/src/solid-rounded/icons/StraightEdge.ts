import { createJsIcon } from '../../createJsIcon';
import { StraightEdge as iconData } from '@dga-icons/core/solid-rounded';

const StraightEdge = createJsIcon('straight-edge', iconData);

export default StraightEdge;

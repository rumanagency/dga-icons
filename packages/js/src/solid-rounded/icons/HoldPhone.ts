import { createJsIcon } from '../../createJsIcon';
import { HoldPhone as iconData } from '@dga-icons/core/solid-rounded';

const HoldPhone = createJsIcon('hold-phone', iconData);

export default HoldPhone;

import { createJsIcon } from '../../createJsIcon';
import { Layout2Row as iconData } from '@dga-icons/core/solid-rounded';

const Layout2Row = createJsIcon('layout-2-row', iconData);

export default Layout2Row;

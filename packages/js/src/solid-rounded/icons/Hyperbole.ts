import { createJsIcon } from '../../createJsIcon';
import { Hyperbole as iconData } from '@dga-icons/core/solid-rounded';

const Hyperbole = createJsIcon('hyperbole', iconData);

export default Hyperbole;

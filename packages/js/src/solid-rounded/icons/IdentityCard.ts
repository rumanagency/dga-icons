import { createJsIcon } from '../../createJsIcon';
import { IdentityCard as iconData } from '@dga-icons/core/solid-rounded';

const IdentityCard = createJsIcon('identity-card', iconData);

export default IdentityCard;

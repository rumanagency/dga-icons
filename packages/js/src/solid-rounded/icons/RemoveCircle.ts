import { createJsIcon } from '../../createJsIcon';
import { RemoveCircle as iconData } from '@dga-icons/core/solid-rounded';

const RemoveCircle = createJsIcon('remove-circle', iconData);

export default RemoveCircle;

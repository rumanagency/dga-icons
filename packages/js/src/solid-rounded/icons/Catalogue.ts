import { createJsIcon } from '../../createJsIcon';
import { Catalogue as iconData } from '@dga-icons/core/solid-rounded';

const Catalogue = createJsIcon('catalogue', iconData);

export default Catalogue;

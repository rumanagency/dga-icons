import { createJsIcon } from '../../createJsIcon';
import { TransitionBottom as iconData } from '@dga-icons/core/solid-rounded';

const TransitionBottom = createJsIcon('transition-bottom', iconData);

export default TransitionBottom;

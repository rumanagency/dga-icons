import { createJsIcon } from '../../createJsIcon';
import { StopCircle as iconData } from '@dga-icons/core/solid-rounded';

const StopCircle = createJsIcon('stop-circle', iconData);

export default StopCircle;

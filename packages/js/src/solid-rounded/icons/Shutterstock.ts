import { createJsIcon } from '../../createJsIcon';
import { Shutterstock as iconData } from '@dga-icons/core/solid-rounded';

const Shutterstock = createJsIcon('shutterstock', iconData);

export default Shutterstock;

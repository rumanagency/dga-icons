import { createJsIcon } from '../../createJsIcon';
import { Prescription as iconData } from '@dga-icons/core/solid-rounded';

const Prescription = createJsIcon('prescription', iconData);

export default Prescription;

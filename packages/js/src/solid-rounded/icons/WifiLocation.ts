import { createJsIcon } from '../../createJsIcon';
import { WifiLocation as iconData } from '@dga-icons/core/solid-rounded';

const WifiLocation = createJsIcon('wifi-location', iconData);

export default WifiLocation;

import { createJsIcon } from '../../createJsIcon';
import { Messenger as iconData } from '@dga-icons/core/solid-rounded';

const Messenger = createJsIcon('messenger', iconData);

export default Messenger;

import { createJsIcon } from '../../createJsIcon';
import { BitcoinLocation as iconData } from '@dga-icons/core/solid-rounded';

const BitcoinLocation = createJsIcon('bitcoin-location', iconData);

export default BitcoinLocation;

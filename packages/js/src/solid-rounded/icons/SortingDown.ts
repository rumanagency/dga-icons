import { createJsIcon } from '../../createJsIcon';
import { SortingDown as iconData } from '@dga-icons/core/solid-rounded';

const SortingDown = createJsIcon('sorting-down', iconData);

export default SortingDown;

import { createJsIcon } from '../../createJsIcon';
import { Prisoner as iconData } from '@dga-icons/core/solid-rounded';

const Prisoner = createJsIcon('prisoner', iconData);

export default Prisoner;

import { createJsIcon } from '../../createJsIcon';
import { TokenCircle as iconData } from '@dga-icons/core/solid-rounded';

const TokenCircle = createJsIcon('token-circle', iconData);

export default TokenCircle;

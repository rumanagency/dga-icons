import { createJsIcon } from '../../createJsIcon';
import { Croissant as iconData } from '@dga-icons/core/solid-rounded';

const Croissant = createJsIcon('croissant', iconData);

export default Croissant;

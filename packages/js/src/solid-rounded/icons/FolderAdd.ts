import { createJsIcon } from '../../createJsIcon';
import { FolderAdd as iconData } from '@dga-icons/core/solid-rounded';

const FolderAdd = createJsIcon('folder-add', iconData);

export default FolderAdd;

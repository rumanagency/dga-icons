import { createJsIcon } from '../../createJsIcon';
import { GlobalSearch as iconData } from '@dga-icons/core/solid-rounded';

const GlobalSearch = createJsIcon('global-search', iconData);

export default GlobalSearch;

import { createJsIcon } from '../../createJsIcon';
import { UserList as iconData } from '@dga-icons/core/solid-rounded';

const UserList = createJsIcon('user-list', iconData);

export default UserList;

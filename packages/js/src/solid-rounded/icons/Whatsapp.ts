import { createJsIcon } from '../../createJsIcon';
import { Whatsapp as iconData } from '@dga-icons/core/solid-rounded';

const Whatsapp = createJsIcon('whatsapp', iconData);

export default Whatsapp;

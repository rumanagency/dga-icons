import { createJsIcon } from '../../createJsIcon';
import { CreditCard as iconData } from '@dga-icons/core/solid-rounded';

const CreditCard = createJsIcon('credit-card', iconData);

export default CreditCard;

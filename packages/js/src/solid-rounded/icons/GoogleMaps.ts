import { createJsIcon } from '../../createJsIcon';
import { GoogleMaps as iconData } from '@dga-icons/core/solid-rounded';

const GoogleMaps = createJsIcon('google-maps', iconData);

export default GoogleMaps;

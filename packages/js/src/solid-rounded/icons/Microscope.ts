import { createJsIcon } from '../../createJsIcon';
import { Microscope as iconData } from '@dga-icons/core/solid-rounded';

const Microscope = createJsIcon('microscope', iconData);

export default Microscope;

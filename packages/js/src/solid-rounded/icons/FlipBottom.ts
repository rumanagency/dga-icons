import { createJsIcon } from '../../createJsIcon';
import { FlipBottom as iconData } from '@dga-icons/core/solid-rounded';

const FlipBottom = createJsIcon('flip-bottom', iconData);

export default FlipBottom;

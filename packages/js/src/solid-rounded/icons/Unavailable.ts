import { createJsIcon } from '../../createJsIcon';
import { Unavailable as iconData } from '@dga-icons/core/solid-rounded';

const Unavailable = createJsIcon('unavailable', iconData);

export default Unavailable;

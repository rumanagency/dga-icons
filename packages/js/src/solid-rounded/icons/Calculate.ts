import { createJsIcon } from '../../createJsIcon';
import { Calculate as iconData } from '@dga-icons/core/solid-rounded';

const Calculate = createJsIcon('calculate', iconData);

export default Calculate;

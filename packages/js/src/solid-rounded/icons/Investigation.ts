import { createJsIcon } from '../../createJsIcon';
import { Investigation as iconData } from '@dga-icons/core/solid-rounded';

const Investigation = createJsIcon('investigation', iconData);

export default Investigation;

import { createJsIcon } from '../../createJsIcon';
import { BlockGame as iconData } from '@dga-icons/core/solid-rounded';

const BlockGame = createJsIcon('block-game', iconData);

export default BlockGame;

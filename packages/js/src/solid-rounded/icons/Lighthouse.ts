import { createJsIcon } from '../../createJsIcon';
import { Lighthouse as iconData } from '@dga-icons/core/solid-rounded';

const Lighthouse = createJsIcon('lighthouse', iconData);

export default Lighthouse;

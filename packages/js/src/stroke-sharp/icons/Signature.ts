import { createJsIcon } from '../../createJsIcon';
import { Signature as iconData } from '@dga-icons/core/stroke-sharp';

const Signature = createJsIcon('signature', iconData);

export default Signature;

import { createJsIcon } from '../../createJsIcon';
import { Laughing as iconData } from '@dga-icons/core/stroke-sharp';

const Laughing = createJsIcon('laughing', iconData);

export default Laughing;

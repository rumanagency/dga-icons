import { createJsIcon } from '../../createJsIcon';
import { Treatment as iconData } from '@dga-icons/core/stroke-sharp';

const Treatment = createJsIcon('treatment', iconData);

export default Treatment;

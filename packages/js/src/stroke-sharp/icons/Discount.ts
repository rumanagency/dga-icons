import { createJsIcon } from '../../createJsIcon';
import { Discount as iconData } from '@dga-icons/core/stroke-sharp';

const Discount = createJsIcon('discount', iconData);

export default Discount;

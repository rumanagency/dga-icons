import { createJsIcon } from '../../createJsIcon';
import { FullScreen as iconData } from '@dga-icons/core/stroke-sharp';

const FullScreen = createJsIcon('full-screen', iconData);

export default FullScreen;

import { createJsIcon } from '../../createJsIcon';
import { WorkoutStretching as iconData } from '@dga-icons/core/stroke-sharp';

const WorkoutStretching = createJsIcon('workout-stretching', iconData);

export default WorkoutStretching;

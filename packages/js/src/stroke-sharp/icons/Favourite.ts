import { createJsIcon } from '../../createJsIcon';
import { Favourite as iconData } from '@dga-icons/core/stroke-sharp';

const Favourite = createJsIcon('favourite', iconData);

export default Favourite;

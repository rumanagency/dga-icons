import { createJsIcon } from '../../createJsIcon';
import { Register as iconData } from '@dga-icons/core/stroke-sharp';

const Register = createJsIcon('register', iconData);

export default Register;

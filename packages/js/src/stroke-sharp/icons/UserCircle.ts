import { createJsIcon } from '../../createJsIcon';
import { UserCircle as iconData } from '@dga-icons/core/stroke-sharp';

const UserCircle = createJsIcon('user-circle', iconData);

export default UserCircle;

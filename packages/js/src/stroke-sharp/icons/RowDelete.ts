import { createJsIcon } from '../../createJsIcon';
import { RowDelete as iconData } from '@dga-icons/core/stroke-sharp';

const RowDelete = createJsIcon('row-delete', iconData);

export default RowDelete;

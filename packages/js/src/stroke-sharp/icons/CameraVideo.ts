import { createJsIcon } from '../../createJsIcon';
import { CameraVideo as iconData } from '@dga-icons/core/stroke-sharp';

const CameraVideo = createJsIcon('camera-video', iconData);

export default CameraVideo;

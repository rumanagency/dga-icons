import { createJsIcon } from '../../createJsIcon';
import { Touchpad01 as iconData } from '@dga-icons/core/stroke-sharp';

const Touchpad01 = createJsIcon('touchpad-01', iconData);

export default Touchpad01;

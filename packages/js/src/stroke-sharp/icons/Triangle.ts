import { createJsIcon } from '../../createJsIcon';
import { Triangle as iconData } from '@dga-icons/core/stroke-sharp';

const Triangle = createJsIcon('triangle', iconData);

export default Triangle;

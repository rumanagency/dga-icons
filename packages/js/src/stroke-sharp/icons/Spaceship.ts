import { createJsIcon } from '../../createJsIcon';
import { Spaceship as iconData } from '@dga-icons/core/stroke-sharp';

const Spaceship = createJsIcon('spaceship', iconData);

export default Spaceship;

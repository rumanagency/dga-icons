import { createJsIcon } from '../../createJsIcon';
import { IdNotVerified as iconData } from '@dga-icons/core/stroke-sharp';

const IdNotVerified = createJsIcon('id-not-verified', iconData);

export default IdNotVerified;

import { createJsIcon } from '../../createJsIcon';
import { LetterSpacing as iconData } from '@dga-icons/core/stroke-sharp';

const LetterSpacing = createJsIcon('letter-spacing', iconData);

export default LetterSpacing;

import { createJsIcon } from '../../createJsIcon';
import { BlackHole as iconData } from '@dga-icons/core/stroke-sharp';

const BlackHole = createJsIcon('black-hole', iconData);

export default BlackHole;

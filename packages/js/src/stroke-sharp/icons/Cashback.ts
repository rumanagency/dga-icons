import { createJsIcon } from '../../createJsIcon';
import { Cashback as iconData } from '@dga-icons/core/stroke-sharp';

const Cashback = createJsIcon('cashback', iconData);

export default Cashback;

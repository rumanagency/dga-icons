import { createJsIcon } from '../../createJsIcon';
import { FileExport as iconData } from '@dga-icons/core/stroke-sharp';

const FileExport = createJsIcon('file-export', iconData);

export default FileExport;

import { createJsIcon } from '../../createJsIcon';
import { SendingOrder as iconData } from '@dga-icons/core/stroke-sharp';

const SendingOrder = createJsIcon('sending-order', iconData);

export default SendingOrder;

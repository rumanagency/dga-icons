import { createJsIcon } from '../../createJsIcon';
import { PiggyBank as iconData } from '@dga-icons/core/stroke-sharp';

const PiggyBank = createJsIcon('piggy-bank', iconData);

export default PiggyBank;

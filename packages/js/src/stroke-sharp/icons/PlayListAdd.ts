import { createJsIcon } from '../../createJsIcon';
import { PlayListAdd as iconData } from '@dga-icons/core/stroke-sharp';

const PlayListAdd = createJsIcon('play-list-add', iconData);

export default PlayListAdd;

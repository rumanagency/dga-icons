import { createJsIcon } from '../../createJsIcon';
import { Hackerrank as iconData } from '@dga-icons/core/stroke-sharp';

const Hackerrank = createJsIcon('hackerrank', iconData);

export default Hackerrank;

import { createJsIcon } from '../../createJsIcon';
import { Keyframe as iconData } from '@dga-icons/core/stroke-sharp';

const Keyframe = createJsIcon('keyframe', iconData);

export default Keyframe;

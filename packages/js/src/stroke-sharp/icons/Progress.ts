import { createJsIcon } from '../../createJsIcon';
import { Progress as iconData } from '@dga-icons/core/stroke-sharp';

const Progress = createJsIcon('progress', iconData);

export default Progress;

import { createJsIcon } from '../../createJsIcon';
import { FileBookmark as iconData } from '@dga-icons/core/stroke-sharp';

const FileBookmark = createJsIcon('file-bookmark', iconData);

export default FileBookmark;

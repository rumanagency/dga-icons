import { createJsIcon } from '../../createJsIcon';
import { Unamused as iconData } from '@dga-icons/core/stroke-sharp';

const Unamused = createJsIcon('unamused', iconData);

export default Unamused;

import { createJsIcon } from '../../createJsIcon';
import { DentalCare as iconData } from '@dga-icons/core/stroke-sharp';

const DentalCare = createJsIcon('dental-care', iconData);

export default DentalCare;

import { createJsIcon } from '../../createJsIcon';
import { SafetyPin01 as iconData } from '@dga-icons/core/stroke-sharp';

const SafetyPin01 = createJsIcon('safety-pin-01', iconData);

export default SafetyPin01;

import { createJsIcon } from '../../createJsIcon';
import { Investigation as iconData } from '@dga-icons/core/stroke-sharp';

const Investigation = createJsIcon('investigation', iconData);

export default Investigation;

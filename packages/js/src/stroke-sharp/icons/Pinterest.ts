import { createJsIcon } from '../../createJsIcon';
import { Pinterest as iconData } from '@dga-icons/core/stroke-sharp';

const Pinterest = createJsIcon('pinterest', iconData);

export default Pinterest;

import { createJsIcon } from '../../createJsIcon';
import { GitCommit as iconData } from '@dga-icons/core/stroke-sharp';

const GitCommit = createJsIcon('git-commit', iconData);

export default GitCommit;

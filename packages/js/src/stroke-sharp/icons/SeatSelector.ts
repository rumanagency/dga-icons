import { createJsIcon } from '../../createJsIcon';
import { SeatSelector as iconData } from '@dga-icons/core/stroke-sharp';

const SeatSelector = createJsIcon('seat-selector', iconData);

export default SeatSelector;

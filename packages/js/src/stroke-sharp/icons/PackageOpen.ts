import { createJsIcon } from '../../createJsIcon';
import { PackageOpen as iconData } from '@dga-icons/core/stroke-sharp';

const PackageOpen = createJsIcon('package-open', iconData);

export default PackageOpen;

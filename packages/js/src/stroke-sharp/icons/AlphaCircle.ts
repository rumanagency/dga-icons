import { createJsIcon } from '../../createJsIcon';
import { AlphaCircle as iconData } from '@dga-icons/core/stroke-sharp';

const AlphaCircle = createJsIcon('alpha-circle', iconData);

export default AlphaCircle;

import { createJsIcon } from '../../createJsIcon';
import { FileEdit as iconData } from '@dga-icons/core/stroke-sharp';

const FileEdit = createJsIcon('file-edit', iconData);

export default FileEdit;

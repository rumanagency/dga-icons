import { createJsIcon } from '../../createJsIcon';
import { HelpCircle as iconData } from '@dga-icons/core/stroke-sharp';

const HelpCircle = createJsIcon('help-circle', iconData);

export default HelpCircle;

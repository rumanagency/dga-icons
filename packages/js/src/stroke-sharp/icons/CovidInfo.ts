import { createJsIcon } from '../../createJsIcon';
import { CovidInfo as iconData } from '@dga-icons/core/stroke-sharp';

const CovidInfo = createJsIcon('covid-info', iconData);

export default CovidInfo;

import { createJsIcon } from '../../createJsIcon';
import { PropertyView as iconData } from '@dga-icons/core/stroke-sharp';

const PropertyView = createJsIcon('property-view', iconData);

export default PropertyView;

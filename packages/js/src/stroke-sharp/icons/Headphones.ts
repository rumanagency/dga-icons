import { createJsIcon } from '../../createJsIcon';
import { Headphones as iconData } from '@dga-icons/core/stroke-sharp';

const Headphones = createJsIcon('headphones', iconData);

export default Headphones;

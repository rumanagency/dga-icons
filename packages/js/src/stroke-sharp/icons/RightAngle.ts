import { createJsIcon } from '../../createJsIcon';
import { RightAngle as iconData } from '@dga-icons/core/stroke-sharp';

const RightAngle = createJsIcon('right-angle', iconData);

export default RightAngle;

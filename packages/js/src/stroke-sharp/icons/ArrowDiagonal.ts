import { createJsIcon } from '../../createJsIcon';
import { ArrowDiagonal as iconData } from '@dga-icons/core/stroke-sharp';

const ArrowDiagonal = createJsIcon('arrow-diagonal', iconData);

export default ArrowDiagonal;

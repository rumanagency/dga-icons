import { createJsIcon } from '../../createJsIcon';
import { Logout01 as iconData } from '@dga-icons/core/stroke-sharp';

const Logout01 = createJsIcon('logout-01', iconData);

export default Logout01;

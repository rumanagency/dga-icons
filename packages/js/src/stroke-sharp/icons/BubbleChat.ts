import { createJsIcon } from '../../createJsIcon';
import { BubbleChat as iconData } from '@dga-icons/core/stroke-sharp';

const BubbleChat = createJsIcon('bubble-chat', iconData);

export default BubbleChat;

import { createJsIcon } from '../../createJsIcon';
import { BitcoinSquare as iconData } from '@dga-icons/core/stroke-sharp';

const BitcoinSquare = createJsIcon('bitcoin-square', iconData);

export default BitcoinSquare;

import { createJsIcon } from '../../createJsIcon';
import { GeologyCrust as iconData } from '@dga-icons/core/stroke-sharp';

const GeologyCrust = createJsIcon('geology-crust', iconData);

export default GeologyCrust;

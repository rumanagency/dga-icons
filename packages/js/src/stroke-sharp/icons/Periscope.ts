import { createJsIcon } from '../../createJsIcon';
import { Periscope as iconData } from '@dga-icons/core/stroke-sharp';

const Periscope = createJsIcon('periscope', iconData);

export default Periscope;

import { createJsIcon } from '../../createJsIcon';
import { BitcoinWallet as iconData } from '@dga-icons/core/stroke-sharp';

const BitcoinWallet = createJsIcon('bitcoin-wallet', iconData);

export default BitcoinWallet;

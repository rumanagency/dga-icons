import { createJsIcon } from '../../createJsIcon';
import { PropertyAdd as iconData } from '@dga-icons/core/stroke-sharp';

const PropertyAdd = createJsIcon('property-add', iconData);

export default PropertyAdd;

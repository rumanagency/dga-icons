import { createJsIcon } from '../../createJsIcon';
import { Temperature as iconData } from '@dga-icons/core/stroke-sharp';

const Temperature = createJsIcon('temperature', iconData);

export default Temperature;

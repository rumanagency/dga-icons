import { createJsIcon } from '../../createJsIcon';
import { InsertTopImage as iconData } from '@dga-icons/core/stroke-sharp';

const InsertTopImage = createJsIcon('insert-top-image', iconData);

export default InsertTopImage;

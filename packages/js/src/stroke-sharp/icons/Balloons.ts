import { createJsIcon } from '../../createJsIcon';
import { Balloons as iconData } from '@dga-icons/core/stroke-sharp';

const Balloons = createJsIcon('balloons', iconData);

export default Balloons;

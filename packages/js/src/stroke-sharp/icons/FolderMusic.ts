import { createJsIcon } from '../../createJsIcon';
import { FolderMusic as iconData } from '@dga-icons/core/stroke-sharp';

const FolderMusic = createJsIcon('folder-music', iconData);

export default FolderMusic;

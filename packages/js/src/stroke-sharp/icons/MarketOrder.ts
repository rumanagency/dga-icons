import { createJsIcon } from '../../createJsIcon';
import { MarketOrder as iconData } from '@dga-icons/core/stroke-sharp';

const MarketOrder = createJsIcon('market-order', iconData);

export default MarketOrder;

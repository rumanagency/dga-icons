import { createJsIcon } from '../../createJsIcon';
import { SwipeDown07 as iconData } from '@dga-icons/core/stroke-sharp';

const SwipeDown07 = createJsIcon('swipe-down-07', iconData);

export default SwipeDown07;

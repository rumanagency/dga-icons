import { createJsIcon } from '../../createJsIcon';
import { FolderAttachment as iconData } from '@dga-icons/core/stroke-sharp';

const FolderAttachment = createJsIcon('folder-attachment', iconData);

export default FolderAttachment;

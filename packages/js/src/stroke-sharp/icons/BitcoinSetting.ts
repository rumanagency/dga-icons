import { createJsIcon } from '../../createJsIcon';
import { BitcoinSetting as iconData } from '@dga-icons/core/stroke-sharp';

const BitcoinSetting = createJsIcon('bitcoin-setting', iconData);

export default BitcoinSetting;

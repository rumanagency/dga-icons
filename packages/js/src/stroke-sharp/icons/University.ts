import { createJsIcon } from '../../createJsIcon';
import { University as iconData } from '@dga-icons/core/stroke-sharp';

const University = createJsIcon('university', iconData);

export default University;

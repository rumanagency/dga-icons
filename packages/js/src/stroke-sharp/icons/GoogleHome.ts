import { createJsIcon } from '../../createJsIcon';
import { GoogleHome as iconData } from '@dga-icons/core/stroke-sharp';

const GoogleHome = createJsIcon('google-home', iconData);

export default GoogleHome;

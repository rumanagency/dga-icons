import { createJsIcon } from '../../createJsIcon';
import { Hierarchy as iconData } from '@dga-icons/core/stroke-sharp';

const Hierarchy = createJsIcon('hierarchy', iconData);

export default Hierarchy;

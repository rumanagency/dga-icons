import { createJsIcon } from '../../createJsIcon';
import { HotPrice as iconData } from '@dga-icons/core/stroke-sharp';

const HotPrice = createJsIcon('hot-price', iconData);

export default HotPrice;

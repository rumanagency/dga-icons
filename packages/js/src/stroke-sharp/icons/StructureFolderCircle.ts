import { createJsIcon } from '../../createJsIcon';
import { StructureFolderCircle as iconData } from '@dga-icons/core/stroke-sharp';

const StructureFolderCircle = createJsIcon('structure-folder-circle', iconData);

export default StructureFolderCircle;

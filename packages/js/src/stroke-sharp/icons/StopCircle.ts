import { createJsIcon } from '../../createJsIcon';
import { StopCircle as iconData } from '@dga-icons/core/stroke-sharp';

const StopCircle = createJsIcon('stop-circle', iconData);

export default StopCircle;

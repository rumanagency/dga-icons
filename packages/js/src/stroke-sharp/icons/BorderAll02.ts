import { createJsIcon } from '../../createJsIcon';
import { BorderAll02 as iconData } from '@dga-icons/core/stroke-sharp';

const BorderAll02 = createJsIcon('border-all-02', iconData);

export default BorderAll02;

import { createJsIcon } from '../../createJsIcon';
import { HierarchyFiles as iconData } from '@dga-icons/core/stroke-sharp';

const HierarchyFiles = createJsIcon('hierarchy-files', iconData);

export default HierarchyFiles;

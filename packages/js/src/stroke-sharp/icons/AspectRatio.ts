import { createJsIcon } from '../../createJsIcon';
import { AspectRatio as iconData } from '@dga-icons/core/stroke-sharp';

const AspectRatio = createJsIcon('aspect-ratio', iconData);

export default AspectRatio;

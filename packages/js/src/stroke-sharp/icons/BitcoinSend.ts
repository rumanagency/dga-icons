import { createJsIcon } from '../../createJsIcon';
import { BitcoinSend as iconData } from '@dga-icons/core/stroke-sharp';

const BitcoinSend = createJsIcon('bitcoin-send', iconData);

export default BitcoinSend;

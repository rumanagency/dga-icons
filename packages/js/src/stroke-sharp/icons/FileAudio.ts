import { createJsIcon } from '../../createJsIcon';
import { FileAudio as iconData } from '@dga-icons/core/stroke-sharp';

const FileAudio = createJsIcon('file-audio', iconData);

export default FileAudio;

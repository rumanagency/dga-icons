import { createJsIcon } from '../../createJsIcon';
import { BodyWeight as iconData } from '@dga-icons/core/stroke-sharp';

const BodyWeight = createJsIcon('body-weight', iconData);

export default BodyWeight;

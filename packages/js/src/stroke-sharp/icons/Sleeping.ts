import { createJsIcon } from '../../createJsIcon';
import { Sleeping as iconData } from '@dga-icons/core/stroke-sharp';

const Sleeping = createJsIcon('sleeping', iconData);

export default Sleeping;

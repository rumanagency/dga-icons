import { createJsIcon } from '../../createJsIcon';
import { FileAttachment as iconData } from '@dga-icons/core/stroke-sharp';

const FileAttachment = createJsIcon('file-attachment', iconData);

export default FileAttachment;

import { createJsIcon } from '../../createJsIcon';
import { TimeSchedule as iconData } from '@dga-icons/core/stroke-sharp';

const TimeSchedule = createJsIcon('time-schedule', iconData);

export default TimeSchedule;

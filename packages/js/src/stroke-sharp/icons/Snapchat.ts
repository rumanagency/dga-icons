import { createJsIcon } from '../../createJsIcon';
import { Snapchat as iconData } from '@dga-icons/core/stroke-sharp';

const Snapchat = createJsIcon('snapchat', iconData);

export default Snapchat;

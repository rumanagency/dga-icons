import { createJsIcon } from '../../createJsIcon';
import { SecurityLock as iconData } from '@dga-icons/core/stroke-sharp';

const SecurityLock = createJsIcon('security-lock', iconData);

export default SecurityLock;

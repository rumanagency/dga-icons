import { createJsIcon } from '../../createJsIcon';
import { Subtitle as iconData } from '@dga-icons/core/stroke-sharp';

const Subtitle = createJsIcon('subtitle', iconData);

export default Subtitle;

import { createJsIcon } from '../../createJsIcon';
import { LoginMethod as iconData } from '@dga-icons/core/stroke-sharp';

const LoginMethod = createJsIcon('login-method', iconData);

export default LoginMethod;

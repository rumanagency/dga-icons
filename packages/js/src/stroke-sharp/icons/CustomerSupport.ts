import { createJsIcon } from '../../createJsIcon';
import { CustomerSupport as iconData } from '@dga-icons/core/stroke-sharp';

const CustomerSupport = createJsIcon('customer-support', iconData);

export default CustomerSupport;

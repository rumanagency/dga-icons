import { createJsIcon } from '../../createJsIcon';
import { SourceCodeSquare as iconData } from '@dga-icons/core/stroke-sharp';

const SourceCodeSquare = createJsIcon('source-code-square', iconData);

export default SourceCodeSquare;

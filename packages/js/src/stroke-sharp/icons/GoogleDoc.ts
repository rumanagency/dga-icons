import { createJsIcon } from '../../createJsIcon';
import { GoogleDoc as iconData } from '@dga-icons/core/stroke-sharp';

const GoogleDoc = createJsIcon('google-doc', iconData);

export default GoogleDoc;

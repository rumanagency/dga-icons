import { createJsIcon } from '../../createJsIcon';
import { FileLink as iconData } from '@dga-icons/core/stroke-sharp';

const FileLink = createJsIcon('file-link', iconData);

export default FileLink;

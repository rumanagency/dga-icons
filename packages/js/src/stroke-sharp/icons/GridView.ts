import { createJsIcon } from '../../createJsIcon';
import { GridView as iconData } from '@dga-icons/core/stroke-sharp';

const GridView = createJsIcon('grid-view', iconData);

export default GridView;

import { createJsIcon } from '../../createJsIcon';
import { StructureFolder as iconData } from '@dga-icons/core/stroke-sharp';

const StructureFolder = createJsIcon('structure-folder', iconData);

export default StructureFolder;

import { createJsIcon } from '../../createJsIcon';
import { Pendulum as iconData } from '@dga-icons/core/stroke-sharp';

const Pendulum = createJsIcon('pendulum', iconData);

export default Pendulum;

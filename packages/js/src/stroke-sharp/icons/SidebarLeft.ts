import { createJsIcon } from '../../createJsIcon';
import { SidebarLeft as iconData } from '@dga-icons/core/stroke-sharp';

const SidebarLeft = createJsIcon('sidebar-left', iconData);

export default SidebarLeft;

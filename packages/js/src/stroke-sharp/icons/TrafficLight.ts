import { createJsIcon } from '../../createJsIcon';
import { TrafficLight as iconData } from '@dga-icons/core/stroke-sharp';

const TrafficLight = createJsIcon('traffic-light', iconData);

export default TrafficLight;

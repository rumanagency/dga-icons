import { createJsIcon } from '../../createJsIcon';
import { CodeFolder as iconData } from '@dga-icons/core/stroke-sharp';

const CodeFolder = createJsIcon('code-folder', iconData);

export default CodeFolder;

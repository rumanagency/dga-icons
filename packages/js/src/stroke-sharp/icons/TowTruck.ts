import { createJsIcon } from '../../createJsIcon';
import { TowTruck as iconData } from '@dga-icons/core/stroke-sharp';

const TowTruck = createJsIcon('tow-truck', iconData);

export default TowTruck;

import { createJsIcon } from '../../createJsIcon';
import { LayoutLeft as iconData } from '@dga-icons/core/stroke-sharp';

const LayoutLeft = createJsIcon('layout-left', iconData);

export default LayoutLeft;

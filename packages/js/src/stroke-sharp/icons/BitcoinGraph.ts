import { createJsIcon } from '../../createJsIcon';
import { BitcoinGraph as iconData } from '@dga-icons/core/stroke-sharp';

const BitcoinGraph = createJsIcon('bitcoin-graph', iconData);

export default BitcoinGraph;

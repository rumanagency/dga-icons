import { createJsIcon } from '../../createJsIcon';
import { Question as iconData } from '@dga-icons/core/stroke-sharp';

const Question = createJsIcon('question', iconData);

export default Question;

import { createJsIcon } from '../../createJsIcon';
import { ZoomOutArea as iconData } from '@dga-icons/core/stroke-sharp';

const ZoomOutArea = createJsIcon('zoom-out-area', iconData);

export default ZoomOutArea;

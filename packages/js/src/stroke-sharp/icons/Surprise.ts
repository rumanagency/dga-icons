import { createJsIcon } from '../../createJsIcon';
import { Surprise as iconData } from '@dga-icons/core/stroke-sharp';

const Surprise = createJsIcon('surprise', iconData);

export default Surprise;

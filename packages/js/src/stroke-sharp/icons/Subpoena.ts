import { createJsIcon } from '../../createJsIcon';
import { Subpoena as iconData } from '@dga-icons/core/stroke-sharp';

const Subpoena = createJsIcon('subpoena', iconData);

export default Subpoena;

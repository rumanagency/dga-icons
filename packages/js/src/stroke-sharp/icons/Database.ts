import { createJsIcon } from '../../createJsIcon';
import { Database as iconData } from '@dga-icons/core/stroke-sharp';

const Database = createJsIcon('database', iconData);

export default Database;

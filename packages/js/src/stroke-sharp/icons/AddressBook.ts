import { createJsIcon } from '../../createJsIcon';
import { AddressBook as iconData } from '@dga-icons/core/stroke-sharp';

const AddressBook = createJsIcon('address-book', iconData);

export default AddressBook;

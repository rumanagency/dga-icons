import { createJsIcon } from '../../createJsIcon';
import { AppleMusic as iconData } from '@dga-icons/core/stroke-sharp';

const AppleMusic = createJsIcon('apple-music', iconData);

export default AppleMusic;

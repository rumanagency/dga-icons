import { createJsIcon } from '../../createJsIcon';
import { AlignVerticalCenter as iconData } from '@dga-icons/core/stroke-sharp';

const AlignVerticalCenter = createJsIcon('align-vertical-center', iconData);

export default AlignVerticalCenter;

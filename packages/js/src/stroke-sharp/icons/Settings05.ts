import { createJsIcon } from '../../createJsIcon';
import { Settings05 as iconData } from '@dga-icons/core/stroke-sharp';

const Settings05 = createJsIcon('settings-05', iconData);

export default Settings05;

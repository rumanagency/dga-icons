import { createJsIcon } from '../../createJsIcon';
import { DeleteThrow as iconData } from '@dga-icons/core/stroke-sharp';

const DeleteThrow = createJsIcon('delete-throw', iconData);

export default DeleteThrow;

import { createJsIcon } from '../../createJsIcon';
import { SearchVisual as iconData } from '@dga-icons/core/stroke-sharp';

const SearchVisual = createJsIcon('search-visual', iconData);

export default SearchVisual;

import { createJsIcon } from '../../createJsIcon';
import { Injection as iconData } from '@dga-icons/core/stroke-sharp';

const Injection = createJsIcon('injection', iconData);

export default Injection;

import { createJsIcon } from '../../createJsIcon';
import { RepeatOff as iconData } from '@dga-icons/core/stroke-sharp';

const RepeatOff = createJsIcon('repeat-off', iconData);

export default RepeatOff;

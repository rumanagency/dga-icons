import { createJsIcon } from '../../createJsIcon';
import { Expander as iconData } from '@dga-icons/core/stroke-sharp';

const Expander = createJsIcon('expander', iconData);

export default Expander;

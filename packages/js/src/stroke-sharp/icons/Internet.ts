import { createJsIcon } from '../../createJsIcon';
import { Internet as iconData } from '@dga-icons/core/stroke-sharp';

const Internet = createJsIcon('internet', iconData);

export default Internet;

import { createJsIcon } from '../../createJsIcon';
import { Fortress as iconData } from '@dga-icons/core/stroke-sharp';

const Fortress = createJsIcon('fortress', iconData);

export default Fortress;

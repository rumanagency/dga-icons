import { createJsIcon } from '../../createJsIcon';
import { Diameter as iconData } from '@dga-icons/core/stroke-sharp';

const Diameter = createJsIcon('diameter', iconData);

export default Diameter;

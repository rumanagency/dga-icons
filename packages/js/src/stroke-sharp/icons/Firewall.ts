import { createJsIcon } from '../../createJsIcon';
import { Firewall as iconData } from '@dga-icons/core/stroke-sharp';

const Firewall = createJsIcon('firewall', iconData);

export default Firewall;

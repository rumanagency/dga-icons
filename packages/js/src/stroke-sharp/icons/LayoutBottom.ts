import { createJsIcon } from '../../createJsIcon';
import { LayoutBottom as iconData } from '@dga-icons/core/stroke-sharp';

const LayoutBottom = createJsIcon('layout-bottom', iconData);

export default LayoutBottom;

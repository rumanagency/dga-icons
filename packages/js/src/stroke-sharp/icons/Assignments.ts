import { createJsIcon } from '../../createJsIcon';
import { Assignments as iconData } from '@dga-icons/core/stroke-sharp';

const Assignments = createJsIcon('assignments', iconData);

export default Assignments;

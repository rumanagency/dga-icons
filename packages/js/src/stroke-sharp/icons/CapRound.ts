import { createJsIcon } from '../../createJsIcon';
import { CapRound as iconData } from '@dga-icons/core/stroke-sharp';

const CapRound = createJsIcon('cap-round', iconData);

export default CapRound;

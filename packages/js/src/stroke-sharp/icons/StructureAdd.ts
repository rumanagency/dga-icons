import { createJsIcon } from '../../createJsIcon';
import { StructureAdd as iconData } from '@dga-icons/core/stroke-sharp';

const StructureAdd = createJsIcon('structure-add', iconData);

export default StructureAdd;

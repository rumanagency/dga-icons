import { createJsIcon } from '../../createJsIcon';
import { Prescription as iconData } from '@dga-icons/core/stroke-sharp';

const Prescription = createJsIcon('prescription', iconData);

export default Prescription;

import { createJsIcon } from '../../createJsIcon';
import { UsbConnected01 as iconData } from '@dga-icons/core/stroke-sharp';

const UsbConnected01 = createJsIcon('usb-connected-01', iconData);

export default UsbConnected01;

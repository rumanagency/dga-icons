import { createJsIcon } from '../../createJsIcon';
import { YenCircle as iconData } from '@dga-icons/core/stroke-sharp';

const YenCircle = createJsIcon('yen-circle', iconData);

export default YenCircle;

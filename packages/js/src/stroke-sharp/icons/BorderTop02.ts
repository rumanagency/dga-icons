import { createJsIcon } from '../../createJsIcon';
import { BorderTop02 as iconData } from '@dga-icons/core/stroke-sharp';

const BorderTop02 = createJsIcon('border-top-02', iconData);

export default BorderTop02;

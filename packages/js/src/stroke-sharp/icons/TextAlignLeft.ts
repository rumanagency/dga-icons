import { createJsIcon } from '../../createJsIcon';
import { TextAlignLeft as iconData } from '@dga-icons/core/stroke-sharp';

const TextAlignLeft = createJsIcon('text-align-left', iconData);

export default TextAlignLeft;

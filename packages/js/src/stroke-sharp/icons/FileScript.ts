import { createJsIcon } from '../../createJsIcon';
import { FileScript as iconData } from '@dga-icons/core/stroke-sharp';

const FileScript = createJsIcon('file-script', iconData);

export default FileScript;

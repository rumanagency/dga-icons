import { createJsIcon } from '../../createJsIcon';
import { BitcoinReceipt as iconData } from '@dga-icons/core/stroke-sharp';

const BitcoinReceipt = createJsIcon('bitcoin-receipt', iconData);

export default BitcoinReceipt;

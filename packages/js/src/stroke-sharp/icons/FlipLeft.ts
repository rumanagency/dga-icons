import { createJsIcon } from '../../createJsIcon';
import { FlipLeft as iconData } from '@dga-icons/core/stroke-sharp';

const FlipLeft = createJsIcon('flip-left', iconData);

export default FlipLeft;

import { createJsIcon } from '../../createJsIcon';
import { GoBackward15Sec as iconData } from '@dga-icons/core/stroke-sharp';

const GoBackward15Sec = createJsIcon('go-backward-15-sec', iconData);

export default GoBackward15Sec;

import { createJsIcon } from '../../createJsIcon';
import { FileVideo as iconData } from '@dga-icons/core/stroke-sharp';

const FileVideo = createJsIcon('file-video', iconData);

export default FileVideo;

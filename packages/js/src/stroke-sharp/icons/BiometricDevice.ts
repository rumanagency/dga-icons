import { createJsIcon } from '../../createJsIcon';
import { BiometricDevice as iconData } from '@dga-icons/core/stroke-sharp';

const BiometricDevice = createJsIcon('biometric-device', iconData);

export default BiometricDevice;

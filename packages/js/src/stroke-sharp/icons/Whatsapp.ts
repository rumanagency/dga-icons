import { createJsIcon } from '../../createJsIcon';
import { Whatsapp as iconData } from '@dga-icons/core/stroke-sharp';

const Whatsapp = createJsIcon('whatsapp', iconData);

export default Whatsapp;

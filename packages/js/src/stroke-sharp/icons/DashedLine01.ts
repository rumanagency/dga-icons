import { createJsIcon } from '../../createJsIcon';
import { DashedLine01 as iconData } from '@dga-icons/core/stroke-sharp';

const DashedLine01 = createJsIcon('dashed-line-01', iconData);

export default DashedLine01;

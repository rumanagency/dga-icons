import { createJsIcon } from '../../createJsIcon';
import { Libraries as iconData } from '@dga-icons/core/stroke-sharp';

const Libraries = createJsIcon('libraries', iconData);

export default Libraries;

import { createJsIcon } from '../../createJsIcon';
import { BioEnergy as iconData } from '@dga-icons/core/stroke-sharp';

const BioEnergy = createJsIcon('bio-energy', iconData);

export default BioEnergy;

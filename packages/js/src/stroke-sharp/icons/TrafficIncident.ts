import { createJsIcon } from '../../createJsIcon';
import { TrafficIncident as iconData } from '@dga-icons/core/stroke-sharp';

const TrafficIncident = createJsIcon('traffic-incident', iconData);

export default TrafficIncident;

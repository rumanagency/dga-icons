import { createJsIcon } from '../../createJsIcon';
import { Highlighter as iconData } from '@dga-icons/core/stroke-sharp';

const Highlighter = createJsIcon('highlighter', iconData);

export default Highlighter;

import { createJsIcon } from '../../createJsIcon';
import { BitcoinTransaction as iconData } from '@dga-icons/core/stroke-sharp';

const BitcoinTransaction = createJsIcon('bitcoin-transaction', iconData);

export default BitcoinTransaction;

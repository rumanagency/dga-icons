import { createJsIcon } from '../../createJsIcon';
import { Logout05 as iconData } from '@dga-icons/core/stroke-sharp';

const Logout05 = createJsIcon('logout-05', iconData);

export default Logout05;

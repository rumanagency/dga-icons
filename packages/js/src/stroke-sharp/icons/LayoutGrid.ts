import { createJsIcon } from '../../createJsIcon';
import { LayoutGrid as iconData } from '@dga-icons/core/stroke-sharp';

const LayoutGrid = createJsIcon('layout-grid', iconData);

export default LayoutGrid;

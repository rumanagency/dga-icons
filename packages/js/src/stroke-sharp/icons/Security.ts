import { createJsIcon } from '../../createJsIcon';
import { Security as iconData } from '@dga-icons/core/stroke-sharp';

const Security = createJsIcon('security', iconData);

export default Security;

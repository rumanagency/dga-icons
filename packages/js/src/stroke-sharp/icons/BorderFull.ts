import { createJsIcon } from '../../createJsIcon';
import { BorderFull as iconData } from '@dga-icons/core/stroke-sharp';

const BorderFull = createJsIcon('border-full', iconData);

export default BorderFull;

import { createJsIcon } from '../../createJsIcon';
import { Coinbase as iconData } from '@dga-icons/core/stroke-sharp';

const Coinbase = createJsIcon('coinbase', iconData);

export default Coinbase;

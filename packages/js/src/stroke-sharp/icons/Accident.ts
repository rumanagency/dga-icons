import { createJsIcon } from '../../createJsIcon';
import { Accident as iconData } from '@dga-icons/core/stroke-sharp';

const Accident = createJsIcon('accident', iconData);

export default Accident;

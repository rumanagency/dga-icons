import { createJsIcon } from '../../createJsIcon';
import { InboxUpload as iconData } from '@dga-icons/core/stroke-sharp';

const InboxUpload = createJsIcon('inbox-upload', iconData);

export default InboxUpload;

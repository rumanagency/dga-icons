import { createJsIcon } from '../../createJsIcon';
import { IdentityCard as iconData } from '@dga-icons/core/stroke-sharp';

const IdentityCard = createJsIcon('identity-card', iconData);

export default IdentityCard;

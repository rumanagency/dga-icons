import { createJsIcon } from '../../createJsIcon';
import { Pipeline as iconData } from '@dga-icons/core/stroke-sharp';

const Pipeline = createJsIcon('pipeline', iconData);

export default Pipeline;

import { createJsIcon } from '../../createJsIcon';
import { Drooling as iconData } from '@dga-icons/core/stroke-sharp';

const Drooling = createJsIcon('drooling', iconData);

export default Drooling;

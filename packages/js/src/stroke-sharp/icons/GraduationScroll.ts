import { createJsIcon } from '../../createJsIcon';
import { GraduationScroll as iconData } from '@dga-icons/core/stroke-sharp';

const GraduationScroll = createJsIcon('graduation-scroll', iconData);

export default GraduationScroll;

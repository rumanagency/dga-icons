import { createJsIcon } from '../../createJsIcon';
import { Mastodon as iconData } from '@dga-icons/core/stroke-sharp';

const Mastodon = createJsIcon('mastodon', iconData);

export default Mastodon;

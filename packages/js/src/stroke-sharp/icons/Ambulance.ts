import { createJsIcon } from '../../createJsIcon';
import { Ambulance as iconData } from '@dga-icons/core/stroke-sharp';

const Ambulance = createJsIcon('ambulance', iconData);

export default Ambulance;

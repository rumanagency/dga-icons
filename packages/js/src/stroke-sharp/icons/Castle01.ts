import { createJsIcon } from '../../createJsIcon';
import { Castle01 as iconData } from '@dga-icons/core/stroke-sharp';

const Castle01 = createJsIcon('castle-01', iconData);

export default Castle01;

import { createJsIcon } from '../../createJsIcon';
import { LockPassword as iconData } from '@dga-icons/core/stroke-sharp';

const LockPassword = createJsIcon('lock-password', iconData);

export default LockPassword;

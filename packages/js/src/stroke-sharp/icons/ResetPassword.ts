import { createJsIcon } from '../../createJsIcon';
import { ResetPassword as iconData } from '@dga-icons/core/stroke-sharp';

const ResetPassword = createJsIcon('reset-password', iconData);

export default ResetPassword;

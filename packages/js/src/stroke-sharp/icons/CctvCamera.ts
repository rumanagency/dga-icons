import { createJsIcon } from '../../createJsIcon';
import { CctvCamera as iconData } from '@dga-icons/core/stroke-sharp';

const CctvCamera = createJsIcon('cctv-camera', iconData);

export default CctvCamera;

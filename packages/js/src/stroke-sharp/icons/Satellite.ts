import { createJsIcon } from '../../createJsIcon';
import { Satellite as iconData } from '@dga-icons/core/stroke-sharp';

const Satellite = createJsIcon('satellite', iconData);

export default Satellite;

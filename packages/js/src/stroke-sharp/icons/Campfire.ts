import { createJsIcon } from '../../createJsIcon';
import { Campfire as iconData } from '@dga-icons/core/stroke-sharp';

const Campfire = createJsIcon('campfire', iconData);

export default Campfire;

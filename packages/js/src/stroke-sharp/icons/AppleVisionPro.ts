import { createJsIcon } from '../../createJsIcon';
import { AppleVisionPro as iconData } from '@dga-icons/core/stroke-sharp';

const AppleVisionPro = createJsIcon('apple-vision-pro', iconData);

export default AppleVisionPro;

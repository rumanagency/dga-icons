import { createJsIcon } from '../../createJsIcon';
import { TouchLocked02 as iconData } from '@dga-icons/core/stroke-sharp';

const TouchLocked02 = createJsIcon('touch-locked-02', iconData);

export default TouchLocked02;

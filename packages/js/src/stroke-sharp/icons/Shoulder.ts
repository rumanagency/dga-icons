import { createJsIcon } from '../../createJsIcon';
import { Shoulder as iconData } from '@dga-icons/core/stroke-sharp';

const Shoulder = createJsIcon('shoulder', iconData);

export default Shoulder;

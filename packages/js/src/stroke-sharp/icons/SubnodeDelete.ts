import { createJsIcon } from '../../createJsIcon';
import { SubnodeDelete as iconData } from '@dga-icons/core/stroke-sharp';

const SubnodeDelete = createJsIcon('subnode-delete', iconData);

export default SubnodeDelete;

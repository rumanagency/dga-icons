import { createJsIcon } from '../../createJsIcon';
import { TextFont as iconData } from '@dga-icons/core/stroke-sharp';

const TextFont = createJsIcon('text-font', iconData);

export default TextFont;

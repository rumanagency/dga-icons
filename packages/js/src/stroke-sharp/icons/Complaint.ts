import { createJsIcon } from '../../createJsIcon';
import { Complaint as iconData } from '@dga-icons/core/stroke-sharp';

const Complaint = createJsIcon('complaint', iconData);

export default Complaint;

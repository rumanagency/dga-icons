import { createJsIcon } from '../../createJsIcon';
import { FingerprintScan as iconData } from '@dga-icons/core/stroke-sharp';

const FingerprintScan = createJsIcon('fingerprint-scan', iconData);

export default FingerprintScan;

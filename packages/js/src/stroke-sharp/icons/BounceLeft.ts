import { createJsIcon } from '../../createJsIcon';
import { BounceLeft as iconData } from '@dga-icons/core/stroke-sharp';

const BounceLeft = createJsIcon('bounce-left', iconData);

export default BounceLeft;

import { createJsIcon } from '../../createJsIcon';
import { Background as iconData } from '@dga-icons/core/stroke-sharp';

const Background = createJsIcon('background', iconData);

export default Background;

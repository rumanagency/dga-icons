import { createJsIcon } from '../../createJsIcon';
import { MailOpen as iconData } from '@dga-icons/core/stroke-sharp';

const MailOpen = createJsIcon('mail-open', iconData);

export default MailOpen;

import { createJsIcon } from '../../createJsIcon';
import { ResourcesAdd as iconData } from '@dga-icons/core/stroke-sharp';

const ResourcesAdd = createJsIcon('resources-add', iconData);

export default ResourcesAdd;

import { createJsIcon } from '../../createJsIcon';
import { LinkBackward as iconData } from '@dga-icons/core/stroke-sharp';

const LinkBackward = createJsIcon('link-backward', iconData);

export default LinkBackward;

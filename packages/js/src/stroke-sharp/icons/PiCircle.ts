import { createJsIcon } from '../../createJsIcon';
import { PiCircle as iconData } from '@dga-icons/core/stroke-sharp';

const PiCircle = createJsIcon('pi-circle', iconData);

export default PiCircle;

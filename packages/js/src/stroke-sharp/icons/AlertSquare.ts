import { createJsIcon } from '../../createJsIcon';
import { AlertSquare as iconData } from '@dga-icons/core/stroke-sharp';

const AlertSquare = createJsIcon('alert-square', iconData);

export default AlertSquare;

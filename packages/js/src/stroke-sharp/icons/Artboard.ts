import { createJsIcon } from '../../createJsIcon';
import { Artboard as iconData } from '@dga-icons/core/stroke-sharp';

const Artboard = createJsIcon('artboard', iconData);

export default Artboard;

import { createJsIcon } from '../../createJsIcon';
import { FilterVertical as iconData } from '@dga-icons/core/stroke-sharp';

const FilterVertical = createJsIcon('filter-vertical', iconData);

export default FilterVertical;

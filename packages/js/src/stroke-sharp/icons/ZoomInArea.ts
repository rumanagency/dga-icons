import { createJsIcon } from '../../createJsIcon';
import { ZoomInArea as iconData } from '@dga-icons/core/stroke-sharp';

const ZoomInArea = createJsIcon('zoom-in-area', iconData);

export default ZoomInArea;

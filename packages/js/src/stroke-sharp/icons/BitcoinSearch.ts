import { createJsIcon } from '../../createJsIcon';
import { BitcoinSearch as iconData } from '@dga-icons/core/stroke-sharp';

const BitcoinSearch = createJsIcon('bitcoin-search', iconData);

export default BitcoinSearch;

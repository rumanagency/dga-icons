import { createJsIcon } from '../../createJsIcon';
import { Pentagon as iconData } from '@dga-icons/core/stroke-sharp';

const Pentagon = createJsIcon('pentagon', iconData);

export default Pentagon;

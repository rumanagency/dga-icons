import { createJsIcon } from '../../createJsIcon';
import { MessageDelay01 as iconData } from '@dga-icons/core/stroke-sharp';

const MessageDelay01 = createJsIcon('message-delay-01', iconData);

export default MessageDelay01;

import { createJsIcon } from '../../createJsIcon';
import { Variable as iconData } from '@dga-icons/core/stroke-sharp';

const Variable = createJsIcon('variable', iconData);

export default Variable;

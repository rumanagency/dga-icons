import { createJsIcon } from '../../createJsIcon';
import { HandGrip as iconData } from '@dga-icons/core/stroke-sharp';

const HandGrip = createJsIcon('hand-grip', iconData);

export default HandGrip;

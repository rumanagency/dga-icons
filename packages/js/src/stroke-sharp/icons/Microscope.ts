import { createJsIcon } from '../../createJsIcon';
import { Microscope as iconData } from '@dga-icons/core/stroke-sharp';

const Microscope = createJsIcon('microscope', iconData);

export default Microscope;

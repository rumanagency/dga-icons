import { createJsIcon } from '../../createJsIcon';
import { BitcoinDatabase as iconData } from '@dga-icons/core/stroke-sharp';

const BitcoinDatabase = createJsIcon('bitcoin-database', iconData);

export default BitcoinDatabase;

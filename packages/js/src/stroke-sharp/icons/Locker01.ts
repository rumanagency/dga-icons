import { createJsIcon } from '../../createJsIcon';
import { Locker01 as iconData } from '@dga-icons/core/stroke-sharp';

const Locker01 = createJsIcon('locker-01', iconData);

export default Locker01;

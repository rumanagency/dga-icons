import { createJsIcon } from '../../createJsIcon';
import { Unavailable as iconData } from '@dga-icons/core/stroke-sharp';

const Unavailable = createJsIcon('unavailable', iconData);

export default Unavailable;

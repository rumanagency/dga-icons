import { createJsIcon } from '../../createJsIcon';
import { FolderEdit as iconData } from '@dga-icons/core/stroke-sharp';

const FolderEdit = createJsIcon('folder-edit', iconData);

export default FolderEdit;

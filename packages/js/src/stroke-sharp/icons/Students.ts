import { createJsIcon } from '../../createJsIcon';
import { Students as iconData } from '@dga-icons/core/stroke-sharp';

const Students = createJsIcon('students', iconData);

export default Students;

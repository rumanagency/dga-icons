import { createJsIcon } from '../../createJsIcon';
import { Avalanche as iconData } from '@dga-icons/core/stroke-sharp';

const Avalanche = createJsIcon('avalanche', iconData);

export default Avalanche;

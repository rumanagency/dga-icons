import { createJsIcon } from '../../createJsIcon';
import { FileView as iconData } from '@dga-icons/core/stroke-sharp';

const FileView = createJsIcon('file-view', iconData);

export default FileView;

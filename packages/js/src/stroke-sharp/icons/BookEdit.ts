import { createJsIcon } from '../../createJsIcon';
import { BookEdit as iconData } from '@dga-icons/core/stroke-sharp';

const BookEdit = createJsIcon('book-edit', iconData);

export default BookEdit;

import { createJsIcon } from '../../createJsIcon';
import { SofaSingle as iconData } from '@dga-icons/core/stroke-sharp';

const SofaSingle = createJsIcon('sofa-single', iconData);

export default SofaSingle;

import { createJsIcon } from '../../createJsIcon';
import { NoSignal as iconData } from '@dga-icons/core/stroke-sharp';

const NoSignal = createJsIcon('no-signal', iconData);

export default NoSignal;

import { createJsIcon } from '../../createJsIcon';
import { DollarSend01 as iconData } from '@dga-icons/core/stroke-sharp';

const DollarSend01 = createJsIcon('dollar-send-01', iconData);

export default DollarSend01;

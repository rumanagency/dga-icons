import { createJsIcon } from '../../createJsIcon';
import { Bluetooth as iconData } from '@dga-icons/core/stroke-sharp';

const Bluetooth = createJsIcon('bluetooth', iconData);

export default Bluetooth;

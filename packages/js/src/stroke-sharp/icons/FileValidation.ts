import { createJsIcon } from '../../createJsIcon';
import { FileValidation as iconData } from '@dga-icons/core/stroke-sharp';

const FileValidation = createJsIcon('file-validation', iconData);

export default FileValidation;

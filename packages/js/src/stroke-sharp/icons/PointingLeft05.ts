import { createJsIcon } from '../../createJsIcon';
import { PointingLeft05 as iconData } from '@dga-icons/core/stroke-sharp';

const PointingLeft05 = createJsIcon('pointing-left-05', iconData);

export default PointingLeft05;

import { createJsIcon } from '../../createJsIcon';
import { TextIndent as iconData } from '@dga-icons/core/stroke-sharp';

const TextIndent = createJsIcon('text-indent', iconData);

export default TextIndent;

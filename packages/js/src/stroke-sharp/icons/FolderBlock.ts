import { createJsIcon } from '../../createJsIcon';
import { FolderBlock as iconData } from '@dga-icons/core/stroke-sharp';

const FolderBlock = createJsIcon('folder-block', iconData);

export default FolderBlock;
